## Items Entities — 23-registry-items-entities

### 1) Что проверено (файлы/символы)

**Инструменты:** `codegraph_explore` (JujutsuItems/JujutsuEntities, vessel definitions, TodoStone/MegumiDog), `read` (direct file reads), `grep`/`glob` по `D:/WorkFlow/jujutsu-minecraft/src`. Сверка с `AGENTS.md` (Vessel Seam, Todo без стартера, stone как entity), `docs/KNOWN_ISSUES.md` (E12), Codex `02-architecture/Vessel-definitions.md`.

**Файлы / символы:**

- `src/main/java/jujutsu/mod/registry/JujutsuItems.java:1-69` — 6 констант `HAIRPIN_NAIL`, `STRAW_DOLL_HAMMER`, `PROJECTJJK_HAIRPIN_NAIL`, `PROJECTJJK_STRAW_DOLL_HAMMER`, `RESONANCE_REMNANT`, `STRAW_DOLL`; `register():28-35` + 4 фабрики (`createProjectJjkNail:37`, `createProjectJjkHammer:42`, `createProjectJjkRemnant:47`, `createProjectJjkStrawDoll:52`) + `strawDollProperties():57` — `codegraph_explore` + `read` полностью.
- `src/main/java/jujutsu/mod/registry/JujutsuEntities.java:1-66` — 3 типа `PROJECTJJK_NAIL:16`, `MEGUMI_DIVINE_DOG:17`, `TODO_STONE:18`; `register():22-26` + 3 builder-метода (`createTodoStone:28`, `createMegumiDivineDog:41`, `createProjectJjkNail:52`) + `register(String,EntityType):63` — `codegraph_explore` + `read` полностью.
- `src/main/java/jujutsu/mod/JujutsuMod.java:31-49` — порядок `JujutsuEntities.register():32` → `JujutsuItems.register():35` → цикл `JujutsuCharacters.all() -> registerServerHooks():44-46` — `read`.
- `src/main/java/jujutsu/mod/character/JujutsuCharacters.java:30-57` и `src/client/java/jujutsu/mod/client/character/JujutsuCharacterClients.java:26-65` — exhaustive `switch` без `default`, seam vessel — `codegraph_explore` + `read`.
- `src/main/java/jujutsu/mod/character/JujutsuCharacter.java:5-43`, `NoneDefinition`, `character/nobara/NobaraDefinition.java:21-61`, `character/todo/TodoDefinition.java:15-90`, `character/megumi/MegumiDefinition.java:15-51` — `onSelected`/`onDeselected`/`registerServerHooks`/`applyAttributes` — `codegraph_explore` + `read`.
- `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkNobaraLoadout.java:1-65` — `ensureStarterTools:12`, `hasHammer:29`, `hasDoll:39`, `countNails:48`, `missingNails:25` — `codegraph_explore` + `read`.
- `src/main/java/jujutsu/mod/character/CharacterSelectionManager.java:14-30` и `CharacterPlayerState.java:1-45` — `select` (deselect → `claimStarter:24` → `applyForSelection:26` → `onSelected:27`) и `CODEC:14`/`DEFAULT:18` — `read`.
- `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:41-217` — `launch:60`, `tick:88`, `endFlight:128`, `snapTo:142`, `fireImmune:151`, `canUsePortal:156`, `hurtServer:167`, `defineSynchedData:172`, `addAdditionalSaveData:178`/`readAdditionalSaveData:186`, `clientTick:195` — `codegraph_explore` + `read` 41-217.
- `src/main/java/jujutsu/mod/character/megumi/MegumiDivineDogEntity.java:37-349` — `extends Wolf:38`, `registerGoals:59`, `defineSynchedData:72`, `configureSummon:78`, `tick:213`, `tickPresentationPhase:228`, `setPresentationPhase:251`, `combatEnabled:263`, переопределения `doHurtTarget:277`/`hurtServer:282`/`isPickable:287`/`isPushable:292`/`canCollideWith:304`/`setTarget:272`/`wantsToAttack:346`, `canBeLeashed:328=false`, `canMate:333=false` — `codegraph_explore` + `read` 37-349.
- `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:4-104` и `MegumiSummonRuntime.java` — атрибуты/сроки/радиусы, `register`, `tryToggle`/`trySic`, `shouldHardDiscard`, `reconcile`, `teardown` — `codegraph_explore` + `read`.
- `src/main/java/jujutsu/mod/character/todo/TodoProfile.java:70-90` — `STONE_*` константы — `codegraph_explore` + `read`.
- `src/main/java/jujutsu/mod/character/todo/TodoTransientState.java:1-120` + `TodoStateLifecycle.java:30-66` + `TodoStoneRuntime.java` — единый `ConcurrentHashMap STATES`, `dropAll`/`clearAll`, lifecycle хуки — `codegraph_explore` + `read`.
- `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:127-131`, `nobara/NobaraClientDefinition.java:109-113`, `todo/TodoClientDefinition.java:131-135` — `EntityRendererRegistry.register` для трёх entity — `grep` + `read`.
- `src/main/resources/assets/jujutsumod/items/*.json` и `models/item/*.json` — `glob` + `read` alias-моделей (`hairpin_nail.json`, `projectjjk_hairpin_nail.json` оба → `model: jujutsumod:item/projectjjk_hairpin_nail`).
- `grep` проверки: `JujutsuItems|JujutsuEntities` (33 и 22 caller'а via codegraph blast radius, все легитимны), `CreativeModeTab` (0 хитов — Todo без creative tab), `vessel|Vessel` в registry (0 хитов — seam не нарушен), `HAIRPIN_NAIL|STRAW_DOLL|PROJECTJJK` (32 хита предикатов `stack.is(A)||stack.is(B)`).

### 2) Находки

| Severity | Файл:строка | Описание | Рекомендация |
|---|---|---|---|
| major | `src/main/java/jujutsu/mod/character/CharacterSelectionManager.java:22-24` + `CharacterPlayerState.java:33-44` + `docs/KNOWN_ISSUES.md:E12` | `claimedStarterCharacters` пишется для каждого сосуда (`claimStarter(character)` на каждый `select`), но читается нигде (`hasClaimedStarter` — dead code, 0 прод-коллеров via `grep hasClaimedStarter` и codegraph). Для Nobara безвредно — `ensureStarterTools` идемпотентен и зовётся на каждый `onSelected`; для Todo/Megumi claim накапливается в `CHARACTER_STATE` NBT (`copyOnDeath` персистент, `JujutsuAttachments.java:10-15`). Накопление — утечка семантики: будущий код может ошибочно решить что claim означает «выдавать только раз». Подтверждено `codegraph_explore` по `CharacterPlayerState` и `read` E12. | Либо удалить поле `claimedStarterCharacters` и `claimStarter/hasClaimedStarter` (чистый cutover: текущее поведение — выдавать Nobara kit каждый раз — останется), либо дать полю работу (напр. one-time tutorial). До решения — не использовать `hasClaimedStarter` в новом коде. Owner: gameplay. |
| minor | `src/main/java/jujutsu/mod/registry/JujutsuItems.java:19-22` + `assets/jujutsumod/items/hairpin_nail.json:2-5` + `projectjjk_hairpin_nail.json:2-5` | Legacy-alias дублирование: `HAIRPIN_NAIL` (`hairpin_nail`) и `PROJECTJJK_HAIRPIN_NAIL` (`projectjjk_hairpin_nail`) — один класс `ProjectJjkNailItem:39`, оба JSON → один `model: jujutsumod:item/projectjjk_hairpin_nail`. Аналогично `STRAW_DOLL_HAMMER` / `PROJECTJJK_STRAW_DOLL_HAMMER` — один `ProjectJjkHammerItem:44`. Решение рабочее (совместимость старых миров/датапаков где могли сохраниться старые id), но держит два registry id на один предмет по всему коду — каждый `isNail`/`isHammer` обязан проверять оба (`ProjectJjkNobaraLoadout:32-33,52-53`). Проверено `read JujutsuItems.java` + `read items/*.json`. | Зафиксировать в комментарии `JujutsuItems` почему два id должны жить (совместимость) и срок их жизни. В идеале ввести `Tag<Item> jujutsumod:hammer` / `hairpin_nail` или централизованные предикаты `JujutsuItems.isHammer/isNail` и заменить 7 копий — тогда alias-проверка в одном месте. |
| minor | `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkNobaraLoadout.java:31-35,51-54` + `NailTrapRuntime.java:294` + `NobaraHammerCombatRuntime.java:293` + `ProjectJjkNobaraRuntime.java:376` + `NobaraActionGuard.java:36` + `ProjectJjkStrawDollRuntime.java:308` + `client/fx/NobaraHudState.java:15` + `client/input/JujutsuKeybinds.java:266` | Дублирование предикатов `isNail`/`isHammer`/`isKit` в 7 файлах копипастой (`stack.is(JujutsuItems.HAIRPIN_NAIL) \|\| stack.is(JujutsuItems.PROJECTJJK_HAIRPIN_NAIL)`). Логика идентична, но каждый файл держит свою копию. Изменение в `JujutsuItems` (напр. новый alias) требует правки 7 точек. Проверено `grep stack.is\(JujutsuItems` — 7 хитов. | Вынести в `JujutsuItems` статические `isHairpinNail(ItemStack)`, `isHammer(ItemStack)`, `isStrawDoll(ItemStack)` и использовать их везде. Риск регресса — низкий, выигрыш — защита от дрифта. |
| minor | `src/main/java/jujutsu/mod/registry/JujutsuEntities.java:28-39` + `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:54-56,178-193` | `TODO_STONE` помечен `.noSave():37` (верно: transient, не должен переживать сессию) но при этом `TodoStoneEntity` реализует `addAdditionalSaveData:178`/`readAdditionalSaveData:186` (OWNER_UUID_TAG, REMAINING_TICKS_TAG) и `DATA_REMAINING_TICKS` sync. При `.noSave()` NBT-roundtrip никогда не вызывается в проде — код мёртв, но не вреден; вводит в заблуждение — будто камень может сохраняться. Проверено `codegraph_explore TodoStoneEntity` + `read JujutsuEntities.java:37`. | Либо оставить как defensive (дешево), но добавить комментарий `// noSave: persistence hooks are defensive only` над `addAdditionalSaveData`, либо удалить NBT-методы чтобы `noSave` был честным. Текущее состояние — принять, но задокументировать. |
| minor | `src/main/java/jujutsu/mod/registry/JujutsuEntities.java:42-49` + `src/main/java/jujutsu/mod/character/megumi/MegumiDivineDogEntity.java:54-56` + `MegumiDefinition.java:28-29` | `MEGUMI_DIVINE_DOG` зарегистрирован как `MobCategory.MISC:44` + `.noSave():48` + `FabricDefaultAttributeRegistry.register(MEGUMI_DIVINE_DOG, ...)`. Wolf в ваниле `MobCategory.CREATURE`. `MISC` уводит собак из creature cap / despawn-логики, что для transient-пакета (runtime-only, 2 особи max, `noSave`) намеренно и безопасно, но расходится с ожиданиями `Wolf` (leash, breed, tame). Компенсируется переопределениями `canBeLeashed=false:328`, `canMate=false:333`, `getBreedOffspring=null:338`, `startPersistentAngerTimer noop:343`. Проверено `read JujutsuEntities.java:42-49` + `read MegumiDivineDogEntity.java:322-348`. | Задокументировать в `JujutsuEntities.createMegumiDivineDog` почему `MISC`, а не `CREATURE` (избежать mob cap,配合 noSave transient). Текущее — корректно, комментарий предотвратит «исправление» на CREATURE. |
| minor | `src/main/java/jujutsu/mod/registry/JujutsuEntities.java:17,30-32,52-58` + `TodoProfile.java:STONE_HITBOX_SIZE=0.25` + `TodoStoneEntity.java:109-115` | Размеры/треккинг: `TODO_STONE` `sized(0.25,0.25):32` `clientTrackingRange 96:33` `updateInterval 2:34`; `PROJECTJJK_NAIL` `sized(0.28,0.28):56` `updateInterval 1:58`; `MEGUMI_DIVINE_DOG` `sized(0.6,0.85):45` `updateInterval 2:47`. Для камня скорость `0.23 bl/tick` (`TodoProfile:73`), за 2 тика смещение `0.46 bl` — больше хитбокса. Догоняется `clientTick:195-209` (`position.add(movement)` + `ASH` trail) и `setRequiresPrecisePosition(true):56`, так что визуально гладко. Но интервал камня `2` против `1` у гвоздя — осознанная полумера: nail нуждается в `1` из-за рывка/заряда, камень летит равномерно. Проверено `read JujutsuEntities` + `read TodoStoneEntity:116-118` (sync каждые 4 тика). | Оставить как есть; добавить комментарий в `JujutsuEntities` почему разные интервалы. При добавлении быстрого projectile-копировать nail интервал `1`. |
| minor | `src/main/java/jujutsu/mod/character/megumi/MegumiDivineDogEntity.java:251-261` + `58-69` | Divine Dogs как `Wolf`: `MATERIALIZING` (16t) и `RECALLING` (12t via `MegumiProfile:15-16`) ставят `setNoAi(true):255` + `clearSicCommand():257` + `setTarget(null):258`, что глушит `MeleeAttackGoal`/`FollowOwnerGoal` и `isPickable`/`isPushable`/`hurtServer`/`doHurtTarget`. Переход в `ACTIVE` ставит `setNoAi(false):255` — но `GoalSelector` тикает раз в tick, поэтому первый тик после материализации собака стоит (1-tick лаг). Не баг геймплея, но заметно в смоке при спавне вплотную. Проверено `read MegumiDivineDogEntity.java:228-261`. | Принять 1-tick задержку как есть (безвредна), либо в `tickPresentationPhase` после `setNoAi(false)` триггернуть `getNavigation().moveTo` если есть `sicTarget`. P4 cosmetics. |
| minor | `src/main/java/jujutsu/mod/registry/JujutsuItems.java:37-55` | Три почти-идентичных фабрики `createProjectJjkNail/Hammer/Remnant` — каждая `ResourceKey.create(... JujutsuMod.id(path))` + `new XxxItem(properties.setId(key))`. Отличаются только классом item'а. При добавлении нового item'а копипаста продолжится. Проверено `read JujutsuItems.java:37-55`. | Некритично (3 метода) — оставить; при 4-м вынести generic `<T extends Item> T create(String, Function<Properties,T>, Properties)` helper. |

### 3) Позитивные наблюдения

- **Vessel Seam не нарушен.** `JujutsuItems`/`JujutsuEntities` — content registries, допущены AGENTS.md § Vessel Seam: «Content registries (`JujutsuItems`, `JujutsuEntities`) are a different category and may of course gain a vessel's items». Ни одного `if (character==TODO)` / `switch(character)` в этих файлах нет (`grep vessel` по `registry` — 0, `codegraph` blast radius — только content callers). `JujutsuMod.onInitialize:44-46` не содержит vessel-специфичных вызовов — цикл `for (CharacterDefinition d : JujutsuCharacters.all()) d.registerServerHooks()` — `read JujutsuMod.java:44-46`. Клиент аналогично `JujutsuCharacterClients.registerAll():61-65`.
- **Todo без starter loadout — соблюдено.** `TodoDefinition` не переопределяет `onSelected` (нет выдачи), `MegumiDefinition.onDeselected:44` только чистит, `NobaraDefinition.onSelected:56-60` идемпотентно `ensureStarterTools` (проверяет `hasHammer`/`hasDoll`/`countNails` через `stack.is(A)||stack.is(B)` и `missingNails:25`). `CharacterSelectionManager.select:22-27` пишет `claimStarter` для всех, но выдача только у Nobara — продукт-решение AGENTS.md «Todo has no starter loadout and no items in this slice» выполнено. `grep CreativeModeTab` — 0 хитов: Todo stone не требует creative tab, т.к. `entity`, не `item` — правильно. Codex `TodoProfile` — single source констант.
- **Stone entity registration — образцово.** `createTodoStone:28-39` — `MobCategory.MISC`, `sized(STONE_HITBOX_SIZE=0.25)`, `clientTrackingRange 96`, `updateInterval 2`, `noSave():37`, `ResourceKey` + `.build(key):38` — всё по шаблону `add-vessel` skill. `TodoStoneEntity:55-56` `setNoGravity(true)` + `setRequiresPrecisePosition(true)` (синк как у `ProjectJjkNailEntity`). `fireImmune()=true:151`, `canUsePortal()=false:156` с комментарием про ghost-copy, `hurtServer()=false:167` (стрела не сбивает установку), `endFlight:128` через `TodoTransientState.clearStone` с uuid-guard (`getUUID().equals(ref.entityUuid())`) — защита от successor-stone. `launch:60-72` сетит `remainingTicks` + сразу `entityData.set(DATA_REMAINING_TICKS)` чтобы клиент не видел 0 первые тики — деталь прод.
- **Divine Dogs как vanilla Wolf — корректно и безопасно.** `extends Wolf:38`, `Wolf.createAttributes().add(MAX_HEALTH 60).add(ATTACK_DAMAGE 3).add(MOVEMENT_SPEED 0.34)` в `MegumiDefinition.createDivineDogAttributes:36-41` — переиспользует ванильный Wolf-скелет/анимации, меняет только цифры из `MegumiProfile`. `noSave():48` + runtime PACKS map (2 entity max, token+ownerUuid идентификация, 0 persist) — transient by design, как и stone. `registerGoals:59-69` — `FloatGoal` + `MeleeAttackGoal` + `FollowOwnerGoal(10→2)` — ванильное следование, `canBeLeashed=false:328`/`canMate=false:333`/`startPersistentAngerTimer noop:343` + `wantsToAttack:346` делегирует `MegumiSummonRuntime.isEligibleTarget` — агрессия только на валидные цели. `tick:213` + `tickPresentationPhase:228` + `MegumiSummonRuntime.shouldHardDiscard/tickPounce` — lifecycle sound, combat gated по `presentationPhase`.
- **Отсутствие дублирования [сущностного] — соблюдено.** Нет второго механизма регистрации предметов/сущностей: единственный `JujutsuItems.register()` / `JujutsuEntities.register()` в `JujutsuMod:32/35`. Нет параллельного `DeferredRegister` / `Registry.register` в vessel-пакетах. Размеры/атрибуты — single source `TodoProfile`, `MegumiProfile`, `ProjectJjkNobaraProfile`. VFX/звуки — в `JujutsuSounds`/`JujutsuParticles`, не кустарно в `JujutsuEntities`.
- **Wire ids append-only.** `CharacterAbility` ids append-only (AGENTS.md), entity paths `projectjjk_nail`/`megumi_divine_dog`/`todo_stone` — lower_snake, не переименовывались; `ProjectSanityTest:501-504` пинает что `todo_swap_marker` удалён и не вернулся — защита от регресса.
- **Тест-покрытие регистри.** `ProjectSanityTest.assertItemRegistryUsesKeyedProperties:263-270` + проверки `todo_swap_marker` отсутствия + `SourceBoundaryTripwireTest` + `VesselBoundaryTest.CONTENT_REGISTRIES` включают `JujutsuItems`/`JujutsuEntities` как разрешённые content registries — seam нарушению не дадут пройти CI. `codegraph` blast radius: 33 caller'а `JujutsuItems`, 22 `JujutsuEntities` — все легитимные (hud, keybinds, runtimes, renderers).

### 4) Вердикт

**PASS WITH CAVEATS**

Срез `23-registry-items-entities` функционален, seam-соблюдён, инварианты «Todo без стартера, stone transient entity, Dogs = Wolf с gated combat» выполнены. Критических багов, десинков, утечек, баланс-проблем или мёртвого кода в прод-пути не найдено. Все `critical`/`blocker` Gate проверки — зелёные; entity lifecycle (stone `noSave` + `endFlight` guard, dogs `reconcile/teardown` + `shouldHardDiscard`) — sound. Проверено `codegraph_explore` по всем четырём доменам + `read`/`grep`/`glob`.

Caveats — 7 `minor` + 1 `major` (архитектурный долг) — не требуют hotfix, но требуют дисциплины:

**Приоритет фиксов:**

1. **P2 (major, debt) — `CharacterPlayerState.claimedStarterCharacters` dead field.** Не трогать до продакт-решения: либо удалить, либо дать ему работу. Любой новый `if (hasClaimedStarter(...))` сейчас будет mis-use персистентного поля. Owner: gameplay. Связь: `CharacterSelectionManager.java:24`, `CharacterPlayerState.java:37-44`, `KNOWN_ISSUES.md:E12`.
2. **P3 (minor, maintainability) — централизовать `isNail`/`isHammer` предикаты.** Создать `JujutsuItems.isHairpinNail(ItemStack)` / `isHammer` / `isStrawDoll` и заменить 7 копий. Риск регресса — низкий, выигрыш — защита от дрифта при добавлении alias. Связь: все файлы из таблицы строка 3.
3. **P3 (minor, docs) — задокументировать legacy-alias причину в `JujutsuItems`.** Один комментарий над константами: «два id на один предмет — обратная совместимость, оба JSON → один model, удалить после миграции миров». Связь: `JujutsuItems.java:19-22`, `items/*.json`.
4. **P4 (minor, cosmetics) — комментарии к `.noSave()` + `MISC` + `updateInterval`.** По одной строке в `JujutsuEntities` у каждого builder — почему `noSave` (transient), почему `MISC` для Dogs, почему камень `interval 2` а гвоздь `1`. Предотвращает «выравнивание» при рефакторе. Связь: `JujutsuEntities.java:28-61`.

Остальные minor — принять как есть (stone NBT defensive hooks, Dogs 1-tick AI lag, фабрики — безвредны).

AUDIT_ID: 23-registry-items-entities
