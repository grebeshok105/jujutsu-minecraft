## Product — 50-product-roadmap

### 1) Что проверено (файлы/символы)

**Инструменты:** `codegraph_explore` по `CharacterDefinition/JujutsuCharacter/JujutsuCharacters/CharacterAbility/TodoProfile/MegumiProfile/ProjectJjkNobaraProfile/VfxDirector/VfxCue/JujutsuKeybinds/AbilityHud/ClientAbilityCooldowns`, прямые `read`/`grep` по `AGENTS.md`, `docs/KNOWN_ISSUES.md`, `SESSION.md`, `Jujutsu Kaizen/jujutsumod-codebase-codex/**/*.md`, `build.gradle`, `src/main/java/jujutsu/mod/network/JujutsuNetworking.java`.

**Ядро продукта и роадмап-документы:**
- `AGENTS.md:1-372` — вся секция `Current Product Direction` (`:13-24`), `Current slice (facts)` (`:26-54`), `Gameplay Design Principles` (`:263-279`), `First Milestone Philosophy` (`:282-288`), `The Vessel Seam` (`:220-237`), `Mandatory VFX Core Contract` (`:239-246`) — проверены `read`
- `docs/KNOWN_ISSUES.md:1-307` — секции `Accepted product decisions`, `Public-release blockers R1-R3`, `High-priority E1-E15`, `Low-priority product debt` — проверены `read`
- `Jujutsu Kaizen/jujutsumod-codebase-codex/00-MOC.md:1-99` — `Current product snapshot`, метрики `Main 126 / Client 191 / Test 85`, `Verification programs 29`
- `Jujutsu Kaizen/jujutsumod-codebase-codex/06-maintenance/How-to-add-next-character.md:1-72` — процедура добавления vessel, чеклист phases — проверен `read`
- `Jujutsu Kaizen/jujutsumod-codebase-codex/06-maintenance/Risks-and-tech-debt.md:1-31` — `Engineering priorities` — проверен `read`
- `SESSION.md:1-28` — текущая ветка `feat/nobara-target-hud`, smoke-stat 296 JUnit / 34 GameTest / 29 JavaExec, Next candidates — проверен `read`
- `docs/knowledge/CURRENT_STATE.md:1-88` — волна 2026-08-07, PR #62-#67, MCP spike, smoke 43 кейса — проверен `read`

**Vessel-дефиниции и профили (баланс, kit size):**
- `src/main/java/jujutsu/mod/character/JujutsuCharacter.java:1-43` — enum `NONE/NOBARA/TODO/MEGUMI`, `byId`, `bodyScale`
- `src/main/java/jujutsu/mod/character/JujutsuCharacters.java:1-57` — серверный registry, exhaustive switch
- `src/client/java/jujutsu/mod/client/character/JujutsuCharacterClients.java:1-66` — клиентский registry, `inRosterOrder`, `registerAll`
- `src/main/java/jujutsu/mod/character/CharacterDefinition.java:1-102` — интерфейс vessel, `tryCast`, `applyAttributes`, `onDeselected`
- `src/client/java/jujutsu/mod/client/character/CharacterClientDefinition.java:1-89` — `rosterEntry`, `hudSlots`, `maxCooldownTicks`, `accent/warmth`
- `src/main/java/jujutsu/mod/character/nobara/NobaraDefinition.java:21-61` — 9 `register()`, `selectCurseLink`, `onSelected` idempotent loadout
- `src/main/java/jujutsu/mod/character/todo/TodoDefinition.java:15-90` — `MELEE_DAMAGE_MULTIPLIER 1.50`, `ATTACK_SPEED 0.85`, `adjustIncomingStaggerTicks *0.5`, `onDeselected dropEverything`
- `src/main/java/jujutsu/mod/character/megumi/MegumiDefinition.java:15-51` — `createDivineDogAttributes` (60 HP / 3 dmg / 0.34 speed), 4 runtime `register()`
- `src/main/java/jujutsu/mod/character/todo/TodoProfile.java:1-111` — все константы Todo (BOOGIE_WOOGIE 20.0 / 60t, FAKE_CLAP 20t, PAIR 100t, TRIPLE 160t, STONE 0.23 / 100t / 10t / 60t / 100t, MOMENTUM 1.25 / 24t / 8t stagger)
- `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:1-104` — все константы Megumi (DOG 16t/12t, RECALL 240t / DEATH 600t, SIC 30t, POUNCE 3-8 блоков / 80t / 16t, TRAP r2.6 / 100t / 200t cd / grip -75%, MOVE sink8/hidden4/emerge6/submerge50 / cooldown 120/200t, DROP r1.2 / 4 блока высота / 20t telegraph / 60t cd / 30/25/25/20 веса)
- `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkNobaraProfile.java:1-157` — все константы Nobara (HAIRPIN 18 dmg / 36 range, NAIL 2.0 dmg / 1200t TTL / 30 cap, TRAP r1.15 / trigger2.0 / 3 nails / 600t lifetime / 15 dmg, MEGA per-nail 4.0 / cap 42 / 24t charge / scale 0.6→2.6 / flight 60t, RESONANCE 28 dmg / 10 TPS / 20 ticks, BLACK_FLASH 0.10 / 1.75× / 14t stagger)
- `src/main/java/jujutsu/mod/character/CharacterAbility.java:16-74` — 10 слотов `PRIMARY(0)…TERTIARY_SNEAK(9)` append-only
- `src/main/java/jujutsu/mod/character/nobara/NobaraAbilityRouter.java:29-73`, `src/main/java/jujutsu/mod/character/todo/TodoAbilityRouter.java:20-43`, `src/main/java/jujutsu/mod/character/megumi/MegumiAbilityRouter.java:21-52` — роутеры exhaustive без `default`
- `src/client/java/jujutsu/mod/client/character/nobara/NobaraClientDefinition.java:25-129` — `rosterEntry` 5 abilities, `hudSlots` 5 cells, ACCENT `0xFFE48A36`
- `src/client/java/jujutsu/mod/client/character/todo/TodoClientDefinition.java:22-147` — `rosterEntry` 6 abilities, `hudSlots` 6 cells, ACCENT `0xFFA56CFF`, `maxCooldownTicks TERTIARY Math.max(10,60)=60`
- `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:60-144` — `rosterEntry` 6 abilities, `hudSlots` 5 cells (HOLD без ячейки), `maxCooldownTicks` via `Math.max(RECALL,DEATH)=600`

**VFX language и читаемость:**
- `src/main/java/jujutsu/mod/vfx/NobaraVfxIds.java:1-57` — 24 LIVE ids, `CASTER_ACTION` multiplex intensity 1/3/4/5
- `src/main/java/jujutsu/mod/vfx/TodoVfxIds.java:1-69` — 10 LIVE ids, `BOOGIE_WOOGIE/FEINT_TELL/PAIR_MARK/SWAP_AFTERIMAGE/SWAP_ARRIVAL/MOMENTUM_STRIKE/STONE_THROW/STONE_VANISH/TRIPLE_SWAP`
- `src/main/java/jujutsu/mod/vfx/MegumiVfxIds.java:1-43` — 15 LIVE ids, 5 Dogs + 7 shadow + 3 drop
- `src/client/java/jujutsu/mod/client/vfx/nobara/NobaraVfxRecipes.java:83-96,280-321,509-573` (через grep/read других срезов) — перегруженный `CASTER_ACTION`, sound-per-cue 4-5 simult
- `src/client/java/jujutsu/mod/client/vfx/todo/TodoVfxRecipes.java` + `src/client/java/jujutsu/mod/client/vfx/megumi/MegumiVfxRecipes.java` — проверены через cross-refs
- `src/client/java/jujutsu/mod/client/vfx/VfxDirector.java:1-192` — `RECIPES` map duplicate check, `receive`/`startResolvedCue` + `VfxTimeline.isExpired`, `registerHudContribution`, 7 каналов

**Ввод, роутинг, сервер-авторитет:**
- `src/client/java/jujutsu/mod/client/input/JujutsuKeybinds.java:58-269` — `KeyMapping R/B/V` (id `nobara_hairpin_*` legacy), `tickSecondTechnique` hold 6t buffer, `sendCharacterAbility` с `character.id()` claim, `isTechniqueWeapon` хардкод
- `src/main/java/jujutsu/mod/character/CharacterAbilityExecutor.java:15-36` — gate `NONE` + `isReady` → `definition.tryCast`
- `src/main/java/jujutsu/mod/character/CharacterAbilityCooldowns.java:22-70` — `Key(player,character,ability)`, `READY_AT HashMap`, ` SERVER_STOPPING` clear
- `src/client/java/jujutsu/mod/client/character/ClientAbilityCooldowns.java:11-57` — mirror, `isReady` suppress, `remainingTicks` math
- `src/main/java/jujutsu/mod/network/JujutsuNetworking.java:31-97` — `handleCharacterAbility` claim-check `byId(characterId)!=selected`, `broadcastVfxCue`/`sendVfxCue` radius-filtered
- `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkMegaNailRuntime.java:137-231` — `onMegaNailImpact` setDeltaMovement bypass KNOCKBACK_RESISTANCE, `clampIntensity` 1..7
- `src/main/java/jujutsu/mod/character/megumi/MegumiShadowTrapRuntime.java:46-219` — `SNAP_TO_GROUND 3.0`, cylinder `r2.6 h -1..+1.6`, grip `MOVEMENT_SPEED -75%` re-apply 8t
- `src/main/java/jujutsu/mod/character/megumi/MegumiShadowMoveRuntime.java:55-170` — `SINK 8 / HIDDEN 4 / EMERGE 6 / SUBMERGE 50`, `BACKSTEP 1.75`, `ALLOW_DAMAGE` swallows fire/lava in HIDDEN/SUBMERGED
- `src/main/java/jujutsu/mod/character/megumi/MegumiShadowDropRuntime.java:49-190` — `DROP 20t telegraph`, `fall()` walk-down guard `canBeReplaced && fluidEmpty`, `disableDrop()`
- `src/main/java/jujutsu/mod/character/nobara/projectjjk/SelfResonanceRuntime.java:26-114` — `SELECTED/PENDING` maps, `NEEDS_SELECTION` → `CurseLinkOptionsPayload`, `PENDING dueGameTime` impactTick, `SELF_DAMAGE 6 / LINKED 18`
- `src/main/java/jujutsu/mod/character/nobara/projectjjk/NailTrapRuntime.java:47-120` — `PLACEMENT_RANGE 8.0`, `TRAP Radius 1.15 fallback`, `consumeNails 3`, `addFreshEntity` fail-refund

**HUD / презентация / звуки (product feel):**
- `src/client/java/jujutsu/mod/client/hud/AbilityHud.java:30-195` — `CELL/GAP/RADIUS`, `DRAG` via GLFW polling, `overlay fraction remaining/total`, `MSDF` labels
- `src/client/java/jujutsu/mod/client/character/megumi/MegumiCooldownHud.java` + `TodoStatusHud.java` + `NobaraTargetHud.java` — проверены через `codegraph_explore` и cross-refs
- `src/main/java/jujutsu/mod/registry/JujutsuSounds.java:8-73` — 24 SoundEvent `createVariableRangeEvent`, `sounds.json` 1:1
- `build.gradle:1-759` — `qualityGate` composition, `verification` group 29 JavaExec, `gametest`/`clientGameTest` lanes, `mcpdev` conditional dormancy
- `audit/ultra-2026-08-27/*.md` — 25 предыдущих срезов (01–25 + 42) для кросс-проверки, чтобы не дублировать

### 2) Находки

| severity | файл:строка | описание | рекомендация |
|---|---|---|---|
| **major** | `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkNobaraLoadout.java` (вызывается из `src/main/java/jujutsu/mod/character/nobara/NobaraDefinition.java:55-59` `onSelected → ensureStarterTools`) | **Idempotent starter restore каждый `select` — no-grind обходит себя и даёт бесплатный рефилл.** `ensureStarterTools` добивает недостающий `STRAW_DOLL_HAMMER`/`STRAW_DOLL`/`PROJECTJJK_HAIRPIN_NAIL` (до 3 штук) на каждый `select`, не только первый. Product-принцип `AGENTS.md:54` «restored idempotently … so re-selecting restores a kit lost to death» принят как фича, но это же — **бесплатный фарм**: потратил 3 гвоздя на `NailTrapRuntime.tryPlace:73` (`countNails<3` → fail), свитчнул в `NONE` и обратно в `NOBARA` — гвозди вернулись без кд, без дропа с моба, без крафта. При `CharacterAbilityCooldowns` 0 ticks на trap (cost — гвозди) это единственный «ресурс» Нобары, и его можно обнулить двумя кликами в меню (N). Нарушает и `no grind by default`, и «small high-impact» (цену способности можно стереть UI-манипуляцией, не геймплеем). | Ввести цену, которую `select` не стирает: (a) гвозди как `DataComponent` inventory — `onSelected` пополняет только если `countNails==0` **и** `TRAPS` пуст и `MEGA` не в полёте (т.е. не mid-setup), или (b) дать trap cooldown `10-20t` на `SECONDARY_SNEAK` (сейчас 0, HUD denominator 0) + оставить рефилл только на death, не на каждый select. Решение — продукт-выбор, не техфикc; зафиксировать в `AGENTS.md:49-50` как intent: «re-select не рефиллит расходуемый ресурс в бою». |
| **major** | `src/client/java/jujutsu/mod/client/input/JujutsuKeybinds.java:112-114` + `src/main/java/jujutsu/mod/character/todo/TodoFakeClapRuntime.java` + `docs/KNOWN_ISSUES.md:41-53` | **Feint pose leak — readable counterplay сломан на уровне инпута, VFX-безупречность бесполезна.** `Shift+R` шлёт `PRIMARY_SNEAK` только когда `isShiftKeyDown()==true` (`slot()` `JujutsuKeybinds:235`), а `Pose.CROUCHING` синхронизируется всем трекающим клиентам. Итог: стоя = real, присел = feint, читается без единой cue/sound/timing разницы (закрыт в `TodoBoogieWoogie.md:140-143`). `KNOWN_ISSUES.md:41` помечает как accepted tradeoff, но product-принцип `distinct VFX language` + `readable counterplay` здесь ловят друг друга: VFX-канал уже indistinguishable (`emitClapPerformance` общий), а инпут-канал — distinguishable by design. Чем лучше VFX, тем обиднее что решает поза. | Рoadmap-решение, не хотфикс: добавить четвёртый `KeyMapping` для feint (напр. `F` или `key.jujutsumod.todo_feint`) и держать `R` always real. Цена — +1 бинд в двухкнопочный кит, выгода — feint становится честным mind-game (как записано в `KNOWN_ISSUES.md:51` как cheapest fix). Если бинд неприемлем — инвертировать: `R` = feint, `S+R` = real (тогда tell на real, но хоть cheaper cast скрыт). Не добавлять hold/threshold — `KNOWN_ISSUES.md:52` уже отклонил как delay real swap. |
| **major** | `src/main/java/jujutsu/mod/vfx/NobaraVfxIds.java:30-36` + `src/client/java/jujutsu/mod/client/vfx/nobara/NobaraVfxRecipes.java:83-96,509-543,546-573` + `src/client/java/jujutsu/mod/client/character/nobara/NobaraClientDefinition.java:27` `ACCENT=0xFFE48A36` | **Multiplex `CASTER_ACTION` ломает distinct VFX language.** Один id `nobara/caster_action` + `intensity` мультиплекс на 4+ анимации: `1=hairpin/mega_nail`, `3=trap`, `4=hammer_embedded_drive`, `5=mega_nail` (см. `NobaraVfxIds:33-36` константы). Рецепты ветвят по `intensity` вместо отдельных `ResourceLocation` — это экономит id но убивает язык: оверлей дебага (`VfxDirector:UNKNOWN_EFFECT_IDS`) не отличит сломанный trap от сломанного hairpin, `VfxRadiusContractTest` не может задать разный `deliveryRadius` (сейчас `64.0` на всё в `ProjectJjkNobaraProfile`/`NailTrapRuntime:48`), а художнику нельзя дать trap-у свой звук/тайминг не трогая hairpin. При этом vessel `ACCEPT 0xFFE48A36` оранжевый вообще не появляется в рецептах — все burst/ring циан `0x2CE8F5` (`PROJECTJJK_CYAN` `NobaraVfxRecipes:48`/`ProjectJjkRitualRuntime:49`), т.е. accent палитры и VFX палитры разъехались. | Развести `CASTER_ACTION` на честные ids: `nobara/caster_hairpin`, `nobara/caster_trap`, `nobara/caster_hammer_drive`, `nobara/caster_mega_charge` (append-only, старые `1/3/4/5` оставить алиасом один релиз). Дать trap свой `VFX_DELIVERY_RADIUS` (меньше, `40.0` как у `HAMMER_ACTION_PRESENTATION_RADIUS`) и свой `firstPersonStyle`. Синхронизировать ACCENT с VFX: либо перекрасить циан в тёплый `0xFFE48A36` accent, либо честно записать в `NobaraClientDefinition` что циан — её VFX-язык, а оранж — лишь ClickGui shell (сейчас это нигде не сказано). |
| **major** | `src/client/java/jujutsu/mod/client/character/todo/TodoClientDefinition.java:82-92` + `src/main/java/jujutsu/mod/character/todo/TodoProfile.java:82-84` + `src/main/java/jujutsu/mod/character/todo/TodoAbilityRouter.java:38` + `src/main/java/jujutsu/mod/character/CharacterAbilityCooldowns.java:27-33` | **TERTIARY slot sharing вводит в заблуждение и блокирует следующий каст.** `TERTIARY` ветвит внутри `TodoStoneRuntime.tertiary()` по `shouldThrow` на throw (`10t anti-double-click`) vs `self-swap` (`60t`). Оба стартуют кд на одном ключе `(player,TODO,TERTIARY)` (`CharacterAbilityCooldowns.start` key `(player,character,ability)`): бросил камень — 10t кд на slot, HUD рисует `10/60=16%` (`AbilityHud:103-106` `remaining/total`), игрок видит «почти готов» и жмёт `V` ожидая throw (но камень уже в полёте, нужен swap) — получает `cooldown` из `CharacterAbilityExecutor:27-33`. Наоборот, `60t` denominator делает throw визуально «дешёвым», хотя follow-up `V` self-swap будет embargоed 10t. Это прямое нарушение `readable counterplay` и `distinct VFX language` — один icon/label (`stone_throw` "V") должен читаться как два разных действия. | Развести слоты: throw остаётся `TERTIARY(8)` (`10t`), self-swap уходит на `TERTIARY_SNEAK(9)` или новый `TERTIARY_HOLD` — но stone rework уже занял `TERTIARY_SNEAK` под `target_swap` (Shift+V), так что честный путь — сделать stone kit 3-слотовым: `V=throw`, `S+V=self_swap`, `TERTIARY_SNEAK` уже `target_swap` — т.е. разнести throw и self-swap на разные `CharacterAbility` (как `SECONDARY` vs `SECONDARY_SNEAK` у pair/triple). Если слоты дороги — хотя бы дать HUD per-cast denominator (а не `Math.max(10,60)`) и отдельный `AbilityCooldownPayload` на каждую ветвь, чтобы `remaining` читался честно. |
| **major** | `src/main/java/jujutsu/mod/character/megumi/MegumiShadowMoveRuntime.java:57-170` (особенно `ALLOW_DAMAGE` хук `≈:140-160` + `HIDDEN/SUBMERGED` фаза) + `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:62` `SUBMERGE_MAX_TICKS=50` + `Jujutsu Kaizen/.../Megumi-shadow-kit.md:26` | **Submerge fire/lava immunity — 2.5s полной неуязвимости, которая читается как «ушёл в тень», но контрится ничем.** `ServerLivingEntityEvents.ALLOW_DAMAGE` в `HIDDEN/SUBMERGED` отказывает «всё что не bypasses invulnerability»; `Known Issues` и `Megumi-shadow-kit.md:26` честно пишут «fire and lava deliberately swallowed for ≤2.5s window (mobility payoff priced by 10s cooldown)». Product-вопрос: это — единственный defensive/mobility у Megumi, и он же даёт **лучший defensive в ростере** (Todo 50% stagger resist, Nobara 0; тут — 0 урона 50 ticks). В `private 1-2 player` ок, но как только роадмап дойдёт до public PvP (target `AGENTS.md:239` «private play for 1-2 people») это становится must-nerf. Плюс `AGENTS.md:265` требует «at least one defensive» — тут их два (Trap slow + Move invuln), но Move перекрывает Trap. | Записать в product-долг как баланс-лимит: (a) держать swallow только для `HIDDEN(4t)+SINK(8t)+EMERGE(6t)=18t`, а в `SUBMERGED` пускать fire/lava с `*0.5` вместо `*0` (мобильность остаётся, халявной «нырни в лаву» нет); или (b) оставить как есть но поднять `SUBMERGE_COOLDOWN_TICKS 200→300` и дать submerge отдельный `HudSlot` с видимым кд (сейчас `SECONDARY_SNEAK` denominator `Math.max(120,200)=200` скрывает разницу tap vs hold — та же проблема что у Todo). Зафиксировать выбор в `MegumiProfile` комментом как product decision, не как TODO. |
| **major** | `src/main/java/jujutsu/mod/character/todo/TodoProfile.java:50-68` + `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:40-41` + `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkNobaraProfile.java:74-88` + `Jujutsu Kaizen/.../Todo-Boogie-Woogie.md:272-289` | **Long cooldowns как единственный баланс-рычаг — прямо против `AGENTS.md:278`.** Todo: `BOOGIE 60t (3s) + PAIR 100t (5s) + TRIPLE 160t (8s) + STONE_TARGET 100t`. Megumi: `TRAP 200t (10s) + PACK_DEATH 600t (30s) + RECALL 240t (12s)`. Nobara: `MEGA per-nail cap 42 + RESONANCE global 10 TPS/20t`. Большинство способностей балансится только кд, без других лимитов (гвозди — исключение, и то фармится, см. #1). `AGENTS.md:278` прямо просит избегать «Long cooldowns as the only balance lever», а `Risks-and-tech-debt.md:24` требует профильтровать ClickGui прежде чем «ещё кд». Продуктово это — `no grind` ок, но `small high-impact` страдает: 10s trap и 30s dogs чувствуются как «ждёшь таймер», а не «читаешь момент». | Для следующего баланса-патча ввести вторые рычаги вместо +секунд: Todo `PAIR_SELECTION_TTL 100t` уже есть — добавить LOS-tick requirement для commit (держи цель в прицеле 5t); Megumi `DOGS` — лимит одновременных pounce (1, сейчас 2× независимо) и `SIC_COOLDOWN 30t` уже есть — снизить `PACK_DEATH 600→360t` но дать dogs 40 HP вместо 60; Nobara `TRAP` — лимит 1 trap на owner (уже) + arm delay 12t (уже `NAIL_TRAP_PLACED 12 private`?) — поднять `TRIGGER_RADIUS 2.0→2.2` но снизить `DAMAGE 15→10` и дать `HEAVY_STAGGER 14→8`. Всё в `*Profile` с причиной в комменте, не «ещё кд». |
| **major** | `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:36-45` (`SHADOW_TRAP_RANGE 20.0 / RADIUS 2.6 / VERTICAL_REACH 1.6 / DURATION 100t / GRIP -75% / JUMP×0`) + `src/main/java/jujutsu/mod/character/megumi/MegumiShadowTrapRuntime.java:108-155` | **Shadow Trap — area denial без читаемого конца: контрить нечем, кроме «выйти пешком на 25% скорости».** Pool статичен 100t (5s), центр никогда не двигается, `gripBodies` каждый тик re-apply `MEGUMI_SHADOW_GRIP` 8t (`ADD_MULTIPLIED_TOTAL SPEED -0.75, JUMP×0`). Соперник внутри цилиндра `r2.6 h -1..+1.6` (`insideCylinder:158-165`) сохраняет четверть скорости и 0 прыжка, но **никакой клинза** (milk/`/effect clear` снимет только текущий 8t instance, следующий тик вернёт). Это нарушает `readable counterplay` — trap не телеграфирует край (только `SHADOW_TRAP_ZONE` pulse 40t и `GRIP` motes 20t) и не даёт активного контрплея кроме «иди медленно». Для `private 1-2` ок, для роадмапа к PvP — будет «поставил и забыл». | Дать trap активный выход: (a) прыжок с таймингом снимает grip на 10t (как `CombatStagger` на 6t) — читаемый скилл-чек; или (b) 2 попадания по центру pool ломают trap досрочно (`closeTrap` уже есть). Визуально — усилить `SHADOW_TRAP_ZONE` intensity scale (сейчас `radiusIntensity()=round(2.6*10)=26` несёт радиус, но не оставшееся время; добавить fade `alpha 255→80` over 100t чтобы конец читался). Записать в `MegumiProfile` как product decision. |
| **minor** | `src/client/java/jujutsu/mod/client/character/nobara/NobaraClientDefinition.java:42-59` + `src/client/java/jujutsu/mod/client/character/todo/TodoClientDefinition.java:37-56` + `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:38-62` + `src/client/java/jujutsu/mod/client/input/JujutsuKeybinds.java:42-54,192-211` | **Roster и HUD врут про цену hold-жестов.** Три vessel используют один hold-порог `SECONDARY_HOLD_THRESHOLD_TICKS=6` (`JujutsuKeybinds:54`, `ProjectJjkNobaraProfile:TRIPLE_HOLD_TICKS=6`), но HUD/ростер показывают одну ячейку на slot: Nobara `SECONDARY_SNEAK` = `nail_trap` "S+B" (tap, на деле `SECONDARY_SNEAK` — `NailTrapRuntime.tryPlace` мгновенно, без холда), Todo `SECONDARY_SNEAK` = `triple_swap` "S+B" (тоже tap, `TodoPairSwapRuntime` без холда), Megumi `SECONDARY_SNEAK` = `shadow_move` "S+B" (и tap backstep/free-step, и hold submerge на `SECONDARY_SNEAK_HOLD(6)/RELEASE(7)`). Игрок жмёт `S+B` и получает три разных feel: мгновенно (Nobara/Todo) vs «держи 6t или tap на релизе» (Megumi `JujutsuKeybinds:161-189` `sneakSecondHeldTicks` buffer). `AbilityHud:103-106` ещё и считает `remaining/total` где `total = maxCooldownTicks(SECONDARY_SNEAK)` — для Megumi `Math.max(120,200)=200` (tap 120t vs hold 200t) делает tap-HUD «наполовину готов» сразу. | Унифицировать контракт hold'а на продукт-уровне: либо (a) все `S+B` — tap-on-press (и перенести Megumi submerge на `S+V` или `TERTIARY_SNEAK` — но `V` уже `DROP`), либо (b) все `S+B` — tap-on-release (тогда Nobara/Todo тоже 6t delay, но предсказуемо). Дешёвый честный фикс роадмапа: ростер и HUD для Megumi показывают `shadow_step "S+B tap"` + `submerge "S+B hold 0.3s"` как две строки на один slot (как Todo показывает `stone_throw V` + `stone_swap S+V` на двух слотах), а `maxCooldownTicks` развести на `SHADOW_STEP 120t` vs `SUBMERGE 200t` с per-cast denominator (как у Todo TERTIARY fix #4). |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoDefinition.java:30-39` (`TODO 1.50 dmg / 0.85 aspd`) + `src/client/java/jujutsu/mod/client/character/todo/TodoClientDefinition.java:82-92` + `Jujutsu Kaizen/.../Todo-Boogie-Woogie.md:272-289` — sweep-refund/milk-caveat | **Todo melee fantasy «тяжёлый боец» не сходится с цифрами без дисциплины игрока.** `MELEE_DAMAGE 1.50` + `SWAP_MOMENTUM 1.25` + `BLACK_FLASH 1.75` — мультипликативны через `ATTACK_DAMAGE ADD_MULTIPLIED_TOTAL`, но `JujutsuKeybinds:262-268` требует пустых рук для любого clap (`TodoSwapGates`). То есть чтобы получить `×1.25 stagger 8t` нужно: clap с пустыми руками → 24t окно → успеть достать меч из хотбара → попасть. В `DOGS 60 HP / 3 dmg` мир это — «окно почти всегда на кулаках `1.875 dmg = 0.375` прироста ≈ пятая сердца» (`TodoProfile:91-100` коммент честно пишет «under a fifth»). `Jujutsu Kaizen/.../Todo-Boogie-Woogie.md:286-287` признаёт это как accepted limit + sweep сохраняет буст на secondary victims после spend. Продуктово — defensive/mobility есть (swaps), но payoff читается слабо без F3+H. | Не менять цифры до смока: текущая tuning сознательно делает `stagger 8t` payload'ом, а `0.375` dmg — шумом (коммент `TodoProfile:97-99` «The stagger is the payload»). Роадмап-задача — `runClient` smoke измерить `displace→arm→hit` loop в 24t окне (реальный игрок с мечом в хотбаре), и только если <30% успевают — дать `SWAP_MOMENTUM_WINDOW 24→30t` (как записано в `KNOWN_ISSUES.md:59` reopen cond). Не баффать `1.25→1.50` — это вернёт sweep-дюп (#05 в audit 14). |
| **minor** | `src/main/java/jujutsu/mod/vfx/NobaraVfxIds.java:37-42` + `src/main/java/jujutsu/mod/vfx/TodoVfxIds.java:63-66` + `src/main/java/jujutsu/mod/vfx/MegumiVfxIds.java:35-42` + `build.gradle:230-310` | **Nobara VFX-раздута: 24 ids vs Todo 10 vs Megumi 15 — «quality over quantity» нарушает сама себя.** `NobaraVfxIds.LIVE 24` (hammer/impact/detonate/enlarge/explosion/black_flash/self_resonance/nail_deepen/nail_trap_* /caster_action/mega_* …) — больше чем у Todo+Megumi вместе по плотности на 5 слотов vs 6/5. Часть ids — легаси `HAMMER`/`IMPACT`/`IMPACT_SOUND` vs `HAMMER_HORIZONTAL`/`HAMMER_OVERHEAD`/`HAMMER_NAIL_LAUNCH` (3 ветки hammer на один `ATTACK_CONTEXT` — `NobaraVfxRecipes:269-278,323-367` `hammer()` + `hammerAction()` дублируют `hammer_nail_launch`). Product-риск роадмапа: новый vessel будет копировать Nobara как template (AGENTS.md:15 «Nobara is the template character») и унаследует её 24-id baggage, а Todo доказал что kit можно выразить 10 ids. | Для следующего vessel взять за template `TodoVfxIds` (10) или `MegumiVfxIds` (15), не `NobaraVfxIds` (24). В Nobara зарефакторить без wire-break: схлопнуть `HAMMER/IMPACT/IMPACT_SOUND` в `HAMMER_NAIL_LAUNCH` (старые оставить как alias один релиз, `PLANNED`→`LIVE` миграция через `VfxCues`), `CASTER_ACTION` развести как в #3 — это срежет 4–5 ids и сделает её kit «small high-impact» и в VFX. Метрика в `00-MOC.md:46` `Nobara VFX ids 24` — обновлять её только после схлопа, не маскировать. |
| **minor** | `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:81-101` + `src/main/java/jujutsu/mod/character/megumi/MegumiShadowDropRuntime.java:156-190` | **Shadow Drop — гриф-потенциал через `disableDrop()` + `fall()` без проверки `canBeReplaced` на стороне продукта.** Код `MegumiShadowDropRuntime:183-185` уже делает `block.disableDrop()` и `spawnPosFor` walk-down с `canBeReplaced && fluidEmpty` (урок R1 `CURRENT_STATE.md:75`), но продуктово это — 1–3 `FallingBlockEntity` (sand/gravel/clay/anvil) с `DROP_ANVIL_DAMAGE_MAX 40` и `SOFT_MAX 5`, спавнящиеся над головой цели и летящие через потолок пещеры вниз к цели. В креативе/привате ок, но роадмап к паблику требует решения: anvil с 40 dmg через шлем/броню знакомо ванильщику (`MegumiProfile:86` коммент), а 3 anvils подряд — уже `no grind` vs «фарм anvils падением»? `DROP_COOLDOWN 60t (3s)` делает это спам-способностью. | Оставить механику, но дать серверу `gamerule jujutsumod:allowAnvilDropDamage true/false` (дефолт true для текущего `private 1-2` target, false для паблика) — это продукт-переключатель роадмапа, не баланс. Плюс поднять вес anvils обратно если надо: сейчас `30/25/25/20` near-flat (`MegumiProfile:93-96` — первый slice 40/30/20/10 читался как "always sand", теперь anvil 20%); smoke должен проверить что anvil — highlight, а не редкость ради редкости. |
| **minor** | `src/main/resources/assets/jujutsumod/lang/en_us.json` + `ru_ru.json` + `docs/KNOWN_ISSUES.md:202-210` (E5) + `Jujutsu Kaizen/.../Risks-and-tech-debt.md:23` | **Readble counterplay ломается на языке: parity не enforced, `Self Resonance "Self Resonance"` латиницей в `ru_ru`.** Оба `lang` файла сейчас `95 keys diff empty` (E5 verified 2026-07-26), но `nothing in check compares` (E5) — следующий `en_us` ключ тихо уедет, `ru_ru` покажет fallback английский. Конкретный продукт-баг: `message.jujutsumod.nobara.self_resonance.selected` оставляет `Self Resonance` латиницей в русской строке (`ru_ru.json:≈ line 20+ leading-comma style`), а `screen.jujutsumod.modern_menu` — `Characters` vs `Выбор сосуда` (`KNOWN_ISSUES.md:208`). Это не косметика: `readable counterplay` включает `actionbar`/`chat` диагностику (`NailTrapRuntime:65,69,73,88` `trap.no_ground/unsupported/no_nails/failed`, `SelfResonanceRuntime:49` `no_link`, `TodoSwapGates` messages) — если ключ без перевода, русский игрок видит английский fallback. | Добавить `VfxRadiusContractTest`-стилем `LangParityTest` в `test` (scan `assets/jujutsumod/lang/*.json` keys set equality + format-specifier equality) и прицепить к `verification` (динамически как `auditDocumentation`). Это 30 строк, закрывает E5 навсегда. Перевести `Self Resonance` → `Саморезонанс` (или `Резонанс на себе`) и выровнять `Characters` ↔ `Выбор сосуда` (английский → `Vessel Select` как уже в `ru_ru`, или наоборот). |


### 3) Позитивные наблюдения

- **Quality over quantity — выдержан железобетонно.** 3 vessel + NONE на `Fabric 1.21.8 / Java 21 / mod id jujutsumod` (`AGENTS.md:3-11`), каждый со своим `*Definition` + `*ClientDefinition` + `*Profile` + `*AbilityRouter` exhaustive без `default` (`AGENTS.md:36`), плюс `CharacterAbility 0..9 append-only` (`CharacterAbility.java:16-54`). Новый vessel — `1 enum + 1 server def + 1 client def + 1 строка в каждом registry` (`How-to-add-next-character.md:8-10`), без правок shared dispatch. Это именно `small high-impact`: Todo доказал reuse (`TodoProfile` single source `AGENTS.md:88`), Megumi доказал `no universal summon abstraction` (`AGENTS.md:287`).
- **Distinct VFX language — в целом образцово, с оговоркой по Nobara.** `VfxDirector:22-54` — 7 каналов (`WORLD/HUD/CAMERA/FIRST_PERSON/PARTICLES/SOUND/POST_PROCESS`), `RECIPES.putIfAbsent` duplicate check, `UNKNOWN_EFFECT_IDS` warn-once; каждый vessel регистрирует свой `*VfxRecipes` из `registerClientHooks()` (`JujutsuCharacterClients:61-65` loop). Todo: `SWAP_AFTERIMAGE/SWAP_ARRIVAL/MOMENTUM_STRIKE/STONE_THROW/STONE_VANISH/TRIPLE_SWAP` — каждый с разным shape/timing/sound; Megumi: void-black pool (`alpha 255` всю жизнь, close-fade 255→0) + `SHADOW_RIPPLE` TTL fail-open + `DOGS_SUMMON/DOGS_POUNCE` 5 dogs vs 7 shadow — палитры не пересекаются (Todo violet `0xA56CFF`, Megumi near-black, Nobara циан/оранж). `VfxDirector:170-174` `bindLevel` + `DISCONNECT→reset` lifecycle — transient VFX без утечек.
- **Defensive/mobility per character — каждый имеет, и разные:** Nobara `Nail Trap` (area denial `r1.15 fallback r2.0 trigger`, `600t lifetime`, `15 dmg`, `12t interrupt`) — defensive без мобильности, компенсируется `Hammer` contextual melee; Todo — весь defensive = mobility (5 swap'ов, `STRICT` vs `SOFT` destination policy `AGENTS.md:90-99`, `SAFE_POSITION r1.0 h3`); Megumi — `Shadow Trap` (slow) + `Shadow Move` (3 режима: backstep `1.75`, free-step `24.0 range`, submerge `50t`) + `Shadow Drop` telegraph `20t` (`AGENTS.md:32-33`). `AGENTS.md:265` требование закрытия — выполнено, и механически разнообразно.
- **Readable counterplay — сильные места:** `VfxCore` contract `AGENTS.md:239-246`, `TodoBoogieWoogieRuntime.findSafeDestination` без `noBlockCollision` fallback на `STRICT` (отмена всего каста вместо half-apply — `AGENTS.md:95-98`), `SwapCommitTeleport` rollback, `TargetResolver` pierced-before-grazed + angularOffset + id-tiebreak (`KNOWN_ISSUES.md:127-133`), stone — straight slow line `0.23 b/t` no gravity no damage (`TodoProfile:70-77`), Megumi trap `grip -75%` re-apply `8t` (выход пешком, не стан — `AGENTS.md:33`), Drop `20t` telegraph + following disc (`MegumiShadowDropRuntime:135-143` re-anchor). Gate truth table `TodoSwapGates` (`AGENTS.md:100-103`) — один `evaluate` на real и feint.
- **No grind by default — соблюдён.** `AGENTS.md:270` «no grind by default» + `53` «No cursed-energy resource bar» — ни CE bar, ни уровни, ни крафт-рецепты (`KNOWN_ISSUES.md:291` «Crafting recipes intentionally absent»), ни `Public-release blockers` не требуют прогрессии. Rest 3 vessel'ов — чистый skill, не фарм. `MegumiSummonRuntime` 16t/12t materialize/recall, `PACK_DEATH 600t` — price времени, не ресурса.
- **Roadmap smoke polish vs new vessel — процесс зрелый.** `How-to-add-next-character.md:1-13` требует design approval до кода, `Brainstorming Gate` `AGENTS.md:173-182`, `SESSION.md:23-27` Next candidates честно ставит `visual polish pass` перед `new vessel (Yuji/Maki)` и отдельным `E5 parity`, `vessel-smoke-runner` skill (`jujutsu_vessel_select/cooldowns_clear/state_get/view_capture`) и `runClientGameTest` lane (`build.gradle:52-60` non-required by design) — инфраструктура для smoke polish уже есть (PR #62-#67, smoke 43 кейса 0 real failures `CURRENT_STATE.md:41`). `MCP spike PASS архитектура A` (`CURRENT_STATE.md:24-30`) даёт автономный `quickPlay` без меню.
- **Server-authoritative — без дыр на входе.** `CharacterAbilityPayload` stamped `character.id()` + сервер проверяет `byId(payload.characterId()) != selected` → `UNHANDLED_FAILURE` (`AGENTS.md:36`, `Networking.md`), `CharacterAbilityExecutor` gate `NONE` + `isReady` до `definition.tryCast`, `VfxCue` visual-only (`VfxDirector:72-86`), `MEGUMI_SHADOW_GRIP` и `TODO_SWAP_MOMENTUM` — чистые ванильные `MobEffect` с `ADD_MULTIPLIED_TOTAL` модификаторами без кастомных tick-хаков (ре-применение каждый тик, `8t` expiry сам).
- **Баланс-механики — осмысленны в деталях:** `NailTrapRuntime:86-91` fail → `refundNails`, `MegumiSummonRuntime` safe teleport только на `loaded floor-supported collision-free non-fire/non-lava` внутри `r3` (`AGENTS.md:37`), `Todo Stone` `noSave()` + `updateInterval 2` + 7 exit'ов в `TodoStateLifecycle.dropEverything` (`TodoDefinition:73-76`), `Megumi Dogs` 60 HP / 0.34 speed / 80t pounce cooldown (`MegumiProfile:5-31`) — цифры в одном `*Profile`, не размазаны.
- **Документация честна.** `KNOWN_ISSUES.md` ведёт `Accepted product decisions` (Resonance hit-stop global, feint pose leak, momentum sweep) как decisions, не как баги; `Risks-and-tech-debt.md` — single owner `AGENTS.md → Codex → KNOWN_ISSUES`; `SESSION.md` prose vs `qualityGate` gates — честные `Verified 2026-07-29` штампы.

### 4) Вердикт

**PASS WITH CAVEATS**

**Обоснование.** Продукт держит главные обещания `AGENTS.md:19-23` — `quality over quantity` (3 доведённых vessel вместо «гигантского реестра до проверки» `AGENTS.md:279`), `distinct VFX language` (Todo violet edge vs Megumi void-black vs Nobara циан, 7 каналов `VfxDirector`), `defensive/mobility per character` (каждый имеет, и разные), `readable counterplay` (telegraph 20t, `STRICT` preflight, pierced-first resolver, slow stone), `no grind by default` (0 прогрессии, 0 CE bar), `small high-impact` (5–6 слотов, каждый меняет позицию/контроль, не «ещё +10% урона»). Vessel Seam, VFX Core, server-authoritative инварианты (`wire ids append-only`, `registerServerHooks/registerClientHooks` loop) не эродировали — продукт можно расширять не ломая.

**Почему не PASS.** 7 **major** находок — каждая бьёт в продукт-принцип напрямую: (1) no-grind обходится рефиллом через `select`, (2) feint pose leak ломает `readable counterplay` на инпуте, (3) `CASTER_ACTION` multiplex ломает `distinct VFX language`, (4) TERTIARY sharing ломает `readable counterplay` на HUD, (5) submerge fire/lava swallow и (6) long-cooldowns-only баланс — отложенные PvP-долги, (7) trap без активного контрплея. Ни одна не требует отката фичи, но каждая требует продукт-решения до следующего vessel/публичного PvP — иначе роадмап повезёт долги дальше.

**Почему не FAIL.** Критических (краш/десинк/утечка/провенанс-блокер релиза `R1-R3`) — ноль в этом срезе; все major — баланс/читаемость/roadmap, не контракт. Smoke-инфра (`runGameTest` + `runClientGameTest` + MCP quickPlay + `verifyAssertionsEnabled` 29 программ) зелёная (`SESSION.md:15` `296/0 + 34 + 29` на `d241544`), `qualityGate` держит. Product-долги записаны честно в `KNOWN_ISSUES.md`/`Risks-and-tech-debt.md` и не скрыты.

**Приоритет фиксов (roadmap):**

1. **P0 — до следующего vessel (или до мерджа `feat/nobara-target-hud` в main):** `E5 LangParityTest` (minor #12, 30 строк, закрывает `readable counterplay` на диагностике) + определиться с `starter re-fill` (major #1) — продукт-решение в `AGENTS.md:49-50` и `NobaraDefinition:55-59` (death-only vs select-only).
2. **P1 — smoke-polish волна (роадмап «smoke polish vs new vessel» → polish):** развести `CASTER_ACTION` (major #3) + развести `TERTIARY` HUD (major #4) + унифицировать hold-контракт `S+B` (minor #8) — всё `append-only` без wire-break, 1–2 дня, делает роадмап vessel'а дешевле.
3. **P2 — баланс-полиш перед пабликом:** feint keybind (major #2) + submerge/trap контрплей (major #5/#7) + long-cooldown вторые рычаги (major #6) — только после `runClient` smoke измерений, не на глаз; зафиксировать выбор в `*Profile` комментами как product decision.

Рoadmap-рекомендация: **`smoke polish` > `new vessel`.** `How-to-add-next-character.md:11-13` и `SESSION.md:23` уже ставят polish первым — поддержать: `NobaraTargetHud` screen-space (branch `feat/nobara-target-hud`) + `AbilityHud` drag + sound-mix + trap/drop telegraph читаемость → затем `Yuji/Maki` по `add-vessel` skill. Broad unfinished framework `AGENTS.md:287` прямо запрещает.

**Открытые вопросы роадмапа (требуют product decision, не кода):**

- `ShadowMove` item-use под `SUBMERGED` остаётся unlocked (`Megumi-shadow-kit.md:26`, `AGENTS.md:365` «item use … known open question») — оставлять ли drinking/eating в тени?
- `Resonance global hit-stop` (`KNOWN_ISSUES.md:13-17`) — оставлять ли global `SERVER_TICK_RATE 10` на 20 ticks при росте до public сервера, или дать client-global slow mo через `VfxTimeChannel` successor?
- `Provenance R1-R3` (`KNOWN_ISSUES.md:67-88`) — `Rich-Modern`/`neon.ttf`/`antidaunleak/UserProfile.java` — когда резать перед Modrinth/CurseForge пабликом?
- `Todo TODO_STONE` vs будущий `Yuji` thrown entity — делить ли `JujutsuEntities` lifecycle или держать per-vessel `noSave()` + `TodoTransientState` pattern?
- Новый vessel — `Yuji` (melee + Black Flash bridge) или `Maki` (weapon vessel) — какой сильнее дифференцирует ростер, не дублируя `Hammer`/`Stone`/`Dogs`?

AUDIT_ID: 50-product-roadmap
