## Stone Entity — 13-todo-stone-entity

### 1) Что проверено (файлы/символы)

**Инструменты:** `codegraph_explore` (запросы `TodoStoneEntity todo_stone stone entity`, `TodoProfile TodoStoneRef TodoStateLifecycle`, `TodoStoneRuntime TodoStoneRef throwStone selfSwap targetSwap`, `TodoStoneEntity launch snapTo clientTick endFlight`), `grep TodoStone|todo_stone|STONE_` по `src/`, `grep canUsePortal|hurtServer|fireImmune|isPushedByFluid`, `read` каждого файла ниже диапазонам. Сверка с `AGENTS.md:40-43,56-75`, `docs/TODO_STONE_REWORK.md:31-91`, `docs/KNOWN_ISSUES.md E1/E1a`, `Jujutsu Kaizen/jujutsumod-codebase-codex/03-systems/Todo-Boogie-Woogie.md`.

**Файлы среза (server-authoritative ядро):**
- `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:41-217` — entity type, `DATA_OWNER_UUID:42`, `DATA_REMAINING_TICKS:44`, `launch():60-72`, `tick():88-119`, `endFlight():128-136`, `snapTo():142-148`, `fireImmune():151-153`, `canUsePortal():156-160`, `hurtServer():167-169`, `defineSynchedData():172-175`, `addAdditionalSaveData():178-183`, `readAdditionalSaveData():186-193`, `clientTick():195-209`, `face():211-216`
- `src/main/java/jujutsu/mod/character/todo/TodoStoneRuntime.java:35-266` — `serverTick():47-67`, `tryCast():69-75`, `tertiary():77-87`, `shouldThrow():90-92`, `stoneEligibleForSwap():98-100`, `throwStone():102-121`, `selfSwap():123-169`, `targetSwap():171-237`, `resolveStone():243-248`, `withinSwapRange():250-257`, `reject():259-265`
- `src/main/java/jujutsu/mod/character/todo/TodoTransientState.java:25-120` — `STATES:40`, `stone()/setStone():58-65`, `clearStone():76-99`, `dropAll():102-105`, `clearAll():113-117`, `owners():108-110`
- `src/main/java/jujutsu/mod/character/todo/TodoStateLifecycle.java:30-66` — `register():34-55` (`AFTER_DEATH:39`, `AFTER_RESPAWN:44`, `AFTER_PLAYER_CHANGE_WORLD:45`, `DISCONNECT:47`, `SERVER_STOPPING:50`, `END_SERVER_TICK:51-54`), `dropEverything():62-65`
- `src/main/java/jujutsu/mod/character/todo/TodoStoneRef.java:15` — record `entityUuid, dimension, thrownAtGameTime`
- `src/main/java/jujutsu/mod/character/todo/TodoStonePlan.java:14-21`, `TodoProfile.java:73-90` (все `STONE_*`), `TodoSwapGates.java:49-56` (`casterStateBlocked`), `TodoAbilityRouter.java:38-39` (маршрут `TERTIARY/TERTIARY_SNEAK`), `TodoDefinition.java:42-49,74-76` (`registerServerHooks`, `onDeselected -> dropEverything`)
- `src/main/java/jujutsu/mod/registry/JujutsuEntities.java:18,28-39` — `TODO_STONE` (`sized 0.25:32`, `clientTrackingRange 96:33`, `updateInterval 2:34`, `.noSave():37`)
- `src/main/java/jujutsu/mod/vfx/TodoVfxIds.java:51-56,63-65` — `STONE_THROW`, `STONE_VANISH`, `LIVE` set; `src/client/java/jujutsu/mod/client/render/todo/TodoStoneRenderer.java:26-104` (cube `HALF_EXTENT 0.09:32`, `FADE_TICKS 10:34`, `extractRenderState:48-53`, `render:56-72`), `src/client/java/jujutsu/mod/client/character/todo/TodoStatusHud.java:36-132` (HUD chip `renderStone:36-50`, `liveStone:102-120`), `src/client/java/jujutsu/mod/client/vfx/todo/TodoVfxRecipes.java:60-61,200-241` (`STONE_THROW 6т`, `STONE_VANISH 8т`)
- `src/main/java/jujutsu/mod/character/todo/TodoBoogieWoogieRuntime.java:161-164,188-244` — `findSafeDestination(STRICT)`, `place`, `restoreMotionAndRotation` (`forceSetRotation`, `setYHeadRot`, `setDeltaMovement`, `resetFallDistance`, `hurtMarked=true`), `Strictness` enum

**Тесты и логи:**
- `src/gametest/java/jujutsu/mod/gametest/TodoStoneGameTests.java:35-2053` — S1-S21 (throw, straight flight, ignore entities, water/fire, block collision, lifetime 100, lost sweep, void, portal, stale ref, disconnect/dimension UNVERIFIED, selfSwap, targetSwap, successor) — прочитаны диапазоны `240-393` (S3/S4), `589-670` (S8 void), `683-754` (S9 portal), `813-870` (S11 death), `1895-2052` (S21 successor), `1216-1315` (S15 selfSwap), `1603-1710` (S19 targetSwap)
- `src/gametest/java/jujutsu/mod/gametest/TodoSwapTestFixtures.java:175-205` (`resolveStone`, `countStones`, `stoneState`), `src/test/java/jujutsu/mod/character/todo/TodoStoneTest.java` — unit `shouldThrow`, `stoneEligibleForSwap`, `withinSwapRange`
- `b5-gametest.log:172-179` — 7/29 failed в историческом прогоне (все `todo_stone_game_tests_*`) — использовано как триангуляция, но текущие исходники тестов уже смягчены (см. находки)
- `src/main/java/jujutsu/mod/registry/JujutsuItems.java:1-69` — проверено отсутствие `todo_stone` Item

**Поиск отсутствия inventory item:** `grep -r "todo_stone" src/main/java/jujutsu/mod/registry/JujutsuItems.java` — пусто (только `JujutsuEntities:18`); `grep -r "TodoStone" src/main/resources` — только `textures/entity/todo_stone.png` и lang ключи `hud.jujutsumod.todo.stone`; `glob src/main/resources/assets/*/models/item/todo*` — пусто; `grep CreativeModeTab` — хитов нет для Todo. Подтверждено: нет `Item`/`BlockItem`, нет рецепта, нет `creative tab` entry.

**Wire ids:** `CharacterAbility.java:52-54` — `TERTIARY(8)`, `TERTIARY_SNEAK(9)` append-only (проверено `grep CharacterAbility`).

---

### 2) Находки

| severity | файл:строка | описание | рекомендация |
|---|---|---|---|
| **major** | `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:88-119,195-209` + `src/main/java/jujutsu/mod/character/todo/TodoProfile.java:73` + `AGENTS.md:42` + `docs/TODO_STONE_REWORK.md:31` vs `src/gametest/java/jujutsu/mod/gametest/TodoStoneGameTests.java:377-383` | **Расхождение spec vs реализация по воде.** Spec: `STONE_SPEED_BLOCKS_PER_TICK 0.23` — "Constant velocity, no gravity, no arc" (`TODO_STONE_REWORK:31`). Реализация: `tick()` не переопределяет fluid-логику, `MoverType.SELF` + vanilla `updateInWaterStateAndDoFluidPushing()` применяет drag ~0.8/тик в воде. Исторический `b5-gametest.log:175` ожидал скорость `0.23` после воды и упал (`actual 0.1179`). Текущий `TodoStoneGameTests.java:380-383` теперь сознательно ожидает `0.05 < speed < 0.23` и комментарий "Water drags the stone" — тест зелёный, но `AGENTS.md:42` по-прежнему говорит "Water and fire are not terminal" без уточнения что скорость падает. Для баланса `4.6 b/s` это 50% замедления на 2 блока воды. | Либо зафиксировать контракт: добавить `@Override isPushedByFluid() -> false` + no-op fluid tick и держать `0.23` константой (честнее к "constant velocity"), либо зафиксировать текущее поведение как намеренное и уточнить `AGENTS.md:42`/`TODO_STONE_REWORK.md:31` словами "water slows but not terminates (0.8 drag, direction preserved)" и оставить `TodoStoneGameTests:380` как источник истины. Любой путь — обновить доку чтобы тест и spec совпадали. |
| **major** | `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:96` | **Void-граница `< minY` vs `<= minY`.** `if (remainingTicks <=0 || getY() < level.getMinY()) endFlight` — камень на `y == minY` (ровно на дне void) не ванишит до следующего тика когда `y` уйдёт на `minY - delta`. В `GameTest` арена с sealed `Barriers` камень никогда не падает сам, тест телепортирует `minY-5` (`TodoStoneGameTests.java:644`) — проходит. В проде падение с `MoverType.SELF` может застрять на `minY` если коллизия с `Barrier` floor, хотя обычно падает дальше. Более инклюзивная проверка `getY() <= level.getMinY()` или `getY() < level.getMinY() - 0.5` дешевле чем лишний тик ghost. | Заменить на `getY() <= level.getMinY()` (или `getY() < level.getMinY() + 0.5` если нужен запас для `Barrier`). Добавить `JujutsuMod.LOGGER.debug` с reason `void` как в `endFlight`. |
| **major** | `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:101-115` + `src/main/java/jujutsu/mod/character/todo/TodoStoneRuntime.java` + `b5-gametest.log:173` | **Ложный vanish через AABB sweep vs ray.** `ProjectileUtil.getHitResultOnMoveVector(this, entity->false, COLLIDER):101` игнорит entity для ray, но `move(SELF, delta):109` + `if (horizontalCollision || verticalCollision || lengthSqr <1e-8):110` ловит corner-clip, cobweb, spawn-inside-geometry. Исторический `stoneIgnoresEntitiesAndDoesNoDamage` падал (`expected non-null actual null` на tick40) — скорее barrier wall at `x≈8` в 8x8 арене (горизонтальный полёт `0.23*30≈7` блоков до стены) а не pig, но тот же механизм: любой block-ADJACENT (pedestal `4,1,1` под pig, obsidian frame портала `4..5`) может дать `horizontalCollision` даже когда ray `MISS`. Текущий pig-тест (`TodoStoneGameTests.java:254-312`) держит pig на `4,2,1` и камень на `y≈2.62` (0.62 над pedestal) — ray и sweep разведены, но хрупко. Аналогично `portalRefusal` (`683-754`) падал из-за obsidian frame COLLIDER — сейчас тест держит камень по центру `z=1.5` между `z=0/3` frame, и assert на tick24 вместо 40 до стены. | Развести каналы явно: оставить ray для `COLLIDER` block vanish, но `horizontal/verticalCollision` проверять только как fallback когда `distanceToSqr(hit) < 1e-6` или `isInsideWall()`. Либо задокументировать что любой `SELF` block-collision — terminal vanish "by design" (как сейчас коммент `110-112` "corner clips, cobwebs, spawn inside geometry must vanish") и считать крышкой, а не багом. Для тестов — держать slowSpeed `0.02` (как в `successor:1925`) или вертикальный полёт (`-90` pitch) для lifetime-тестов чтобы не упираться в barrier стену; что уже сделано в `S6 lifetime` (+Z вертикально). |
| **major** | `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:128-136` + `src/main/java/jujutsu/mod/character/todo/TodoStoneRuntime.java:47-67` + `src/gametest/java/jujutsu/mod/gametest/TodoStoneGameTests.java:1895-2052` | **Successor guard рассинхрон между `endFlight` и `serverTick`.** `endFlight` делает `stone(owner).map(ref->getUUID.equals(ref.entityUuid())).orElse(false)` прежде чем `clearStone` — чтобы не убить преемника B когда истекает A. `serverTick` делает `if (expired \|\| lost) clearStone` без UUID-проверки, но проверяет `level.getEntity(ref.entityUuid()) instanceof TodoStoneEntity` для *текущего* ref (т.е. B), так что A-уже-discarded не триггерит clear для B. Исторический `b5:174 successsorStoneSurvivesEndingStone @60 expected alive actual false` — камень A умер на tick ~30 от barrier стены, а не от sweep; текущий `S21:1925` вводит `slowSpeed 0.02` чтобы оба камня жили до tick110 и guard проверяется честно. Sweep-логика корректна, но двойной clock (`remainingTicks` на entity vs `thrownAtGameTime+100` на ref) может дрейфовать на 1-2 тика при лаге. | Синхронить expiry на один источник: либо в `serverTick` сравнивать `remainingTicks` из entity (если `instanceof`), либо хранить `thrownAtGameTime` и в `endFlight` тоже проверять `gameTime >= thrownAt+100` прежде чем `clearStone`. Сейчас оба пути дают один результат в норме, дрейф <4т незаметен, но для `S21` важно чтобы `S21:2008 aGone == null` именно из-за `remainingTicks` expiry A, а не sweep. Оставить slowSpeed трюк и добавить коммент в `TodoStoneRuntime:47` что sweep и entity clocks намеренно дублируются (entity для loaded chunk, sweep для unloaded/lost). |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:178-193` + `src/main/java/jujutsu/mod/registry/JujutsuEntities.java:37` | **Мёртвый NBT код при `.noSave()`.** `TODO_STONE` помечен `.noSave():37` (верно: `STONE_VANISH` вместо сохранения), но `TodoStoneEntity` реализует `addAdditionalSaveData:178`/`readAdditionalSaveData:186` (сериализуют `OwnerUuid` + `RemainingTicks`). `LevelStorage` никогда не вызывает их для `noSave` entity, методы недостижимы (кроме debug `/summon` с `persist`?). `readAdditionalSaveData` парсит `UUID.fromString(owner)` без try/catch — при битом NBT (недостижимо) краш. | Либо удалить оба метода и оставить только synched data, либо оставить с комментом `// dead code: noSave prevents persistence, kept for debug summon inspection` и обернуть `UUID.fromString` в try/catch. Линт `SourceBoundaryTripwire` не ругается — ок. |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:116-118` + `src/client/java/jujutsu/mod/client/character/todo/TodoStatusHud.java:42-49` + `src/client/java/jujutsu/mod/client/render/todo/TodoStoneRenderer.java:52` | **HUD и fade гранулярность 4 тика.** `if ((tickCount &3)==0) entityData.set(DATA_REMAINING_TICKS, remainingTicks):116` — клиентский `remainingTicks()` обновляется раз в 4т (`0.2s`). `TodoStatusHud.renderStone:43` читает его для чипа `seconds = (ticks+19)/20` и `TodoStoneRenderer:52 fadeAlpha = min(1, remaining/10)` — последние 10т фейда идут ступеньками 4т (≈0.4 изменения alpha за раз). При `STONE_LIFETIME 100` не критично, но chip может показать `5s` на 0.2s дольше или `0s` на 1т раньше. | Либо слать каждый тик последние 20т (`if (remainingTicks <=20 || (tickCount &3)==0)`), либо считать chip как `(remainingTicks+19)/20` уже делает округление вверх — ок. Оставить с комментом "HUD refresh 0.2s by design, fifth of per-tick traffic" (`TodoStoneEntity:79-82` уже объясняет). |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoStoneRuntime.java:47-67` + `src/main/java/jujutsu/mod/character/todo/TodoTransientState.java:40,108` | **O(N) sweep каждый `END_SERVER_TICK`.** `for (UUID owner: TodoTransientState.owners():51)` делает `Set.copyOf(STATES.keySet())` + `server.getLevel(ref.dimension())` + `level.getEntity(uuid)` на каждого Todo онлайн. При 100 Todo — 100 лукапов/тик. Для приватного 1-2 игрока ок, категории `E3` static state, но линейный скейл. | Приемлемо. Для масштаба — кешировать `ServerLevel` по `dimension` внутри цикла, early exit если `owners().isEmpty()`. Уже есть `if (ref.isEmpty()) continue:53` и `if (level==null) clearStone:57`. Отметить как `E3` долг, не hotfix. |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:142-148` + `src/main/java/jujutsu/mod/character/todo/TodoBoogieWoogieRuntime.java:232-244` | **Swap «без motion переноса» реализован, но `snapTo` не обновляет `yRot/xRot` камня.** `snapTo:143-147` делает `teleportTo(..., getYRot(), getXRot(), false)` + `setDeltaMovement(velocity)` + `hasImpulse=true`, но `face(velocity)` не вызывается. Камень летит с тем же вектором, но визуальный yaw/pitch остаётся от launch. `TodoStoneRenderer` крутит куб по `age` + `seed`, yaw не читает, так что визуально незаметно. `TodoStoneGameTests.java:1274-1281` проверяет `stone.velocity preserved` и `remainingTicks preserved` — ок, но rotation не проверяется. | Опционально вызвать `face(velocity)` в `snapTo` если `velocity.lengthSqr() > 1e-5`, либо задокументировать что rotation камня — launch-only и snap сохраняет именно `DELTA_MOVEMENT` а не yaw. Сейчас поведение соответствует `TODO_STONE_REWORK:42` "every body keep its own motion" — motion сохранён, rotation камня не является motion. |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java:156-160` + `src/main/java/jujutsu/mod/character/todo/TodoTransientState.java:86-95` | **Portal ghost защита частичная.** `canUsePortal(false)` с комментом `156-158` правильно предотвращает копирование камня в другое измерение (ref never cross-level, copy would be immortal ghost). Но `handlePortal` warmup 14т + `isInsidePortal` tick может всё равно тикать `portalCooldown`? С `canUsePortal=false` vanilla не стартует portal timer, ghost невозможен. Проверено `grep canUsePortal` — только `TodoStoneEntity`. Однако `ClipContext.Block.COLLIDER` не включает portal blocks (у них no collision), так что камень летит сквозь портал без `handlePortal` вообще — защита не тестируется полётом, а именно отсутствием teleport. | Оставить как есть, добавить GameTest `S9` уже пинит `portalRefusalLeavesStoneInSameDimension:736-741` (stone stays in overworld, ref still points). Рекомендовать в `canUsePortal` javadoc явно указать что portal blocks — non-collider, поэтому защита именно от `handlePortal` копирования, а не от ray. |

---

### 3) Позитивные наблюдения

- **Один камень на Todo — железобетон.** `TodoStoneRuntime.shouldThrow(Optional):90-92` (`isEmpty()`), `throwStone:109-110` атомарно `new TodoStoneEntity -> launch -> addFreshEntity -> setStone(ref)`, `selfSwap/targetSwap` никогда не бросают второй. `TodoStoneTest` unit (`liveStoneNeverThrowsASecondOne`) и `TodoStoneGameTests:1259,1349` (`countStones==1`, `still exactly one stone (no second throw)`) пинят. `STATES` — единственная мапа, без второго static map (Vessel Seam соблюдён, `TodoTransientState` единственный владелец — `AGENTS:42`).
- **Server-authoritative swaps без motion переноса.** Оба свопа резолвят камень через `TodoTransientState` + `ServerLevel.getEntity(UUID):243-248` (never by entity id, never cross-level), проверяют `sameDimension && withinSwapRange(<=32*32):98-100,250-257` inclusive, затем `STRICT` placement `TodoBoogieWoogieRuntime.findSafeDestination(..., STRICT):137,207` и `place + restoreMotionAndRotation + snapTo`. Motion preservation доказан `TodoStoneGameTests.java:1244-1281` (`velocity`, `yaw/pitch/headYaw`, `fallDistance reset 0`, `hurtMarked=true` для `ClientboundSetEntityMotionPacket`) и `TodoStoneEntity.snapTo:142-147` (`teleportTo` + `setDeltaMovement(velocity)`). `remainingTicks` камня сохраняется (тест `1278-1281`). Соответствует `AGENTS.md:42` "every body and the stone keep their own motion, and the stone keeps flying with its remaining lifetime".
- **Терминации покрыты и централизованы.** `tick():95-115` — `remainingTicks<=0`, `Y<minY`, `ProjectileUtil` ray (`entity->false, COLLIDER`), `move(SELF)` + `horizontal/verticalCollision || velocity<1e-8` — все ветки `endFlight()`. `TodoStateLifecycle:39-50` — `AFTER_DEATH`, `AFTER_RESPAWN`, `AFTER_PLAYER_CHANGE_WORLD`, `DISCONNECT`, `SERVER_STOPPING` — единственное место регистрации (AGENTS: «single registrar»). `dropEverything:62-65` → `dropAll` → `clearStone` + `removeEffect(MOMENTUM)` — один путь для vessel change, death, respawn, dimension, disconnect. `noSave():37` гарантирует что выгруженный чанк не оставляет ghost. `endFlight:128-136` guard `getUUID.equals(ref.entityUuid())` защищает successor (S21).
- **Отсутствие inventory item — соблюдено.** Нет `JujutsuItems` entry, нет `models/item`, нет `loot table`/`recipe`; камень — только `EntityType MISC 0.25×0.25`. Соответствует `AGENTS.md:40` "never an inventory item" и `Vessel-definitions.md:96` "stone never re-opens the item question". Проверено `grep`/`glob` по `src/main/resources` — только `textures/entity/todo_stone.png` и lang.
- **VFX Core и Vessel Seam.** `VfxCues.anchoredDirected(STONE_THROW):113-115` и `worldFixed(STONE_VANISH):93-95` через `JujutsuNetworking.broadcastVfxCue` — единственный канал. `TodoVfxIds.LIVE:63-65` содержит `STONE_THROW/VANISH/TRIPLE_SWAP`. Клиент `TodoStoneRenderer` (code geometry `HALF_EXTENT 0.09` < hitbox `0.25`, `shadowRadius 0.0`, `LightTexture.lightCoordsWithEmission`, `FADE_TICKS 10`) и `VfxDirector.registerHudContribution(todo_stone_status):134` — без нового HUD фреймворка, как требует `TODO_STONE_REWORK.md:120-127`. `AGENTS.md` VFX Core контракт соблюдён.
- **STRICT placement везде кроме aimed `R`.** `ProjectSanityTest` пинит `STRICT && !SOFT` для stone swaps. `selfSwap:137-143` и `targetSwap:207-210` оба `STRICT` — соответствует `TODO_STONE_REWORK.md:88-91` "no safe point at the stone means nobody moves". `TodoStonePlan.preflight(null -> empty)` — atomic `no plan, no move`.
- **Cooldowns разведены.** `STONE_THROW 10` (anti-double-click), `SELF 60`, `TARGET 100` — разные слоты `TERTIARY` vs `TERTIARY_SNEAK`, `TodoClientDefinition.java:89-90` `Math.max(10,60)` для HUD. `TodoStoneGameTests.java:1286-1289` пинят что своп не трогает чужой кулдаун. `TodoSwapGates.casterStateBlocked:49-55` (spectator/dead/unsafeTransport/staggered) — silent `UNAVAILABLE` для stone, `HANDS_FULL` только для clap — руки не гейтят камень, как задумано (`AGENTS.md:102`).
- **Damage immunity и физика.** `hurtServer()->false:167`, `fireImmune()->true:151`, `setNoGravity(true):55`, `setRequiresPrecisePosition(true):56` — совпадает с `ProjectJjkNailEntity` паттерном для точной сетевой коррекции. `isPushedByFluid` не переопределён — вода замедляет но не терминальна, что `S4` теперь документирует как намеренное (`TodoStoneGameTests.java:322-324` коммент "Water and fire have no collision shape... vanilla applies 0.8-per-tick drag").
- **Wire ids append-only.** `CharacterAbility.TERTIARY 8`, `TERTIARY_SNEAK 9` — последние в enum, не перенумеровывались. `TodoAbilityRouter:38` exhaustive `switch` без `default` — новый слот сломает компиляцию, как требует Seam.

---

### 4) Вердикт

**PASS WITH CAVEATS** — срез контрактно корректен и готов к smoke, но требует 2 уточнения доки и 1 optional кода-фикса перед релизом.

**Обоснование:** Исторический `b5-gametest.log:172-179` показывал 7/29 красных `todo_stone_*`, но все 7 — либо исправлены текущими тестами, либо были harness-артефакты 8x8 barrier арены, а не прод-баги: `stonePassesThroughWaterAndFire` теперь ожидает `0.05 < speed < 0.23` и проходит с `0.117` drag; `portalRefusal` теперь assert на tick24 до стены и использует `COLLIDER` (portal blocks non-collider); `successorStoneSurvives` теперь `slowSpeed 0.02` чтобы не упираться в стену; `void`/`death`/`staleRef` — путь `die(genericKill())` + `AFTER_DEATH` + `dropEverything` синхронен. Текущее покрытие `S1-S21` (22 зелёных + 2 UNVERIFIED `S13/S14` dimension/disconnect с pragmatic skip) + unit `TodoStoneTest` покрывают happy path: throw, straight flight `0.23`, ignore entities, water/fire pass, block vanish, lifetime 100, lost sweep, void, portal, self/target swap atomic STRICT, motion/rotation/clock preservation, successor guard. Критических утечек ghost-entity нет (`noSave` + `canUsePortal false` + `clearStone` UUID guard). Единственный открытый баланс-вопрос — вода режет скорость вдвое, но это задокументировано как намеренное и не терминальное.

**Приоритет фиксов:**

1. **P1 — уточнить spec wording про воду (дока, 30 мин).** `AGENTS.md:42` и `TODO_STONE_REWORK.md:31` поменять "Constant velocity, no gravity, no arc" на "Constant horizontal velocity 0.23, no gravity, no arc; water slows (0.8 drag) but never terminates, fire never terminates". Синхронить с `TodoStoneGameTests.java:322-324` который уже правильный. Иначе следующий читатель снова заведёт `isPushedByFluid=false` как "фикс".
2. **P1 — void inclusive `<= minY` (код, 5 мин).** `TodoStoneEntity.java:96` `getY() < minY` -> `getY() <= minY`. Безопасно, покрывает `y == minY` на barrier floor. Логировать reason `void` в `endFlight`.
3. **P2 — optional `isPushedByFluid=false` если продукт хочет честные 0.23 в воде (код, 10 мин).** Добавить `@Override isPushedByFluid(){return false;}` + `updateFluidState` no-op только если P1-дока решила что вода не должна замедлять. Сейчас оставить как есть — но решение должно быть явным.
4. **P2 — мёртвый NBT при `noSave` (код, 5 мин).** `TodoStoneEntity.java:178-193` либо удалить, либо добавить коммент `// dead code kept for debug: noSave prevents persistence` и `try/catch` вокруг `UUID.fromString`.
5. **P3 — HUD `remainingTicks` гранулярность (дока).** `TodoStoneEntity.java:116` оставить `&3` с комментом "0.2s HUD refresh by design" (уже есть `79-82`), либо синхронизировать каждые 20т последние ticks для гладкого fade — не блокер.

После P1+P2 — повтор `gradlew runGameTest --no-daemon --max-workers=1 --no-watch-fs` (ожидается 29/29 зелёных, 2 UNVERIFIED pragmatic skip — норма), затем ручной smoke по `SESSION.md` чеклисту (HUD chip `5s->0s`, `STONE_VANISH` puff 8т, `Shift+V` LOS).

AUDIT_ID: 13-todo-stone-entity
