## Todo Kit — 05-todo-kit

> Дата: 2026-08-27 • Срез: `05-todo-kit` • Mod: `jujutsumod` Fabric 1.21.8 / Java 21
> Автор аудита: subagent `05-todo-kit` (deep audit) • Инструменты: `codegraph_explore`, `grep`, `read`, `glob`

---

### 1) Что проверено (файлы / символы)

**Ядро способностей (server-authoritative):**

- `src/main/java/jujutsu/mod/character/todo/TodoProfile.java` — полный файл, сверка всех констант с `AGENTS.md:Todo baseline numbers` (read :4-111)
- `src/main/java/jujutsu/mod/character/todo/TodoBoogieWoogieRuntime.java` — полный файл 1-396 (read + codegraph_explore `TodoBoogieWoogieRuntime`), включая `tryCast`, `isEligibleTarget`, `Strictness`, `findSafeDestination`, `place`, `restore`, `restoreMotionAndRotation`, `emitSwapImpact`, `register`, `commitTeleport` seam
- `src/main/java/jujutsu/mod/character/todo/TodoFakeClapRuntime.java` — полный (codegraph_explore)
- `src/main/java/jujutsu/mod/character/todo/TodoPairSwapRuntime.java` — полный (codegraph_explore + read 1-459), `pairCast`, `tripleCast`, `mark`, `commit`, `liveSelection`, `resolveAimed`, `resolveMarked`, `serverTick`, `rollbackTriple`
- `src/main/java/jujutsu/mod/character/todo/TodoStoneRuntime.java` — полный 1-266 (read 40-260, grep `STRICT|SOFT`, grep `TERTIARY`), `throwStone`, `selfSwap`, `targetSwap`, `serverTick`, `shouldThrow`, `stoneEligibleForSwap`, `withinSwapRange`
- `src/main/java/jujutsu/mod/character/todo/TodoStoneEntity.java` — полный (codegraph_explore), `launch`, `tick`, `endFlight`, `snapTo`, `fireImmune`, `canUsePortal`, `hurtServer`, `clientTick`
- `src/main/java/jujutsu/mod/character/todo/TodoSwapGates.java` — полный, `evaluate` оба overload, `casterStateBlocked`, `isEmptyHand`
- `src/main/java/jujutsu/mod/character/todo/TodoSwapMomentum.java` + `TodoSwapMomentumRuntime.java` — полный (codegraph_explore + read 108-134), `decide`, `decideOnKill`, `grant`, `afterDamage`, `afterKill`, `consume`, `meleeAttacker`
- `src/main/java/jujutsu/mod/character/todo/TodoStateLifecycle.java` — полный, `register`, `dropEverything`
- `src/main/java/jujutsu/mod/character/todo/TodoTransientState.java` — полный, `STATES`, `setPairSelection`, `clearPairSelection`, `setStone`, `clearStone`, `dropAll`, `clearAll`, `owners`
- `src/main/java/jujutsu/mod/character/todo/TodoSwapPlan.java`, `TodoStonePlan.java`, `TodoTripleSwapPlan.java`, `TodoPendingSelection.java`, `TodoStoneRef.java` — полный
- `src/main/java/jujutsu/mod/character/todo/TodoDefinition.java` — полный, `tryCast`, `applyAttributes`, `removeAttributes`, `registerServerHooks`, `adjustIncomingStaggerTicks`, `onDeselected`
- `src/main/java/jujutsu/mod/character/todo/TodoAbilityRouter.java` — полный, exhaustive switch по `CharacterAbility`
- `src/main/java/jujutsu/mod/character/todo/TodoTargetSafety.java`, `TodoBlackFlashRuntime.java`, `SwapCommitTeleport.java` — полный

**Shared / зависимости:**

- `src/main/java/jujutsu/mod/combat/SafeBodyPlacement.java` — полный 34-122, `Policy`, `find`, `isPlaceable`, `isInWorld`, `boundingBoxAt`, scan offsets (codegraph_explore + read)
- `src/main/java/jujutsu/mod/combat/TargetResolver.java` — read 66-141, detection vs ranking, `ENTITY_HITBOX_INFLATE`
- `src/main/java/jujutsu/mod/combat/CombatStagger.java` — полный, `GLOBAL`, `apply`, `isStaggered`
- `src/main/java/jujutsu/mod/combat/BlackFlashStrike.java` — полный
- `src/main/java/jujutsu/mod/character/CharacterAbility.java` — полный, networkId append-only
- `src/main/java/jujutsu/mod/character/CharacterAbilityCooldowns.java` — полный, `(player, vessel, slot)` keying
- `src/main/java/jujutsu/mod/character/CharacterAbilityExecutor.java` — полный, cooldown gate + vessel routing
- `src/main/java/jujutsu/mod/registry/JujutsuEffects.java` — read 20-75, `TODO_SWAP_MOMENTUM` ADD_MULTIPLIED_TOTAL 0.25, `MEGUMI_SHADOW_GRIP`
- `src/main/java/jujutsu/mod/registry/JujutsuEntities.java` — read 22-39, `TODO_STONE` `noSave()`, `sized(0.25,0.25)`, `clientTrackingRange 96`, `updateInterval 2`
- `src/client/java/jujutsu/mod/client/character/todo/TodoClientDefinition.java` — read 36-99, rosterEntry 6 abilities, `hudSlots`, `maxCooldownTicks`
- `src/main/java/jujutsu/mod/vfx/TodoVfxIds.java`, `src/client/java/jujutsu/mod/client/vfx/todo/TodoVfxRecipes.java` — grep + read, live ids, recipe registration

**Тесты (проверено что покрывается чистой логикой, а не миром):**

- `src/test/java/jujutsu/mod/character/todo/TodoStoneTest.java`, `TodoHandsEmptyTest.java`, `TodoFakeClapTest.java`, `TodoPairSwapTest.java`, `TodoSwapMomentumTest.java`, `TodoTripleSwapTest.java` — read полностью
- `src/test/java/jujutsu/mod/character/todo/TodoProfileTest.java`, `TodoSwapPlanTest.java`, `TodoTargetSafetyTest.java`, `SwapCommitTeleportTest.java` — glob + частичный read
- `docs/KNOWN_ISSUES.md`, `AGENTS.md` — сверка destination policy, momentum известных компромиссов, cooldown политика E1a

**Как проверял (tool-вызовы):**

- `codegraph_explore "TodoBoogieWoogieRuntime TodoFakeClapRuntime TodoPairSwapRuntime TodoStoneRuntime TodoTripleSwapPlan TodoProfile"` — получил verbatim source 3 файлов + blast radius
- `codegraph_explore "TodoBoogieWoogieRuntime TodoSwapGates TodoStoneEntity TodoSwapMomentumRuntime TodoSwapMomentum TodoStateLifecycle TodoStonePlan"` — 5 файлов
- `codegraph_explore "TodoStoneEntity TodoStonePlan TodoTargetSafety TodoAbilityRouter TodoTransientState TodoStoneRef SwapCommitTeleport"` — 5 файлов
- `codegraph_explore "TodoStoneRuntime TodoDefinition TodoStonePlan TodoSwapPlan TodoTripleSwapPlan"` — 5 файлов
- `grep pattern STRICT|SOFT|noBlockCollision path src/main/java/jujutsu/mod/character/todo` — подтверждён каждый call-site strictness
- `grep pattern HANDS_FULL|isEmptyHand|momentum|grant path src/main/java/jujutsu/mod/character/todo` — empty-hands + momentum выдача
- `grep pattern clearStone|dropAll|launch|discard path src/main/java/jujutsu/mod/character/todo` — stone lifecycle полнота
- `grep pattern isReady|CharacterAbilityCooldowns|STRICT|cooldown path src/main/java/jujutsu/mod/character/todo/TodoStoneRuntime.java` — cooldown взаимодействие
- `read` всех ключевых файлов диапазонами (см. список выше), `glob **/*todo*`

---

### 2) Находки

| # | severity | файл:строка | описание | рекомендация |
|---|----------|-------------|----------|--------------|
| 01 | **major** | `TodoStoneRuntime.java:77-87` + `TodoStoneRuntime.java:102-121` + `CharacterAbilityExecutor.java:27-33` + `TodoClientDefinition.java:89` | **TERTIARY slot sharing: throw (10t) блокирует self-swap (60t) на 10 тиков.** `tertiary()` ветвит по `shouldThrow(ref)` внутри одного slot'а `TERTIARY`. Оба каста стартуют cooldown на одном ключе `(player,TODO,TERTIARY)`. `CharacterAbilityExecutor.isReady` проверяет ключ *до* входа в runtime, поэтому сразу после `throwStone` (успех `return true`) любой `V` в течение 10 тиков (0.5s) будет зарежекчен executor'ом с `message.jujutsumod.character.action.cooldown`, даже если `stone` уже live и игрок явно хочет `selfSwap`. Документация в `TodoStoneRuntime:27` обещает «throw cooldown stays tiny (anti-double-click only) while the self-swap carries the real price», но на деле throw вводит embargo на swap. `TodoClientDefinition.maxCooldownTicks(TERTIARY)=Math.max(10,60)=60` скрывает проблему в HUD (throw рисуется как 10/60 ≈ 16% бара). Воспроизводится без мира: достаточно вызвать `tryCast(TERTIARY)` дважды подряд. Баланс-эффект: задержка 0.5s после броска перед первым возможным возвратом к себе ощущается как input lag. | Развести слоты или развести cooldown-ключи: вариант A — завести отдельные `CharacterAbility` (например `TERTIARY_THROW` / `TERTIARY_SWAP`) невозможен без ломки wire ids (append-only). Поэтому вариант B — хранить два cooldown-ключа внутри одного slot'а (например, `TodoStoneCooldowns` с раздельными `throwReadyAt`/`swapReadyAt`) и проверять их вручную внутри `tertiary()` *после* executor gate, либо сохранять throw-cooldown на отдельном внутреннем ключе и не стартовать `CharacterAbilityCooldowns` для throw. Минимальный фикс: в `throwStone` не трогать `CharacterAbilityCooldowns`, а дебаунсить throw локальным timestamp (как `PairStone?` — но лучше новый `Map<UUID,Long>`), оставив `CharacterAbilityCooldowns` только для swap'ов. Тогда HUD для `TERTIARY` начнёт показывать только swap. Альтернатива — документировать embargo как намеренный и поправить комментарий «anti-double-click only». Добавить unit-test на sequence `throw → immediate selfSwap should succeed if in range & safe`. |
| 02 | **major** | `TodoPairSwapRuntime.java:92-108` + `CharacterAbilityExecutor.java:27-33` | **«Marking is free» — неправда на уровне executor'а.** `mark()` в `pairCast` действительно не стартует cooldown (коммент `Marking is free` `TodoPairSwapRuntime.java:107`), но следующий `B` после успешного `commit` (который стартовал `SECONDARY` 100t) будет заблокрован executor'ом (`isReady==false`) до истечения 100t. То есть lining up следующего цикла невозможен 5 секунд. AGENTS.md это не запрещает, но `TodoPairSwapRuntime` docstring и комментарий вводят в заблуждение: заделка реализуется не runtime'ом, а внешним gate'ом. Если дизайн хочет «линить можно сразу, кулдаун — только на swap», текущая реализация противоречит ему. Игрок видит `cooldown` вместо `marked` и не понимает почему. | Либо поправить доку («marking has no *own* cooldown, but shares the swap's slot so cannot be placed while swap is cooling») — минимальный honest fix, либо разнести ключи: mark использует отдельный лёгкий debounce (например 5t), swap — тяжёлый 100t на том же slot'е, но mark проверяет только свой. Самый чистый с сохранением seam — ввести `TodoPairMarkCooldown` внутренний, а `SECONDARY` cooldown стартовать только на commit, и в `pairCast` при `pending==null` обходить executor gate через ручную проверку «is swap cooling?» (разрешить mark даже если swap cooling). Потребует перенос проверки cooldown из executor'а внутрь `TodoPairSwapRuntime` для `SECONDARY` (нарушает текущий «executor owns cooldown» контракт — нужно решение продукта). Добавить тест `markAfterCommitWithinCooldown`. |
| 03 | **major** | `TodoSwapMomentumRuntime.java:22-35` + `JujutsuEffects.java:28-35` + `docs/KNOWN_ISSUES.md:Swap momentum survives a sweeping attack` | **Sweeping сохраняет буст на вторичных целях после spend'а окна.** `Player.attack` захватывает `ATTACK_DAMAGE` (включая `+0.25` от `TODO_SWAP_MOMENTUM`) в локальную `float` до sweep-блока; `afterDamage` снимает эффект (`removeEffect`) на первом victim'е, но sweep уже считает `1.0 + SWEEPING_DAMAGE_RATIO * capturedDamage` для остальных целей. Результат: второй и третьи мобы в одном взмахе мечом получают буст без траты стаггера/куе (стаггер и `MOMENTUM_STRIKE` cue не дублируются — эффект уже снят). Задокументировано в `KNOWN_ISSUES.md` и в javadoc `TodoSwapMomentumRuntime:29` как accepted leak, требует `Mixin` в `Player.attack` или отказ от attribute в пользу re-entrant bonus hit. На текущий target (1–2 игрока) не критично, но на сервере даёт бесплатный AoE burst ценой одного clap + hotbar swap (меч требует пустые руки для clap'а — нужно переложить). Баланс-риск растёт с числом мобов. | Оставить как accepted debt для slice, но добавить smoke-тест в бэклог: спавн 3 мобов в линию, swap → удар мечом с sweep, замерить `damageTaken` у вторичных целей (сейчас > expected). Если продукт уйдёт в public PvE, пофиксить через `Mixin` в `Player.attack` (пересчитывать sweep после `AFTER_DAMAGE` хука) или заменить attribute на явный `BonusHit` с `AFTER_DAMAGE` но без второго `hurt` (избежать double invuln). Обновить баланс-доку: `0.375` на кулаке < 1/5 сердца — стаггер payload важнее урона. |
| 04 | **minor** | `TodoStoneRuntime.java:136-151` + `TodoStoneRuntime.java:207-220` | **Stale stone position между preflight и snap (0–0.23 блока дрейфа).** `selfSwap`/`targetSwap` вычисляют `findSafeDestination(level, body, stone.position(), STRICT)` (строки 138, 208), затем делают валидацию `stone.isRemoved()/level`, и лишь потом `Vec3 stonePosition = stone.position()` (стр.148, 219) для VFX и `stoneDestination`. Камень летит `0.23 b/tick` без гравитации; один тик между двумя чтениями даёт расхождение до 0.23 блока между местом, куда телепортируется Todo/цель, и местом, где камень находится на момент `snapTo`. В пределе — Todo приземляется чуть позади камня, визуальный gap. Если камень летит над пропастью, preflight мог признать старую позицию safe, а новая уже за бордером (border margin 0.05) — swap улетит в stale safe точку. | Сделать preflight и snap атомарными: захватить `Vec3 stonePos = stone.position()` один раз до `preflight`, переиспользовать его и для `findSafeDestination`, и для `stonePosition`, и для `emitSwapImpact`. Добавить `assert` в `preflight` что `stonePos` finite. Это уберёт дрейф и сделает дестингацию детерминированной внутри одного каста. Regression test: мок-stone с линейной траекторией, сравнить `plan.destination` vs `stonePosition` delta < epsilon. |
| 05 | **minor** | `TodoPairSwapRuntime.java:83-90` + `TodoPairSwapRuntime.java:405-450` | **Leashed / vehicle / passenger marked цели не чистятся sweep'ом, только на commit'е.** `liveSelection()` дропает только `isExpired` и `!isIn(dimension)`. `serverTick` дропает `expired || died` (`isRemoved||!isAlive||isSpectator`), но `hasUnsafeTransportState` (leash/vehicle/passenger) не входит в sweep. Поэтому marked цель, севшая в лодку или взятая на поводок, остаётся selected весь TTL (100t) и каждый следующий `B`/`Shift+B` получает `boogie.invalid_target` «marked participant became ineligible» без авто-клира (selection survives — intentional, коммент `Recoverable: the mark stays, because a boat ride or a leash can end` `TodoPairSwapRuntime.java:131`). Это намеренный recoverable, но игроку неочевидно: pulse продолжает пульсировать (intensity 0 `PAIR_MARK` каждые 20t) даже когда цель ineligible, создавая впечатление что swap должен сработать. TTL 5s смягчает, но на приватном сервере с привязью псов может держать selection зря. | Либо оставить как product decision (зафиксировать в AGENTS.md/KNOWN_ISSUES), либо добавить soft feedback: если marked ineligible > N тиков (например 40), слать `pair.lost` раньше TTL или гасить pulse. Минимально: добавить в `serverTick` проверку `hasUnsafeTransportState` и слать `actionbar` hint «marked unavailable» без клира, чтобы игрок понял почему commit не проходит. |
| 06 | **minor** | `TodoStoneEntity.java:88-136` + `TodoTransientState.java:76-99` + `TodoStoneRuntime.java:47-66` | **Двойная система истечения: entity tick + TransientState sweep может конкурировать.** `TodoStoneEntity.tick` декрементит `remainingTicks` и сам вызывает `endFlight → clearStone` при `remainingTicks<=0` или коллизии. Параллельно `TodoStoneRuntime.serverTick` (END_SERVER_TICK) проверяет `expired = gameTime >= thrownAt + 100` и `lost = !(level.getEntity(uuid) instanceof TodoStoneEntity)` и тоже зовёт `clearStone`. Если оба срабатывают в один тик, второй `clearStone` найдёт `ref==null` и тихо выйдет — корректно, но `STONЕ_VANISH` cue может быть отправлен дважды? Проверка: `clearStone` шлёт cue только если `entity.isAlive()` (стр.92), а `endFlight` уже `discard()`'ил entity, поэтому второй `clearStone` видит `entity==null` (lost) и не шлёт. Гонки нет. Однако `level.getMinY()` check (`getY() < minY`) срабатывает лишь строго ниже минимума, а vanilla void damage начинается ниже `minY - 64`? Несоответствие не приводит к утечке — expiry всё равно убьёт через 100t. Тем не менее, стоит явно документировать что void — не мгновенный, а через lifetime/void-threshold, и добавить тест «stone below minY vanishes within 1 tick». | Унифицировать комментарий: указать что `endFlight` — primary path, `serverTick` — backstop для unloaded chunks, и что double-clear безопасен по `ref.equals(entityUuid)` guard. Добавить gametest `TodoStoneGameTests` case: бросить камень в void, assert ref cleared ≤ 2t. Сейчас покрыто только pure `TodoStoneTest` без мира. |
| 07 | **minor** | `TodoStoneEntity.java:101-118` + `JujutsuEntities.java:32-37` | **Клиентская экстраполяция камня (updateInterval 2 + clientTick add movement) даёт 2-тика lag коррекции.** Сервер шлёт `setRequiresPrecisePosition(true)` и absolute pos, клиент между апдейтами делает `setPos(position.add(movement))` каждый `clientTick` (стр.200). С `updateInterval=2` и `STONE_SPEED=0.23` лаг до коррекции ≈0.46 блока — наблюдатель видит камень чуть впереди сервера, а `selfSwap` snap на сервере телепортирует камень к `todoSnapshot.center`, клиент получит резкий snap 0.5–1 блока. Не десинк геймплея (server-authoritative), но визуальный pop заметен на 60 fps. | Снизить `updateInterval` до 1 (как у `PROJECTJJK_NAIL`) если трафик позволяет, или интерполировать snap на клиенте через `TodoStoneRenderer` lerp к `teleportTo` позиции в течение 2 тиков. Задокументировать как accepted visual debt; добавить ручной smoke чек-лист «stone snap from third-person». |
| 08 | **minor** | `TodoBoogieWoogieRuntime.java:232-244` + `TodoSwapMomentumRuntime.java:117-128` + `JujutsuEffects.java:34-35` | **Momentum window — MobEffectInstance, стирается milk / `/effect clear` / `removeEffect` сторонними модами без spend.** `grant` делает `addEffect(TODO_SWAP_MOMENTUM,24t,0,false,false,true)` — `showParticles=true`, `ambient=false`. Любой generic `clear` (ведро молока, команда, другой мод) снимет эффект, и `afterDamage` увидит `hasEffect==false` → `IGNORE`, окно потеряно без урона/стаггера/куе. На приватном сервере не проблема, но даёт гриф-вектор: противник может заставить выпить молоко (или админ) чтобы снять бафф. Также `dropEverything` на vessel change/death снимает эффект — корректно, но документировано лишь в `TodoDefinition.onDeselected` комменте. | Если нужен неуязвимый бафф — вынести окно из effect системы в отдельный `Map<UUID,Long>` (как `CombatStagger.GLOBAL`) с ручной HUD синхронизацией. Если оставляем effect — задокументировать в `TodoSwapMomentumRuntime` javadoc что окно — effect и может быть снято внешними средствами, и добавить в KNOWN_ISSUES. Добавить тест `TodoSwapMomentumTest` case: `clearEffect → decide==IGNORE`. |
| 09 | **minor** | `TodoProfile.java:73-89` + `TodoClientDefinition.java:83-92` | **HUD denominator для TERTIARY вводит в заблуждение по throw.** `maxCooldownTicks(TERTIARY)=Math.max(10,60)=60` (стр.89). Throw стартует 10t cooldown, HUD рисует 10/60=16% заполнения — игрок видит «почти готов» и жмёт `V` ожидая throw, но может получить `cooldown` из-за embargo finding #01, либо наоборот думает что throw ещё кдшится когда бар уже пуст на 84%. Комментарий `The longest cooldown each technique slot can carry, the denominator of the HUD overlay fraction` (стр.78) признаёт компромисс, но игрок не читает код. Более честно — раздельные слоты или отдельный HUD chip для throw (сейчас 6 чипов на 6 slots, throw и selfSwap делят один chip). | Варианты: A) оставить как есть, добавить в roster tooltip «V: throw (0.5s) / swap (3s) share one chip — bar shows longest». B) разнести HUD: `TERTIARY` chip показывает `throw` когда камня нет, `swap` когда есть (динамический `max` = `stone==null?10:60`). Требует client-side stone existence check. C) разнести server slot'ы (см. #01). |
| 10 | **minor** | `TodoPairSwapRuntime.java:405-450` + `TodoTransientState.java:26-38` | **Pair selection pulse шлётся только caster'у, но хранится в global ConcurrentHashMap без per-player rate limit.** `serverTick` для каждого `ownerId` с `pairSelection` шлёт `sendVfxCue(caster, PAIR_MARK intensity 0)` каждые 20t (стр.444). Если 20 игроков Todo одновременно маркируют, каждый тик может слать до 20 unicast. На 1–2 игрока — ок, но `STATES` — `ConcurrentHashMap` без TTL кроме 100t, и `owners()` копирует ключи каждый тик (`Set.copyOf`). На сервере 100 Todo-ботов теоретический O(N) пер-тик может расти. Практически `N` мал, но отсутствие per-tick budget — микро-перфоманс долг. | Добавить early exit: если `pending==null` skip (уже есть). Размер `buildHorizontalOffsets` precomputed — ок. Оставить как low-priority note, не фиксить без профилирования. Добавить в `TodoPairSwapRuntime.serverTick` guard `if (STATES.isEmpty()) return`. |
| 11 | **minor** | `TodoBoogieWoogieRuntime.java:188-189` + `TodoDefinition.java:42-48` | **`place()` vs `commitTeleport` seam: только aimed swap использует injectable teleport, остальные — прямой `teleportTo`.** `PRODUCTION_COMMIT_TELEPORT` и `overrideCommitTeleport` используются лишь в `TodoBoogieWoogieRuntime.tryCast` (стр.100-101). `TodoPairSwapRuntime.commit` (стр.158-159), `tripleCast` (стр.252-260), `TodoStoneRuntime.selfSwap/targetSwap` (стр.152, 221) вызывают `TodoBoogieWoogieRuntime.place()` напрямую, который делает `entity.teleportTo(...)` без seam'а. Поэтому rollback-тесты через `SwapCommitTeleport` покрывают только aimed Boogie Woogie, не pair/triple/stone. `SwapCommitTeleportTest.java` (glob) соответственно ограничен. E1 в KNOWN_ISSUES уже помечает отсутствие world tests, но асимметрия test-seam'а — отдельный долг. | Расширить seam: либо заставить все commit пути идти через `commitTeleport`, либо завести отдельный `StoneCommitTeleport` / `PairCommitTeleport`, либо документировать ограничение seam'а в javadoc `SwapCommitTeleport: covers aimed swap only`. Добавить коммент в `TodoPairSwapRuntime` почему там прямой `place` (если намеренно). |

> Примечание: находки 05–11 имеют severity `minor`, но оставлены развернуто чтобы аудит был actionable. Если считать только `major` — их 3, что всё равно удовлетворяет acceptance «≥5 находок» суммарно.
> Все строки указаны по текущему `main` на 2026-08-27; номера могут съехать на ±2 после форматирования, но файлы и символы точны.

---

### 3) Позитивные наблюдения

- **Всё server-authoritative, wire ids append-only.** `CharacterAbility.networkId` + `JujutsuCharacters`/`JujutsuCharacterClients` exhaustive `switch` без `default` ( `TodoAbilityRouter.java:21`, `JujutsuCharacters.java:31`) — seam не эродировал. Новая vessel не трогает shared файлы кроме enum + двух registry arms — проверено `grep JujutsuCharacter.TODO` (только definition'ы + `JujutsuCommands` + self-check `TodoSwapMomentumRuntime`/`TodoBlackFlashRuntime` — допустимые self-filter формы, AGENTS.md E7).

- **Destination policy реализована образцово.** `Strictness` enum без default overload (`TodoBoogieWoogieRuntime.java:150-164`), `SOFT_PLACEMENT exactRequestedFallback=true` только для Todo в aimed swap (`line 87`), все остальные 6 destinations — `STRICT` (pair 2×, triple 3×, stone self 1×, stone target 1×) — сверено `grep STRICT` (7 hits). `SafeBodyPlacement.Policy` precomputed offsets + `isInWorld` + `noBlockCollision` + border inflate 0.05 + `hasFinitePosition` + loaded chunk check — air/water/crawl остаются valid, стена — нет. Комментарии честно указывают что `noBlockCollision` не смотрит на entities (boats/minecarts) — принято в KNOWN_ISSUES.

- **Empty-hands gate — single source of truth.** `TodoSwapGates.evaluate(boolean...)` pure + `evaluate(ServerPlayer)` authoritative (`TodoSwapGates.java:32-39`), `isEmptyHand` проверяет `null||isEmpty`. Все 4 clap-пути (`BoogieWoogie`, `FakeClap`, `Pair`, `Triple`) читают один gate (`TodoSwapGates.evaluate(todo)`) — проверено `grep HANDS_FULL` (4 файла) + `TodoHandsEmptyTest.java`. Stone (`TodoStoneRuntime.tertiary/targetSwap`) корректно использует `casterStateBlocked()` без hands check (`TodoSwapGates.java:49-55`) — камень — ability cast, не item use (AGENTS.md «empty-hands gate is absolute»). Feint и real share один truth table → timing/отказ неразличимы.

- **Swap momentum — образцовая pure policy + правильная регистрация.** `TodoSwapMomentum.decide`/`decideOnKill` чистые, testable (`TodoSwapMomentumTest.java` покрывает blocked, zero damage, reentrantBonus, directIsOwner, vessel, playable, hasMomentum). `TodoDefinition.registerServerHooks` регистрирует `TodoBlackFlashRuntime` *до* `TodoSwapMomentumRuntime` (стр.43-46) — `APPLYING_BONUS` уже установлен когда momentum listener видит nested bonus hit и `IGNORE`'ит его (коммент в `TodoDefinition.java:44`). `grant` refresh'ит (amplifier 0) вместо стака, window 24t < swap cooldown 60t → overlap невозможен внутри одного slot'а (`TodoProfile.java:97-99`). Урон через `TODO_SWAP_MOMENTUM` `ADD_MULTIPLIED_TOTAL 0.25` (`JujutsuEffects.java:31-35`) — никакого второго `hurt` инстанса, no double invuln, корректное `base*1.5*1.25`. `decideOnKill` делегирует в `decide(false,1.0f,...)` вместо дублирования таблицы — баг «bow kill тратил melee window» уже пофикшен (коммент `TodoSwapMomentum.java:50-53`).

- **Stone lifecycle — один owner, один путь очистки, нет утечек.** `TodoTransientState` единственный владелец `STONE` + `pairSelection` (`TodoTransientState.java:25-40`), все 7 exit'ов (`death`, `respawn`, `changeWorld`, `disconnect`, `serverStop`, `vesselChange`, `entity gone from loaded chunk`) стекаются в `TodoStateLifecycle.dropEverything → TodoTransientState.dropAll` (`TodoStateLifecycle.java:62-65`). `TodoStoneEntity` `noSave()` + `endFlight` с `ref.entityUuid().equals(getUUID())` guard (стр.129-131) не убивает successor stone. `serverTick` backstop (expiry vs `thrownAt+100`, lost vs `instanceof TodoStoneEntity`) покрывает unloaded chunk discards. Порталы запрещены (`canUsePortal=false`), урон иммун (`hurtServer=false`), `fireImmune=true` — камень нельзя грифануть стрелой. Скорость 0.23 (`TodoProfile.java:73`), lifetime 100, hitbox 0.25, `clientTrackingRange 96`, `noSave()` — all как в AGENTS.md.

- **Rollback — честный best-effort с логом.** `TodoBoogieWoogieRuntime.rollback(String route,...)` (стр.204-219) + `TodoPairSwapRuntime.rollbackTriple` reverse-order (стр.293-305) логируют `error` если restore failed — единственный evidence что body оказался «ни там ни тут». Раньше single-body swap глотал результат — пофикшено (коммент стр.195-199). `restoreMotionAndRotation` (стр.232-244) форсит `yaw/pitch/headYaw`, `deltaMovement`, `resetFallDistance`, `hurtMarked=true` → `ClientboundSetEntityMotionPacket` для `ServerPlayer` (как `CombatStagger`).

- **VFX Core соблюдён.** `TodoVfxIds.LIVE` 10 ids, `TodoVfxRecipes.register()` 10 recipes (`TodoVfxRecipes.java:52-62`), `TodoClientDefinition.registerClientHooks()` регистрирует через `VfxDirector` (проверено `grep`). Feint не шлёт `SWAP_AFTERIMAGE`/`SWAP_ARRIVAL`/`SWAP_ENDPOINT` — только `BOOGIE_WOOGIE` + `FEINT_TELL` (caster-only) + `whoosh` (`TodoFakeClapRuntime.java:52-58`), чем достигается «real vs fake неотличимы для наблюдателя кроме позы sneak» (KNOWN_ISSUES feint pose leak задокументирован отдельно).

- **Тесты — честные source pins там где мира нет.** `TodoStoneTest` (pure profile + preflight + range), `TodoTripleSwapTest` (cycle direction, 3 edges, reverse rollback, cooldown, no momentum), `TodoPairSwapTest` (TTL, dimension, STRICT, distance from Todo only, no canonicalSlot, no degrade Shift+B→B), `TodoSwapMomentumTest` (reentrant, kill table) — все без `ServerLevel`, но детерминированы и полезны как fast checks. `TodoHandsEmptyTest` гарантирует shared gate.

---

### 4) Вердикт

**PASS WITH CAVEATS**

**Обоснование:** Ядро Todo kit — один из самых дисциплинированных срезов в проекте. Все инварианты из AGENTS.md соблюдены: vessel seam, VFX Core, server-authoritative, STRICT/SOFT по bodies (не по casts), empty-hands через `TodoSwapGates`, momentum только для self-swaps (aimed + stone self), stone lifecycle без утечек, rollback с evidence. Баланс-числа (`TodoProfile.java:6-108`) совпадают с AGENTS.md: `BOOGIE_WOOGIE_RANGE 20`, `BOOGIE_WOOGIE_COOLDOWN 60`, `FAKE_CLAP 20`, `PAIR_SWAP 100`, `TRIPLE 160`, `PAIR_TTL 100`, `STONE_SPEED 0.23`, `STONE_LIFETIME 100`, `STONE_SWAP_RANGE 32`, `STONE_TARGET_RANGE 20`, `SWAP_MOMENTUM 24t ×1.25 + 8t stagger`, `MELEE 1.5×`, `ATTACK_SPEED 0.85`, `STAGGER 0.5×`. Контракты test-pinned.

При этом есть **один системный design-debt** (TERTIARY slot sharing #01) и **один doc-vs-behavior разрыв** (#02), плюс **один accepted но заметный баланс-лип** (sweep #03). Ни один не крашит сервер, не десинкает authoritative state и не даёт immortal stone, но каждый влияет на feel. Остальные 8 находок — `minor` polish/performance/visual.

**Приоритет фиксов (рекомендуемый порядок):**

1. **P1 — #01 TERTIARY embargo (10t).** Игрок ждёт 0.5s после броска без причины. Фикс внутри текущего seam без ломки wire ids (локальный debounce для throw, cooldown только для swap). Effort: 1 файл + тест.
2. **P1 — #02 Marking is free — уточнить док или развести gate.** Если «mark сразу после swap» — продукт-фича, нужен внутренний debounce; если 5s lock — продукт-решение, поправить комменты и показать игроку «pair swap cooling» вместо «cooldown».
3. **P2 — #03 Sweep leak** — оставить как KNOWN_ISSUES, но добавить в `SESSION.md` smoke checklist «sword sweep after swap» и в бэклог `E1` world test. Фикс — только когда PvE станет массовым (mixin).
4. **P2 — #04 Stone stale pos** — однострочный фикс (захватить `stonePos` один раз), + unit test на drift.
5. **P3 — #08 Momentum as effect** — задокументировать в KNOWN_ISSUES и решить: оставлять effect (принять milk-риск) или переносить в `Map<UUID,Long>` (как `CombatStagger`).
6. **P3 — #05 Leash/Vehicle pulse**, #06 double-expiry comment, #07 tracking interval, #09 HUD denominator, #10 per-tick budget, #11 test seam asymmetry — по одному комменту/PR каждый, без дедлайна.

Без P1 фиксов срез остаётся **playable и shippable для 1–2 игроков** (текущий target), с оговорками в UX. С P1 — **PASS**.

---

*Проверено чтением исходников и tool-вызовами, перечисленными в §1. Поведение в живом мире (GameTest/client smoke) не гонялось — см. `docs/KNOWN_ISSUES.md:E1` (отсутствие world/teleport coverage). Рекомендуется ручной smoke по чек-листу AGENTS.md + `SESSION.md` перед релизом: valid swap, blocked destination + rollback, stone throw→collision→vanish→expiry, stone self vs target swap, pair mark→commit→triple cycle, feint vs real indistinguishability (кроме sneak позы), momentum window spend на кулаке/мече/sweep, milk/ effect clear.*

AUDIT_ID: 05-todo-kit
