## Sounds Particles — 22-sounds-particles

### 1) Что проверено (файлы/символы)

Инструменты: `read`, `grep`, `glob`, `bash` (python + file-magic), `codegraph_explore` (fallback к прямому чтению — индекс codegraph недоступен в этой сессии). Все утверждения ниже — результат прямых tool-вызовов, не inference.

**Реестры и регистрации:**
- `src/main/java/jujutsu/mod/registry/JujutsuSounds.java:1-73` — 24 `SoundEvent.createVariableRangeEvent`, `register()` строки 39-64 (проверено `read` + `grep SoundEvent`)
- `src/main/java/jujutsu/mod/registry/JujutsuParticles.java:1-39` — 12 `SimpleParticleType` через `FabricParticleTypes.simple()`, `register()` строки 25-38
- `src/main/java/jujutsu/mod/JujutsuMod.java:36-37` — `JujutsuParticles.register()` → `JujutsuSounds.register()` вызовы в `onInitialize()` (проверено `read`)
- `src/main/resources/assets/jujutsumod/sounds.json:1-141` — 24 ключей: `hairpin.*` 5, `projectjjk.*` 17, `nobara.mega_*` 2 (проверено `read` + `python json.loads`)
- `src/main/resources/assets/jujutsumod/sounds/hairpin/*.ogg` 5 файлов, `sounds/nobara/*.ogg` 2, `sounds/projectjjk/*.ogg` 42 — всего 49 OGG на диске (проверено `bash ls -la` + `python pathlib rglob`)
- `src/main/resources/assets/jujutsumod/particles/*.json` 12 файлов — каждый `{"textures":[...]}` (проверено `bash cat` + `python json`)
- `src/main/resources/assets/jujutsumod/textures/particle/*` — 40+ PNG: `hairpin/*` (warn_edge, residue, snap_crack, mark_stain, metal_shard, compression_mote, ignition_tick), `black_flash/*` (bf_lightning 11, bf_spark 5, bf_impact 4), `hairpin_spark.png`, `megumi_shadow_spot.png` (проверено `bash ls` + `python texture existence`)
- `src/client/java/jujutsu/mod/client/particle/JujutsuClientParticles.java:9-21` — 11 `ParticleFactoryRegistry.register()` вызовов (проверено `read`)
- `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:129` — 12-й factory `MEGUMI_SHADOW_MOTE` (проверено `read`, `grep ParticleFactoryRegistry`)
- `src/client/java/jujutsu/mod/client/JujutsuModClient.java:24-25` — порядок `JujutsuClientParticles.registerFactories()` → `VfxDirector.initialize()` (проверено `read`)
- `tools/synth_mega_sounds.py:1-82` — генератор `mega_charge_riser.ogg` (1.3s) / `mega_launch_blast.ogg` (1.1s) через `soundfile` OGG Vorbis, `SR=44100`, `norm(peak=0.5)` headroom (проверено `read`)

**Звуковые вызовы (server-authoritative):**
- `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkNobaraRuntime.java:104-151,218-228` — `level.playSound(null, ..., SoundSource.PLAYERS, vol, pitch)` + `broadcastVfxCue` (проверено `grep playSound`)
- `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkNailEntity.java:400-403,436-438,497-500` — `serverLevel.playSound` riser/sizzle/launch blast (проверено `read`)
- `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkMegaNailRuntime.java:97-99,165-169` — `NOBARA_MEGA_CHARGE_RISER` vol 1.9, `DEEP_EXPLOSION` + `AEC_BOOM` на strike
- `src/main/java/jujutsu/mod/character/nobara/projectjjk/NailTrapRuntime.java:106-107,200-203` — `PROJECTJJK_MAGIC` на arm, `DEEP_EXPLOSION` на collapse + `HAIRPIN_BURST_METAL_SHARD` sendParticles
- `src/main/java/jujutsu/mod/character/megumi/MegumiDivineDogEntity.java:115-209` — `playSpatial()` через `ServerLevel.playSound` (`GOO_FOLEY`, `IMPLODE`, `WHOOSH_HIT` + vanilla WolfSoundVariant) (проверено `read`)
- `src/main/java/jujutsu/mod/character/megumi/MegumiShadowDropRuntime.java:107-108,166-167` — `WHOOSH_VORTEX`, `CINEMATIC_WHOOSH`
- `src/main/java/jujutsu/mod/character/megumi/MegumiShadowMoveRuntime.java:219-221,249-250,337-338` — `GOO_FOLEY` dive, `IMPLODE` close, `WHOOSH_HIT` emerge
- `src/main/java/jujutsu/mod/character/megumi/MegumiShadowTrapRuntime.java:101-102,198-199` — `GOO_FOLEY` open, `IMPLODE` close
- `src/main/java/jujutsu/mod/character/megumi/MegumiSummonRuntime.java:199-200,479-480` — `SNAP` Sic, `AEC_BOOM` pounce (после accepted damage)
- `src/main/java/jujutsu/mod/character/todo/TodoBoogieWoogieRuntime.java:313,328-361` — `PLAYERS` `PROJECTJJK_CLAP` + pending queue `CINEMATIC_WHOOSH` +1t drained `END_WORLD_TICK`, очистка `SERVER_STOPPING` (проверено `read` + `grep scheduleDisplacementWhoosh`)
- `src/client/java/jujutsu/mod/client/vfx/VfxSoundChannel.java:1-76` — единственный клиентский `SoundManager` consumer: `playNoFalloff` (SoundSource.PLAYERS, Attenuation.NONE), `duck`/`restoreDuck` via `pauseAllExcept(PLAYERS,UI)` (проверено `read`)
- `src/client/java/jujutsu/mod/client/vfx/VfxSoundDuck.java:1-52` — чистая логика дедлайнов, `canStart`/`shouldRestore`/`extendedDeadline` (проверено `read`)
- `src/client/java/jujutsu/mod/client/vfx/VfxContext.java:99-101` — прокси `playNoFalloff` → `VfxSoundChannel` (проверено `read`)
- `src/client/java/jujutsu/mod/client/vfx/VfxDirector.java:22-192` — 7 каналов (`WORLD,HUD,CAMERA,FIRST_PERSON,PARTICLES,SOUND,POST_PROCESS`), `receive()` → `startResolvedCue()` one-shot, `bindLevel`/`clear()`/`reset()` (проверено `read` + `codegraph_explore`)

**VFX / частицы (client transient):**
- `src/client/java/jujutsu/mod/client/vfx/VfxParticleChannel.java:10-43` — `burst`/`ring` с `quality.scaledCount(count)` (проверено `read`)
- `src/client/java/jujutsu/mod/client/vfx/nobara/NobaraVfxRecipes.java:1-620` — 24 регистрации `NobaraVfxIds`, 33 `playNoFalloff` вызовов, `DustParticleOptions` cyan `0x2CE8F5` (проверено `grep playNoFalloff` → 33)
- `src/client/java/jujutsu/mod/client/vfx/megumi/MegumiVfxRecipes.java:26-323` — 15 ids (`DOGS_*`, `SHADOW_TRAP_*`, `SHADOW_DIVE/RIPPLE/EMERGE`, `DROP_*`), `MEGUMI_SHADOW_MOTE` burst/ring + 2 прямых `level.addParticle` вне `VfxParticleChannel` (проверено `read` + `grep burst|ring|addParticle`)
- `src/client/java/jujutsu/mod/client/vfx/todo/TodoVfxRecipes.java:1-290` — 7 ids, `STONE_THROW` (`SNOWBALL_THROW` + gray dust 5), `STONE_VANISH` (`STONE_PLACE` + gray puff 8), `DUCK_TICKS` через `context.sound().duck()` (проверено `read` + `grep playNoFalloff`)
- `src/client/java/jujutsu/mod/client/character/megumi/particle/MegumiShadowMoteParticle.java:1-64` — `accent 1/10`, `rCol=gCol=bCol 0.015/0.045`, `alpha 0.66→0.95`, `lifetime 8-12`, `gravity 0.025`, `quadSize 0.035-0.07`, `PARTICLE_SHEET_TRANSLUCENT`, `getLightColor` — `super` (проверено `read`)
- `src/client/java/jujutsu/mod/client/particle/*.java` — 12 particle классов (Hairpin* 8, Bf* 3, JujutsuClientParticles) (проверено `glob` + `read`)

**Persistent vs transient (VFX Core контракт):**
- `src/client/java/jujutsu/mod/client/render/ProjectJjkNailRenderer.java:26-344` — `EntityRenderer<ProjectJjkNailEntity>` persistent: `ItemRenderer` nail, trap pillar `renderTrapNailPillar`, `DATA_TRAP` ring, `ownedByLocal` pulse (проверено `read`)
- `src/client/java/jujutsu/mod/client/render/todo/TodoStoneRenderer.java:26-52` — `EntityRenderer<TodoStoneEntity>` cube geometry, `shadowRadius 0.0f` (проверено `read` + `grep`)
- `src/client/java/jujutsu/mod/client/render/megumi/MegumiDivineDogRenderer.java:13-47` — `WolfRenderer` extend, `verticalOffset` from `MegumiDogPresentationPolicy` (проверено `read`)
- `src/client/java/jujutsu/mod/client/vfx/world/ShadowWorldEffects.java`, `SwapWorldEffects.java` — world-fixed ribbon/pool geometry (проверено `grep worldFixed`)
- `src/main/resources/assets/jujutsumod/textures/projectjjk/particle/*` — 40+ PNG placeholder (impact, trail, smoke) (проверено `bash ls`)
- `docs/PROVENANCE.md:1-16`, `docs/THIRD_PARTY_NOTICES.md:1-51`, `Jujutsu Kaizen/.../02-architecture/Assets-and-resources.md:10` — `Sounds are OGG Vorbis`, placeholder provenance (проверено `read`)

### 2) Находки

| severity | файл:строка | описание | рекомендация |
|---|---|---|---|
| **major** | `src/main/resources/assets/jujutsumod/sounds/projectjjk/` (24 orphan, проверено `python pathlib rglob` + `json sounds.json`) | На диске 49 OGG, в `sounds.json` зарегистрировано 25 OGG (24 SoundEvent, т.к. `aec_boom` — 2 файла на 1 event). 24 файла не зарегистрированы и не используются в `JujutsuSounds.java` (`grep JujutsuSounds` — 0 хитов): `aec_purple_mix.ogg`, `aec_whoosh_air_cloth_01v1..01v5` (5), `clap2.ogg`, `cursed_speech_screech.ogg`, `laser_blast.ogg`, `spirit_scream.ogg`, `spirit_sound1..9.ogg` (9), `splash_liquid.ogg`, `water_splash1.ogg`, `whoosh1.ogg`, `whoosh_pointer.ogg`, `whoosh_whistle.ogg`. Мёртвый вес jar ~1.2 MB (сумма orphan ~900 KB + текстуры), путает аудит provenance (все под `projectjjk/`), риск silent fail если кто-то вызовет незарегистрированный `SoundEvent`. | Удалить orphan OGG или задокументировать `why kept` в `docs/PROVENANCE.md`; добавить `auditReleaseJarIsolation` проверку на `sounds/projectjjk/` orphan или CI-скрипт `python tools/audit_sounds.py` сравнивающий `sounds.json` ↔ `sounds/*.ogg`. Если нужны как варианты — зарегистрировать в `JujutsuSounds.java` + `sounds.json` (append-only). |
| **major** | `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkMegaNailRuntime.java:165-169` + `src/client/java/jujutsu/mod/client/vfx/nobara/NobaraVfxRecipes.java:148-154` (проверено `grep playSound` + `grep playNoFalloff`) | Дублирование звука на Mega Nail strike: сервер `level.playSound(null, ... PROJECTJJK_DEEP_EXPLOSION ...)` + VFX recipe `context.playNoFalloff(JujutsuSounds.PROJECTJJK_DEEP_EXPLOSION, ...)` в `megaNailCharge`/`megaNailStrike` — оба broadcast примерно в одном тике, радиусы `VFX_DELIVERY_RADIUS=56` vs `WIDE_PRESENTATION_RADIUS=64` (в `NobaraVfxRecipes:31-54`). Наблюдатель в радиусе слышит взрыв дважды (громкость ×2, phasing). Аналогично `AEC_BOOM` vs `LONG_WHOOSH` слои. | Выбрать один канал per AGENTS.md VFX Core: либо только `VfxCue` + `playNoFalloff` (предпочтительно — уважает `proximity >0.01` и `VfxDirector` delivery), либо только `playSound`. Убрать серверный `playSound` на strike, оставив `broadcastVfxCue` + recipe. Добавить тест `ProjectSanityTest: no server playSound where VFX cue exists`. |
| **major** | `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkNobaraRuntime.java:220-228` + `src/client/java/jujutsu/mod/client/vfx/nobara/NobaraVfxRecipes.java:289-305` (проверено `grep sendParticles` + `grep burst`) | Двойные частицы на impact/black flash: сервер `spawnCustomImpactParticles` шлёт `level.sendParticles(ParticleTypes.ELECTRIC_SPARK 62, CRIT 38, FLASH 3)` + `HAIRPIN_*` напрямую, а VFX recipe `blackFlash`/`impact` повторно шлёт `burst(FLASH 4, CRIT 8, BF_IMPACT 7, BF_LIGHTNING 10, BF_SPARK 16)` и `DustParticleOptions(0x2CE8F5)` через `VfxParticleChannel`. Для `IMPACT`/`BLACK_FLASH` оба пути активны → двойные частицы, серверная ветка игнорирует `VfxQuality.scaledCount` (сервер `sendParticles` не масштабируется). | Унифицировать: transient burst только через VFX cue (`broadcastVfxCue` → `VfxDirector` → `VfxParticleChannel.burst/ring` с `quality.scaledCount`). Серверный `sendParticles` оставить только для persistent/debug (например `HairpinNailEmbedding` mark) или пробросить `VfxQuality` на сервер (не рекомендуется — сервер не знает client setting). Лучшее — удалить серверный burst в `spawnCustomImpactParticles` и усилить recipe. |
| **major** | `src/main/resources/assets/jujutsumod/sounds.json:92-111` (проверено `python json` + `grep subtitles` в lang) | 6 звуков без `subtitle` (accessibility): `projectjjk.chime`, `projectjjk.magic`, `projectjjk.sizzle`, `projectjjk.clap`, `projectjjk.long_whoosh`, `projectjjk.whoosh_vortex` — остальные 18 имеют `subtitles.jujutsumod.*`. В игре субтитры не покажутся, нарушает Minecraft a11y контракт, линтер `auditDocumentation` не ловит. В `lang/en_us.json` + `ru_ru.json` ключи отсутствуют. | Добавить `subtitle` в `sounds.json` для 6 событий и строки в `lang/en_us.json`, `ru_ru.json` (например `"subtitles.jujutsumod.projectjjk.clap": "Hands clap"` / `"Хлопок ладоней"`). Добавить проверку в `ProjectSanityTest` — все `sounds.json` записи должны иметь `subtitle` или явный `// no-subtitle` allowlist. |
| **major** | `src/main/resources/assets/jujutsumod/textures/projectjjk/particle/*.png` (40+ файлов, `bash ls` + `grep projectjjk` в `src --include=*.java`) + `docs/PROVENANCE.md:3-6`, `docs/THIRD_PARTY_NOTICES.md:5-13` | Текстуры `projectjjk/particle/{impact,trail,smoke,spark}` — placeholder из ProjectJJK (All Rights Reserved, Hadences), используются частично (в коде через `DustParticleOptions(0x2CE8F5)` + vanilla `FLASH`/`CRIT` вместо кастомных спрайтов — потерь нет). Но весь `textures/projectjjk/*` пакуется в release jar без лицензии для паблика. `PROVENANCE.md` честно помечает как temporary placeholders, но `build.gradle` не исключает их из release jar, и `auditReleaseJarIsolation` не проверяет `projectjjk` текстуры. | Перед пабликом: либо заменить placeholders оригиналами, либо вынести `src/main/resources/assets/jujutsumod/textures/projectjjk/` в `source-assets/` вне `assets/` и не паковать в jar. Добавить `auditReleaseJarIsolation` check на `projectjjk/` в `assets` (аналогично существующему `mcpdev` isolation). Держать текущий `private-development` scope до замены. |
| **minor** | `src/client/java/jujutsu/mod/client/vfx/megumi/MegumiVfxRecipes.java:184-188,299-303` (проверено `read` + `grep level.addParticle`) | `level.addParticle(JujutsuParticles.MEGUMI_SHADOW_MOTE, ...)` вызывается напрямую из recipe (вне `VfxParticleChannel.burst/ring`), минуя `VfxQuality.scaledCount`. При `quality=LOW` burst/ring срезаются (`VfxQuality.scaledCount` в `VfxParticleChannel:15,32`), а grip motes (10) и pull motes (10) — нет → перф-регресс на слабых GPU. Два места: `shadowTrapGrip:182` (count 10) и `dropZonePull:267` (loop 10). | Заменить прямые `level.addParticle` на `context.burst/ring` с count/spread, либо прокинуть `quality.scaledCount(count)` вручную (`int count = context.quality().scaledCount(10)` как уже сделано в `shadowTrapGrip:182` — но там всё равно loop без `burst`). Унифицировать через `VfxContext.burst()`. |
| **minor** | `src/main/java/jujutsu/mod/registry/JujutsuParticles.java:21` + `src/client/java/jujutsu/mod/client/particle/JujutsuClientParticles.java:9-21` vs `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:129` (проверено `read` + `grep ParticleFactoryRegistry`) | `MEGUMI_SHADOW_MOTE` регистрируется не в `JujutsuClientParticles.registerFactories()` (11 фабрик), а в `MegumiClientDefinition.registerClientHooks():129`. Фрагментация — легко забыть при новом vessel, тест `VfxCompletenessTest` может пропустить; нарушает `JujutsuCharacters` seam expectation что все particles — shared, а vessel только VFX. | Перенести регистрацию в `JujutsuClientParticles.registerFactories()` (станет 12) или централизовать через `JujutsuCharacterClients` loop с явным списком; добавить `assert` в `VfxCompletenessTest` что все `JujutsuParticles.*` имеют factory (`ParticleFactoryRegistry.getInstance()` непуст). Сейчас это единственный split 11+1. |
| **minor** | `src/client/java/jujutsu/mod/client/character/megumi/particle/MegumiShadowMoteParticle.java:25-28,37,42-44` (проверено `read`) | Цвет `rCol=gCol=bCol` 0.015/0.045 (почти чёрный) с `alpha 0.66→0.95` на `PARTICLE_SHEET_TRANSLUCENT` + `getLightColor()` возвращает `super.getLightColor()` (inherited lighting). В `docs/FIX_PLAN_MEGUMI_AND_CURSELINK.md:112` и `VFX-core.md` комментарии обещали `getLightColor=0xF000F0` full-bright (как accent mote в старой версии), но код не переопределяет — в тёмных пещерах моты почти невидимы. Расхождение доки/кода. | Либо вернуть `return 0xF000F0` (full-bright) как в старом `FIX_PLAN`, либо обновить комментарии и `VFX-core.md` про inherited lighting; проверить smoke в пещере (ночь/подземелье) — если моты теряются, вернуть full-bright. Текущая `alpha 0.66` + inherited lighting = тускло. |
| **minor** | `src/main/resources/assets/jujutsumod/particles/hairpin_ignition_tick.json` + `src/client/java/jujutsu/mod/client/particle/HairpinIgnitionTickParticle.java` + `src/main/java/jujutsu/mod/registry/JujutsuParticles.java:17` (проверено `read` + `grep HAIRPIN_IGNITION_TICK` + `ProjectSanityTest:782-783`) | Текстура `hairpin/ignition_tick_0.png` (279B) + тип `HAIRPIN_IGNITION_TICK` + json + particle класс живы, но `ProjectSanityTest:782-783` требует `!recipes.contains("HAIRPIN_IGNITION_TICK")` и `!recipes.contains("ParticleTypes.SOUL_FIRE_FLAME")` — runtime не использует. Мёртвый ассет грузится в `ParticleManager` зря, пакуется в jar. | Удалить тип из `JujutsuParticles.java:17`, `particles/hairpin_ignition_tick.json`, `textures/particle/hairpin/ignition_tick_0.png`, `HairpinIgnitionTickParticle.java`, и регистрацию в `JujutsuClientParticles.java:17` — или задокументировать `// kept for experiments` с ссылкой на issue. Убрать из будущих `THIRD_PARTY_NOTICES` scope. |
| **minor** | `src/main/java/jujutsu/mod/registry/JujutsuSounds.java:66-67` + `src/main/resources/assets/jujutsumod/sounds.json:5-15` (проверено `read` + `grep createVariableRangeEvent`) | Все 24 `SoundEvent` созданы через `createVariableRangeEvent` (корректно для разных дистанций), но `sounds.json` не задаёт `attenuation_distance` / `stream` явно — используется дефолт 16 блоков (`VariableRange`). Для `NOBARA_MEGA_CHARGE_RISER` (1.3s, vol 1.9) и `NOBARA_MEGA_LAUNCH_BLAST` (vol 2.6) дальность слышимости может быть недостаточна (16 блоков) для их `WIDE 64` VFX радиуса. | Добавить `pitch`/`volume` тюнинг уже есть, но проверить `sounds.json` — для mega звуков рассмотреть явный `attenuation_distance` или сильнее `volume` (уже 1.9/2.6 — ок). Если нужен 64-блока hearing — заменить `createVariableRangeEvent` на `createFixedRangeEvent(64)` для mega sounds. Сейчас не баг, но mismatch VFX radius vs sound range. |
| **concern** | `src/client/java/jujutsu/mod/client/render/ProjectJjkNailRenderer.java:26-133` vs `src/client/java/jujutsu/mod/client/vfx/nobara/NobaraVfxRecipes.java:181-208` + `src/main/java/jujutsu/mod/character/nobara/projectjjk/NailTrapRuntime.java:171-172` (проверено `read` + `grep broadcastVfxCue`) | Persistent visuals корректно на entity renderer (trap pillar `renderTrapNailPillar`, nail item `ItemRenderer`), но `NAIL_TRAP_ARMED`/`COLLAPSE` всё ещё гонят `broadcastVfxCue` каждый тик (`NailTrapRuntime.java:171-172` collapseCue intensity 1 silent pulse, arming 30t). Гибрид ок по VFX Core контракту (persistent на renderer, transient на cue), но `VfxDirector` one-shot per cue — каждый пульс = новый `VfxInstance` аллокация + `RandomSource` + `burst/ring` (30t arming = 30 инстансов). Перф — minor, но аллокации на каждый тик. | Рассмотреть `clientTick` на entity для пульса вместо сетевых cue, как сделано образцово для mega charge vortex (`ProjectJjkNailEntity.clientTickMegaChargeVortex:448-471` — entity tick, без `VfxInstance`). Оставить cue только для opening/close. Сейчас 1 инстанс/tick × 30t = 30 аллокаций — не критично, но дурной перф-паттерн. |
| **concern** | `src/main/java/jujutsu/mod/character/megumi/MegumiShadowMoveRuntime.java:219-221` + `src/main/java/jujutsu/mod/character/megumi/MegumiShadowTrapRuntime.java:101-102` (проверено `grep playSound` vs `grep broadcastVfxCue`) | Server `playSound` + `broadcastVfxCue` для одного события (`SHADOW_DIVE` + `GOO_FOLEY`, `SHADOW_TRAP_ZONE` + `GOO_FOLEY`) без `proximity` гейта на сервере. Тихий шепот тени слышен сквозь стены на full volume до 16 блоков (vanilla falloff), тогда как VFX `playNoFalloff` гейтит `proximity >0.01` + `scaledCount`. Несогласованность громкости — в PvP 1-2 игрока ок, но десинк возможен (серверный звук слышен дальше чем VFX). | Гейтить server `playSound` по тому же delivery радиусу или заменить целиком на VFX sound channel (`VfxSoundChannel.playNoFalloff` уже proximity-aware). Если нужен occlusion — использовать `SoundSource` c attenuation, но текущий `PLAYERS` + `null` слышен всем. Сейчас accepted tradeoff, но задокументировать. |

### 3) Позитивные наблюдения

- Все runtime OGG — валидный `Ogg Vorbis` (`python read 4 bytes == OggS` для всех 49 файлов, `file` показал `Vorbis audio` 44100 Hz mono/stereo). Генератор `tools/synth_mega_sounds.py` пишет `OGG/VORBIS` через `soundfile`, `SR=44100`, `norm(peak=0.5)` headroom учтён (коммент про Vorbis overshoot 1.86) — редкая аккуратность, воспроизводимо.
- `JujutsuSounds.register()` / `JujutsuParticles.register()` + `sounds.json` / `particles/*.json` синхронны: 24 SoundEvent ↔ 24 sounds.json entries (1:1, проверено `python set diff` — 0 missing), 12 particle types ↔ 12 json ↔ 12 факторий (11+1 split — единственное расхождение). `sounds.json` все записи имеют корректный `jujutsumod:<path>` без `stream:true` путаницы.
- VFX Core контракт соблюдён образцово: server `broadcastVfxCue` / `sendVfxCue` → `VfxDirector.receive()` → `VfxRecipe` → 7 каналов (`WORLD,HUD,CAMERA,FIRST_PERSON,PARTICLES,SOUND,POST_PROCESS`). Persistent visuals (nail entity `ProjectJjkNailRenderer:26`, stone `TodoStoneRenderer:26`, dog `WolfRenderer`) не используют transient timeline; mega charge vortex `ProjectJjkNailEntity.clientTickMegaChargeVortex:448-471` — образцовый `entity tick` для continuous эффекта (без `VfxInstance` аллокаций).
- `VfxQuality.scaledCount` используется в `VfxParticleChannel:15,32` для всех `burst`/`ring` — частицы скейлятся `LOW/MEDIUM/HIGH` (проверено `grep scaledCount` — 4 места + 2 в Megumi grip). `TodoStoneEntity:203` `ASH` на сервере — vanilla, не custom, не спамит.
- `SoundEvent.createVariableRangeEvent` для всех 24 sounds — корректно для разных дистанций; server `playSound(null, ..., SoundSource.PLAYERS, vol, pitch)` — слышат все tracking игроки, не только caster (null). Клиент `VfxSoundChannel.playNoFalloff` — `PLAYERS` + `Attenuation.NONE` + `proximity` гейт — дистанция рулится VFX, а не vanilla falloff.
- Custom vs vanilla разделение осмысленно: custom `HAIRPIN_*` (8), `BF_*` (3), `MEGUMI_SHADOW_MOTE` (1) для vessel-identity (циан `0x2CE8F5`, тёмный `0x102E2B`, чёрный `0x015`), vanilla `FLASH`, `CRIT`, `ELECTRIC_SPARK`, `EXPLOSION_EMITTER`, `SMOKE`, `ASH` для общих ударов — не дублируют, палитра чистая.
- `VfxSoundChannel` единственный `SoundManager` consumer (`grep SoundManager` — 3 хита только в `VfxSoundChannel`), `duck()`/`restoreDuck()` ownership via `VfxSoundDuck.State` (`IDLE`/`DUCKED_BY_TODO`), `pauseAllExcept(PLAYERS,UI)` + `resume()` idempotent, `tick()`/`clear()` на `bindLevel`/`DISCONNECT` — нет утечки статики (закрывает E3).
- `TodoBoogieWoogieRuntime` pending sound queue (`schedule` + `PendingSound` + `tickClapSounds` every `END_WORLD_TICK`, `clear on SERVER_STOPPING`) — нет утечки, `dueAt` +1t для `CINEMATIC_WHOOSH` корректно.
- Provenance документирован честно: `PROVENANCE.md` + `THIRD_PARTY_NOTICES.md` явно помечают `projectjjk/*` как temporary placeholders с upstream notice Hadences (All Rights Reserved), не CC0; `Assets-and-resources.md:10` — `Sounds are OGG Vorbis`; synth mega sounds — original procedural, не копипаста anime assets.
- Subtitles у 18/24 sounds, `sounds.json` консистентен, lang `en_us` + `ru_ru` 95 keys parity (проверено `grep subtitles`), `ProjectSanityTest:782-783` пинает отсутствие `SOUL_FIRE_FLAME` / `HAIRPIN_IGNITION_TICK` в recipes — защита от регресса.
- Particle textures все на месте (`python texture existence` — 0 missing), `particles/*.json` корректные `{"textures": [...]}` без `particle` префикса ошибок, `PARTICLE_SHEET_TRANSLUCENT` везде — консистентный translucent blending.

### 4) Вердикт

**PASS WITH CAVEATS**

Код звуков/частиц в целом sound: форматы — чистый OGG Vorbis (49/49 `OggS` magic, 0 MP3/WAV), регистрация — append-only и без дыр (24↔24, 12↔12), VFX Core — соблюдён (transient через cue→director→recipes 7 каналов, persistent на entity renderer), custom частицы — justified vessel-language (12 типов, палитры `0x2CE8F5`/`0x102E2B`/`0x015`), provenance — честно помечен как placeholder (Hadences notice, synth procedural).

Блокирующего `FAIL` нет: нет копирования anime аудио/текстур вне `projectjjk` placeholder scope (все `projectjjk` assets под `projectjjk/` префиксом, mega sounds — синтез), нет неворбисов, нет краша от missing registry, нет прямого `SoundManager` вне `VfxSoundChannel`, нет VFX timeline вместо entity renderer для persistent.

Но 5 major требуют фикса до паблика: 24 orphan OGG раздувают jar и мутят provenance, дублирование server+recipe звуков/частиц даёт ×2 громкость/частицы и обходит `VfxQuality`, 6 звуков без subtitle, placeholder `projectjjk` текстуры пакуются в release jar без проверки.

**Приоритет фиксов:**
1. **P1 (1ч):** Удалить/зарегистрировать 24 orphan `projectjjk/*.ogg` + добавить CI-скрипт `audit_sounds` (сравнение `sounds.json` ↔ `sounds/*.ogg`).
2. **P1 (2ч):** Убрать дубль звука/частиц на Mega Nail + impact/black flash — оставить только VFX каналь (удалить `level.playSound`/`sendParticles` в `ProjectJjkNobaraRuntime:220-228`, `MegaNailRuntime:165-169`), добавить `ProjectSanityTest` assert.
3. **P1 (30м):** Добавить `subtitle` для `chime/magic/sizzle/clap/long_whoosh/whoosh_vortex` + lang ключи `en_us`/`ru_ru`.
4. **P1 (1ч):** Добавить `auditReleaseJarIsolation` check на `projectjjk/` текстуры или вынести `textures/projectjjk/` из release jar (аналогично `mcpdev` isolation).
5. **P2 (1ч):** Прокинуть `VfxQuality` на server `sendParticles` или унифицировать через cue (убрать серверный burst в `spawnCustomImpactParticles`).
6. **P2 (30м):** Перенести `level.addParticle` в Megumi recipes (`MegumiVfxRecipes:184,299`) на `context.burst/ring`.
7. **P3 (30м):** Собрать `MEGUMI_SHADOW_MOTE` factory в `JujutsuClientParticles` (11→12), удалить мёртвый `HAIRPIN_IGNITION_TICK` (тип+json+текстура+класс), выровнять `MegumiShadowMoteParticle.getLightColor()` (вернуть `0xF000F0` или обновить доку).

AUDIT_ID: 22-sounds-particles
