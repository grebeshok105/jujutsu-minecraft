## Docs Codex — 27-docs-codex-known-issues

### 1) Что проверено (файлы/символы)

- `AGENTS.md:1-372` — `Project Identity`, `Current slice (facts):27-54`, `Documentation Authority:160-171`, `Verification Policy:321-339`, `The Vessel Seam:219-237`, `Todo baseline numbers:56-88`, `Boogie Woogie destination policy:90-99`
- `README.md:1-52` — Current product, Requirements, Build and verification, Documentation hierarchy
- `SESSION.md:1-28` — State/Verification/Deploy/Next candidates, branch `feat/nobara-target-hud` HEAD `d241544`
- `docs/README.md:1-28` — Source-of-truth order, Files in this directory, Maintenance
- `docs/KNOWN_ISSUES.md:1-307` — Accepted decisions, R1-R3, E1/E1a/E1b/E2/E3/E4/E5/E6/E7/E8/E9/E10/E11/E12/E13/E14/E15, Resolved
- `docs/BUILDING_IN_SANDBOX.md:1-204` — Normal verification, Focused verification, Client verification checklist `Todo — thrown marker:141-148`
- `docs/PROVENANCE.md:1-16`, `docs/THIRD_PARTY_NOTICES.md:1-52`, `docs/START_HERE.md:1-40`
- `Jujutsu Kaizen/jujutsumod-codebase-codex/00-MOC.md:1-99` — Authority:9-16, Current product snapshot:19-34, Code-derived metrics:35-49
- `tools/audit_docs.py:1-143` — `CURRENT_DOCS:13-23`, `REMOVED_DOC_DIRS:24-33`, `FORBIDDEN_REFERENCES:34-50`, `code_metrics:71-82`, `main:97-139`
- Git state: `git ls-files -- "*.md" | sort`, `git status --short`, `ls -R docs/knowledge`, `python3 tools/audit_docs.py` — audit passed `88 files metrics={main_java:126 client_java:191 test_java:85 verification_programs:29 client_mixins:6 nobara_vfx_ids:24}` EXIT 0
- Cross-check: `grep -n "wire ids append"` , `grep -rn "REMOVED_DOC_DIRS|FORBIDDEN_REFERENCES"` , `read` ветки MOC `28-31` (Megumi 12 ids vs Nobara 24)

Инструменты: `read`, `grep`, `bash` (`git ls-files`, `ls -R`), `codegraph_explore` не требуется — срез документационный, символы grep достаточны.

---

### 2) Находки

| # | severity | файл:строка | описание | рекомендация |
|---|----------|-------------|----------|--------------|
| 1 | **major** | `docs/BUILDING_IN_SANDBOX.md:141-148` + `AGENTS.md:41-42` + `Jujutsu Kaizen/jujutsumod-codebase-codex/00-MOC.md:31` | **Stale client-smoke секция про marker.** Checklist `### Todo — thrown marker (R with no target)` описывает удалённую механику: `Give yourself a Boogie Woogie Marker, right click to throw…` (стр.141-148). AGENTS `41-42` и MOC `31` фиксируют: marker system deleted, stone exists only in flight (`V` throw/self-swap, `Shift+V` target swap). Тест/живой игрок по чеклисту будет искать несуществующий item и сочтёт отсутствие багом. | Удалить секцию `141-148` или переписать под stone: `V throws slow 0.23 b/tick, 100t lifetime, vanish on collision; repeat V self-swap, Shift+V target swap; STRICT placement cancels whole`. Синхронизировать с `AGENTS.md:41-42`. |
| 2 | **major** | `docs/README.md:9-18` + `git ls-files -- docs/*:1-18` | **Инвентарь `docs/` неполон.** `docs/README.md:11-18` перечисляет 8 файлов (BUILDING_IN_SANDBOX, KNOWN_ISSUES, MCP_*, PROVENANCE, THIRD_PARTY, TODO_BOOGIE_WOOGIE, TODO_STONE_REWORK). Фактически `git ls-files docs/*` содержит 18 трекаемых md: дополнительно `ABILITY_RESULT_CONTRACT.md`, `FIX_PLAN_MEGUMI_AND_CURSELINK.md`, `MEGUMI_SHADOW_DROP.md`, `MEGUMI_SHADOW_DROP_REVIEW.md`, `MEGUMI_SHADOW_KIT.md`, `NOBARA_ESP_AND_MEGA_NAIL.md`, `START_HERE.md`, `TEST_ARCHITECTURE_PLAN.md`, `VESSEL_DEFINITION_REFACTOR.md`, `VFX_CORE_REFACTORING_PLAN.md` — 10 файлов вне списка. Читатель по `docs/README.md` не найдёт актуальные design notes. | Синхронизировать `docs/README.md:9-18` с реальным `git ls-files docs/*.md`. Либо пометить design notes как `docs/*.md — approved design notes whose rationale is still worth keeping` и перечислить все, либо вынести устаревшие в `archive/` и удалить из `docs/`. |
| 3 | **major** | `tools/audit_docs.py:24-33` + `SESSION.md:11` + `docs/knowledge/CURRENT_STATE.md:1-10` (untracked) | **SESSION.md ложно объясняет запрет `docs/knowledge`.** `SESSION.md:11` пишет: `docs/knowledge/CURRENT_STATE.md stays untracked … it names a docs subfolder that the documentation audit forbids, so committing it fails auditDocumentation. The 2026-08-21 record lives in KNOWN_ISSUES E14 instead.` Но `audit_docs.py:24-33 REMOVED_DOC_DIRS` содержит `docs/research, docs/reviews, docs/session-handoffs, docs/superpowers, docs/specs, docs/vfx, docs/gui, docs/visual-targets` — `docs/knowledge` отсутствует. `python3 tools/audit_docs.py` пропускает `docs/knowledge` без ошибки (проверено: passed 88 files). Объяснение в SESSION вводит в заблуждение. На диске `docs/knowledge/CURRENT_STATE.md` реально существует untracked (подтверждено `git status --short: ?? docs/knowledge/`). | Либо добавить `ROOT / "docs" / "knowledge"` в `REMOVED_DOC_DIRS` (если запрет намеренный) и вскрыть `FORBIDDEN_REFERENCES`, либо исправить `SESSION.md:11` — убрать ссылку на audit как причину untracked и честно назвать `docs/knowledge` project memory вне git по решению команды. Обновить `docs/KNOWN_ISSUES.md:E14` запись о сужении. |
| 4 | **major** | `tools/audit_docs.py:71-82` + `Jujutsu Kaizen/jujutsumod-codebase-codex/00-MOC.md:35-45` | **Метрики Codex покрывают только Nobara.** `code_metrics:73` считает `nobara_vfx_ids = len(VFX_ID_PATTERN.findall(NobaraVfxIds.java))` (24), MOC `44-45` публикует только `Nobara VFX ids | 24`. На текущем срезе Megumi владеет `MegumiVfxIds` 15 ids (MOC `28`: `his twelve megumi/* wire ids are five Divine Dogs plus seven shadow-kit`), Todo — 10 ids. Ни Megumi, ни Todo метрики не аудитируются, `audit_docs.py:118-130 metric_tokens` не падает если их число дрейфует. При добавлении/удалении shadow ids gate остаётся зелёным. | Расширить `code_metrics()` на `megumi_vfx_ids` и `todo_vfx_ids` (или `total_vfx_ids`), добавить токены в `metric_tokens` и соответствующую строку в MOC `35-45`. Альтернатива — явно задокументировать в MOC `47` что Nobara row — единственная code-derived VFX метрика и почему. |
| 5 | **minor** | `SESSION.md:1-8` + `AGENTS.md:27` + `git log --oneline: ce3d655 vs d241544` | **SESSION.md stale для текущего среза.** Заголовок `Session Handoff — nobara-target-hud branch (Aug 21 wave)` HEAD `d241544` на `main ce3d655`, PR open. Фактический рабочий каталог `D:/WorkFlow/jujutsu-minecraft` на `main` (проверено `git status` — модифицирован SESSION.md, ветка не `feat/nobara-target-hud`). AGENTS `27` требует: `SESSION.md keeps only what changed on the active branch`. На момент аудита `27-docs-codex-known-issues` SESSION описывает чужую ветку, не текущий аудит, и дата `Aug 21 wave` старше MOC `Verified: 2026-08-03` и KNOWN_ISSUES `Last code verification: 2026-07-29`. | Обновить `SESSION.md:1-8` под активный срез или вернуть на `main` generic handoff. Не держать две истины: либо branch поле отражает реальный checkout, либо файл помечается `Status: STALE`. Для doc-only среза достаточно секции `State: audit 27-docs-codex…`. |
| 6 | **minor** | `Jujutsu Kaizen/jujutsumod-codebase-codex/00-MOC.md:4-5` + `docs/KNOWN_ISSUES.md:5` + `AGENTS.md:31-33` | **Даты верификации расползлись, свежесть не аудитируется.** MOC `Verified: 2026-08-03`, KNOWN_ISSUES `Last code verification: 2026-07-29` (E3/E4/E6 — `2026-07-23`), SESSION `2026-08-21`. `audit_docs.py` не проверяет, что `Verified:` моложе N дней или совпадает с `git log`. Читатель не понимает, актуальны ли цифры `Main 126 / Client 191 / Test 85` на 27 августа. | Добавить в `audit_docs.py` soft-check: `Verified:` дата в MOC должна быть ≤14 дней от HEAD commit date, иначе warning. Или хотя бы синхронизировать три даты в одном коммите при обновлении среза. |
| 7 | **minor** | `AGENTS.md:25-27` + `README.md:11-16` + `Jujutsu Kaizen/jujutsumod-codebase-codex/00-MOC.md:19-34` | **Единственность current-slice facts — на грани.** AGENTS `25-27` объявляет: `This block is the single owner of current-slice facts. README keeps only pitch+controls; SESSION keeps only branch changes.` Фактически tuning `STONE_LIFETIME 100t / 5s` дублируется в `AGENTS.md:65-66` (таблица TodoProfile), `README.md:14` (`lives about five seconds`), `MOC:31` (`exists only in flight`), `BUILDING_IN_SANDBOX.md:113` (policy). Формулировки разные (`about five seconds` vs `100 (5 s)`), но число живёт в 4 местах. AGENTS `88` называет `TodoProfile is source of truth; do not restate them elsewhere` — README уже рестейтит. Риск дрейфа при ребалансе. | Оставить в README только `about five seconds` (pitch) без точного `100t` — ок, но зафиксировать в AGENTS `88` что README/MOC используют округлённые pitch-формулировки и не считаются restatement. Или вынести числа полностью в `TodoProfile` и в доках ссылаться `see TodoProfile`. |
| 8 | **minor** | `tools/audit_docs.py:34-50` + `docs/KNOWN_ISSUES.md:300-307` | **FORBIDDEN_REFERENCES неполон, архивы не ловятся полностью.** `FORBIDDEN_REFERENCES` банит 8 строк (`docs/research/` etc., 3 хеша, `8 assertion tests`). `git ls-files` показал `archive/character-player-gecko/README.md` и `archive/character-skin-animation/` — эти архивы **намеренно** вне `docs/` и разрешены (MOC `27` честно описывает `retained outside runtime under archive/`). Но `docs/MEGUMI_SHADOW_DROP_REVIEW.md:199-203` и `docs/ABILITY_RESULT_CONTRACT.md` уже содержат point-in-time research, который по `AGENTS.md:170` должен быть `summarize durable conclusion … rather than restoring an archive`. Audit не отличает `docs/*.md` design notes от `docs/research/` archive — оба проходят. | Решение принято сознательно (docs — current-only), но стоит добавить в `FORBIDDEN_REFERENCES` паттерн `Status: HISTORICAL REFERENCE` уже есть (`114`), а также `archive/` не банить. Текущее поведение ок — фиксируем как positive, но отмечаем границу: `audit_docs.py` не ловит устаревшие `docs/*.md` если они без historical marker. Нужна ручная сверка `docs/README.md` инвентаря (см. #2). |

Итого 8 находок (4 major, 4 minor), critical 0.

---

### 3) Позитивные наблюдения

- **Авторитет консистентен.** Иерархия `AGENTS.md:160-168` → `MOC:9-15` → `docs/KNOWN_ISSUES.md:9` → `docs/README.md:5-7` → `docs/START_HERE.md:3` идентична: `code/tests > AGENTS.md > SESSION.md > Codex MOC > KNOWN_ISSUES.md`. Конфликтов не найдено, `docs/README.md:6` честно делегирует `Owned by AGENTS.md under Documentation Authority`.
- **Audit gate зелёный и встроен в qualityGate.** `python3 tools/audit_docs.py` → `Documentation audit passed: 88 files metrics={126,191,85,29,6,24}` EXIT 0, MOC `35-45` цифры совпадают 1-в-1 (`Main 126 | Client 191 | Test 85 | Verification 29 | Client mixins 6 | Nobara VFX 24`). `AGENTS.md:170` и `MOC:49` корректно описывают `inside ./gradlew qualityGate` + `auditDocumentation`.
- **Current-only + отсутствие архивов соблюдены.** `git ls-files` не содержит `docs/research/*`, `docs/reviews/*`, `docs/session-handoffs/*`, `docs/superpowers/*`, `docs/specs/*`, `docs/vfx/*`, `docs/gui/*`, `docs/visual-targets/*` — все 8 `REMOVED_DOC_DIRS` чисты. Исторические маркеры `Status: HISTORICAL REFERENCE` отсутствуют (проверено `audit_docs.py:114-115`). `archive/` используется только для runtime assets (`character-player-gecko`, `character-skin-animation`) и честно описан в MOC `27`, не как docs archive.
- **E1/E12 и debt — честно и единственно.** `AGENTS.md:49` → `E12:272` (`hasClaimedStarter no production callers`), `AGENTS.md:52` → глобальный hit-stop, `AGENTS.md:90-92` → Boogie Woogie no-floor/no-occupancy — все три указывают в `KNOWN_ISSUES.md` как владельца rationale. E1 `91-109` остаётся открыт с widened списком (stone, triple cycle), E1a/E1b детализированы, E14/E15 с датами и связью с `SourceBoundaryTripwireTest`. Дублирования debt вне KNOWN_ISSUES нет.
- **Wire ids append-only образцовый.** `AGENTS.md:36` `CharacterAbility network ids are wire format: append, never renumber`, `CharacterAbilityPayload:16-18` vessel claim, `MOC:28` `his twelve megumi/* wire ids are five Divine Dogs plus seven shadow-kit ids, appended and never renumbered`. Метрики и тесты (`CharacterAbilityWireFormatTest`) держат инвариант.
- **SESSION.md честно маркирует unverified.** `SESSION.md:15-17` `NOT yet verified in-game (gate proves none of this): card layout/scale by eye …` + `docs/BUILDING_IN_SANDBOX.md:86-95` `Compilation does not prove rendering …` — соответствует `AGENTS.md:335` `What a green gate proves, and what it does not.`
- **Линки валидны.** `validate_links:85-94` резолвит все относительные ссылки (`AGENTS.md → docs/KNOWN_ISSUES.md`, `MOC → 06-maintenance/How-to-add-next-character.md`) — битых ссылок нет, audit это проверил.

---

### 4) Вердикт

**PASS WITH CAVEATS** (условно PASS, 4 major требуют правок до следующего релиза docs, но gate зелёный и авторитет не сломан).

**Обоснование:** Документационный авторитет един (AGENTS > SESSION > Codex > KNOWN_ISSUES) и подтверждён кодом; `audit_docs.py` проходит и ловит stale links/metrics/removed dirs; current-only и отсутствие архивов соблюдены; debt E1/E12 и wire ids задокументированы единственно в своих владельцах. Критических рассинхронов, ломающих сборку или вводящих в заблуждение по authoritative поведению, нет. Caveats — 4 major (#1 stale smoke checklist про marker, #2 неполный инвентарь docs/README, #3 ложное объяснение запрета docs/knowledge, #4 отсутствие Megumi/Todo VFX метрик) — не валят gate, но дают дрейф при следующем изменении среза и должны быть закрыты в ближайшем doc-коммите.

**Приоритет фиксов**

1. **P0 (до следующего doc PR):** #1 `docs/BUILDING_IN_SANDBOX.md:141-148` — переписать thrown marker → stone (5 мин, риск — тестер пойдёт по несуществующему пути).
2. **P1 (в том же PR):** #2 `docs/README.md:9-18` — синхронизировать список с `git ls-files docs/*.md`; #3 `SESSION.md:11` + `tools/audit_docs.py:24-33` — либо добавить `docs/knowledge` в REMOVED_DOC_DIRS, либо поправить SESSION.
3. **P2 (следующий tech-debt слот):** #4 `tools/audit_docs.py:71-82` + `MOC:35-45` — добавить Megumi/Todo VFX метрики; #5 `SESSION.md:1-8` — актуализировать branch/head под активный срез; #6 даты `Verified:` — синхронизировать или добавить soft-check.

AUDIT_ID: 27-docs-codex-known-issues
