## Divine Dogs — 15-divine-dogs-lifecycle

### 1) Что проверено (файлы/символы)

**Инструменты:** `codegraph_explore` (запросы `MegumiSummonRuntime MegumiDivineDogPack MegumiDivineDogEntity MegumiSummonState`, `MegumiAbilityRouter MegumiDefinition`), `grep` (паттерны `isSafe|findLeash|recoverLeash`, `presentationPhase|combatEnabled|setNoAi`, `ConcurrentHashMap|TEARDOWN|ownerUuid`), `read` по диапазонам, `glob` по `src/**/megumi/**`.
Проверено вручную по исходникам; `codegraph` вернул verbatim source с call paths, `grep` подтвердил blast radius.

**Серверный slice (Megumi):**
- `src/main/java/jujutsu/mod/character/megumi/MegumiSummonRuntime.java:51-606` — `PACKS:52` (`Map<UUID,Pack>`), `TEARDOWN_IN_PROGRESS:53`, `NEXT_SUMMON_TOKEN:54` (`AtomicLong`), `register():58-94` (7 хуков: `END_SERVER_TICK`, `AFTER_DEATH`, `ENTITY_UNLOAD`, `AFTER_RESPAWN`, `AFTER_PLAYER_CHANGE_WORLD`, `DISCONNECT`, `SERVER_STOPPING`), `nextSummonToken():96`, `packView():105`, `tryToggle():123-162`, `trySic():164-207`, `isEligibleTarget():209`, `createDog():220`, `shouldHardDiscard():248`, `reconcile():261-289`, `teardown():291-333` (guarded `TEARDOWN_IN_PROGRESS.add`, cross-level sweep `302-305`, `dogCleanupAction`, `shouldApplyTeardownCooldown:330`), `recoverLeash():527-543`, `tick():348-356`, `tickPounce():358-446`, `resolvePounceImpact():448-483`, `startCooldownIfLonger():567-577`, `TeardownReason:585-605`, `RemovalCause:579`
- `src/main/java/jujutsu/mod/character/megumi/MegumiDivineDogPack.java:1-29` — record `dimension, whiteId, blackId, summonToken, summonedAtGameTime`, `contains():20` (token+dimension+id), `wasSummonedAt():26`, `dogIds():16`
- `src/main/java/jujutsu/mod/character/megumi/MegumiDivineDogEntity.java:38-349` — `DATA_PRESENTATION_PHASE:39`, `DATA_PRESENTATION_TICKS:41`, поля `ownerUuid:44`, `summonToken:45`, `recallDimension:46`, `sicTargetUuid:47`, `pounceTargetUuid:48`, `pounceSicTargetUuid:49`, `nextPounceReadyGameTime:50`, `pounceDeadlineGameTime:52`, `configureSummon():78`, `beginRecall():101`, `canFinishRecallWithoutPack():109`, `launchPounce():158` (`setNoAi(true)`, `hurtMarked`), `finishPounce():174` (`setNoAi(false)` только если `ACTIVE`), `tick():212` (`tickPresentationPhase` -> `shouldHardDiscard` -> `tickPounce`), `tickPresentationPhase():228` (16т -> ACTIVE, 12т recall -> discard, `setPresentationPhase:251`), `combatEnabled():263`, `acceptsSicCommand():267`, `setTarget:272`, `doHurtTarget:277`, `hurtServer:282`, `isPickable:287`, `isPushable:292`, `push:297`, `canCollideWith:304`, `canBeCollidedWith:309`, `wantsToAttack:346` (все гвард `combatEnabled`), `registerGoals:59` (Float, MeleeAttack, FollowOwner 10.0/2.0, LookAt, RandomLook, OwnerHurt* )
- `src/main/java/jujutsu/mod/character/megumi/MegumiDogPresentationPolicy.java:1-63` — `phaseAfterTick():7` (16т MATERIALIZING->ACTIVE), `recallComplete():14` (12т), `combatEnabled():18` (только `ACTIVE`), `progress():22`, `verticalOffset():32` (MATERIALIZING `-1..0`, RECALLING `0..-1`), `Phase:45` (MATERIALIZING 0, ACTIVE 1, RECALLING 2, `fromNetworkId` default MATERIALIZING)
- `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:4-104` — `DOG_HEALTH 60:5`, `DOG_ATTACK_DAMAGE 3:6`, `DOG_MOVEMENT_SPEED 0.34:7`, `NAVIGATION_SPEED_MODIFIER 1.0:8`, `FOLLOW_START 10.0:9`, `FOLLOW_STOP 2.0:10`, `LEASH_DISTANCE 32.0:11`, `LEASH_SAFE_SEARCH_RADIUS 3.0:12`, `LEASH_RETRY_TICKS 10:13`, `SIC_RANGE 20.0:14`, `DOG_MATERIALIZATION_TICKS 16:15`, `DOG_RECALL_TICKS 12:16`, `RECALL_COOLDOWN 240:17`, `PACK_DEATH_COOLDOWN 600:18`, `SIC_COOLDOWN 30:19`, `POUNCE_MIN 3.0:20`, `POUNCE_MAX 8.0:21`, `POUNCE_COOLDOWN 80:22`, `POUNCE_TIMEOUT 16:23`, `POUNCE_HORIZONTAL 0.92:24`, `POUNCE_VERTICAL 0.42:25`, `POUNCE_MAX_VERTICAL 0.58:26`, `POUNCE_GRAVITY 0.08:27`, `POUNCE_EXIT_DAMPING 0.35:28`, `POUNCE_BONUS 2.0:29`, `POUNCE_KNOCKBACK 2.4:30`, `POUNCE_STAGGER 6:31`, `VFX_CUE_RADIUS 48.0:32`
- `src/main/java/jujutsu/mod/character/megumi/MegumiGroundSafety.java:1-165` — `VERTICAL_OFFSETS:21` (`0,1,-1,2,-2,3,-3`), `LEASH_OFFSETS:22-23` (radius 3), `findSummonPair():27-43` (forward+right, 2 набора, `!intersects`), `findVertical():45-54`, `findLeashPosition():56-64` (`getDimensions(STANDING)` + `firstSafe` + `entityCollision` + `isSafe`), `leashCandidates():66-76`, `buildLeashOffsets():86-106` (центр -> кольца по `distanceSquared`), `isSafe():112-133` (`loaded`, `sturdyFloor isFaceSturdy`, `hazard FIRE/CAMPFIRES/LAVA`, `collisionFree noCollision`, `entityCollision`, `loaded` по каждому `occupied` в `bounds`), `accepts():135` (все 5 условий), `isHazard():143` (BlockTags.FIRE/CAMPFIRES, FluidTags.LAVA)
- `src/main/java/jujutsu/mod/character/megumi/MegumiPouncePolicy.java:1-188` — `canLaunch():10` (active+currentPack+validOwner+currentTargetMatches+eligible+lineOfSight+3<=d<=8+deadlineReady), `deadlineReady():22`, `timedOut():26` (`>`), `launchVelocity():30` (horiz `0.92`, vert `0.42 + deltaY*0.10` capped `0.58`), `steerVelocity():41` (сохраняет `y`), `sweptTargetHit():50` (`inflate 0.30 + clip`), `postMoveAction():55` (IMPACT > FINISH > CONTINUE), `exitVelocity():65` (ZERO для CLEANUP/INVALID или ORDINARY+onGround, иначе damped horiz), `impactDirection():75`, `canResumeNavigation():86`, `flightAction():97`, `inFlightAction():104` (CLEAR_SIC vs FINISH_POUNCE vs CONTINUE), records `LaunchFacts:119`, `InFlightFacts:129`, `ResumeFacts:179`
- `src/main/java/jujutsu/mod/character/megumi/MegumiSummonState.java:1-23` — `isSameTickDuplicate():11` (`wasSummonedAt`), `belongsToPack():15` (pack!=null+contains), `retainsPack():20` (`living>0`)
- `src/main/java/jujutsu/mod/character/megumi/MegumiLifecyclePolicy.java:1-41` — `reconcileAction():7` (teardownInProgress||!packPresent -> IGNORE, иначе RETAIN/FINAL_LOSS), `shouldApplyTeardownCooldown():17` (`packRecordExisted || foundOwnedDog`), `dogCleanupAction():21` (manualRecall && belongs -> BEGIN_RECALL иначе HARD_DISCARD), `dogOwnsTeardownCooldown():27` (`phase != RECALLING`)
- `src/main/java/jujutsu/mod/character/megumi/MegumiCooldownPolicy.java:1-24` — `duration():7` (NONE 0, RECALL 240, FINAL_LOSS 600), `preservedRemaining():15` (`max`)
- `src/main/java/jujutsu/mod/character/megumi/MegumiTargetPolicy.java:1-26` — `accepts():7` (`!owner && alive && loaded && sameLevel && !spectator && !ownDog && !allied`)
- `src/main/java/jujutsu/mod/character/megumi/MegumiDefinition.java:1-51` — `registerServerHooks():27` (attributes + 4 runtime `register`), `onDeselected():44` (`teardown DESELECTED` + `clear` trap/drop + `teardown` move), `createDivineDogAttributes():36`
- `src/main/java/jujutsu/mod/character/megumi/MegumiAbilityRouter.java:1-52` — `tryCast():21` (locksAbilities gate, exhaustive `switch` без `default`, `PRIMARY->tryToggle:27`, `PRIMARY_SNEAK->trySic:29`, `SECONDARY/SHADOW*`, `TERTIARY`)
- `src/main/java/jujutsu/mod/character/CharacterAbilityCooldowns.java` (через `MegumiSummonRuntime:567-577` `startCooldownIfLonger`) — `READY_AT` по `(player UUID, vessel, slot)`, `remainingTicks`, `start`, `preservedRemaining`
- `src/main/java/jujutsu/mod/combat/TargetResolver.java` (вызов `MegumiSummonRuntime:184-195`) — `resolve(level, player, SIC_RANGE, isEligibleTarget)` + повторный `isEligibleTarget` + `hasLineOfSight`
- `src/main/java/jujutsu/mod/registry/JujutsuEntities.java:41-50` — `MEGUMI_DIVINE_DOG` `noSave()` (`sized 0.6x0.85`, tracking 96, interval 2)

**Клиент / презентация:**
- `src/client/java/jujutsu/mod/client/render/megumi/MegumiDivineDogRenderer.java:1-47` — `extractRenderState:24` (`phase`, `progress`, `verticalOffset`), `render:36` (`translate 0, verticalOffset, 0`, authoritative pos не двигается)
- `src/client/java/jujutsu/mod/client/render/megumi/MegumiDivineDogRenderState.java:6-10` — `phase`, `progress`, `verticalOffset` (extends `WolfRenderState`)
- `src/client/java/jujutsu/mod/client/character/megumi/MegumiCooldownHud.java:1-46` — `visible:19` (`localPlayerExists && megumiSelected && remaining>0`), `render:23` (`ClientAbilityCooldowns.remainingTicks(MEGUMI, PRIMARY)`, `+19/20`, `fill 0xB8121818`, teal `0xFF2F8F83`)
- `src/client/java/jujutsu/mod/client/character/megumi/MegumiAnimationHooks.java` — `triggerDivineDogs`/`triggerShadowDive/Emerge` через `VfxCue`
- `src/client/java/jujutsu/mod/client/vfx/megumi/MegumiVfxRecipes.java` — 5 Divine Dogs ids: `DOGS_SUMMON_BODY` (1т), `DOGS_SUMMON` (16т), `DOGS_RECALL` (12т), `DOGS_SIC` (8т), `DOGS_POUNCE` (6т); `broadcastVfxCue` радиус `VFX_DELIVERY_RADIUS 48.0` через `JujutsuNetworking:340-346`
- `src/main/java/jujutsu/mod/character/CharacterSelectionManager.java:14-30` — `select` (old `onDeselected` до `setAttached`, `refreshDimensions`, broadcast)

**Тесты (сверка инвариантов):**
- `src/test/java/jujutsu/mod/character/megumi/MegumiLifecyclePolicyTest.java:11-119`, `MegumiPouncePolicyTest.java:13-287`, `MegumiFollowSafetyTest.java:11-55`, `MegumiSummonStateTest.java:11-39`, `MegumiTargetPolicyTest.java`, `MegumiCooldownPolicyTest.java:7-21`, `MegumiDogPresentationPolicyTest.java:9-54`, `MegumiProfileTest.java:20-50`, `MegumiAbilitySlotsTest.java` — pure-policy JUnit, без `ServerLevel`

**Документы:**
- `AGENTS.md:37-40` — инварианты среза (owner UUID+token, one guarded teardown, reconcile сиблинга, 10т/32 блока, 16т/12т, AI/collision disable вне ACTIVE, pounce 3-8/LOS/80т/16т)
- `Jujutsu Kaizen/jujutsumod-codebase-codex/03-systems/Megumi-Divine-Dogs.md:1-51`
- `docs/KNOWN_ISSUES.md` (E1/E1a/E12, `E1b TargetResolver`)

---

### 2) Находки

| severity | файл:строка | описание | рекомендация |
|---|---|---|---|
| **critical** | `MegumiSummonRuntime.java:261-289` (`reconcile`), `MegumiSummonRuntime.java:567-577` (`startCooldownIfLonger`); `CharacterAbilityCooldowns.java` key `(player UUID, vessel, slot)` | **Cooldown финальной потери не применяется если игрок оффлайн.** `reconcile` на `FINAL_LOSS` делает `PACKS.remove(ownerId, pack)` + `startCooldownIfLonger(server.getPlayerList().getPlayer(ownerId), PRIMARY, 600)` — если `owner==null` (дисконнект, смерть оффлайн, unload пока владелец в другом измерении/оффлайн), `startCooldownIfLonger` сразу `return`. Аналогично `teardown` с `TeardownReason.DISCONNECT/SERVER_STOPPING/RESPAWN/NONE` вообще `0т`. Собаки могут умереть пока владелец оффлайн (чистый `noSave` не спасает если чанк с собаками загружен другим игроком) — игрок перезаходит без 600т кулдауна и сразу `tryToggle` успешно. Обход баланса; `CHARACTER_STATE copyOnDeath` не помогает т.к. кулдаун не записан. | Хранить `FINAL_LOSS` deadline вне `CharacterAbilityCooldowns` — напр. `Map<UUID, Long> offlinePackDeathDeadline` в `SavedData` / `WorldData` или как `Attachment` с `copyOnDeath` и TTL по `gameTime`. При `getPlayer==null` писать туда; при `select`/`tryToggle` проверять `max(remainingTicks, offlineDeadline - gameTime)`. Или сделать `CharacterAbilityCooldowns` способным хранить дедлайн по `UUID` без `ServerPlayer`. Покрыть тестом `reconcile` с `server.getPlayerList().getPlayer==null`. |
| **major** | `MegumiSummonRuntime.java:553-565` (`livingDogs`), `MegumiSummonRuntime.java:261-289` (`reconcile`), `MegumiSummonRuntime.java:527-543` (`recoverLeash`) | **`livingDogs()` ищет только в `pack.dimension` (`server.getLevel(pack.dimension())`).** `teardown:302-306` делает cross-level sweep по `server.getAllLevels()` с фильтром `ownerId.equals(candidate.ownerUuid())`. Несоответствие: если хотя бы один пёс оказался в другом измерении (nether portal `OutsideWorldBorder` телепорт, `/execute in`, баг `teleportTo` без смены `pack.dimension`, future `noSave` всё равно может мигрировать через портал пока чанк загружен), `livingDogs` вернёт `[]` или `1` вместо `2`, `reconcile` решит `FINAL_LOSS` и удалит `PACKS` + `PACK_DEATH_COOLDOWN 600`, хотя сиблинг жив в другом мире и `shouldHardDiscard` его ещё не тронул. `recoverLeash` так же слеп — собаки вне pack-измерения никогда не получат tether и застрянут. `codegraph_explore` показал `livingDogs` вызывается из `reconcile`, `recoverLeash`, `packView`, `trySic` — все страдают. | Унифицировать: `livingDogs` должен сканировать `server.getAllLevels()` и фильтровать по `isLivePackDog` + `belongsToPack` (как `teardown`). Или явно задокументировать `noSave` + отсутствие portal-телепортов для собак как инвариант и добавить `assert` что `dog.level().dimension().equals(pack.dimension())` в `tick` (fail-fast). Тест `MegumiSummonStateTest` покрывает `belongsToPack` с dimension, но не `livingDogs` scope. |
| **major** | `MegumiSummonRuntime.java:291-333` (`teardown`), `MegumiDivineDogEntity.java:109-113` (`canFinishRecallWithoutPack`), `MegumiDivineDogEntity.java:219-222` (`shouldHardDiscard:248-259`) | **Cross-level sweep в `teardown` матчит только по `ownerUuid`, без `summonToken`/`dimension`.** При быстром `RECALL (12т) -> tryToggle новый summon` новая `PACKS.put(ownerId, newPack)` случится до окончания 12т `RECALLING` старых собак. Следующий `teardown` новой pack (напр. `DESELECTED`) пройдёт по всем уровням, найдёт живых `RECALLING` собак старой pack (тот же `ownerUuid`), `dogOwnsTeardownCooldown` для `RECALLING`=false, `belongsToRemovedPack = belongsToPack(newPack, oldDogId, oldToken, oldDim)` = `false` (токен не совпал) => `HARD_DISCARD` — старая анимация обрывается досрочно, без 12т `recallComplete` и без `DOGS_RECALL` завершения. Обратный случай: `shouldHardDiscard` после `PACKS.put` новой pack проверит `PACKS.get(ownerId)` = новая pack, `belongsToPack(newPack, oldDogId, oldToken)` false => старый `RECALLING` в чужом измерении будет `discard()`-нут мгновенно, хотя `canFinishRecallWithoutPack` требует совпадающего измерения. | Для `RECALL` пути `teardown` должен фильтровать `belongsToPack(pack, dogId, token, dim)` уже на этапе sweep, а не после: `level.getEntities(..., candidate -> ownerId.equals(candidate.ownerUuid()) && isCurrentPackDog(candidate, ownerId))` для `BEGIN_RECALL` ветки. Старым `RECALLING` собакам разрешить доигрывать 12т независимо от `PACKS` (они уже в `canFinishRecallWithoutPack` режиме) — не `HARD_DISCARD` их пока `recallComplete==false`. Добавить тест на `RECALL -> summon` в тот же тик. |
| **major** | `MegumiSummonRuntime.java:123-162` (`tryToggle:127-133`), `MegumiSummonState.java:11` (`isSameTickDuplicate`), `MegumiLifecyclePolicy.java:7` (`reconcileAction`) | **Гонка `RECALL 240т` vs `FINAL_LOSS 600т` — `tryToggle` может перезаписать 600т на 240т.** Если обе собаки уже мертвы (`livingDogs==[]`), `reconcile` должен удалить `PACKS` и дать `600т`. Но `reconcile` тикает только в `END_SERVER_TICK:348`, а `tryToggle` вызывается из `CharacterAbilityExecutor` внутри того же тика по пакету. Если игрок жмёт `R` в тот же тик когда вторая собака умерла, `tryToggle` уйдёт в ветку `existing!=null` (`PACKS` ещё не удалён) и вызовет `teardown(RECALL)` с `240т` вместо `600т`. `isSameTickDuplicate` защищает только `summon` в тот же тик, не `recall` мёртвой стаи. `shouldApplyTeardownCooldown:17` (`packRecordExisted || foundOwnedDog`) тут `true`, но `dogOwnsTeardownCooldown` для уже мёртвых собак (нет entity) даст `false`, так что `foundCooldownOwningDog` зависит от sweep результата. | В `tryToggle` перед `teardown(RECALL)` проверить `livingDogs(server, ownerId, existing).isEmpty()` — если пусто, вызывать `reconcile` синхронно или напрямую `teardown(FINAL_LOSS)` (600т). Или в начале `tryToggle` вызывать `reconcile` принудительно (как `trySic:169` уже делает `if(pack!=null && livingPackDogs.isEmpty()) reconcile`). Унифицировать политику: `tryToggle` должен различать "живой сиблинг -> RECALL" vs "оба мертвы -> FINAL_LOSS" до выбора `TeardownReason`. |
| **major** | `MegumiDivineDogEntity.java:228-249` (`tickPresentationPhase`), `MegumiDogPresentationPolicy.java:14` (`recallComplete`), `MegumiSummonRuntime.java:248-259` (`shouldHardDiscard`) | **`canFinishRecallWithoutPack()` требует `recallDimension.equals(level.dimension())` — смена измерения обрывает 12т recall.** Если `RECALLING` собаку толкнёт nether portal / chorus fruit / `/tp @e` / `teleportTo` из `recoverLeash` другой собаки (?) в другое измерение в течение 12т, `shouldHardDiscard:254` вернёт `true` и `tick():219` `discard()`-ит мгновенно, без `DOGS_RECALL` завершения и без `playRecallSound` второй раз (он уже сыгран). `AGENTS.md:38` требует 12т recall как материализация; клиент увидит резкий pop (entity внезапно исчезла, `verticalOffset` оборвался). `recallDimension` хранится только для этого чека. `JujutsuEntities:48` `noSave()` не предотвращает портал пока entity загружена. | Разрешить `RECALLING` догу доигрывать 12т независимо от измерения (хранить `recallDimension` только для отладки/логов) — `canFinishRecallWithoutPack` проверять только `phase==RECALLING && recallDimension!=null && phaseTicks < RECALL_TICKS`, без сравнения `dimension`. Либо явно телепортировать `RECALLING` собак обратно в `recallDimension` перед `discard` если они сменили мир. Документировать как инвариант "RECALLING dogs never change dimension". |
| **minor** | `MegumiGroundSafety.java:108-133` (`isSafe:112-133`), `MegumiSummonRuntime.java:539-541` (`recoverLeash` -> `teleportTo:540`) | **Leash `isSafe` кандидат `new Vec3(desired.x, baseY+offset, desired.z)` где `baseY = BlockPos.containing(desired).getY()` — блочный центр, не точный feet; `dimensions.makeBoundingBox(candidate)` от feet, `level.noCollision(bounds)` и `isFaceSturdy` могут дать `false` на полуслябах/ступенях/коврах, где ванильный `noCollision` для собаки 0.6x0.85 прошёл бы.** Кроме того, `isHazard` проверяет только `floor` и каждый `occupied` внутри AABB на `FIRE/CAMPFIRES/LAVA` — `MAGMA_BLOCK`, `WITHER_ROSE`, `POINTED_DRIPSTONE`, `POWDER_SNOW`, `CACTUS` не считаются hazard, собака может быть телепортирована на них. `entityCollision` проверяется через `level.getEntities(dog, bounds, !isSpectator)` — включает саму собаку? Нет, т.к. `dog` excluded. | Для полублоков использовать `candidate` как точный feet с `+0.05` epsilon и проверять `floorState.isFaceSturdy` уже корректно; добавить `MAGMA_BLOCK`/`WITHER_ROSE` в `isHazard` или явно документировать "только огонь/лава — остальное считается safe". Проверить в игре на slab лестницах и коврах. `isSafe` уже требует `collisionFree && !hazard && sturdyFloor` — поведение на воде над sturdy floor корректно (вода не hazard). |
| **minor** | `MegumiCooldownHud.java:19-33` (`visible:19`, `render:23`) | **HUD не проверяет отсутствие pack, только `remainingTicks>0 && megumiSelected`.** `AGENTS.md:38` "...cooldown HUD ... appears only while Megumi has no pack and that deadline remains positive". Сейчас инвариант держится косвенно (summon не стартует кулдаун, старт только после `PACKS.remove`), но если будущий `SIC` или `SHADOW` слот начнёт писать на `PRIMARY` (нарушение Vessel Seam, но компилятор не мешает), HUD покажется при живой стае. Также `visible(true, ...)` первым аргументом всегда `true` — мёртвый параметр `localPlayerExists`. `MegumiSummonRuntime.packView` доступен для проверки, но HUD его не читает. | Передавать `hasPack = MegumiSummonRuntime.pack(packView?)` или `ClientAbilityCooldowns` зеркало с `hasPack` из `ClientCharacterSelection` — `visible = megumiSelected && !hasPack && remaining>0`. Убрать фиктивный `localPlayerExists` или заменить на `client.player != null` внутри. Добавить тест `MegumiCooldownHudTest` на `hasPack==true -> !visible`. |
| **minor** | `MegumiDivineDogEntity.java:263-310` (`combatEnabled` гварды), `MegumiDivineDogEntity.java:58-69` (`registerGoals`) | **Дублирование `combatEnabled()` в 7 override — легко забыть при добавлении нового пути урона.** Покрыто `isPickable/isPushable/push/canCollideWith/canBeCollidedWith/doHurtTarget/hurtServer/wantsToAttack`, но `Wolf` имеет ещё `isInvulnerable`, `isAttackable`, `skipAttackInteraction`, `canBeHitByProjectile` (через `isPickable` частично), `hurt` (не `hurtServer` в некоторых версиях), `actuallyHurt`. Если Mojang добавит новый `hurt` путь, он пройдёт мимо гвард. `codegraph_explore` показал только 2 `SynchedEntityData.defineId` — pounce состояние не синхронизируется, что правильно, но и не защищает от нового `hurt` пути. | Централизовать в одном `canBeTargeted/combatEnabled` и добавить ArchUnit/ source-text тест как `MegumiLifecyclePolicyTest:99-114` ("любой `hurt*`/`is*` override в `MegumiDivineDogEntity` должен вызывать `combatEnabled()`"). Или переопределить `isInvulnerableTo` с гвард. |
| **minor** | `MegumiSummonRuntime.java:52-54` (`PACKS`/`TEARDOWN_IN_PROGRESS` как `ConcurrentHashMap`, `NEXT_SUMMON_TOKEN:54` как `AtomicLong`) | **Серверный тик однопоточный, но concurrent типы маскируют отсутствие синхронизации и намекают что можно звать с других потоков.** `ConcurrentHashMap`/`AtomicLong` без персиста сбросится в `0` после рестарта сервера и может повторно выдать старый токен для того же `ownerUuid` (чей старый pack уже в `shouldHardDiscard`), теоретически позволив старой `RECALLING` собаке из `shouldHardDiscard` пройти `belongsToPack` с новым токеном при коллизии значений (вероятность низкая: `AtomicLong` 64-бит). `CharacterAbilityCooldowns:22` использует обычные `HashMap`. `grep` показал `ConcurrentHashMap` только в `Megumi*` пакетах — консистентность стиля нарушена. | Заменить на `HashMap`/`HashSet` + `long` (как в `CharacterAbilityCooldowns`), либо персистить `NEXT_SUMMON_TOKEN` в `SavedData` / `Attachment`. При текущем `noSave` сущностей коллизия не критична, но типы вводят в заблуждение — добавить комментарий "single-threaded, concurrent types for `computeIfAbsent` convenience only" или упростить. |
| **minor** | `MegumiPouncePolicy.java:30-48` (`launchVelocity:30`, `steerVelocity:41`), `MegumiSummonRuntime.java:394-410` (`tickPounce`) | **Pounce вертикаль падает на `GRAVITY 0.08` каждый тик независимо от дистанции; на цели выше на 4+ блока `launchVelocity` даёт `0.58`, но уже через 7 тиков `steerVelocity(...).add(0,-0.08)` сделает вертикаль отрицательной и пёс начнёт падать до цели.** `sweptTargetHit:50` `inflate 0.30 + clip` компенсирует на 3-8 блоках, но на `8` блоках с перепадом `>3` блока (напр. цель на холме) pounce промахнётся чаще ожидаемого. `AGENTS.md:39` обещает `3-8 блоков LOS` — вертикальный компонент не учтён в `distance` (3D `distanceTo`), так что цель на 6 блоках горизонтали + 4 блока высоты уже `7.2` 3D, но `launchVelocity` vert capped `0.58` может не добить. Баланс-вопрос, не креш. | Проверить в игре на перепадах высот; либо масштабировать `POUNCE_VERTICAL_SPEED` от `deltaY` сильнее (сейчас `*0.10` capped `0.58`), либо применять гравитацию только после `elapsedTicks>2` или снижать `POUNCE_GRAVITY` для pounce. Зафиксировать в `MegumiProfileTest` инвариант `POUNCE_MAX_VERTICAL_SPEED < POUNCE_HORIZONTAL_SPEED`. |
| **minor** | `MegumiSummonRuntime.java:261-270` (`reconcile` first guard `reconcileAction(...,0)`), `MegumiLifecyclePolicy.java:7-15` | **Первая проверка `reconcile` передаёт фиктивный `livingDogCount=0` только чтобы проверить `teardownInProgress||!packPresent`.** Читается как баг (всегда `FINAL_LOSS`), но `==IGNORE` ветка掩饰. Наличие двух вызовов `reconcileAction` вместо прямой проверки `TEARDOWN_IN_PROGRESS.contains` + `pack==null` усложняет чтение и может сломаться если `reconcileAction` добавит новую ветку зависящую от `livingDogCount`. `codegraph_explore` показал что `reconcile` вызывается из `AFTER_DEATH`, `ENTITY_UNLOAD`, `END_SERVER_TICK`. | Упростить: `if (TEARDOWN_IN_PROGRESS.contains(ownerId) || pack==null) return;` без вызова pure policy с фиктивным `0`. Оставить один вызов `reconcileAction(false,true,livingDogs.size())` для `RETAIN/FINAL_LOSS`. |

### 3) Позитивные наблюдения

- **Owner UUID + summonToken идентификация образцовая.** `MegumiDivineDogPack` хранит `dimension + whiteId + blackId + summonToken + summonedAtGameTime:9-14`, `MegumiSummonState.belongsToPack:15` проверяет все три поля (`token+dimension+id`), `PACKS` ключ — только `ownerUuid:52`. Нигде нет сравнения по `entityId` как identity. `codegraph_explore` подтвердил `isLivePackDog:545` (`ownerUuid.equals` + `summonToken==`), `isCurrentPackDog:521` (`belongsToPack`). `Codex/Megumi-Divine-Dogs.md:13` соблюдён. `noSave():48` в `JujutsuEntities` гарантирует что токен не устареет на диске.
- **One guarded teardown действительно единственный деструктивный путь.** Все выходы (`AFTER_DEATH` собаки/игрока:60, `ENTITY_UNLOAD:67`, `AFTER_RESPAWN:72`, `AFTER_PLAYER_CHANGE_WORLD:74`, `DISCONNECT:76`, `SERVER_STOPPING:78`, `onDeselected:DESELECTED:45`, `tryToggle:RECALL:131`/`SUMMON_ROLLBACK:151`, `FIXTURE_RESET` через MCP tool) идут через `teardown():291` с `TEARDOWN_IN_PROGRESS.add` guard и `finally remove:328`. `reconcile:261` никогда не удаляет собак напрямую, только `PACKS` + кулдаун. `MegumiLifecyclePolicy.dogCleanupAction:21` чисто разделяет `BEGIN_RECALL` vs `HARD_DISCARD`, `dogOwnsTeardownCooldown:27` защищает от двойного кулдауна для `RECALLING`. `grep` по `discard()` показал только `teardown:323` и `tick:220` (shouldHardDiscard) и `tickPresentationPhase:237` — все через политику.
- **Reconcile живого сиблинга корректен и race-safe.** `reconcileAction(IGNORE/RETAIN/FINAL_LOSS):7` + `retainsPack(living>0):20` + `PACKS.remove(ownerId, pack):285` (compare-and-remove) — не снесёт новую pack, поставленную между `livingDogs` и `remove` (ABA-safe). Ветка `RETAIN:274-283` чистит невалидный `Sic` target у выжившей собаки (`isEligibleTarget`), что предотвращает залипание цели на мертвеце. `reconcile` вызывается из `END_SERVER_TICK:350` каждый тик — живой сиблинг выживает без кулдауна.
- **16т materialize / 12т recall вынесены в pure policy и синхронизированы по `SynchedEntityData`.** `MegumiDogPresentationPolicy` — единственное место с магическими числами (`MATERIALIZING 16:15`, `RECALL 12:16`), `MegumiDivineDogEntity.tickPresentationPhase:228` — единственный writer (`entityData.set`), `MegumiDivineDogRenderer:28-31` — единственный reader (`progress`/`verticalOffset`). Authoritative `position` не двигается, только `matrices.translate(0, verticalOffset, 0):40` — нет десинка. `setPresentationPhase:251` атомарно `setNoAi(!combatEnabled)` + `clearSicCommand` + `setTarget(null)` + `getNavigation().stop()`.
- **AI/collision disable вне `ACTIVE` полный и консистентный.** `setPresentationPhase:251-260` ставит `setNoAi(!combatEnabled)`, чистит `sicTarget`, `setTarget(null)`, `getNavigation().stop()`. `isPickable:287`, `isPushable:292`, `push:297`, `canCollideWith:304`, `canBeCollidedWith:309`, `doHurtTarget:277` (`!pounceInFlight` дополнительно), `hurtServer:282`, `wantsToAttack:346` — все гвардятся `combatEnabled()` (`ACTIVE` only). `launchPounce:165` дополнительно `setNoAi(true)` на время полёта и `finishPounce:181` возвращает `setNoAi(false)` только если фаза всё ещё `ACTIVE` (не `RECALLING`). `MegumiLifecyclePolicyTest:98-114` ловит отсутствие гвард.
- **Leash каждые 10т / 32 блока реализован точно по AGENTS.md и без fallback на точную позицию владельца.** `recoverLeash:531` (`gameTime % LEASH_RETRY_TICKS 10`), `leashDistanceSquared 32*32:534`, `findLeashPosition:56` перебирает радиус `3` с сортировкой "центр → кольца по distanceSquared" (`buildLeashOffsets:86` + `VERTICAL_OFFSETS 0,1,-1,2,-2,3,-3`) + `isSafe:112` (`loaded && sturdyFloor isFaceSturdy && !hazard FIRE/CAMPFIRES/LAVA && collisionFree noCollision && !entityCollision`), `teleportTo:540` с `Set<Relative>` пустым, без fallback на exact owner pos. При отсутствии точки — `Optional.empty` и `ifPresent` не телепортирует (нормальный pathing продолжается). `MegumiFollowSafetyTest` покрывает кольца и `isSafe`.
- **Pounce — независимый per-dog, серверный, с полным набором гейтов.** `canLaunch:10` (`active && currentPack && validOwner && currentTargetMatches && eligible && lineOfSight && 3.0<=d<=8.0 && deadlineReady 80т`), `launchVelocity:30` `0.92 horiz + 0.42-0.58 vert`, `steerVelocity:41` каждый тик правит горизонталь на цель без сброса вертикали, `tickPounce:394-410` (`move(MoverType.SELF)`, `resolvedVelocity = pos - beforeMove`, `sweptTargetHit:50` `inflate 0.30 + clip`, `postMoveAction:55` IMPACT приоритет), один `hurtServer:472` с `playerAttack(owner)` = `5.0` dmg (`3+2` `POUNCE_BONUS_DAMAGE`), `knockback 2.4:476`, `stagger 6т:478`, звук `AEC_BOOM` + `DOGS_POUNCE` только после `hurt` success (`>acceptedDamage`). `nextPounceReady:50` + `pounceDeadline:52` per-dog, `deadlineReady/timedOut` независимы. `MegumiPouncePolicyTest` покрывает все ветки.
- **Vessel Seam не нарушен.** Весь Divine Dogs код живёт в `jujutsu.mod.character.megumi` и `jujutsu.mod.client.character.megumi` / `render/megumi` / `vfx/megumi`; shared файлы (`CharacterAbilityExecutor`, `TargetResolver:184`, `JujutsuCharacters`, `JujutsuMod:44`) не ветвятся по `MEGUMI` (проверено `grep -rn JujutsuCharacter.MEGUMI src/main --include="*.java"` — только `MegumiDefinition:18` id и `JujutsuCharacters` binding). `MegumiDefinition`/`MegumiClientDefinition` — единственные точки регистрации. `grep` по `MEGUMI` в shared пакетах — 0.
- **VFX Core соблюдён.** 5 `MegumiVfxIds` (`DOGS_SUMMON_BODY`, `DOGS_SUMMON`, `DOGS_RECALL`, `DOGS_SIC`, `DOGS_POUNCE`) зарегистрированы из `MegumiClientDefinition.registerClientHooks()`, `MegumiVfxRecipes` — единственный recipe-pack, `broadcastCue:340-346` радиус `VFX_CUE_RADIUS 48.0:32` (`VFX_DELIVERY_RADIUS`) через `JujutsuNetworking.broadcastVfxCue`, `VfxCue.anchoredWithOffset` с `anchorEntityId` и `anchorOffset`. Summon emit `SUMMON_BODY` + 2×`SUMMON` exact-origin, recall — 1×`RECALL` на собаку (`teardown:315`).
- **Дисциплина `noSave` + `isLivePackDog` + `shouldHardDiscard` предотвращает утечки.** `JujutsuEntities:48` `noSave` — собаки не пишутся на диск; `isLivePackDog:545` (`alive && !removed && ownerUuid.equals && token==`) и `shouldHardDiscard:248` (`pack==null && canFinishRecallWithoutPack -> false` иначе `!belongsToPack`) гарантируют что чужая/устаревшая собака `discard()` в `tick:219` за 1 тик. Нет `addAdditionalSaveData`.

### 4) Вердикт

**PASS WITH CAVEATS**

Срез `15-divine-dogs-lifecycle` реализован аккуратно: token-идентификация, guarded teardown, reconcile сиблинга, 16т/12т фазы, AI/collision гейты, leash 10т/32 блока, pounce 3-8 блоков/LOS/80т/16т — всё присутствует и соответствует `AGENTS.md:37-40` и `Codex/Megumi-Divine-Dogs.md`. Критических десинков позиции/урона, утечек entity или второго механизма рядом с существующим не найдено. Код читается как pure-policy + runtime, покрыт `Megumi*Test` на уровне инвариантов.

Одна **critical** находка (cooldown финальной потери теряется если игрок оффлайн — баланс-обход, но требует оффлайн-убийства в загруженном чанке) и три **major** (dimension-scoped `livingDogs`, cross-level sweep без токена, гонка `RECALL 240т` vs `FINAL_LOSS 600т` в тот же тик) требуют фикса до релиза, но не ломают happy-path для онлайн-игрока в одном измерении (`recoverLeash` 10т, 16т/12т, pounce гейты — всё verified). Остальное — `minor` (HUD без проверки pack, `isSafe` нюансы на slab, concurrent-типы, pounce-вертикаль, запутанный first-guard в `reconcile`).

**Приоритет фиксов:**
1. **P0 — critical:** сохранять `FINAL_LOSS` 600т оффлайн-игроку (`reconcile` + `teardown` с `getPlayer==null`) — `SavedData`/`Attachment` deadline.
2. **P1 — major:** `livingDogs`/`recoverLeash` сканировать `server.getAllLevels()` с `belongsToPack` (унифицировать с `teardown` sweep); `teardown` для `RECALL` фильтровать по `belongsToPack` и не `HARD_DISCARD` старые `RECALLING` пока `!recallComplete`.
3. **P1 — major:** `tryToggle` различать `RECALL` (живой сиблинг) vs `FINAL_LOSS` (оба мертвы) до выбора `TeardownReason` — синхронный `reconcile` или `livingDogs.isEmpty()` чек.
4. **P2 — minor:** `canFinishRecallWithoutPack` не завязывать на `dimension` (доигрывать 12т везде), HUD добавить `!hasPack` проверку, `PACKS`/`TEARDOWN_IN_PROGRESS` заменить на `HashMap`/`HashSet` + персист `NEXT_SUMMON_TOKEN` или комментарий, упростить first guard в `reconcile`.
5. **P3 — polish:** `isHazard` расширить или документировать, pounce вертикаль проверить на перепадах высот, добавить ArchUnit тест на `combatEnabled` гварды.

AUDIT_ID: 15-divine-dogs-lifecycle
