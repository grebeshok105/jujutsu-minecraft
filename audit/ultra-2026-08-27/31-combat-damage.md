## Combat — 31-combat-damage

### 1) Что проверено (файлы/символы)

**Цель среза:** урон, stagger (Todo 0.5x, Black Flash 14т), Black Flash 10% 1.75x, attribute modifiers, hit-stop Resonance, криты, knockback 2.4, pounce 5 damage 6т stagger, защита от бесконечного скейла.

**Инструменты:** `read`, `grep`, `glob`, `codegraph_explore` (через поиск), ручная верификация линеек.

**Файлы и символы:**

| Файл | Символы / зоны |
|---|---|
| `src/main/java/jujutsu/mod/character/todo/TodoProfile.java:4-111` | `MELEE_DAMAGE_MULTIPLIER 1.50:6`, `ATTACK_SPEED_MULTIPLIER 0.85:7`, `STAGGER_DURATION_MULTIPLIER 0.50:10`, `SWAP_MOMENTUM_DAMAGE_MULTIPLIER 1.25:101`, `SWAP_MOMENTUM_WINDOW_TICKS 24:102`, `SWAP_MOMENTUM_STAGGER_TICKS 8:104`, `BLACK_FLASH_CHANCE 0.10:106`, `BLACK_FLASH_DAMAGE_MULTIPLIER 1.75:107`, `BLACK_FLASH_STAGGER_TICKS 14:108` |
| `src/main/java/jujutsu/mod/character/todo/TodoDefinition.java:15-90` | `applyAttributes:30-33`, `removeAttributes:36-39`, `adjustIncomingStaggerTicks:53-58` (`ceil*0.5 max 1`), `onDeselected->TodoStateLifecycle.dropEverything:74-76`, `registerServerHooks:42-49` |
| `src/main/java/jujutsu/mod/character/CharacterCombatModifiers.java:15-46` | `applyForSelection sweep:29-34`, `reapply:36-38`, `adjustedStaggerTicks:40-45` (только `ServerPlayer`) |
| `src/main/java/jujutsu/mod/combat/CombatStagger.java:9-44` | `GLOBAL:10`, `apply(UUID,gameTime,ticks):13-14 merge max`, `apply(LivingEntity):17-22` (`adjustedStaggerTicks` + `stopUsingItem` + `scale 0.15` + `hurtMarked`), `isStaggered:31-39` |
| `src/main/java/jujutsu/mod/registry/JujutsuEffects.java:14-82` | `RESONANT_MOMENTUM:15`, `TODO_SWAP_MOMENTUM:28-35` (`ADD_MULTIPLIED_TOTAL 0.25` на `ATTACK_DAMAGE`), `MEGUMI_SHADOW_GRIP:44-57` (`-0.75` + `-1.0`) |
| `src/main/java/jujutsu/mod/combat/BlackFlashStrike.java:16-83` | `APPLYING_BONUS Set<UUID>:17`, `rolls:21-23`, `isApplyingBonus:25`, `resolve:41-59` (`bonus=base*(mult-1)` + `CombatStagger` + `knockback` + `BlackFlashFocus`), `applyBonus pierce:61-82` (`invulnerableTime=0` try/finally + `Math.max` restore) |
| `src/main/java/jujutsu/mod/character/todo/TodoBlackFlashRuntime.java:20-58` | `register AFTER_DAMAGE:24`, `afterDamage:29-57` (`blocked/damageTaken<=0/isApplyingBonus` gate, `direct==owner==todo` + `selected==TODO` + `rolls 0.10` + `resolve true/14/2.0` + `broadcast NobaraVfxIds.BLACK_FLASH:54-56`) |
| `src/main/java/jujutsu/mod/character/todo/TodoSwapMomentumRuntime.java:40-133` + `TodoSwapMomentum.java:20-75` | `grant:48-60` (`addEffect 24t` + `LOGGER.debug` veto), `afterDamage:62-78` (`TodoSwapMomentum.decide`), `afterKill:93-108` (`decideOnKill` via `getLastDamageSource`), `consume:117-128` (`removeEffect` + `CombatStagger 8t` + `MOMENTUM_STRIKE` cue), `meleeAttacker:131-133` (`direct==entity`) |
| `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkNobaraProfile.java:3-157` | `HAMMER_HORIZONTAL 5.0:109`, `HAMMER_OVERHEAD 8.0:110`, `LIGHT_STAGGER 5:126`, `HEAVY_STAGGER 14:127`, `RESONANCE_DAMAGE 28:91`, `RESONANCE_SERVER_TICK_RATE 10:93`, `RESONANCE_SERVER_SLOW 20:95`, `RESONANT_MOMENTUM 1200t/1.15:51-52`, `BLACK_FLASH 0.10/1.75/chain 2.25/heal 4.0:101-106`, `MEGA_NAIL cap 42:72` |
| `src/main/java/jujutsu/mod/character/nobara/projectjjk/NobaraHammerCombatRuntime.java:35-363` | `PENDING/OVERHEAD_NEXT/BF_STREAK:37-40`, `handleInput:49-88` (`heldHammerHand` + `isStaggered` + `PENDING.contains`), `resolveImpact HORIZONTAL 120-134 sweep + deepen + LIGHT_STAGGER`, `OVERHEAD 142-149`, `tryProcPreparedNailBlackFlash:152-170` (`amplifyFlight` only), `tryProcLivingBlackFlash:172-204` (`BlackFlashStrike.resolve false/14/2.0-2.4`) |
| `src/main/java/jujutsu/mod/character/nobara/projectjjk/ResonantMomentum.java:8-40` | `grant 1200t:11-19`, `damageMultiplier 1+(1.15-1)*level:21-26`, `scaleTicks:28-35`, `accelerateElapsedTicks:37-39` |
| `src/main/java/jujutsu/mod/character/nobara/projectjjk/ServerTimeDilation.java:7-57` | `trigger:13-27` (overlap `min appliedRate`, `max remaining`), `tick:29-34`, `clear/restore:36-50` (`EPSILON 0.001`, `tickRate()==appliedRate` check) |
| `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkStrawDollRuntime.java:35-406` | `RESONANCE_TIME ServerTimeDilation:41`, `onOrdinaryNailHit remnant:65-99`, `tryStart DOLL_STRIKE:108-132`, `resolveImpact` (hurt 28 + `ResonantMomentum.grant` + `CombatStagger heavy` + `ServerTimeDilation.trigger 10/20` + `DOLL_STRIKE/RESONANCE_RELEASE` cues) |
| `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:4-104` | `POUNCE_BONUS_DAMAGE 2.0:29` (+ `DOG_ATTACK_DAMAGE 3.0:6` = 5), `POUNCE_KNOCKBACK 2.4:30`, `POUNCE_STAGGER 6:31`, `POUNCE 3-8 range/80t/16t:20-23`, `POUNCE_GRAVITY 0.08:27` |
| `src/main/java/jujutsu/mod/character/megumi/MegumiSummonRuntime.java:370-485` | `tickPounce:394-412`, `resolvePounceImpact:448-483` (`hurtServer DOG 3+2=5 owner-attributed`, `knockback 2.4 -direction`, `CombatStagger 6t` after `hurt==true`), `canLaunch: via MegumiPouncePolicy:10` |
| `src/main/java/jujutsu/mod/character/megumi/MegumiPouncePolicy.java:1-188` | `canLaunch 3-8 LOS 80t:10`, `launchVelocity 0.92+0.42-0.58:30`, `steerVelocity y-preserve:41`, `postMoveAction:55`, `exitVelocity damping 0.35:65` |
| `src/main/java/jujutsu/mod/character/megumi/MegumiDivineDogEntity.java:38-349` | `combatEnabled==ACTIVE only:263`, `hurtServer/isPickable guarded:276-289`, `launchPounce setNoAi:158`, `finishPounce setNoAi(false) only if ACTIVE:174` |
| `src/main/java/jujutsu/mod/combat/BlackFlashFocus.java:1-22` + `ForcedBlackFlash.java` | `grant tag + payload sync`, `rolls override` |
| `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkMegaNailRuntime.java:34-242` + `ProjectJjkNailEntity.java:382-501` | `MEGA_NAIL damage per-nail 4.0*depthWeight cap 42`, `knockback 1.9+0.2*count cap 3.0`, `CHARGE 24t FLIGHT 60t` |
| `src/main/java/jujutsu/mod/character/nobara/projectjjk/NailTrapRuntime.java:90-180` | `NAIL_TRAP_DAMAGE 15 12t stagger`, collapse 6t |

Дополнительно сверены: `AGENTS.md:43,80-83,363` (momentum/stagger канон), `docs/KNOWN_ISSUES.md` (hit-stop accepted, sweeping debt), `Jujutsu Kaizen/jujutsumod-codebase-codex/03-systems/Todo-Boogie-Woogie.md` и `Straw-Doll-resonance.md`, `TodoSwapMomentumTest.java`, `BlackFlashWindowTest.java`, `MegumiPouncePolicyTest.java`.

### 2) Находки

| severity | файл:строка | описание | рекомендация |
|---|---|---|---|
| **critical** | `src/main/java/jujutsu/mod/character/nobara/projectjjk/ServerTimeDilation.java:13-27` + `ProjectJjkStrawDollRuntime.java:41, resolveImpact` | **Resonance hit-stop — глобальный server TPS 10 на 20 тиков.** `ServerTimeDilation.trigger(10,20)` ставит `MinecraftServer.tickRate` в 10 через `TickRateAccess` для всего мира. При 1-2 игроках это accepted (`AGENTS.md:52, KNOWN_ISSUES.md`), но при публичном сервере фризит всех игроков, ломает редстоун/фармы/мобов, усугубляет десинк при оверлапе (overlap берет `min(appliedRate)` и `max(remaining)` — два Resonance за 1с держат 10 TPS 20 тиков вместо суммирования). Нет per-player изоляции. | Если продукт останется 1-2 игрока — оставить, но добавить в `KNOWN_ISSUES` явный лимит "не для >2 игроков". Для масштабирования — заменить глобальный `tickRate` на per-player `ServerTimeDilation` через `ServerPlayNetworking` + клиентский `tickDelta`/`camera hit-stop`, либо на `VfxCameraChannel` + `CombatStagger` без трогания серверного TPS. Сейчас `restore` с `EPSILON 0.001` и проверкой `tickRate==appliedRate` уже защищает от чужого изменения, но не от глобальности. |
| **major** | `src/main/java/jujutsu/mod/character/nobara/projectjjk/ResonantMomentum.java:21-26` | **Нет капа от бесконечного скейла через `amplifier`.** `damageMultiplier = 1 + (1.15-1)*level`, где `level = amplifier+1` без верхней границы. `grant` всегда `amplifier 0`, но `/effect give ... resonant_momentum 100 10` или сторонний мод дает `level 11` → `1+0.15*11=2.65x`. При `amplifier 255` → `39.4x`. Тогда `HAMMER 5*39.4=197`, `RESONANCE 28*39.4=1103`, `HAIRPIN 5*39.4=197` per nail. `scaleTicks` и `accelerateElapsedTicks` также ускоряют в `39x`. Защита от бесконечного скейла отсутствует. | Добавить `int level = Math.min(effect.getAmplifier()+1, 3)` (или 4) и `Math.min(multiplier, 1.45f)` — как у `MEGA_NAIL cap 42`. Или `damageMultiplier = 1 + 0.15 * Math.min(level, 3)`. Покрыть тестом `ResonantMomentumTest.levelCapped`. Тот же риск у `MEGUMI_SHADOW_GRIP` и `TODO_SWAP_MOMENTUM` не критичен (они не читают amplifier). |
| **major** | `src/main/java/jujutsu/mod/character/todo/TodoSwapMomentumRuntime.java:22-35,117-128` + `src/main/java/jujutsu/mod/registry/JujutsuEffects.java:28-35` | **Sweeping сохраняет 1.25x на вторичных целях после списания окна (accepted debt).** `Player.attack` копирует `ATTACK_DAMAGE` (включая `+0.25` от `TODO_SWAP_MOMENTUM ADD_MULTIPLIED_TOTAL`) в локальную `float` до sweep-блока (`1.0 + SWEEPING_DAMAGE_RATIO * damage`). `consume()` снимает эффект на первом `AFTER_DAMAGE`, но стековая локаль уже захвачена — цели 2..N получают буст бесплатно. Stagger и cue не дублируются (эффект уже снят). `AGENTS.md:363` и `KNOWN_ISSUES.md` фиксируют как deliberate, но для аудита боя это баланс-баг: один своп → зачистка толпы с 1.25x. | Либо принять как `PASS WITH CAVEATS` (стоимость — хотбар-своп: нужны пустые руки для clap → меч, 24т окно < обоих swap кулдаунов 60/100т, так что оверлап невозможен), либо фиксить миксином в `Player.attack` (пересчитать damage после `consume`) или заменой атрибута на `hurtServer` bonus hit (но тогда конфликт с Black Flash `APPLYING_BONUS`). Минимально: добавить в `TodoProfile.java:95-99` явный коммент про sweeping и в `TodoSwapMomentumTest` pin `sweepDoesNotDuplicateStagger`. Сейчас это уже частично задокументировано — не регрессировать. |
| **major** | `src/main/java/jujutsu/mod/character/nobara/projectjjk/NobaraHammerCombatRuntime.java:152-170` | **Black Flash на prepared гвозде не дает урона, только скорость.** `tryProcPreparedNailBlackFlash` при успехе `rollBlackFlash 0.10` делает `nail.amplifyFlight(mult)` (`1.75` / `2.25` при chain≥2) + `BlackFlashFocus.grant + heal 4.0 + ResonantMomentum.grant` при chain, но `resolveNailImpact` всегда наносит `NAIL_DAMAGE 2.0 * depthWeight` без `bonus = base*(mult-1)`. Т.е. визуально — Black Flash, геймплейно — 0 доп урона. Для `tryProcLivingBlackFlash:186-194` тот же proc дает `BlackFlashStrike.resolve` с реальным `hurtServer bonus` + `stagger 14` + `knockback 2.0/2.4`. Несоответствие контракту "Black Flash 1.75x". | Либо честно переименовать в "Black Flash haste" и зафиксировать в `ProjectJjkNobaraProfile`/`NobaraHammerCombatRuntime` javadoc, либо добавить `bonus` в `resolveNailImpact` (но гвоздь уже в полете — бонус надо вешать на `hurtServer` в точке импакта, а не на `amplifyFlight`). Дешевый фикс — при `roll` на prepared гвозде также применить `ResonantMomentum.grant` (уже есть при chain) и `CombatStagger 14` на цель при импакте (сейчас нет). Обновить `Jujutsu Kaizen/.../Combat-timing-and-black-flash.md`. |
| **major** | `src/main/java/jujutsu/mod/character/CharacterCombatModifiers.java:40-45` + `src/main/java/jujutsu/mod/character/todo/TodoDefinition.java:53-58` | **Stagger резист 0.5x применяется только к `ServerPlayer`, мобы/волки получают полный 100%.** `adjustedStaggerTicks` делает `if (!(entity instanceof ServerPlayer)) return requested`. Значит `Pounce 6т` на Todo-игроке → `ceil(6*0.5)=3т`, на Nobara/Megumi-игроке → `6т`, на мобе → всегда 6т, на волке → всегда 6т. Асимметрия не задокументирована, но балансно заметна: Todo почти не станится от собак. Обратная сторона: `NobaraHammerCombatRuntime LIGHT 5т` → Todo `3т` (`ceil 2.5`), `HEAVY 14т` (Black Flash, overhead, Mega Nail) → `7т`. `MEGA_NAIL`/`NailTrap 12т` → `6т`. | Если это намеренно — добавить в `TodoProfile.java:10` и `Vessel-definitions.md:35` фразу "только игроки, мобы без резиста". Если нет — расширить `adjustedStaggerTicks` на `LivingEntity` с vessel-зависимым резистом или вынести `STAGGER_DURATION_MULTIPLIER` в `MegumiProfile`/`NobaraProfile` (сейчас 1.0). Добавить тест `CharacterCombatModifiersTest.todoHalvesPounceStagger`. |
| **major** | `src/main/java/jujutsu/mod/combat/BlackFlashStrike.java:72-81` | **Pierce через `invulnerableTime=0` + `Math.max` restore может сокращать или продлевать i-frames.** `AFTER_DAMAGE` уже поставил `invulnerableTime=20` (или 10). `applyBonus` ставит `0`, `hurtServer` ставит `10` (или 20 в зависимости от source), `finally` делает `max(10,20)=20` — ок. Но если до `AFTER_DAMAGE` у цели был `invulnerableTime=0` (первый хит), после бонуса остается `10` вместо `20` — цель получает окно 10т вместо 20т для следующего хита, что делает Black Flash "пробивающим" сильнее чем 1.75x (дает еще один хит в окно). И наоборот, если цель уже имела `invulnerableTime=15` от прошлого хита, `max(15,10)=15` — бонус не продлевает. | Зафиксировать контракт: pierce должен *заменять* i-frame бонуса, а не миксовать. Вариант: сохранять `previous`, ставить `0`, после `hurtServer` делать `target.invulnerableTime = Math.max(previous, target.invulnerableTime)` — текущая логика уже так, но документировать что `previous` — это post-vanilla 20т, а не pre-hit. Добавить тест `BlackFlashStrikeTest.pierceDoesNotShortenInvuln`. Альтернатива — использовать `DamageSource` с `bypassesInvulnerability` вместо ручного нуля (но тогда armor bypass тоже). |
| **minor** | `src/main/java/jujutsu/mod/combat/CombatStagger.java:17-22` + `src/main/java/jujutsu/mod/combat/BlackFlashStrike.java:55-57` | **Порядок `stagger scale 0.15` vs `knockback` — `CombatStagger.apply` damp'ит `deltaMovement` до `knockback`.** `apply()` делает `setDeltaMovement(scale 0.15)` + `hurtMarked`, затем `BlackFlashStrike.resolve` делает `target.knockback(2.4)` — ок (knockback перезаписывает). Но в `MegumiSummonRuntime.resolvePounceImpact:476-478` порядок `knockback(2.4)` → `CombatStagger.apply(6т scale 0.15)`. Если `apply` после `knockback`, он умножит только что установленный knockback на 0.15 → фактический knockback `2.4*0.15=0.36` вместо 2.4. Проверка показала: в pounce текст-тесте `knockback` до `stagger`, а в Black Flash наоборот. Инконсистентно. | Унифицировать порядок: всегда `hurtServer` → `CombatStagger.apply` → `knockback` (как в `BlackFlashStrike`). Для pounce поменять местами `target.knockback` и `CombatStagger.GLOBAL.apply` в `MegumiSummonRuntime.java:476-478`. Добавить контрактный тест `staggerDoesNotDampKnockback`. Сейчас pounce knockback 2.4 визуально слабее чем Black Flash 2.4 из-за этого. |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoBlackFlashRuntime.java:44-53` + `src/main/java/jujutsu/mod/combat/BlackFlashStrike.java:58-59` | **Криты стакаются мультипликативно без капа.** Todo использует ванильный `Player.attack` → крит `1.5x` при падении уже включен в `baseDamageTaken`/`damageTaken` до `AFTER_DAMAGE`. `BlackFlashStrike.bonus = baseDamageTaken*(1.75-1)` берет *уже критовый* base, итого `1.5 (Todo attr) * 1.25 (momentum) * 1.5 (крит) * 1.75 (BF) = 4.92x` базы кулака. С мечом `sharpness` еще выше. Нет капа 42 как у Mega Nail, нет `min(bonus, cap)`. Nobara молот бьет `hurtServer` фиксой `5/8` без крита (криты отключены), так что там стака нет. | Для Todo принять как фичу (редкость `0.10 * крит-окно ~0.02 = 0.2%`) или добавить кап как у Mega Nail: `bonus = Math.min(base*0.75f, 20.0f)`. Или умножать `bonus = (base / critMultiplier)*0.75` чтобы крит не усиливал BF. Задокументировать в `TodoProfile.java:106-108` что BF считается от `baseDamageTaken` (уже с критом/атрибутом). |
| **minor** | `src/main/java/jujutsu/mod/character/megumi/MegumiSummonRuntime.java:448-478` + `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:29-31` | **Pounce урон 5 без скейла — защита от бесконечного скейла есть, но нет защиты от armor/reduction.** `DOG_ATTACK_DAMAGE 3.0 + POUNCE_BONUS 2.0 = 5.0` передается как `level.damageSources().playerAttack(owner)` через `hurtServer`. Это значит броня, `Resistance`, `Protection` режут 5 до ~2-3. Зато нет скейла от `ResonantMomentum`/`Momentum`/`BF` — правильно (изоляция). Но нет капа на частоту: `POUNCE_COOLDOWN 80т` per-dog, два пса → `5 dmg` каждые `40т` в среднем + `stagger 6т` + `knockback 2.4`. При `Sic` на афк-цели — бесконечный 2.5 dps без затрат. | Добавить `SIC_COOLDOWN 30т` уже есть на `trySic`, но не на pounce. Текущий per-dog `80т` + `3-8` дистанция + LOS уже ограничивает DPS до `5/4с=1.25` per dog → `2.5` total, что ниже `Nobara hammer 5/0.5с`. Оставить, но добавить в `MegumiProfile` коммент "pounce не скейлится от vessel баффов — deliberate". Проверить что `hurtServer` c `playerAttack` правильно атрибутирует килл Megumi (а не волку). |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoSwapMomentumRuntime.java:48-60` + `src/main/java/jujutsu/mod/registry/JujutsuEffects.java:28-35` | **Momentum окно — `MobEffectInstance` 24т `BENEFICIAL` с `showParticles=true`, стирается молоком/`/effect clear`/сторонними модами без `consume`.** `grant` делает `addEffect(TODO_SWAP_MOMENTUM,24,0,false,false,true)` — `showParticles=true`. Любой `clear` (ведро молока, команда, другой мод чистящий эффекты) снимет `TODO_SWAP_MOMENTUM` без `consume` → без stagger/куе, окно молча пропадает. `applyForSelection` и `CharacterCombatModifiers.reapply` не трогают effect-модификаторы, так что пере-заход сохраняет окно (правильно), но смена vessel `onDeselected` чистит через `TodoStateLifecycle.dropEverything`. | Сделать `TODO_SWAP_MOMENTUM` `HARMFUL`? Нет, тогда молоко тоже снимет. Лучше `MobEffectCategory.NEUTRAL` + `showParticles=false` + не давать молоку снимать через `MobEffectInstance` флаг? В ванилле молоко снимает все, кроме `NEUTRAL`? Проверить: молоко снимает все кроме `MobEffectCategory`? Фактически снимает все. Альтернатива — хранить окно не в эффекте, а в `TodoTransientState`/`CombatStagger` map. Минимально: поставить `showParticles=false` (сейчас `true:55` дает лишние частицы 24т) и документировать в `JujutsuEffects.java:28` что окно — gameplay state, не бафф. |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoProfile.java:101-104` + `src/main/java/jujutsu/mod/combat/CombatStagger.java:14` | **Bare fists баланс: 1.25x на 1.5 урона = +0.375 (0.19 сердца).** Кулак ванилла 1.0, Todo `1.5`, буст `1.875`. `ADD_MULTIPLIED_TOTAL` корректно дает `base*1.50*1.25` (javadoc `JujutsuEffects.java:24-26` верно отличает от `ADD_MULTIPLIED_BASE` который дал бы 1.75). Вывод `TodoProfile.java:95-99` честно пишет "stagger — payload, damage only if weapon". Но игрок ожидает видимый урон, а получает `+0.375`. | Оставить как есть (задумка — `displace → arm → hit`), но добавить в туториал/подсказку что `momentum` без оружия — почти только стан 8т. Или дать Todo стартовый меч/топор (сейчас `TodoProfile` без стартового лута — `AGENTS.md:40`). Не менять числа без продукта. |
| **minor** | `src/main/java/jujutsu/mod/character/todo/TodoDefinition.java:30-33` + `src/main/java/jujutsu/mod/character/CharacterCombatModifiers.java:29-33` | **Attribute modifiers — transient, пересоздаются на `JOIN/AFTER_RESPAWN/AFTER_PLAYER_CHANGE_WORLD`, но не на `DISCONNECT` другого игрока.** `CharacterCombatModifiers.register` слушает `JOIN`, `AFTER_RESPAWN`, `AFTER_PLAYER_CHANGE_WORLD`. Если атрибут слетел из-за `/attribute` команды или мода, он восстановится только на respawn/world-change, не на tick. Нет per-tick reapply. Зато `applyForSelection` делает `for(all) remove + selected.apply` — добавление новой vessel не трогает shared файл, инвариант Vessel Seam соблюден. Операция `ADD_MULTIPLIED_TOTAL` верна (не `ADD_VALUE`, не `ADD_MULTIPLIED_BASE`). | Добавить в `ServerTickEvents.END_SERVER_TICK` периодическую сверку раз в 100т (как `PairSelection` pulse) или оставить как есть — ваниль редко сбрасывает атрибуты. Сейчас `TodoDefinition.removeAttributes`/`applyAttributes` идемпотентны (`addOrUpdateTransientModifier` / `removeModifier`), так что повторный `reapply` безопасен. |

### 3) Позитивные наблюдения

- **Attribute modifiers — образцово через Vessel Seam.** `TodoDefinition.applyAttributes/removeAttributes:30-39` держат `DAMAGE_ID`/`ATTACK_SPEED_ID` + `TodoProfile` числа в одном файле, `CharacterCombatModifiers.applyForSelection:29-33` делает `for(all) remove + selected.apply` без именования Todo — новый vessel не трогает shared файл. `JujutsuEffects.TODO_SWAP_MOMENTUM:28-35` честно документирует `ADD_MULTIPLIED_TOTAL` (`1.50*1.25` vs `ADD_MULTIPLIED_BASE 1.75`). `reapply` на `JOIN/AFTER_RESPAWN/AFTER_PLAYER_CHANGE_WORLD:18-22` закрывает ванильные дропы атрибутов.
- **Stagger 0.5x — корректно `ceil` + `max 1` + `never invent`.** `TodoDefinition.adjustIncomingStaggerTicks:53-58` — `if <=0 return 0`, `max(1, ceil(requested*0.50))`. `CombatStagger.apply:13-14` — `merge max`, только `ticks>0`. `CharacterCombatModifiers.adjustedStaggerTicks:40-45` делегирует в vessel. Ни один путь не превращает `0` в `1`. `NobaraAbilityRouter:30-35` и `TodoSwapGates:37` гейтят касты через `isStaggered`, Megumi — через `locksAbilities` (отдельный аудит).
- **Black Flash 10% 1.75x 14т — честный shared путь.** `BlackFlashStrike.rolls 0.10` + `ForcedBlackFlash` debug-оверрайд, `bonus=base*(mult-1)` от `baseDamageTaken` (не `damageTaken` — не скейлится от брони), `resolve` атомарно `bonus hurt + stagger 14 + knockback 2.0/2.4 + focus`. `APPLYING_BONUS Set<UUID>` с `invulnerableTime=0` try/finally + `Math.max` restore — pierce один раз, без двойного `AFTER_DAMAGE`. `TodoBlackFlashRuntime:30-37` фильтрует `direct==owner==todo` + `selected==TODO` + `!spectator` + `damageTaken>0` + `!blocked` + `!isApplyingBonus`. `NobaraHammerCombatRuntime.tryProcLivingBlackFlash:186-194` переиспользует тот же `BlackFlashStrike` с `chain 2.25/heal 4.0/knockback 2.4`.
- **Hit-stop Resonance — изолированный `ServerTimeDilation` с overlap-политикой.** `ServerTimeDilation.trigger:18-26` — `restoreRate = tickRate()` только если `remaining<=0`, иначе `appliedRate = min(appliedRate, requested)` + `remaining = max(remaining, duration)`. `tick:29-34` декремент + `restore` только если `tickRate==appliedRate` (±`EPSILON 0.001`). `clear` на `SERVER_STOPPING:47-50` + `DISCONNECT`/`DEATH` в `ProjectJjkStrawDollRuntime:47-62`. Глобальность задокументирована как accepted для 1-2 игроков.
- **Защита от бесконечного скейла — многослойная.** `MEGA_NAIL cap 42 = 1.5*RESONANCE_DAMAGE 28` (`ProjectJjkNobaraProfile:72`), `EmbeddedNailRegistry` `MAX 30 per owner` + `TTL 1200т`, `MARK_MAX 4`, `Todo swap momentum window 24т < 60т cooldown` → рефреш невозможен структурно, `ResonantMomentum grant level 0` (не amplifier), `Todo momentum` — атрибут, а не `hurtServer` instance, `BlackFlashFocus` tag без стака, `Pounce` `3-8` дистанция + `80т` per-dog + LOS + `16т` timeout + `Sic` `30т` + owner-attributed один `hurtServer` на pounce.
- **Pounce 5 / 6т / 2.4 — чистая политика + сервер-авторитет.** `MegumiProfile:29-31` + `MegumiPouncePolicy.canLaunch 3-8 LOS 80т` + `launchVelocity 0.92/0.42-0.58` + `steerVelocity y-preserve` + `resolvePounceImpact:448-483` — один `hurtServer(DOG 3+2)` с `playerAttack(owner)`, затем `knockback 2.4` + `CombatStagger 6т` только если `hurt==true` + `DOGS_POUNCE` cue + звук. `ACTIVE` gate (`MegumiDivineDogEntity:263`) + `hurtServer guarded:276-289` + `leash 32 блока/10т` без `noBlockCollision` bypass.
- **Knockback 2.4 — консистентно, кроме одного порядка.** `ProjectJjkNobaraProfile: HEAVY_STAGGER 14` + `BlackFlash chain 2.4` (`NobaraHammerCombatRuntime:194`), `TodoBlackFlash 2.0` vs `2.4 chain` — читаемо, `Megumi pounce 2.4` (`MegumiProfile:30`) — единый максимум, `MEGA_NAIL cap 3.0` (`NobaraProfile:88`) — чуть выше как ультимативный расход, `NailTrap 1.9+0.2*count cap 3.0` — аналогично. Все через `LivingEntity.knockback(strength, -look.x, -look.z)` с `hurtMarked=true`.
- **Server-authoritative урон.** Все `hurtServer`/`damageSources().playerAttack` вызываются на `ServerLevel`/`ServerPlayer`, VFX только `broadcastVfxCue` после `hurt==true`. Нет клиентского урона, нет второго `hurtServer` у momentum (атрибут), нет `noSave` персиста у `TODO_STONE`/`MEGUMI_DIVINE_DOG`.

### 4) Вердикт

**PASS WITH CAVEATS**

Срез `31-combat-damage` **функционален и безопасен для текущего продукта (приват 1-2 игрока)**, Vessel Seam и server-authoritative инварианты не нарушены, все tuning-константы централизованы в `*Profile`, защита от бесконечного скейла есть на каждом пути (cap 42, 30 гвоздей/4 метки, 24т < 60т, 80т pounce, LOS/дистанция).

**Caveats — 4 deliberate + 2 скрытых баланса (не блокеры для PASS, но требуют решения до паблика):**
- `critical` глобальный hit-stop 10 TPS 20т — принять для 1-2 игроков, переписать на per-player для паблика.
- `major` sweeping leak 1.25x на толпе — accepted debt, задокументирован в `AGENTS.md:363`, не эксплуатируется без хотбар-свопа.
- `major` prepared-nail BF без урона — визуальный обман, требует либо дока, либо `bonus` в импакте.
- `major` `ResonantMomentum` без капа amplifier — внешний `/effect` ломает баланс в 39x.
- `minor` pounce `knockback→stagger` порядок damp'ит 2.4 до 0.36 — инвертировать.
- `minor` `TODO_SWAP_MOMENTUM showParticles=true` + milk-clear — скрытая потеря окна.

**Приоритет фиксов:**

1. **P0 (до паблика):** `ServerTimeDilation` — per-player hit-stop или удаление глобального TPS (`31-combat-damage.md: critical ServerTimeDilation`). Сейчас `PASS` только для `AGENTS.md:52` "1-2 player target".
2. **P1 (ближайший спринт):** `ResonantMomentum` кап `level≤3` (`ResonantMomentum.java:24`), `NobaraHammerCombatRuntime.tryProcPreparedNailBlackFlash` — либо урон, либо честный javadoc, `MegumiSummonRuntime pounce knockback→stagger` порядок (`MegumiSummonRuntime.java:476-478`).
3. **P2 (баланс/полировка):** `TodoSwapMomentum sweeping` — pin тестом + коммент, `BlackFlashStrike pierce` доку + тест `invuln`, `TODO_SWAP_MOMENTUM particles false`, `bare fists` туториал.
4. **P3 (тесты/доки):** добавить `CharacterCombatModifiersTest.todoHalvesPounceStagger`, `BlackFlashStrikeTest.pierce`, `ResonantMomentumTest.levelCapped`, `MegumiPouncePolicyTest.knockbackAfterStagger`.

AUDIT_ID: 31-combat-damage
