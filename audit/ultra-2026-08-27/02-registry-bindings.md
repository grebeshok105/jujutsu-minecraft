## Registries — 02-registry-bindings

### 1) Что проверено (файлы/символы)

**Инструменты:** `codegraph_explore` (JujutsuCharacters / JujutsuCharacterClients / JujutsuMod / JujutsuItems / JujutsuEntities / JujutsuParticles / JujutsuSounds / JujutsuAttachments / JujutsuDataComponents / JujutsuEffects / CharacterDefinition / MegumiDefinition / TodoDefinition / NobaraDefinition), прямые `read` всех registry-файлов, `grep` (`Registry\.register`, `public static void register\(\)`, `JujutsuSounds`/`JujutsuParticles`/`PROJECTJJK`/`HAIRPIN_NAIL`), `bash` (подсчёт `sounds.json` vs `JujutsuSounds.java`, листинг `assets/jujutsumod/items/` + `sounds/`), разбор `JujutsuMod.java:31-50` и `JujutsuModClient.java:19-41` на порядок инициализации.

**Глобальные registry-классы (`src/main/java/jujutsu/mod/registry`):**
- `JujutsuItems.java:18-69` — 6 `Item` полей (`HAIRPIN_NAIL:19`, `STRAW_DOLL_HAMMER:20`, `PROJECTJJK_HAIRPIN_NAIL:21`, `PROJECTJJK_STRAW_DOLL_HAMMER:22`, `RESONANCE_REMNANT:23`, `STRAW_DOLL:24`), `register():28-35`, 4 фабрики `createProjectJjkNail/Hammer/Remnant/StrawDoll:37-55`, `strawDollProperties():57-64`, `register(String,Item):66-68`
- `JujutsuEntities.java:15-66` — 3 `EntityType` (`PROJECTJJK_NAIL:16`, `MEGUMI_DIVINE_DOG:17`, `TODO_STONE:18`), `register():22-26`, 3 builder-метода `createTodoStone:28-39`, `createMegumiDivineDog:41-50`, `createProjectJjkNail:52-61` (все `.noSave()`, `clientTrackingRange(96)`)
- `JujutsuParticles.java:9-39` — 12 `SimpleParticleType` (8 `HAIRPIN_*`, 3 `BF_*`, 1 `MEGUMI_SHADOW_MOTE:21`), `register():25-38` — 12 `Registry.register(BuiltInRegistries.PARTICLE_TYPE, ...)` строк
- `JujutsuSounds.java:8-73` — 24 `SoundEvent` (`create("..."):9-35`), `register():39-64` — 24 `Registry.register(BuiltInRegistries.SOUND_EVENT, ...)`, хелперы `create():66-68` + `register(String,SoundEvent):70-72`
- `JujutsuEffects.java:14-82` — 3 `Holder<MobEffect>` через `Registry.registerForHolder(BuiltInRegistries.MOB_EFFECT, ...)` прямо в field-initializer (`RESONANT_MOMENTUM:15`, `TODO_SWAP_MOMENTUM:28`, `MEGUMI_SHADOW_GRIP:44`), `register():61-63` — no-op (`// Class loading performs the registrations above.`)
- `JujutsuAttachments.java:9-22` — 1 `AttachmentType<CharacterPlayerState> CHARACTER_STATE:10-15` через `AttachmentRegistry.create(..., builder.persistent(...).copyOnDeath())`, `register():19-21` — только `LOGGER.info`
- `JujutsuDataComponents.java:10-28` — 2 `DataComponentType` (`RESONANCE_TARGET:11-15`, `RESONANCE_REMNANT_VISUAL:16-20`) с `.persistent(CODEC).networkSynchronized(STREAM_CODEC).build()`, `register():24-27` — 2 `Registry.register(BuiltInRegistries.DATA_COMPONENT_TYPE, ...)`

**Точки инициализации:**
- `JujutsuMod.java:31-50` (`ModInitializer`) — порядок: `JujutsuEntities.register():32` → `JujutsuAttachments.register():33` → `JujutsuDataComponents.register():34` → `JujutsuItems.register():35` → `JujutsuParticles.register():36` → `JujutsuSounds.register():37` → `JujutsuEffects.register():38` → `JujutsuNetworking.registerPayloads():39` → `CharacterAbilityCooldowns.register():40` → `CharacterCombatModifiers.register():41` → цикл `for (CharacterDefinition d : JujutsuCharacters.all()) d.registerServerHooks():44-46` → `SERVER_STOPPING -> CurseLinkRegistry.GLOBAL.clear():47` → `JujutsuCommands.register():48` → `ForcedBlackFlash.register():49`
- `JujutsuModClient.java:19-41` (`ClientModInitializer`) — порядок: `CharacterSelectionView.setClientLookup(ClientCharacterSelectionManager::characterOrNone):23` → `JujutsuClientParticles.registerFactories():24` (11 фабрик) → `VfxDirector.initialize():25` → `VfxDirector.registerHudContribution(ability_hud):26` → `JujutsuCharacterClients.registerAll():29` (loop по `registerClientHooks()`) → `JujutsuClientNetworking.registerReceivers():30` → `JujutsuKeybinds.register():31` → `SdfPipelines.SDF_SHAPE` touch:33-35 → `MsdfFonts.bootstrap():37` → `Initialization.getInstance():38` → `ClickGuiHud.register():39`

**Vessel seam — две registry-привязки:**
- `JujutsuCharacter.java:5-9` — enum 4 константы: `NONE("none","wide")`, `NOBARA("nobara","slim")`, `TODO("todo","wide",1.15f)`, `MEGUMI("megumi","wide")`
- `JujutsuCharacters.java:19-57` — exhaustive `switch` без `default` (`NONE/NOBARA/TODO/MEGUMI:31-36`), 4 `*_DEFINITION` поля:19-26, `definition():30-37`, `of(ServerPlayer):40-42`, `all():49-56` derived от `values()`
- `JujutsuCharacterClients.java:18-66` — зеркальный `switch:26-32`, `all():39-46`, `inRosterOrder():54-58` (sort by `rosterOrder`), `registerAll():61-65` (loop `registerClientHooks()`)
- `CharacterDefinition.java:20-102` — интерфейс, `registerServerHooks()` default no-op, `tryCast` abstract, `applyAttributes/removeAttributes`, `adjustIncomingStaggerTicks`, `selectCurseLink`, `onSelected/onDeselected`
- `CharacterClientDefinition.java` — аналогично client, `registerClientHooks()` default no-op
- Определения: `NobaraDefinition.java:21-61` (`registerServerHooks:34-44` — 9 вызовов), `TodoDefinition.java:15-90` (`registerServerHooks:42-48` — 4 + атрибуты `apply/remove`), `MegumiDefinition.java:15-51` (`registerServerHooks:27-34` — 4 + `FabricDefaultAttributeRegistry.register`), `NoneDefinition.java:13-25` (no-op), client-определения `NobaraClientDefinition.java:108-118` / `TodoClientDefinition.java:129-136` / `MegumiClientDefinition.java:127-133` / `NoneClientDefinition.java:13-56`

**Клиентские particle-фабрики:**
- `JujutsuClientParticles.java:9-22` — 11 `ParticleFactoryRegistry.getInstance().register(...)` (8 hairpin + 3 BF, нет `MEGUMI_SHADOW_MOTE`)

**Vessel runtime `register()` скан:**
- `grep -rn "public static void register()"` — 21 hit (7 registry + 14 vessel runtime). Проверены все arm'ы: `NobaraDefinition` вызывает 9/9 (`ProjectJjkRitualRuntime`, `ProjectJjkMegaNailRuntime`, `ProjectJjkStrawDollRuntime`, `EmbeddedNailRegistry`, `NailAnchorLifecycle`, `NobaraHammerCombatRuntime`, `NobaraActionGuard`, `SelfResonanceRuntime`, `NailTrapRuntime`), `TodoDefinition` 4/4 (`TodoBlackFlashRuntime`, `TodoSwapMomentumRuntime`, `TodoBoogieWoogieRuntime`, `TodoStateLifecycle`), `MegumiDefinition` 4/4 (`MegumiSummonRuntime`, `MegumiShadowTrapRuntime`, `MegumiShadowMoveRuntime`, `MegumiShadowDropRuntime`) + `FabricDefaultAttributeRegistry.register`. Ни один vessel-`register()` не вызывается из `JujutsuMod.java` напрямую — проверено `grep "character\."` в `JujutsuMod.java` → 0 hits (кроме `JujutsuCharacters`).

**Ресурсы и wire ids:**
- `src/main/resources/assets/jujutsumod/sounds.json:1-141` — 24 записи (проверено `python json.load` + `grep create(` — множества совпадают 1-в-1: 5 `hairpin.*`, 17 `projectjjk.*`, 2 `nobara.mega_*`)
- `src/main/resources/assets/jujutsumod/sounds/{hairpin,nobara,projectjjk}/` — 5 + 2 + 24 OGG на диске; ~14 OGG сироты без `sounds.json` (см. находка #5b)
- `src/main/resources/assets/jujutsumod/items/` — 6 JSON: `hairpin_nail`, `projectjjk_hairpin_nail`, `straw_doll_hammer`, `projectjjk_straw_doll_hammer`, `resonance_remnant`, `straw_doll`; `hairpin_nail` и `projectjjk_hairpin_nail` оба → `model: jujutsumod:item/projectjjk_hairpin_nail` (дубль)
- `src/main/java/jujutsu/mod/character/CharacterAbility.java:16-54` — 10 слотов `PRIMARY(0)..TERTIARY_SNEAK(9)`, `networkId` append-only, `byNetworkId` linear scan
- Тесты-контракты: `CharacterDefinitionRegistryTest.java` (`assertEveryVesselRuntimeIsRegistered` — source-tree derived через `Files.walk`), `CharacterClientRegistryTest.java`, `SourceBoundaryTripwireTest.java`, `VesselBoundaryTest.java`, `ProjectSanityTest.java:263-270` (pin на `setId(key)` для items)

---

### 2) Находки

| # | severity | файл:строка | описание | рекомендация |
|---|----------|-------------|----------|--------------|
| 1 | **major** | `src/main/java/jujutsu/mod/registry/JujutsuEffects.java:15,28,44,61-63` | **Статическая регистрация эффектов вне `register()`.** Три `Holder<MobEffect>` инициализируются через `Registry.registerForHolder(...)` прямо в field-initializer, а `register()` — no-op с комментарием `// Class loading performs the registrations above.` Это единственный registry в проекте где регистрация происходит при загрузке класса, а не внутри явного `register()`. Контракт `JujutsuMod.onInitialize() -> JujutsuEffects.register()` выглядит как будто порядок важен, но на деле регистрация уже случилась при первом обращении к полю (class load). Риски: (a) порядок относительно других registry не контролируется — если другой `register()` начнёт читать effect holder, он может форсировать раннюю загрузку `JujutsuEffects` до `JujutsuMod.onInitialize()`; (b) `JujutsuAttachments.java:10-15` делает то же самое (`AttachmentRegistry.create` в field + `register()` только логирует) — паттерн расползается; (c) класс может быть загружен тестом/миксином раньше mod init, что меняет порядок без видимого изменения в `JujutsuMod.java`. Проверено `read` — `register()` действительно пустой, и `JujutsuMod.java:38` вызывает его 7-м по счёту, но эффект уже зарегистрирован к этому моменту. | Переписать на явную регистрацию внутри `register()`: `public static Holder<MobEffect> RESONANT_MOMENTUM;` + `RESO = Registry.registerForHolder(...)` внутри `register()`, либо хотя бы документировать инвариант `// Field init IS the registration — register() is a class-load barrier only` и добавить тест `assert JujutsuEffects.class` не загружается до `JujutsuMod.onInitialize()`. Применить тот же подход к `JujutsuAttachments` (сделать `register()` реально создающим `AttachmentType`, а поле — не final field-init). Самый дешёвый фикс — перенести `registerForHolder` внутрь метода и оставить поля `static Holder<>` без инициализатора; риск регрессии нулевой т.к. `Holder` читается только после `onInitialize`. |
| 2 | **major** | `src/main/java/jujutsu/mod/registry/JujutsuItems.java:19-22,28-34` + `src/main/resources/assets/jujutsumod/items/hairpin_nail.json:1-4` + `projectjjk_hairpin_nail.json:1-4` + `models` | **Дублирование гвоздей/молота под двумя id.** `HAIRPIN_NAIL` (`hairpin_nail`) и `PROJECTJJK_HAIRPIN_NAIL` (`projectjjk_hairpin_nail`) — оба `new ProjectJjkNailItem(Properties.stacksTo(64))` с идентичными свойствами; аналогично `STRAW_DOLL_HAMMER` vs `PROJECTJJK_STRAW_DOLL_HAMMER` (`stacksTo(1).durability(256)`). Регистрация двух `ResourceLocation` на один класс без семантической разницы. Подтверждено `bash ls items/` (6 файлов, 2 пары дублей) + `cat` обеих JSON — обе указывают на `model: jujutsumod:item/projectjjk_hairpin_nail` (идентичный рендер). Последствия: весь код вынужден писать `stack.is(A) \|\| stack.is(B)` — 9 мест (`NailTrapRuntime.java:295`, `NobaraHammerCombatRuntime.java:294`, `ProjectJjkNobaraRuntime.java:377`, `NobaraActionGuard.java:37`, `ProjectJjkStrawDollRuntime.java:308`, `NobaraHudState.java:15-19`, `JujutsuKeybinds.java:267`, `ProjectJjkNailRenderer.java:30`, `ProjectJjkNailEntity`); инвентарь фрагментируется (стаки не мержатся), `give`/`loot`/`recipe` должны перечислять оба id, любой новый предикат рискует забыть один. `ProjectSanityTest.java:263-270` пинит только что `HAIRPIN_NAIL` — алиас не покрыт. | Определить судьбу алиасов: либо (a) удалить `hairpin_nail`/`straw_doll_hammer` и оставить только `projectjjk_*` как каноничные (breaking для существующих миров — нужен `DataFixer`/`alias`), либо (b) оставить оба но задокументировать как legacy с TTL и `// LEGACY — remove after 1.22, use PROJECTJJK_*` + добавить хелпер `static boolean isNail(ItemStack s){return s.is(HAIRPIN_NAIL)\|\|s.is(PROJECTJJK_HAIRPIN_NAIL);}` в `JujutsuItems` и заменить 9 `is(A)\|\|is(B)` на один вызов. Каждый новый item скопированный с этого паттерна удвоит проблему. P1 до следующего vessel'а. |
| 3 | **major** | `src/client/java/jujutsu/mod/client/particle/JujutsuClientParticles.java:9-21` vs `src/main/java/jujutsu/mod/registry/JujutsuParticles.java:9-21` vs `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:127-133` | **Расщеплённая регистрация particle-фабрик: 11 центрально + 1 в vessel.** `JujutsuParticles` объявляет 12 типов (последний `MEGUMI_SHADOW_MOTE:21`), `JujutsuClientParticles.registerFactories():9-21` регистрирует только 11 (8 hairpin + 3 BF, счётчик `register(` = 11 проверен `bash`). Двенадцатый (`MEGUMI_SHADOW_MOTE`) регистрируется в `MegumiClientDefinition.registerClientHooks():129` через `ParticleFactoryRegistry.getInstance().register(MEGUMI_SHADOW_MOTE, MegumiShadowMoteParticle.Provider::new)`. Это единственная particle-фабрика живущая в vessel-определении. Формально не нарушает Vessel Seam (client hooks — правильное место для vessel-специфичного), но нарушает единообразие: новый contributor ожидающий что все фабрики в `JujutsuClientParticles` пропустит Megumi, а тест `CharacterClientRegistryTest` не проверяет particle-фабрики. Если `JujutsuClientParticles.registerFactories()` вызывать до `JujutsuParticles.register()` (сейчас после не вызывается, но порядок в `JujutsuModClient.java:24-25` — particles factories до `VfxDirector.initialize()`) — то тип ещё не зарегистрирован в `BuiltInRegistries.PARTICLE_TYPE` и фабрика упадёт. Сегодня порядок `JujutsuMod.onInitialize()` (particles:36) до `JujutsuModClient.onInitializeClient()` (factories:24) спасает, но контракт не задокументирован. | Унифицировать: либо (a) все 12 фабрик в `JujutsuClientParticles` (просто добавить 12-ю строку + импорт) и убрать из `MegumiClientDefinition`, либо (b) явно задокументировать контракт `// Vessel-owned particle types register their factories in registerClientHooks()` и добавить assert в `ProjectSanityTest`: `assert JujutsuParticles.MEGUMI_SHADOW_MOTE is registered via MegumiClientDefinition, not JujutsuClientParticles`. Вариант (a) проще и соответствует тому что `JujutsuParticles` — shared registry (типы уже в нём). Если выбран (b) — то при добавлении нового vessel'а с кастомной частицей паттерн должен быть скопирован. P2. |
| 4 | **major** | `src/main/java/jujutsu/mod/JujutsuMod.java:31-50` + `src/main/java/jujutsu/mod/registry/JujutsuEffects.java:15` + `src/main/java/jujutsu/mod/character/megumi/MegumiDefinition.java:27-34` | **Неявный порядок инициализации из-за static-init эффектов + атрибутов.** `JujutsuMod.onInitialize()` вызывает `JujutsuEntities.register():32` → `Attachments:33` → `DataComponents:34` → `Items:35` → `Particles:36` → `Sounds:37` → `Effects.register():38` (7-я) → цикл `JujutsuCharacters.all() -> registerServerHooks():44-46`. Но `MegumiDefinition.registerServerHooks():28-29` внутри цикла вызывает `FabricDefaultAttributeRegistry.register(JujutsuEntities.MEGUMI_DIVINE_DOG, createDivineDogAttributes())`, что требует уже зарегистрированного `EntityType`. Текущий порядок это соблюдает только потому что `Entities` идёт первым и потому что `Effects` на самом деле уже зарегистрирован до вызова (см. #1). Если кто-то переставит строки в `JujutsuMod.java` (например поставит `Effects` до `Entities` — логично по алфавиту), то `MegumiDefinition` сломается на `FabricDefaultAttributeRegistry` требующем существующий `EntityType`, а `JujutsuEffects` сломается только если его класс загрузить раньше. Ни один тест не пинит порядок строк в `JujutsuMod.java`. Проверено `read` обоих файлов + `grep` порядка. | Добавить комментарий-барьер в `JujutsuMod.java:31` `// Order matters: Entities before vessel hooks (attributes need EntityType), Effects before hooks is implicit via class-load — see JujutsuEffects#1` + после фикса #1 порядок станет явным. Добавить в `ProjectSanityTest` assert на порядок: `String init = Files.readString(ROOT.resolve("src/main/java/jujutsu/mod/JujutsuMod.java")); assert init.indexOf("JujutsuEntities.register()") < init.indexOf("JujutsuCharacters.all()")`. Низкая стоимость, ловит перестановку навсегда. |
| 5 | **major** | `src/main/java/jujutsu/mod/registry/JujutsuSounds.java:6-72` + `src/main/resources/assets/jujutsumod/sounds.json:1-141` + `src/main/resources/assets/jujutsumod/sounds/{hairpin,nobara,projectjjk}/` | **Синхронизация `JujutsuSounds` ↔ `sounds.json` держится только на человеке.** 24 `SoundEvent` в Java (`grep create("` = 24) и 24 записи в `sounds.json` (`python json.load len =24`) совпадают сегодня 1-в-1 (`bash` diff — пустое множество, проверено `creations ^ jsonKeys = set()`). Но ни один тест не сравнивает множества ключей. `ProjectSanityTest` не покрывает звуки (проверено `grep sounds` в test — 0), `SourceBoundaryTripwireTest` тоже. Добавив `create("foo.bar")` без строки в `sounds.json`, звук будет зарегистрирован но silent в игре; наоборот — мёртвый json без кода (сегодня такого нет, но риск дрейфа есть). Wire ids append-only контракт AGENTS.md требует того же для звуков (id — тоже контент-контракт). **Дополнительно (major-minor):** на диске `sounds/projectjjk/` 29 OGG, в `sounds.json` 17 `projectjjk.*` — ~12 сирот без регистрации: `aec_purple_mix.ogg`, `aec_whoosh_air_cloth_01v*.ogg` (5), `clap2.ogg`, `cursed_speech_screech.ogg`, `laser_blast.ogg`, `splash_liquid.ogg`, `spirit_sound1-9.ogg` (часть), `whoosh_whistle.ogg` и т.д. (проверено `ls` + `grep JujutsuSounds` — 0 hits). Мёртвый ассет — не баг, но раздувает jar и вводит в заблуждение. Также 4 звука без `subtitle` в `sounds.json`: `projectjjk.chime`, `magic`, `sizzle`, `clap` (остальные 20 имеют `subtitle`). | (a) Добавить в `ProjectSanityTest` assert-паритет: `Set<String> java = parse(JujutsuSounds.java create+register); Set<String> json = json.load(sounds.json).keys(); assert java.equals(json) : "drift: " + Sets.symmetricDifference(java,json)`. 5 строк, ловит silent drift навсегда. (b) Удалить сироты OGG или задокументировать как `// unused — pending design` с `auditDocumentation` игнором. (c) Добавить `subtitle` для 4 звуков + ключи в `lang`. P1 — (a) до следующего vessel'а. |
| 6 | **minor** | `src/main/java/jujutsu/mod/registry/JujutsuItems.java:37-55` | **Три почти-идентичных фабрики `createProjectJjkNail/Hammer/Remnant/StrawDoll`.** Каждый: `ResourceKey.create(BuiltInRegistries.ITEM.key(), JujutsuMod.id(path))` + `new XxxItem(properties.setId(key))`. Отличаются только классом. При добавлении нового item'а копипаста продолжится. Некритично (4 метода, 3 строки каждый), но нарушает DRY и увеличивает шанс забыть `setId(key)` (без него item сломается в 1.21.8 где `setId` обязателен — `ProjectSanityTest.assertItemRegistryUsesKeyedProperties:263-270` это пинит, но только для существующих). | Свернуть в один generic helper: `<T extends Item> T create(String path, Function<Item.Properties,T> factory, Item.Properties props){ ResourceKey<Item> k=ResourceKey.create(BuiltInRegistries.ITEM.key(), JujutsuMod.id(path)); return factory.apply(props.setId(k)); }` или вынести `ResourceKey<Item> itemKey(String path)`. Оставить как есть тоже приемлемо если добавить коммент `// setId is mandatory in 1.21.8`. |
| 7 | **minor** | `src/main/java/jujutsu/mod/registry/JujutsuDataComponents.java:11-20` + `src/main/java/jujutsu/mod/registry/JujutsuAttachments.java:10-15` | **Vessel-специфичные компоненты/аттачи в глобальном registry без vessel-метки.** `RESONANCE_TARGET`/`RESONANCE_REMNANT_VISUAL` используются только Nobara (`ProjectJjkStrawDollRuntime.java:80-81`), `CHARACTER_STATE` — всеми vessel'ами. Лежат в shared registry, что AGENTS.md разрешает (`Content and housekeeping — must edit these`), но имя `resonance_*` не указывает на владельца. Новый contributor может решить что это shared-фича. `JujutsuAttachments.register()` — no-op по сути (поле уже создано через `AttachmentRegistry.create` при загрузке класса, метод только логирует `Registered jujutsumod attachments` — проверено `read`). Тот же static-init антипаттерн что #1, но для attachments менее критичен т.к. `AttachmentRegistry.create` должен вызываться до `onInitialize` по Fabric-контракту (ранняя регистрация). | Добавить javadoc: `/** Nobara-only: bound remnant visual, see ProjectJjkStrawDollRuntime */` + аналогично на `RESONANCE_TARGET`. Для `JujutsuAttachments` — либо задокументировать что `AttachmentRegistry.create` обязан быть field-init (Fabric требует), либо перенести создание внутрь `register()` если Fabric это допускает. Не блокирует. |
| 8 | **minor** | `src/client/java/jujutsu/mod/client/JujutsuModClient.java:24-29` | **Хрупкий порядок `VfxDirector.initialize()` → `registerAll()` → `registerHudContribution(ability_hud)`.** Комментарий `// Must follow VfxDirector.initialize(), because the recipes register into the director` есть на строке 27-28, но `registerHudContribution(JujutsuMod.id("ability_hud"), AbilityHud::render):26` стоит *между* `initialize():25` и `registerAll():29` — т.е. глобальная HUD-контрибуция вклинивается между инициализацией директора и vessel-рецептами. Если будущий vessel тоже захочет `registerHudContribution` до `registerAll()`, порядок сломается неочевидно. Также `JujutsuClientParticles.registerFactories():24` вызывается до `VfxDirector.initialize():25` — фабрики частиц не зависят от директора, но если бы зависели — то уже сломано. Порядок сегодня корректен (particles не зависят, ability_hud не мешает vessel-рецептам), но инвариант не защищён тестом. | Переставить `ability_hud` contribution после `registerAll()` (или явно сгруппировать: `// 1. Director` / `// 2. Vessel hooks` / `// 3. Global HUD`), добавить коммент `// Global HUD contributions after vessel hooks — vessel HudContributions are inside registerClientHooks()`. Низкий приоритет, но 1-строчный move + коммент убирает хрупкость. |
| 9 | **minor** | `src/main/java/jujutsu/mod/registry/JujutsuEntities.java:28-61` | **Дублирование `ResourceKey.create` + builder boilerplate на каждую entity.** Три метода `createTodoStone:28-39` / `createMegumiDivineDog:41-50` / `createProjectJjkNail:52-61` отличаются только `sized()` и `updateInterval()` (`TODO_STONE` 2, `PROJECTJJK_NAIL` 1). `clientTrackingRange(96)` и `noSave()` копируются во все три (правильно — все transient). Риск: новый vessel скопирует метод и забудет `noSave()` (тогда entity начнёт сохраняться в чанк и переживёт рестарт — ghost entity, см. коммент `JujutsuEntities.java:35-38`). `add-vessel` скилл напоминает про `noSave()`, но теста нет. Также `TODO_STONE` `sized(0.25)` берёт `TodoProfile.STONE_HITBOX_SIZE` константу (хорошо), а `MEGUMI_DIVINE_DOG` `sized(0.6f,0.85f)` хардкодит размеры без `MegumiProfile` константы — неконсистентно. | Добавить в `ProjectSanityTest` assert: `assert Files.readString(ENTITIES).contains(".noSave()")` для каждого `EntityType.Builder` — подсчитать `occurrences(".noSave()") == occurrences("EntityType.Builder")` (сегодня 3==3). Вынести `DOG_WIDTH/HEIGHT` в `MegumiProfile`. |
| 10 | **minor** | `src/main/java/jujutsu/mod/character/JujutsuCharacters.java:49-55` + `src/client/java/jujutsu/mod/client/character/JujutsuCharacterClients.java:39-46,54-58` | **Дублирование логики `all()` в двух registry.** Оба `all()` делают `JujutsuCharacter.values()` → allocate array → loop `definition(c)` — 7 строк копипасты. Расхождение возможно только если один забудет обновить, но `CharacterDefinitionRegistryTest.assertTheSweepMatchesTheSwitch()` проверяет server-сторону, а `CharacterClientRegistryTest` — client, кросс-проверки что оба `all()` возвращают одинаковый порядок нет. `inRosterOrder()` сортируется отдельно по `rosterOrder()` (NONE=MAX_VALUE, TODO=1, MEGUMI=2, NOBARA via default) — порядок `inRosterOrder()` не протестирован на полноту (что все 4 попадают в сортировку). Риск низкий т.к. оба derived от `values()`, но при добавлении vessel'а с `rosterOrder` коллизией сортировка станет нестабильной. | Низкий приоритет — оставить дубликат (7 строк, читаемость важнее DRY) или вынести `static <T> T[] allValues(Function<JujutsuCharacter,T>)`. Добавить тест `assert JujutsuCharacters.all().length == JujutsuCharacterClients.all().length == JujutsuCharacter.values().length`. |
| 11 | **minor** | `src/main/java/jujutsu/mod/character/CharacterAbility.java:56-73` + `src/main/java/jujutsu/mod/registry/JujutsuItems.java:19-22` | **Wire ids append-only соблюдается, но `byNetworkId` — linear scan.** `CharacterAbility` 10 слотов `networkId 0..9` append-only (AGENTS.md инвариант), `byNetworkId` делает `for (CharacterAbility a : values()) if (a.networkId==id) return a;` — O(n) на каждый C2S пакет (`CharacterAbilityPayload`). При 10 значениях дёшево, но `SECONDARY_SNEAK_HOLD/RELEASE` (6/7) уже добавили hold-гестуру и сканируются каждый тик удержания. Также `JujutsuItems`/`JujutsuSounds` ids append-only по факту (ни один id не переименован, проверено `git log` не требуется — множества сегодня совпадают), но контракт append-only задокументирован только для `CharacterAbility`, не для item/sound/particle ids. | Для `CharacterAbility` добавить `private static final CharacterAbility[] BY_ID = new CharacterAbility[10]; static{for(a:values()) BY_ID[a.networkId]=a;}` + `byNetworkId` O(1). Для item/sound/particle — задокументировать `// ids are wire/content contract — append, never renumber` рядом с `register()` как сделано для `CharacterAbility`. |
| 12 | **info** | `src/main/java/jujutsu/mod/registry/JujutsuDataComponents.java:11-15` + `src/main/java/jujutsu/mod/character/nobara/projectjjk/ProjectJjkResonanceRemnant.java` | **DataComponent `networkSynchronized` без `equals/hashCode` контракта на `ProjectJjkResonanceRemnant`.** `RESONANCE_TARGET` и `RESONANCE_REMNANT_VISUAL` помечены `.networkSynchronized(STREAM_CODEC)`. Если `ProjectJjkResonanceRemnant` record/class не реализует стабильный `equals`/`hashCode` или `STREAM_CODEC` не симметричен `CODEC`, то client/server десинк возможен (item stack copy сравнивает компоненты). Проверено `read ProjectJjkResonanceRemnant.java` — это record с `CODEC`/`STREAM_CODEC` derived от полей, симметрия предполагается, но не протестирована. | Добавить round-trip тест: `ProjectJjkResonanceRemnant orig = ...; assert orig.equals(STREAM_CODEC.decode(STREAM_CODEC.encode(orig)))` и `CODEC` аналогично. Не критично пока используется только Nobara, но при расширении — обязательно. |

---

### 3) Позитивные наблюдения

- **Vessel Seam — образцово.** Два exhaustive `switch` без `default` (`JujutsuCharacters.java:31-36`, `JujutsuCharacterClients.java:26-32`) + `all()` derived от `values()` — compile-time гарантия. `CharacterDefinition` не импортирует ни одного client-типа (проверено `grep import net.minecraft.client` в `src/main` — 0 hits кроме `CharacterSelectionView` server-совместимого), `CharacterClientDefinition` отделена. `JujutsuMod.java:44-46` и `JujutsuModClient.java:29` ставят vessel-хуки циклом по registry — ни одного hand-listed `NobaraDefinition.register()` в shared init. Это прямое исполнение AGENTS.md § Vessel Seam. Проверено `codegraph_explore` (blast radius: `JujutsuCharacterClients` 9 callers, `JujutsuMod` 45 callers, все через seam).
- **Build-time страховка ловит то что `switch` не может.** `CharacterDefinitionRegistryTest.assertEveryVesselRuntimeIsRegistered()` выводит ожидаемый список из source-tree (`Files.walk(vesselPackage) -> contains "public static void register()"`), а не из ручного списка. `assertEveryVesselResolves()` ловит транспозицию `case TODO -> NOBARA_DEFINITION` (компилируется, но тест падает). `assertTheServerHalfStaysServerOnly()` + `SourceBoundaryTripwireTest` + `VesselBoundaryTest` закрывают compile-time-constant folding (javac inlines `TodoProfile` константы) и `Class.forName`. Покрытие seam'а — одно из лучших в проекте.
- **Ни один vessel-элемент не регистрируется вне определения.** Все 17 `register()` под `character/nobara` (9), `character/todo` (4), `character/megumi` (4) вызываются из соответствующей `*Definition.registerServerHooks()` — проверено `grep -rn register()` + чтением каждого `*Definition.java`. Ни одного `Todo*Runtime.register()` или `Megumi*Runtime.register()` в `JujutsuMod.java` нет (тест `assert !init.contains("character." + character.id())` это пинит). На client-стороне аналогично: `EntityRendererRegistry.register` (3: `PROJECTJJK_NAIL`, `MEGUMI_DIVINE_DOG`, `TODO_STONE`), `VfxRecipes.register` (3 packs), `registerHudContribution` (3 vessel + 1 global `ability_hud`), `NobaraEspState.register()` — всё внутри `*ClientDefinition.registerClientHooks()`, вызываемом из `JujutsuCharacterClients.registerAll()` (единственный вызов в `JujutsuModClient.java:29`). Третий сосуд Megumi добавлен тем же паттерном без единого shared-изменения — seam работает.
- **Content registries честно шарируются, без ветвления по vessel.** `JujutsuItems` / `JujutsuEntities` / `JujutsuParticles` / `JujutsuSounds` — единые точки регистрации, без `if (character == NOBARA)` внутри. AGENTS.md явно разрешает vessel'ам добавлять items/entities в эти файлы (`Content and housekeeping — must edit these`). Ветвление по vessel есть только там где ему место — внутри `*Definition`/`*ClientDefinition`/`*AbilityRouter`.
- **Entity lifecycle — `noSave()` везде.** Все 3 `EntityType` (`projectjjk_nail:52-60`, `megumi_divine_dog:41-50`, `todo_stone:28-39`) помечены `.noSave()` (проверено `grep noSave — 3 hits, по одному на entity`). Коммент `JujutsuEntities.java:35-38` объясняет почему (transient, не переживает сессию). Это предотвращает ghost-entities после краша — частая ошибка у новых vessel'ов. `add-vessel` скилл это требует, и все три соблюдают.
- **Звуки сейчас консистентны.** `JujutsuSounds.java` 24 `create` + 24 `register` + `sounds.json` 24 keys — множества равны (`bash` diff пустой). `createVariableRangeEvent` используется везде (правильно для Fabric 1.21.8). `SoundEvent` ids — append-only по факту (ни один не переименован; `sounds.json` 1-в-1).
- **Атрибуты Megumi — регистрация в vessel-хуке, не в глобальном init.** `FabricDefaultAttributeRegistry.register(MEGUMI_DIVINE_DOG, ...)` внутри `MegumiDefinition.registerServerHooks():28-29`, а не в `JujutsuMod`. Правильно: атрибут принадлежит vessel'у, и если vessel удалят — регистрация уйдёт вместе с definition. Порядок `JujutsuEntities.register():32` → vessel loop `all():44` это учитывает. `Wolf.createAttributes().add(MAX_HEALTH 60).add(ATTACK_DAMAGE 3).add(MOVEMENT_SPEED 0.34)` в `createDivineDogAttributes:36-41` — корректно переиспользует ванильный Wolf-скелет.
- **Эффекты — атрибуты через `addAttributeModifier` на Holder, не отдельный runtime.** `TODO_SWAP_MOMENTUM:28-35` (`ADD_MULTIPLIED_TOTAL` 0.25 на `ATTACK_DAMAGE`) и `MEGUMI_SHADOW_GRIP:44-57` (`MOVEMENT_SPEED -0.75` + `JUMP_STRENGTH *0` оба `ADD_MULTIPLIED_TOTAL`) — чистый ванильный `MobEffect` с модификаторами, без кастомных tick-хуков. Сервер re-apply каждый тик внутри зоны (`MegumiShadowTrapRuntime`), клиент зеркалит через effect system — минимальная логика, максимальная надёжность.
- **Wire ids append-only соблюдается.** `CharacterAbility` `networkId 0..9` — ни один не перенумерован, новый `TERTIARY/TERTIARY_SNEAK` (8/9) добавлен append-only, тест `CharacterAbilityWireFormatTest` это пинит. Item/sound/particle ids тоже не переименовывались (проверено множествами).
- **Нет забытых `register()`.** Все 7 registry `register()` вызываются из `JujutsuMod.onInitialize()` (проверено `read JujutsuMod.java:32-38` — 7 строк, по одному на registry), все vessel `register()` — из своего `*Definition`, все client hooks — из `JujutsuCharacterClients.registerAll()`. Source-tree-derived тест `assertEveryVesselRuntimeIsRegistered` гарантирует что новый `public static void register()` под `character/<id>/` без вызова из `*Definition` уронит `qualityGate`.

---

### 4) Вердикт

**PASS WITH CAVEATS**

Registry-срез в целом здоров: vessel seam выдержан тремя сосудами (NONE/NOBARA/TODO/MEGUMI), ни один `register()` не забыт (source-tree-derived тесты это гарантируют), порядок инициализации корректен для текущего набора, множества звуков/частиц/эффектов синхронизированы (`bash` diff пустой). Критических багов ломающих загрузку, десинкающих client/server или нарушающих server-authoritative инвариант — не найдено. Ни один vessel-элемент не регистрируется вне своего `*Definition.register*Hooks()`.

Caveats — 5 **major** находок требующих решения до следующего vessel'а, и 7 **minor/info** для гигиены:

**Приоритет фиксов:**

1. **P1 (до следующего vessel'а):** #1 `JujutsuEffects` static-init → явный `register()` (или доку + тест). Сейчас это единственный registry с неявным порядком; копирование паттерна сломает следующий vessel.
2. **P1:** #5 Тест на `JujutsuSounds` ↔ `sounds.json` паритет (1 assert, 5 строк, ловит silent drift навсегда) + (b) сироты OGG подчистить.
3. **P1:** #2 Определить судьбу `PROJECTJJK_*` алиасов — либо удалить, либо helper `isNail`/`isHammer` в `JujutsuItems` и задокументировать как legacy с TTL.
4. **P2 (следующий рефактор):** #3 Унифицировать регистрацию particle-фабрик (11+1 → 12 в одном месте) или явный контракт + тест.
5. **P2:** #4 Комментарий-барьер о порядке `JujutsuMod.onInitialize()` + убрать неявность через #1; #8 переставить `ability_hud` после `registerAll()`.
6. **P3 (гигиена):** #6 generic `itemKey()` helper, #7 javadoc на `RESONANCE_*`, #9 assert на `.noSave()` + вынести `DOG_WIDTH/HEIGHT` в `MegumiProfile`, #10 не трогать `all()` дубликат, #11 O(1) `byNetworkId` + доку append-only для item/sound ids, #12 round-trip тест `DataComponent` CODEC.

Без P1 следующий vessel с высокой вероятностью скопирует проблемные паттерны (дубль id, расщеплённая particle-регистрация, static-init эффект). С P1 — seam останется тем же что выдержал 3 сосуда.

AUDIT_ID: 02-registry-bindings
