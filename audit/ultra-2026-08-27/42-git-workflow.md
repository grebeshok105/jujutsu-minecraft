## Git — 42-git-workflow

### 1) Что проверено (файлы/символы)

**Контракт workflow:**
- `AGENTS.md:104-116` — Non-Negotiable Workflow: `Work in git only`, `Use an isolated worktree for feature work`, `Commit every meaningful change immediately`, `Commit messages are in English and conventional-style (feat/fix/chore)`, `Keep changes small enough that a single commit can be reviewed`, `Do not batch unrelated work`, `Do not leave uncommitted project changes at a handoff`, `keep SESSION.md on the active worktree current`.
- `AGENTS.md:117-135` — Agent Tools: codegraph, MCP dev-lane, knowledge-rag.

**Handoff и состояние ветки:**
- `SESSION.md:1-28` — рабочая копия vs `HEAD:SESSION.md` (сравнение через `git diff HEAD -- SESSION.md` и `git show HEAD:SESSION.md | head`).
- `git status --porcelain --branch -uall` — `M SESSION.md` + 28 untracked (`?? .factorypath`, `?? .superpowers/...`, `?? audit/...`, `?? docs/knowledge/...`).
- `git status -v` — ahead of `origin/feat/nobara-target-hud` by 3, dirty working tree на handoff-точке.
- `git branch -vv` — `feat/nobara-target-hud 4268c4a [origin/feat/nobara-target-hud: ahead 3]`.
- `git log --oneline origin/main..HEAD` (12 коммитов) + `git log origin/feat/nobara-target-hud..HEAD --oneline` (3 коммита ahead).
- `git diff origin/feat/nobara-target-hud --stat` — 65 files changed `+1844 -332` (не запушено).
- `git diff HEAD --stat` — `SESSION.md | 3 ++-` (единственный unstaged).
- `git diff-tree --stat` per commit (`10f9a67`, `5df6742`, `b7c741d`, `d241544`, `6f61381`, `b461452`, `ae52d7e`, `2568700`, `5d95a0b`, `372bcd7`, `42efd83`, `4268c4a`) — размеры коммитов.
- `git reflog --oneline -n 10` — подтверждает sequence коммитов, amend `d241544` (`HEAD@{8}` amend, `HEAD@{9}` original `812f299`).
- `git log --format=%s -n 30 | grep -Ev "^(feat|fix|chore|docs|test|refactor|build|ci|perf|revert)(\(.+\))?: "` — проверка conventional commits (на активном срезе 0 нарушений, исторические `balance`, `spike`, `merge:` вне allowlist).
- `git log --format="%H %s %an %ad" --date=short -n 20` — авторы `grebeshok105`, даты.

**Worktree изоляция:**
- `git worktree list` + `git worktree list --porcelain` — единственный worktree `D:/WorkFlow/jujutsu-minecraft 4268c4a [feat/nobara-target-hud]`, `.worktrees/` отсутствует (`ls` → No such file).
- `git rev-parse --git-dir` → `.git`, `git rev-parse --git-common-dir` → `.git` — не linked worktree.
- `docs/knowledge/CURRENT_STATE.md:46-52` — документирует историческую практику `.worktrees/<имя>` (пример `codex/character-animation-overhaul → .worktrees/character-skin-animation`), но текущая волна её не использует.

**Gitignore и артефакты:**
- `.gitignore:1-74` — игнор `.gradle/`, `build/`, `.worktrees/`, `out/`, `.idea/`, `.vscode/`, `.qoder/`, `.obsidian/`, `.omp/`, `docs/research/`, `docs/specs/`, `.grace-tools/node_modules/` и др.
- `git check-ignore -v` для `.omp` (`.gitignore:48:.omp/`), `.superpowers` (не игнорит), `.factorypath` (не игнорит), `docs/knowledge/CURRENT_STATE.md` (не игнорит), `audit/` (не игнорит), `git ls-files --others --exclude-standard --ignored` — подтверждает отсутствие игнора.
- `.factorypath:1` — содержит абсолютный `C:\Users\KOMP1\.gradle\.../lombok-1.18.30.jar` — machine-specific Eclipse artifact, untracked.
- `.superpowers/rule-of-four/nobara-hud-refinement/plan-spec.md:1-106` — untracked plan-spec v3 (10569 bytes, guiHeight-нормировка), активная спека redesign wave.
- `audit/ultra-2026-08-27/*` — 25 файлов `01-...42...` untracked (текущий ультра-аудит).
- `docs/knowledge/CURRENT_STATE.md:1` — 21KB, untracked, статус `CURRENT`, обновлено `2026-08-05`, упоминается в `SESSION.md:11` как намеренно untracked из-за `auditDocumentation` fail на незадекларированную подпапку `docs/knowledge/`.
- `.grace-tools/bin/grace`, `.grace-tools/bin/grace.cmd`, `.grace-tools/bun.lock`, `.grace-tools/package.json` — закоммичены в `372bcd7`, `node_modules/` gitignored.

**История и конвенции:**
- `git show HEAD --stat` (4268c4a: 5 files `+278 -329`), `HEAD~1` (42efd83: 1 file), `HEAD~2` (372bcd7: 59 files `+1545`) — детализация batching.
- `git show 372bcd7 --stat` body — `Add project-local GRACE skills and CLI install` с `verified: grace --version 4.0.5`.
- `git log --all --oneline | grep -i "balance\|spike\|merge"` — исторические `balance(megumi):` (`3bc353b`), `spike(mcp):` (`ac67610`), `merge:` lower-case.

Инструменты: `read`, `bash git status/log/diff/worktree/rev-parse/check-ignore`, `grep` по commit messages, `ls` worktrees.

---

### 2) Находки

| severity | файл:строка | описание | рекомендация |
|----------|-------------|----------|--------------|
| **critical** | `SESSION.md:3` (рабочая копия vs `HEAD:SESSION.md:3`) | `SESSION.md` просрочен и не закоммичен 2 дня. Рабочая копия пишет `HEAD d241544+glass canary (5 commits on main)` с секцией `Redesign wave (2026-08-21, active)` (+869 символов, pixelMetrics 1254×1254, plan-spec v3), а `HEAD:SESSION.md:3` всё ещё на `d241544 (4 commits)` без redesign-контекста. `git status` = `M SESSION.md` (`b7856b7..5965ac0`), `ahead 3`. Прямое нарушение `AGENTS.md:115-116` `Do not leave uncommitted project changes at a handoff unless explicitly blocked` + `keep SESSION.md on the active worktree current`. Следующий клон `origin/feat/nobara-target-hud` получает устаревший handoff без контекста активной волны. | Немедленно: `git add SESSION.md && git commit -m "docs(session): sync nobara-target-hud handoff to 4268c4a + redesign wave" && git push`. Далее правило: любое редактирование `SESSION.md` → commit в том же часу, не держать dirty на ночь. |
| **critical** | `git worktree list` (porcelain) : `D:/WorkFlow/jujutsu-minecraft 4268c4a [feat/nobara-target-hud]` | Нарушен `AGENTS.md:107` `Use an isolated worktree for feature work instead of editing the main checkout directly`. Единственный worktree — main checkout напрямую на `feat/nobara-target-hud`. Исторически проект использовал `.worktrees/<имя>` (`docs/knowledge/CURRENT_STATE.md:46-50` — два чекаута `Jujutsu Minecraft` с пробелом + `jujutsu-minecraft` с дефисом, linked worktrees под `.worktrees/<имя>`), но текущая волна abandon'ила практику. Риски: нельзя держать `main` для hotfix параллельно, грязный main checkout, конфликт с AGENTS для всех агентов. Проверено: `ls .worktrees` → No such file, `rev-parse --git-common-dir` → `.git` (не linked). | Восстановить: `git worktree add .worktrees/nobara-target-hud feat/nobara-target-hud && git -C D:/WorkFlow/jujutsu-minecraft checkout main` — вернуть main checkout на `main`. Если отказ от worktree осознанный — задокументировать исключение в `AGENTS.md:107` с причиной (напр. single-worktree на этой машине). |
| **major** | `git status --porcelain` : `?? .factorypath:1`, `?? .superpowers/rule-of-four/nobara-hud-refinement/plan-spec.md:1`, `?? audit/ultra-2026-08-27/*` (25 файлов), `?? docs/knowledge/CURRENT_STATE.md:1` | Массовый `no uncommitted handoff` violation — 28+ untracked/modified на handoff-точке. `.factorypath:1` с абсолютным `C:\Users\KOMP1\.gradle\...` — утечка локального пути, при `git add -A` уйдёт в индекс, сломает CI на Linux. `.superpowers/.../plan-spec.md:1` (active spec v3) — либо артефакт должен быть gitignored, либо закоммичен как decision record. `audit/ultra-2026-08-27/*.md` — текущий ультра-аудит, но handoff уже грязный (аудит сам создаёт грязь — chicken-egg). `docs/knowledge/CURRENT_STATE.md:1` — намеренно untracked per `SESSION.md:11` из-за `auditDocumentation` reject на незадекларированную `docs/knowledge/` (project memory невидима для `git log/blame`, новый клон не получит). | 1) Добавить в `.gitignore`: `.factorypath`, `.superpowers/`, `audit/` (audit — либо игнорить, либо коммитить как `docs/audit/` с allowlist). 2) Удалить `.factorypath` из worktree (`rm .factorypath`) или `git rm --cached` если попал. 3) Решить судьбу `CURRENT_STATE.md`: либо добавить `docs/knowledge/` в allowlist `tools/audit_docs.py` + `AGENTS.md` и закоммитить, либо переместить memory в `knowledge-rag`/`research.db` per `CLAUDE.md` Retrieval router, либо явно игнорить с комментом `! intentionally untracked — see SESSION.md:11`. 4) `plan-spec.md` — либо gitignore, либо commit как `docs/plans/nobara-hud-v3.md`. |
| **major** | `git show 372bcd7 --stat` : 59 files `+1545 -0` + `AGENTS.md:113-114` | Нарушение `Keep changes small enough that a single commit can be reviewed honestly. Do not batch unrelated work into one commit`. Коммит `372bcd7 chore(grace): add project-local GRACE skills and CLI install` — 59 файлов в одном: 15 скиллов `grace-*` (каждый `SKILL.md` + `agents/openai.yaml` + `references/*.md` + `assets/.grace/**/*`), `.grace-tools/bin/*`, `bun.lock`, `package.json`, правка `AGENTS.md:12`. Обзор формален, revert невозможен по частям, blame зашумлён. Контраст: остальные коммиты волны образцовые (`ae52d7e` 1 file, `b461452` 3 files 7+7 строк, `10f9a67` pure helper). | Разбивать инфраструктурные импорты: `chore(grace): add skill skeletons` → `chore(grace): add CLI shims and bun lock` → `docs: document GRACE call paths in AGENTS.md`. Если импорт атомарен — в commit body явно `This is an atomic import — review by `git diff --stat` + spot-check one skill`. Ввести soft gate: >15 files или >500 lines → требовать разбиение или explicit `BREAKING` justification. |
| **major** | `git branch -vv` : `feat/nobara-target-hud ahead 3` + `git log origin/feat/nobara-target-hud..HEAD --oneline` | Ветка ahead of origin на 3 коммита (`372bcd7 2026-08-24`, `42efd83 2026-08-25`, `4268c4a 2026-08-25`) и не пушена 2-3 дня. Формально `Do not leave uncommitted project changes` про коммиты, но handoff с непушеными коммитами = риск потери при переустановке/падении диска, невидимость для ревьюера/CI, `origin/feat/nobara-target-hud` отстаёт на 65 files `+1844`. Двойной лаг: и коммиты не пушены, и `SESSION.md` даже не закоммичен. | Пушить после каждого `commit every meaningful change`: `git push origin feat/nobara-target-hud` (или `--force-with-lease` после rebase). Добавить в `SESSION.md` поле `pushed: <sha> / 2026-08-25` и в `AGENTS.md` checklist `commit → push`. Настроить `git push.autoSetupRemote` + pre-push hook напоминание если ahead >1 сутки. |
| **minor** | `D:/WorkFlow/jujutsu-minecraft/.factorypath:1` + `.gitignore:9-26` | Machine-specific артефакт `.factorypath` (Eclipse JDT annotation processing, абсолютный Windows-путь `C:\Users\KOMP1\.gradle\caches\...lombok-...jar`) утёк в untracked. `.gitignore` игнорит `.classpath`/`.project`/`.settings` но не `.factorypath` — дыра в Eclipse/Lombok игноре. При `git add -A` (частый у агентов) попадёт в индекс, сломает CI на Ubuntu, засветит username в истории. `git ls-files --others --exclude-standard --ignored` не показывает его — именно потому что не игнорит. | Добавить в `.gitignore`: `.factorypath` и для полноты `.apt_generated/` (аннотации). Удалить файл: `rm .factorypath`. Если уже попал в историю — `git rm --cached .factorypath && git commit -m "chore(gitignore): ignore Eclipse factorypath"`. Проверить `git check-ignore -v .factorypath` после фикса. |
| **minor** | `.superpowers/rule-of-four/nobara-hud-refinement/plan-spec.md:1` + `git check-ignore -v` | Активная спека redesign v3 untracked и не gitignored. `git check-ignore -v .superpowers/.../plan-spec.md` → пусто (не игнорит). При `git status -uall` шумит, при `git add -A` риск уехать в индекс как 10KB markdown с raw RGBA blob id. Проект держит `docs/` под `auditDocumentation` аудитом, но `.superpowers/` — вне `docs/` и вне `.gitignore`. | Либо добавить `.superpowers/` в `.gitignore` (если это локальный scratch агента), либо переместить decision record в `docs/plans/` и закоммитить с allowlist в `tools/audit_docs.py`. Сейчас — ни то ни другое, висит в dirty. Рекомендация: `.superpowers/` → `.gitignore` (agent scratch), а финальную спеку копировать в `docs/plans/` при `docs:` коммите. |
| **minor** | `git log --all --oneline` + `AGENTS.md:109-110` | `spike(mcp): validate upstream bridge on Minecraft 1.21.8 (#62)` (`ac67610`) и исторические `balance(megumi): tune divine dog durability` (`3bc353b`), `merge:` lower-case (`19f16a3` etc.) — не входят в declared prefixes примера `AGENTS.md:109-110` (`feat/fix/chore` examples). Strict `grep -Ev "^(feat|fix|chore|docs|test|refactor|build|ci|perf|revert)(\(.+\))?: "` флагует их (на активном срезе `origin/main..HEAD` 0 хитов, но на `--all` есть). Changelog-тулы и `git log --grep` по типу сломаются. `spike` — осознанный research префикс, `balance` — геймдизайн, но не задокументированы. | Зафиксировать allowlist в `AGENTS.md:109`: `feat|fix|chore|docs|test|refactor|perf|build|ci|revert|spike|balance` или заменить `balance:` → `perf(megumi):` / `feat(megumi):` и `spike:` → `chore(spike):` если нужен strict conventional. `merge:` — использовать `Merge branch ...` (Git default) или `chore(merge):`. Не ретроактивно переписывать историю, только зафиксировать правило для будущих. |
| **minor** | `docs/knowledge/CURRENT_STATE.md:1` + `SESSION.md:11` + `tools/audit_docs.py` (косвенно) | `CURRENT_STATE.md` сознательно untracked потому что `auditDocumentation` падает на незадекларированную `docs/knowledge/` (workaround описан в `SESSION.md:11`). Легитимный trade-off, но нарушает `commit every meaningful change` и делает project memory невидимой для `git log/blame/fork`. Новый участник клоном не получит `CURRENT_STATE.md` — потеря deploy-рецепта (`D:/Games/instances/Jujutsu/mods/` md5), worktree карты, PR-статусов. `CLAUDE.md` Retrieval router предписывает `research.db`/`knowledge-rag` для долговременной памяти, но memory осталась файлом. | Варианты по приоритету: 1) Добавить `docs/knowledge/` в allowlist `tools/audit_docs.py` и закоммитить `CURRENT_STATE.md` (простейший, сохраняет grep). 2) Переместить в `knowledge-rag` (`D:/RAG/jujutsu`) + `research.db` (`findings.py add`) per `CLAUDE.md` — тогда `SESSION.md:11` можно удалить. 3) Оставить untracked но добавить `.gitignore:docs/knowledge/` с комментом `! intentionally untracked — see SESSION.md:11` чтобы `git status` не шумел. Сейчас — недокументированный игнор через отсутствие, а не через `.gitignore`. |

Дополнительно (не в таблице, для контекста): conventional commits на активном срезе `origin/main..HEAD` (12 коммитов `10f9a67`→`4268c4a`) — идеальные `feat(hud):`/`fix(nobara):`/`test(gametest):`/`docs(project):`/`chore(grace):` с body где нужно (`372bcd7` с `verified: grace --version`, `5d95a0b` с `mip` root-cause). `grep -vE` на этом срезе — 0 хитов. Размеры остальных коммитов reviewable (`ae52d7e` 1 file, `b461452` 3 files, `10f9a67` 258 lines pure helper+JUnit). Squash-merge PR (#67 `#4dd5729`, #66 и др.) — корректно, `origin/main` (`ce3d655`) и `main` синхронизированы.

---

### 3) Позитивные наблюдения

- **Conventional commits дисциплина высокая.** Весь активный хвост `feat/nobara-target-hud` (12 коммитов) — точный `prefix(scope): subject` на English, scope осмысленный (`hud`, `nobara`, `gametest`, `grace`, `project`, `ui`), body объясняет *почему* (`mip` arg bug в `copyTextureToTexture`, `SdfRenderer` device crash defer, `glass` lens refraction). На `origin/main..HEAD` 0 нарушений allowlist.
- **Small reviewable commits (кроме одного батча).** `ae52d7e` (1 file glass cards), `b461452` (3 files 7+7 строк — method reference fix crash), `10f9a67` (2 files pure `WorldToScreen` + JUnit, no Minecraft imports) — каждый честно reviewable, сообщение объясняет причину. `d241544` (4 files, FPS-independent chaser) — узкий scope.
- **Commit every meaningful change соблюдался в истории.** Каждый VFX-шаг `glassy cards → liquid-glass → thin bracket` — отдельный коммит с верификацией, `test(gametest): SDF glass canary + fix mip` сразу после `feat(hud): liquid-glass` — образцовый `fix+test` пайплайн с red proof (mip slot bug пойман канарейкой).
- **No batching unrelated work (кроме GRACE).** `feat(ui): world-to-screen` отделён от `feat(nobara): target HUD`, `fix(nobara): method reference` — отдельный hotfix, `docs:` — отдельно от `feat`. Только `372bcd7` нарушил.
- **Git hygiene.** `origin/main` и `main` синхронизированы на `ce3d655`, `feat/nobara-target-hud` корректно tracking `origin/feat/nobara-target-hud`, HEAD ahead но не diverged, merge-base чистый, `reflog` показывает amend корректно (`d241544` amend с сохранением `812f299` original). Авторы консистентны (`grebeshok105`), даты корректны.
- **SESSION.md содержателен когда актуален.** Метрики `296 JUnit / 34 GameTest / MOC 191/85`, deploy-рецепт `assemble → build/libs/jujutsumod-1.0.0.jar → D:/Games/instances/Jujutsu/mods/ md5`, `NOT yet verified in-game` — честный handoff, не формальность. Redesign wave секция (когда закоммичена) — детальный контекст для следующего агента.

---

### 4) Вердикт (PASS / PASS WITH CAVEATS / FAIL) + приоритет фиксов

**Вердикт: FAIL**

При сильном стиле commit-сообщений (conventional, small, meaningful) handoff-часть workflow сломана по трём из шести пунктов `AGENTS.md:104-116`: `isolated worktree` (main checkout на feature-ветке, 0 linked worktrees), `no uncommitted handoff` (28+ untracked/dirty на handoff-точке), `SESSION.md current` (просрочен на 3 коммита + redesign-контекст только в dirty). Четвёртый пункт `small commits` частично нарушен одним батчем 59 файлов. Это не косметика — клон `origin/feat/nobara-target-hud` даёт устаревший `SESSION.md` (`d241544` вместо `4268c4a`) без redesign-контекста, а `git add -A` утащит `.factorypath` с абсолютным путём в историю.

Основание — `AGENTS.md:104-116` Non-Negotiable Workflow, проверено `git status/worktree/log/diff/check-ignore` выше; нарушены 3/6 инвариантов + 1 частично.

**Приоритет фиксов:**

1. **P0 — закрыть handoff (10 мин):** `git add SESSION.md && git commit -m "docs(session): sync handoff to 4268c4a + redesign wave" && git push origin feat/nobara-target-hud`. Без этого ветка несдаваема — следующий агент стартует с ложным `HEAD d241544`.
2. **P0 — почистить untracked (15 мин):** добавить в `.gitignore` строки `.factorypath`, `.superpowers/`, `audit/`; удалить `.factorypath` (`rm .factorypath`); решить судьбу `docs/knowledge/CURRENT_STATE.md` (allowlist vs gitignore vs `knowledge-rag`); `plan-spec.md` — либо игнорить, либо коммитить как `docs/plans/`.
3. **P1 — восстановить worktree дисциплину (15 мин):** `git worktree add .worktrees/nobara-target-hud feat/nobara-target-hud && git checkout main` в основном чекауте; либо явно задокументировать в `AGENTS.md:107` отказ от worktree с причиной (single-worktree на этой машине).
4. **P1 — пушить регулярно (5 мин):** ввести правило `commit → push` для feature-веток, добавить в `SESSION.md` поле `pushed: <sha>`; `git push` сейчас (3 коммита ahead с 2026-08-24).
5. **P2 — нормализовать prefixes (5 мин):** зафиксировать allowlist в `AGENTS.md:109` (`feat|fix|chore|docs|test|refactor|perf|build|ci|revert|spike|balance`) или заменить будущие `balance:`→`perf:` .
6. **P2 — лимит размера коммита (процесс):** soft gate 15 files / 500 lines, инфраструктурные импорты разбивать на 2-3 коммита с `This is an atomic import` body если неделим.

AUDIT_ID: 42-git-workflow
