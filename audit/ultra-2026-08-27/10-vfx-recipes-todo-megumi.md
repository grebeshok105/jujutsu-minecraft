## VFX Todo+Megumi — 10-vfx-recipes-todo-megumi

### 1) Что проверено (файлы/символы)

Срез `10-vfx-recipes-todo-megumi`: VfxIds/Recipes Todo и Megumi, stone visuals, swap afterimages/arrival, shadow pools (void-black), Divine Dogs summon pool, camera dive, TTL HiddenBodyRenderGate, cue коды. Сверка с инвариантами: Vessel Seam (shared код не спрашивает which character), VFX Core (cue→director→recipes), server-authoritative, wire ids append-only.

**Инструменты:** `codegraph_explore` (VfxIds/VfxRecipes/Todo/Megumi/shadow/swap), `grep -rn` по `VfxIds|VfxRecipes|HiddenBodyRenderGate|ShadowBodySink|STONE_*|SWAP_AFTERIMAGE|SHADOW_TRAP`, прямые `read` файлов.

**Основные файлы и символы:**

- `src/main/java/jujutsu/mod/vfx/TodoVfxIds.java:8-68` — 10 LIVE (`BOOGIE_WOOGIE`, `SWAP_ENDPOINT`, `FEINT_TELL`, `PAIR_MARK`, `SWAP_AFTERIMAGE`, `SWAP_ARRIVAL`, `MOMENTUM_STRIKE`, `STONE_THROW`, `STONE_VANISH`, `TRIPLE_SWAP`), `PLANNED=empty`
- `src/main/java/jujutsu/mod/vfx/MegumiVfxIds.java:8-42` — 15 LIVE (`DOGS_SUMMON_BODY`, `DOGS_SUMMON`, `DOGS_RECALL`, `DOGS_SIC`, `DOGS_POUNCE`, `SHADOW_TRAP_OPEN/ZONE/GRIP/CLOSE`, `SHADOW_DIVE/RIPPLE/EMERGE`, `DROP_ZONE_OPEN/ZONE/CLOSE`)
- `src/client/java/jujutsu/mod/client/vfx/todo/TodoVfxRecipes.java:19-314` — `register()` 10 рецептов, `BOOGIE_WOOGIE_PRESENTATION_RADIUS=56` vs delivery 64, `AFTERIMAGE_TICKS=4`, `ARRIVAL_TICKS=6`, `STONE_THROW=6`/`STONE_VANISH=8`/`TRIPLE_SWAP=10`, `DUCK_TICKS=6`
- `src/client/java/jujutsu/mod/client/vfx/megumi/MegumiVfxRecipes.java:26-323` — 15 рецептов, `SUMMON=16`/`RECALL=12`/`SHADOW_TRAP_OPEN=16`/`ZONE=42`/`GRIP=8`/`CLOSE=12`/`DIVE=12`/`RIPPLE=8`/`EMERGE=12`/`DROP_*=10/7/8`, `RIPPLE_HIDE_TTL_TICKS=8`, `TRAP_POOL_RADIUS` из `MegumiProfile`
- `src/client/java/jujutsu/mod/client/vfx/world/SwapWorldEffects.java:54-144` — `renderSwapAfterimage`/`renderSwapArrival` (`silhouetteWidth/Height`, `SILHOUETTE_DRIFT=0.30`, `ARRIVAL_STREAK_MIN_SPEED=0.06`)
- `src/client/java/jujutsu/mod/client/vfx/world/ShadowWorldEffects.java:7-90` — `renderMegumiShadowPool`/`renderShadowTrapPool` (`RenderType.debugQuads()`, `void-black` alpha 255 open/zone, close fade 255→0, `trapPoolScale` 0.26→1.0)
- `src/client/java/jujutsu/mod/client/vfx/VfxWorldChannel.java:20-135` — `MAX_IMPACT_FLASHES=48`, `ImpactStyle` 5 shadow + 3 todo, `triggerImpact`, `renderImpactFlashes` (`getBuffer(debugQuads)` для shadow, `lightning()` для todo)
- `src/client/java/jujutsu/mod/client/vfx/VfxCameraChannel.java:14-333` — `triggerSwapSnap(1/2)`, `diveOffsetBlocks/diveFadeAlpha` → `ShadowBodySink`, `DIVE_*_DEPTH_BLOCKS 0.85/0.35/0.5`, `DIVE_FADE*_ALPHA`
- `src/client/java/jujutsu/mod/client/render/HiddenBodyRenderGate.java:1-43` — `markHidden(entityId,ttlTicks)` wall-clock `System.currentTimeMillis()+ttl*50`, `markRevealed`, `isHidden` lazy expire fail-open
- `src/client/java/jujutsu/mod/client/render/ShadowBodySink.java:18-154` — `ENTRIES ConcurrentHashMap`, `UNDER_TTL_TICKS=8`, `windowTtlMillis`, `beginSink/completeSink/beginEmerge/sinkProgress/emergeProgress/smoothstep`, injectable `clock` для тестов, guards vs EMERGING
- `src/client/java/jujutsu/mod/client/mixin/CharacterSkinAnimationMixin.java:64-138` — `HiddenBodyRenderGate.isHidden` скрывает модель, `ShadowBodySink.sinkProgress/emergeProgress` → `sinkOffsetY` `-1.9*smoothstep`
- `src/main/java/jujutsu/mod/character/todo/TodoBoogieWoogieRuntime.java:28-396` — `SOFT/STRICT Policy`, `emitSwapImpact` (broadcast afterimage/arrival per MovedBody), `broadcastAfterimage` (anchorOffset=bbWxH+yaw, direction=delta), `broadcastArrival` (offset=speed+bb), `TodoSwapArrivalPayload`, `PENDING_SOUNDS` whoosh
- `src/main/java/jujutsu/mod/character/todo/TodoFakeClapRuntime.java:28-70` — `emitClapPerformance` shared, `FEINT_TELL` `sendVfxCue(todo,...)` caster-only, `scheduleDisplacementWhoosh` 1×
- `src/main/java/jujutsu/mod/character/todo/TodoStoneRuntime.java:35-265` — `STONE_THROW` `anchoredDirected`, `STONE_VANISH` через `TodoTransientState.clearStone`, `stoneEligibleForSwap`, `withinSwapRange 32.0`
- `src/main/java/jujutsu/mod/character/todo/TodoTransientState.java:25-99` — `clearStone` broadcast `STONE_VANISH` только если `entity != null && isAlive`
- `src/main/java/jujutsu/mod/character/todo/TodoPairSwapRuntime.java:45-459` + `TodoPairSelectionClientState` — `PAIR_MARK` caster-only pulse `intensity 0` silent, `TRIPLE_SWAP` `worldFixedDisplacement` offset=full travel vector
- `src/main/java/jujutsu/mod/character/megumi/MegumiSummonRuntime.java:51-606` — `DOGS_SUMMON_BODY` player-anchored zero offset, `DOGS_SUMMON` per-dog `broadcastDogCue` (`anchoredWithOffset` origin=dog.position), `DOGS_RECALL` per living dog, `DOGS_SIC/Pounce` target-anchored, `shouldHardDiscard`/`reconcile`/`teardown` single authority
- `src/main/java/jujutsu/mod/character/megumi/MegumiShadowTrapRuntime.java:46-219` — `SHADOW_TRAP_OPEN+ZONE` на каст, `ZONE` пульс `SHADOW_TRAP_ZONE_PULSE_TICKS=40` vs duration 42, `SHADOW_TRAP_GRIP` каждые 20 ticks, `snapToGround` clip 3 блока, `gripBodies` cylinder `radius 2.6 / vertical 1.6`
- `src/main/java/jujutsu/mod/character/megumi/MegumiShadowMoveRuntime.java:57-438` — `SINK 8→ HIDDEN 4 → EMERGE 6 / SUBMERGED ≤50`, `hide()` setInvisible(true) + periodic ripple, `broadcast SHADOW_DIVE/RIPPLE/EMERGE` anchored, `cancelSink` no cooldown, `resolveExit` strict
- `src/main/java/jujutsu/mod/character/megumi/MegumiShadowDropRuntime.java:35-291` — `DROP_ZONE_OPEN/ZONE/CLOSE`, pulse `DROP_ZONE_PULSE_TICKS=5` vs duration 7, scatter 0.9 внутри радиуса 1.2, `disableDrop()`
- `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:4-104` — все тюнинги (VFX radius 48/64/56, sink 8, hidden 4, emerge 6, ripple 5, trap radius 2.6 и т.д.)
- `src/client/java/jujutsu/mod/client/vfx/VfxDirector.java:22-192` — `RECIPES putIfAbsent duplicate → IllegalStateException`, `receive/startResolvedCue` (late age reject/seek), `reset/clear` НЕ чистит `HiddenBodyRenderGate/ShadowBodySink`, `DISCONNECT → reset()`, `bindLevel → clear()`
- `src/main/java/jujutsu/mod/vfx/VfxCues.java` + `VfxCue.java` + `TodoSwapArrivalPayload.java:6-9` — factory нормализация direction, `worldFixedDisplacement` для endpoint/triple, raw `VfxCue` для afterimage/arrival с перегруженным offset
- `src/main/java/jujutsu/mod/character/megumi/MegumiClientDefinition.java:15,130` / `TodoClientDefinition.java:19,133` — регистрация `*VfxRecipes.register()` из `registerClientHooks()` после `VfxDirector.initialize()` (Vessel Seam соблюдён)
- Проверены тесты/контракты: `VfxRadiusContractTest`, `VfxCompletenessTest`, `VfxDurationContractTest`, `ShadowBodySinkTest`, `VfxWorldMegumiShadowTest`, `VfxWorldSplitContractTest`, `TodoFakeClapTest`, `TodoTripleSwapTest`, `MegumiShadowPresentationTest`, `MegumiPouncePolicyTest`

Сверено с `AGENTS.md` (Vessel Seam, VFX Core Contract, wire ids append-only), `docs/KNOWN_ISSUES.md` (E4 delivery radius, E7 seam, E14 package), `Jujutsu Kaizen/jujutsumod-codebase-codex/04-client-vfx/VFX-core.md` (cue shapes, delivery≤presentation, one-shot recipes).

### 2) Находки

| severity | файл:строка | описание | рекомендация |
|---|---|---|---|
| major | `src/client/java/jujutsu/mod/client/vfx/megumi/MegumiVfxRecipes.java:231-244` + `src/client/java/jujutsu/mod/client/render/HiddenBodyRenderGate.java:19-29` + `src/client/java/jujutsu/mod/client/render/ShadowBodySink.java:59-68` | **Re-hide race: потерянный/переставленный `SHADOW_RIPPLE` после `SHADOW_EMERGE` повторно скрывает тело.** `ShadowBodySink.completeSink` имеет guard `if(state==EMERGING) return`, но `HiddenBodyRenderGate.markHidden` в `shadowRipple` guards нет. Порядок пакетов не гарантирован. Сценарий: `EMERGE` пришёл → `markRevealed` → поздний `RIPPLE` (период 5 ticks, TTL 8) → `markHidden(...,8)` снова true на 400 мс. Тело уже телепортировано к exit и должно быть видимо; 400 мс невидимого стояния — десинк третьего лица и camera dive (dive уже в EMERGING). Сервер `tickSubmerged` шлёт ripple каждый 5 тик, `tickSink` один раз при hide; клиент не может отличить старый ripple от актуального. | Добавить тот же guard в `shadowRipple` перед `markHidden`: проверять `ShadowBodySink.emergeProgress != -1` или хранить `lastEmergeGameTime` в gate и игнорировать `cue.startGameTime < lastEmerge`. Минимум: обернуть `markHidden` условием `ShadowBodySink.sinkProgress/emergeProgress` — если уже EMERGING, не перевооружать TTL. Покрыть тестом `ShadowBodySinkTest` reorder case уже есть для sink (стр.116-122), добавить аналог для gate. |
| major | `src/client/java/jujutsu/mod/client/vfx/VfxDirector.java:177-191` + `HiddenBodyRenderGate.java:1-43` + `ShadowBodySink.java:18-54` | **Глобальные VFX кэши не сбрасываются при смене мира/дисконнекте.** `VfxDirector.clear()` чистит `WORLD/HUD/CAMERA/FIRST_PERSON/PARTICLES/SOUND/POST_PROCESS`, но `HiddenBodyRenderGate.HIDDEN_UNTIL_MILLIS` и `ShadowBodySink.ENTRIES` — отдельные `static ConcurrentHashMap` — не трогает. `DISCONNECT` и `bindLevel` зовут только `clear()`. Эффект: после `ServerPlayerEvents.AFTER_RESPAWN / AFTER_PLAYER_CHANGE_WORLD / DISCONNECT` сервер уже `reveal`/`teardown`, но клиент держит `isHidden=true` до wall-clock expiry (≤400 мс) и `sinkProgress=1` до `windowTtlMillis`. При быстром релогине и переиспользовании `entityId` (ванила переиспользует ID) новый игрок/та же сущность может стартовать невидимой/утопленной без cue. TTL fail-open смягчает, но не убирает класс утечки. | В `VfxDirector.clear()`/`reset()` добавить `HiddenBodyRenderGate.clear()` + `ShadowBodySink.clear()` ( exposes `ENTRIES.clear()` + `HIDDEN_UNTIL_MILLIS.clear()` ). Альтернатива — подписать те же `ClientPlayConnectionEvents.DISCONNECT` и `level change` напрямую в gate/sink. Код уже имеет `SYSTEM_CLOCK` инъекцию в sink — clear сделать симметрично. |
| major | `src/client/java/jujutsu/mod/client/render/HiddenBodyRenderGate.java:19-23` vs `ShadowBodySink.java:23-24,33-34` | **TTL HiddenBody построен на wall-clock (`System.currentTimeMillis()`), а не на gameTime, и неинжектируем.** `ShadowBodySink` использует `LongSupplier clock` с `setClockForTests`, `HiddenBodyRenderGate` всегда `System.currentTimeMillis()` напрямую, без тестового шва. Последствия: (а) детерминизм тестов невозможен — `ShadowBodySinkTest` детерминирован, gate нет; (б) реальный client lag/pause (SP pause, дебаг-брейкпойнт, OS stall) съедает TTL 400 мс быстрее чем server `SHADOW_RIPPLE_PERIOD 5ticks=250мс + slack 150мс`. При задержке >150 мс между ripple (GC hitch, фриз) тело моргнёт видимым посреди `SUBMERGED` (до 50 тиков). Сервер шлёт gameTime, клиент мог бы считать TTL в тиках gameTime, а не в millis. | Выровнять с sink: завести в `HiddenBodyRenderGate` инжектируемый `LongSupplier clock` + `clear()/setClockForTests`, либо перейти на gameTime TTL (`expireGameTime = startGameTime + ttlTicks`, проверка по `Minecraft.level.getGameTime()`). Если оставляем wall-clock — увеличить `RIPPLE_HIDE_TTL_TICKS` с 8 до 10-12 чтобы slack 5-7 тиков покрывал типичный hitch, и добавить тестовый шов. Документировать выбор в `MegumiVfxRecipes:58`. |
| major | `src/main/java/jujutsu/mod/character/todo/TodoBoogieWoogieRuntime.java:259-343` + `src/main/java/jujutsu/mod/character/todo/TodoFakeClapRuntime.java:52-55` | **Несимметрия звука whoosh раскрывает feint.** `emitSwapImpact` планирует 2 отложенных звука: `scheduleDisplacementWhoosh(ribbonFrom)` + `scheduleDisplacementWhoosh(ribbonTo)` (оба `PROJECTJJK_CINEMATIC_WHOOSH` с `BOOGIE_WOOGIE_MOVE_SOUND_DELAY_TICKS`). `TodoFakeClapRuntime.tryCast` планирует 1 whoosh в `origin` только. Оба пути делят `PENDING_SOUNDS` + `tickPendingSounds → level.playSound(..., PLAYERS)` — broadcast, слышен всем в ~16 блоках. Если оба конца свопа в радиусе слышимости наблюдателя — слышно 2 whoosh подряд vs 1 у feint. Формально нарушает инвариант `AGENTS.md:44` «Everything a completed swap shows rides on SWAP_AFTERIMAGE/SWAP_ARRIVAL cues that feint never emits. The feint shares BOOGIE_WOOGIE clap cue by design» — whoosh не cue, а `playSound`, но «nothing that only a real swap earns may be added to that recipe» + общий `emitClapPerformance` — whoosh — часть performance. Сейчас whoosh различает. | Либо (а) feint тоже планирует 2 whoosh: второй в `origin.add(look*0.5)` или в `origin` с небольшой задержкой чтобы число событий совпало; либо (б) убрать второй whoosh из real swap когда `ribbonFrom.distanceTo(ribbonTo) < WHOOSH_MERGE_RADIUS` (тогда 1 whoosh всегда); либо задокументировать как accepted tell в `KNOWN_ISSUES.md` аналогично pose-tell feint (`E: feint pose leak`). Самый дешёвый фикс — `TodoFakeClapRuntime` вызвать `scheduleDisplacementWhoosh` дважды (второй с +1 tick jitter). |
| major | `src/main/java/jujutsu/mod/character/todo/TodoTransientState.java:76-99` | **Stone vanish cue теряется если chunk выгружен.** `clearStone` делает `level.getEntity(ref.entityUuid())` — если чанк с камнем выгружен, `entity == null` → `broadcastVfxCue` не шлётся и `discard` не вызывается (но state уже очищен). Камень визуально исчезает без `STONE_VANISH` puff (8 ticks gray). Аналогично `dropAll/clearAll` через этот путь. Сервер `TodoStoneRuntime` + `TodoStateLifecycle` уже имеют sweep `serverTick` для expiry/void/collision, но выгрузка чанка — отдельный путь `ENTITY_UNLOAD`? Для камня нет. Игрок видит камень зависшим до ре-логина. Контракт в комментарии `// emitted only while the stone is still alive to be seen, at its last position` — но `last position` уже потерян если entity null. | Хранить `lastPosition` в `TodoStoneRef` (или в `State`) и при `entity==null` всё равно `broadcastVfxCue(LEVEL, lastPos, ...)`. Либо не полагаться на `getEntity` в выгруженном чанке: слушать `ServerEntityEvents.ENTITY_UNLOAD` для `TodoStoneEntity` и там слать vanish по `entity.position()` до деспауна. Текущий `TodoStateLifecycle` не подписан на unload камня — добавить. |
| minor | `src/client/java/jujutsu/mod/client/vfx/megumi/MegumiVfxRecipes.java:39-42` + `MegumiShadowTrapRuntime.java:133-134` + `MegumiProfile.java:44` | **Пульс zone 40 ticks vs duration 42 — запас всего 2 тика.** Комментарий `// open 16 t beats the 40-tick zone pulse, whose 42-tick window must overlap it without a hole` верен в идеале, но при packet loss одного `SHADOW_TRAP_ZONE` (delivery 48 блоков, blind broadcast) клиент видит дыру 38 тиков (40-2). Для сравнения `DROP_ZONE` имеет пульс 5 vs duration 7 (запас 2 тика ≈40% окна) — здесь запас 5%. На медленном клиенте или вне радиуса 48 блоков дыра заметна как моргание void-black диска. | Увеличить `SHADOW_TRAP_ZONE_DURATION_TICKS` до 48-50 (запас 8-10) или уменьшить пульс до 30-35. Или ввести client-side hold как у `HiddenBody` — переиспользовать `ShadowBodySink` паттерн для trap zone (держать последний cue до `expireGameTime`). Сейчас рецепт `shadowTrapZone` — one-shot без TTL hold. |
| minor | `src/client/java/jujutsu/mod/client/vfx/world/ShadowWorldEffects.java:40-63` + `VfxWorldChannel.java:40,86-90` | **Z-fight / double-pool merge при близких trap.** `renderPoolDisk` рисует 24 треугольника на `y+0.025` с `RenderType.debugQuads()` (depth test/writes зависят от pipeline). На неровной земле (склон, полублок) отступ 0.025 внутри блока → z-fighting; на ровной земле два trap в 3 блоках друг от друга дают два отдельных `center`, но `debugQuads` batch один — фрагменты между центрами не соединяются? Код корректно избегает triangle-fan join (комментарий в Codex `independent quad sectors`), но визуально два чёрных круга с промежутком 0.2 блока всё равно сливаются в «восьмёрку» и читаются как один большой pool, вводя в заблуждение о реальном `SHADOW_TRAP_RADIUS 2.6`. Нет второй геометрии для перешейка. | Увеличить `y` offset до `0.04-0.05` или использовать `polygonOffset` в RenderType; для близких pool добавить `VfxWorldChannel` проверку чтобы не рендерить перекрывающиеся pool в одном кадре дважды (culling). Как минимум задокументировать что trap pool — истинная геометрия `insideCylinder` (2.6) и визуальный merge не расширяет grip. Низкий приоритет — дефект чисто косметический, баланс не ломает. |
| minor | `src/client/java/jujutsu/mod/client/vfx/VfxCameraChannel.java:256-296` + `ShadowBodySink.java:104-126` | **Поздний join (client присоединился mid-submerge) не видит dive, но видит hidden.** `VfxCameraChannel.advanceDive` переходит в `UNDER` если `sinkProgress==1` без прохождения `SINKING`. При `completeSink`-only join `sinkProgress==1` сразу, но `diveUnderSinceGameTime` инициализируется как `frameTime - DIVE_UNDER_EASE_TICKS` (стр.293) — корректно считает себя «уже settled». Однако `HiddenBodyRenderGate` для того же join уже `markHidden` (TTL 8) — тело скрыто, а камера не ныряла (резкий cut). На длинном `SUBMERGE 50 ticks` игрок, зашедший на 20-м тике, увидит невидимого соперника без собственного погружения камеры. | Приемлемо для transient VFX (E4 radius-filtered), но стоит добавить `VfxDirector` catch-up state для dive: если первый полученный cue для entity — `SHADOW_RIPPLE`, инициировать `beginSink` с `startGameTime = now - SHADOW_SINK_TICKS` чтобы dive проигрался как late seek. Сейчас `ShadowBodySink.completeSink` без sink создаёт `UNDER` напрямую (стр.67 `entry==null ?0L`), что и делает camera settled — пропущенный sink теряется намеренно. Задокументировать как accepted для E4. |
| minor | `src/main/java/jujutsu/mod/character/megumi/MegumiSummonRuntime.java:158-160,315,340-345` + `VfxRadiusContractTest.java:55-89` | **Divine Dogs summon/recall cue не несут интенсивность радиуса, но world pool масштаб фиксирован.** `broadcastDogCue` всегда `intensity=1`, рецепт `summon/recall` рисует `MEGUMI_SHADOW_OPEN/CLOSE` с `SUMMON_POOL_Z_SCALE=0.78` и фиксированным радиусом из `ShadowWorldEffects.shadowPoolRadius`. В отличие от trap (`intensity = radius*10`), dog pool не масштабируется от `MegumiProfile`. Если в будущем радиус призыва изменится — рецепт не подхватит. Также `DOGS_SUMMON_BODY` (1 tick) несёт только анимацию `triggerDivineDogs`, без world pool — наблюдатель вне 48 блоков не увидит summon, но увидит появившихся волков (entity spawn). | Вынести `DOG_POOL_RADIUS` в `MegumiProfile` и передавать через `intensity` как у trap, либо явно задокументировать что dog pool — decorative (не геймплейный радиус) и потому фиксирован. Сейчас `VfxRadiusContractTest:89` помечает dog cues как `finiteNone` (без proximity attenuation) — консистентно, но стоит добавить комментарий в `MegumiVfxRecipes:80-90` почему intensity не используется. |
| minor | `src/client/java/jujutsu/mod/client/vfx/todo/TodoVfxRecipes.java:104-109,117-134` + `SwapWorldEffects.java:54-108` | **Swap afterimage читаемость vs arrival overlap 4 vs 6 ticks.** `AFTERIMAGE_TICKS=4` держит силуэт где тело стояло, `ARRIVAL_TICKS=6` рисует landing ring где тело уже стоит. При коротком свопе (<2 блока) оба эффекта перекрываются в пространстве — силуэт просвечивает сквозь прибывшее тело, т.к. оба используют `RenderType.lightning()` additive. Комментарий `// Held for four ticks and no longer: overlaps where the other body is now arriving` осознаёт проблему, но на дистанциях 1.5-2 блока overlap всё равно заметен как «двойник». Увеличение `LOCAL_ARRIVAL_RADIUS_SQR=2.25` для camera snap не влияет на world. | Рассмотреть `AFTERIMAGE_TICKS=3` для дистанций <3 блоков (динамическая длительность) или уменьшить `SILHOUETTE_PEAK_ALPHA` со 168 до 120 когда `edgeLength <3`. Текущие 4 тика — уже минимум для читаемости; оставить как есть если playtest не жалуется. Зафиксировать в `SwapWorldEffects` комментарий о threshold. |

### 3) Позитивные наблюдения

- **Vessel Seam соблюдён образцово для VFX.** `TodoVfxIds`/`MegumiVfxIds` лежат в `jujutsu.mod.vfx` (shared, но без vessel-логики), рецепты в `jujutsu.mod.client.vfx.<vessel>` — разрешённый `SourceBoundaryTripwireTest` паттерн. Регистрация только из `*ClientDefinition.registerClientHooks()` после `VfxDirector.initialize()` — единый путь, без дублирования. `JujutsuVfxRecipes` агрегат удалён — дрифт списка невозможен. Проверено: `MegumiClientDefinition.java:130`, `TodoClientDefinition.java:133`, `VfxDirector.java:48` `putIfAbsent` duplicate = `IllegalStateException`.
- **Append-only wire ids.** `ResourceLocation` строки `todo/*`, `megumi/*` стабильны, `LIVE`/`PLANNED` множества явные, новые строки только добавляются. `VfxCueTest`/`VfxCuesTest` проверяют wire стабильность и нормализацию direction. `PLANNED` пуст — честно.
- **Server-authoritative, cue-visual-only.** Все эффекты идут `server-confirmed action → VfxCue/VfxCuePayload → JujutsuClientNetworking → VfxDirector → recipes → director-owned channels`. Нет per-effect receivers, HUD callbacks, camera managers вне director. Проверено: каждый `broadcastVfxCue` в `*Runtime` сопровождается gameplay гейтом (`isEligibleTarget`, `SafeBodyPlacement`, `TodoSwapGates`, `CharacterAbilityCooldowns`).
- **Devils in details продуманы:** `SHADOW_DIVE` deliberately не скрывает (sink watchable), первый `SHADOW_RIPPLE` скрывает + `ShadowBodySink.completeSink` — читаемое окно прерывания. `SHADOW_EMERGE` → `beginEmerge` до `markRevealed` (rise до reveal). `Todo` после свопа `hurtMarked=true` для `ClientboundSetEntityMotionPacket` — корректный vanilla путь. `TodoBoogieWoogieRuntime.findSafeDestination` без `default` overload — strictness явно на каждом call site (`SOFT` только Todo own arrival).
- **Feint indistinguishability на cue уровне.** `TodoFakeClapRuntime` вызывает `TodoBoogieWoogieRuntime.emitClapPerformance` — один метод, один звук `PROJECTJJK_CLAP`, один cue `BOOGIE_WOOGIE`. `FEINT_TELL` — caster-only (`sendVfxCue(todo, ...)`), `BOOGIE_WOOGIE` — broadcast. Ничего из `SWAP_AFTERIMAGE/ARRIVAL/ENDPOINT/MOMENTUM` не шлётся feint. Комментарий в `TodoVfxRecipes:65-68` явно предостерегает.
- **Shadow pools void-black корректно.** `ShadowWorldEffects.render*` использует `setColor(0,0,0,alpha)` + `debugQuads()` отдельным батчем от `lightning()` (Codex `VfxWorldChannel` lifecycle). `MEGUMI_SHADOW_POOL` — `renderShadowTrapPool(center, progress, radius)` с `alpha 255` всю жизнь, `CLOSE` fade `255→0` ease-out — дыра растворяется, не мигает. Каждый pool — 24 сектора с собственным центром — join центров исключён. `intensity/10.0f` несёт authoritative радиус (2.6 / 1.2) — рецепт не переопределяет баланс.
- **TTL HiddenBody fail-open.** `HiddenBodyRenderGate.isHidden` lazy expire, `markRevealed` immediate, потерянный пакет → тело станет видимым, никогда не застрянет невидимым. `ShadowBodySink` mirror: `UNDER_TTL_TICKS=8`, `windowTtlMillis`, re-arm на каждый ripple, `completeSink` idempotent, `beginEmerge` backdates из текущей глубины — плавный dive/rise даже при прерывании mid-sink. Покрыто `ShadowBodySinkTest` (sink 0→1, hold re-arm, TTL expiry, reorder ripple after emerge, late join, mid-sink emerge).
- **Camera dive continuous.** `VfxCameraChannel.diveOffsetBlocks/diveFadeAlpha` читают `ShadowBodySink` по `frameTime = gameTime+partialTick` — движение каждый кадр, не раз в тик. `DIVE_UNDER_EASE_TICKS=3` сглаживает hold, `advanceDive` idempotent. `CharacterSkinAnimationMixin.sinkOffsetY` `-1.9*smoothstep(progress)` + third-person dive синхронизирован с первым.
- **Divine Dogs summon pool корректно разделён.** `DOGS_SUMMON_BODY` (player-anchored, `Vec3.ZERO`, 1 tick, `triggerDivineDogs` + `firstPerson sign`) + 2×`DOGS_SUMMON` (dog-anchored, 16 ticks `MEGUMI_SHADOW_OPEN`). `DOGS_RECALL` — per living dog (12 ticks `CLOSE`), никогда synthetic owner pool. `DOGS_SIC/Pounce` — target-anchored, `DOGS_POUNCE` только после `accepted server damage` (`MegumiSummonRuntime:481` broadcast после `CombatStagger` check — pin `MegumiPouncePolicyTest`). В teardown перебираются все `ServerLevel` + `EntityTypeTest` — cross-dimension cleanup, single authority `TEARDOWN_IN_PROGRESS`.
- **Stone visuals mundane.** `STONE_THROW` (`anchoredDirected` от caster, 6 ticks, `SNOWBALL_THROW 0.5/1.12` + gray dust 5 частиц) и `STONE_VANISH` (worldFixed 8 ticks, `STONE_PLACE 0.45/0.85` + gray burst) — лёгкие, не конкурируют с violet clap. `STONE_GRAY/DARK` палитры отделены от `TODO_VIOLET/EDGE`. `TodoStoneRenderer` — persistent entity, transient cue только beat.
- **Triple swap направлен.** `TRIPLE_SWAP` 3× `worldFixedDisplacement` с `anchorOffset = full travel vector` + `direction = normalized flow` (10 ticks, 10 violet частиц + gray puff start + `TODO_EDGE` ring head) — направление `A→B→C→A` читается. `PAIR_MARK` silent pulse `intensity 0` каждую 20 ticks держит HUD чип и перерисовывает ring в актуальной позиции, не раскрывая наблюдателям.

### 4) Вердикт

**PASS WITH CAVEATS**

Срез `10-vfx-recipes-todo-megumi` в целом соответствует контрактам: Vessel Seam, VFX Core (cue→director→recipes, director-owned channels), server-authoritative, wire ids append-only — всё соблюдено. Todo 10 ids + 10 recipes и Megumi 15 ids + 15 recipes зарегистрированы из своих `CharacterClientDefinition`, дублирование невозможно (`IllegalStateException`). Stone visuals, swap afterimages/arrival, shadow pools void-black, Divine Dogs pools, camera dive — все реализованы через существующие director каналы без per-effect менеджеров, durée/timing централизованы в `*Profile`, late-cue seek корректен.

Критических блокеров, ломающих игру или десинхронящих authoritative состояние, не найдено — все swap/trap/drop/dive гейты server-side, клиенты только визуализируют. Однако 5 находок уровня **major** требуют фикса до широкой публичной сессии (или явного accept в `KNOWN_ISSUES.md`):

**Приоритет фиксов:**

1. **P0 — Re-hide race ripple после emerge** (`MegumiVfxRecipes shadowRipple` vs `HiddenBodyRenderGate`) — 2-3 часа, guard на EMERGING, тест reorder.
2. **P0 — Clear gate/sink на `VfxDirector.reset()`** — 30 мин, добавить `clear()` в обе мапы.
3. **P1 — Wall-clock TTL gate + неинъектируемость** — 1-2 часа, выровнять с sink (injectable clock или gameTime TTL) + slack.
4. **P1 — Whoosh asymmetry feint vs real** — 1 час, второй whoosh в feint или merge radius.
5. **P1 — Stone vanish при выгруженном чанке** — 2 часа, хранить lastPos / подписать `ENTITY_UNLOAD`.

Миноры (pulse запас trap 40/42, debugQuads z-fight, late-join dive cut, dog pool intensity) — polish, не блокируют `PASS`. После P0 фиксов вердикт становится чистым `PASS`; сейчас — `PASS WITH CAVEATS` с 9 находками (4 major + 5 minor), требующими адресации в следующем проходе.

AUDIT_ID: 10-vfx-recipes-todo-megumi
