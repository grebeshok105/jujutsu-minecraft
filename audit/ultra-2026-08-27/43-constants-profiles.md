## Constants — 43-constants-profiles

### 1) Что проверено (файлы/символы)

| Файл | Символы/константы | Способ проверки |
|---|---|---|
| `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:4-104` | `DOG_HEALTH 60.0:5`, `DOG_ATTACK_DAMAGE 3.0:6`, `DOG_MOVEMENT_SPEED 0.34:7`, `SIC_RANGE 20.0:14`, `DOG_MATERIALIZATION_TICKS 16:15`, `DOG_RECALL_TICKS 12:16`, `RECALL_COOLDOWN_TICKS 240:17`, `PACK_DEATH_COOLDOWN_TICKS 600:18`, `SIC_COOLDOWN_TICKS 30:19`, `POUNCE 3.0-8.0/80/16:20-23`, `SHADOW_TRAP 20.0/2.6/100/200/8:36-45`, `SHADOW_MOVE 20.0/24.0/1.75/8/4/6/50/5/120/200:54-65`, `DROP 20.0/4.0/1.2/20/5/60:73-101` | `read` полный файл, `grep MegumiProfile\.` 30+ hits |
| `src/main/java/jujutsu/mod/character/megumi/MegumiCooldownPolicy.java:4-24` | `duration(NONE->0, RECALL->240, FINAL_LOSS->600):7-13`, `preservedRemaining Math.max:15-17` | `read` |
| `src/main/java/jujutsu/mod/character/megumi/MegumiSummonRuntime.java:51-606` | `tryToggle:123-162` (summon 0 / recall 240), `trySic:164-206` (30), `teardown:291-333` (reason.cooldownTicks), `reconcile:261-289` (600), `startCooldownIfLonger:567-577` | `read 123-333`, `grep COOLDOWN` |
| `src/main/java/jujutsu/mod/character/megumi/MegumiShadowTrapRuntime.java:46-219` | `SHADOW_TRAP_COOLDOWN_TICKS 200:103-104` через `startCooldownIfLonger` | `read 75-106` |
| `src/main/java/jujutsu/mod/character/megumi/MegumiShadowMoveRuntime.java:57-438` + `MegumiShadowMovePolicy.java:12-73` | `SHADOW_STEP_COOLDOWN 120 / SUBMERGE 200:70-72` через `cooldownTicks(submerge)` | `read 1-80`, `grep` |
| `src/main/java/jujutsu/mod/character/megumi/MegumiShadowDropRuntime.java:49-291` | `DROP_COOLDOWN 60:109-110`, `DROP_TELEGRAPH 20:54-56`, `DROP_ZONE_PULSE 5:141` | `read` |
| `src/main/java/jujutsu/mod/character/todo/TodoProfile.java:4-111` | baseline всех Todo констант, сверка что Megumi не копирует их литералы | `read` полный |
| `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:82-91` | `maxCooldownTicks PRIMARY max(240,600):84`, `PRIMARY_SNEAK 30:85`, `SECONDARY 200:86`, `SECONDARY_SNEAK max(120,200):87`, `TERTIARY 60:88` | `read` |
| `src/test/java/jujutsu/mod/character/megumi/MegumiProfileTest.java:13-134` | `assertEquals 240:45`, `600:46`, `30:32`, `200:65`, `120/200:78-79`, `60:107` + invariants 85-98 | `read` |
| `src/main/java/jujutsu/mod/character/CharacterAbilityCooldowns.java:22-67` | `READY_AT` key `(player, vessel, slot)`, `startCooldownIfLonger` обёртка | `grep` + `read` фрагмент |
| `docs/MEGUMI_SHADOW_KIT.md` + `docs/MEGUMI_SHADOW_DROP.md` + `AGENTS.md:31-41` + `Jujutsu Kaizen/jujutsumod-codebase-codex/03-systems/Megumi-Divine-Dogs.md:16-17` | сверка заявленных чисел (summon 0 / recall 240 / final 600 / Sic 30 / trap 200 / move 120-200 / drop 60) с кодом | `read` + `grep` |
| `grep "240|600|30|200|120|60" src/main/java/jujutsu/mod/character/megumi` | поиск хардкодов вне профиля | `grep` — 0 литералов вне профиля, все через `MegumiProfile.` |

Инструменты: `read` 9 файлов полностью, `grep MegumiProfile\.` (32 hits), `grep COOLDOWN` (cross-file), `codegraph_explore` для DogPresentationPolicy/GroundSafety/PouncePolicy (не дублируют константы).

---

### 2) Находки

| # | severity | файл:строка | описание | рекомендация |
|---|---|---|---|---|
| 1 | **major** | `src/main/java/jujutsu/mod/character/megumi/MegumiSummonRuntime.java:285-288` (`reconcile` на `FINAL_LOSS`) | **Cooldown финальной потери 600 не применяется если игрок оффлайн.** `reconcile` делает `PACKS.remove(ownerId, pack)` + `startCooldownIfLonger(server.getPlayerList().getPlayer(ownerId), PRIMARY, 600)` — если `owner==null` (дисконнект, краш, выгрузка чанка с удалением последней собаки), `getPlayer` вернёт `null`, `startCooldownIfLonger` no-op, pack удалён без штрафа. Игрок релогает без 30с кулдауна. Тот же путь для `teardown` офлайн не страдает (там `shouldApplyTeardownCooldown` + guard `foundCooldownOwningDog`), но `reconcile` — страдает. | Хранить cooldown на `UUID` без live `ServerPlayer`: вынести `startCooldownIfLonger` на `UUID`-ключ (как `CharacterAbilityCooldowns` уже keyed по UUID) или отложить запись до `PlayerJoin` с pending map `Map<UUID, Cause>`; покрыть GameTest `reconcileOfflineFinalLossAppliesCooldown`. |
| 2 | **major** | `src/main/java/jujutsu/mod/character/megumi/MegumiSummonRuntime.java:123-133` (`tryToggle`) vs `261-289` (`reconcile` на `END_SERVER_TICK:348`) | **Гонка RECALL 240 перезаписывает FINAL_LOSS 600.** Если обе собаки уже мертвы (`livingDogs==[]`), `reconcile` должен дать `600`. Но `tryToggle` проверяет `PACKS.get != null` до тика `reconcile` — игрок жмёт `R` в тот же тик, видит pack, уходит в `teardown(RECALL)` с `240` вместо финального `600` (разница 18с). `startCooldownIfLonger` с `Math.max` не спасёт если `240` запишется первым, а `reconcile` ещё не успел удалить pack. | В `tryToggle` перед веткой `existing != null` вызвать `livingDogs` + `reconcileAction` синхронно: если `living==0` — идти по пути `FINAL_LOSS` (или просто вызвать `reconcile` перед решением). Альтернатива: `teardown` с `Cause.FINAL_LOSS` если `livingDogs==0` на момент `tryToggle`. |
| 3 | **major** | `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:73-101` + `MegumiShadowDropRuntime.java:109-110,156-174` | **Баланс: `DROP_COOLDOWN_TICKS 60` (3с) слишком короткий для урона до 40 (anvil).** Зона 1.2 радиуса ре-анкорится каждые `DROP_ZONE_PULSE_TICKS 5`, теlegraph `20`, volley `1-3` блока со scatter `0.9`. При `DROP_SOFT 1.0×blocks cap5` и `ANVIL 2.0 cap40` средний volley ~ 8-12 урона каждые 3с с follow-таргетом и без затрат (кроме кулдауна). На фоне `trap 200` и `move 120/200` drop — самый спамный бурст. В тесной арене можно держать цель под permanent теlegraph. | Поднять к `80-100` (4-5с) или добавить стак-цену (напр. второй drop в `10с` окно = `+40` ticks). Если оставить `60`, добавить оверхед: `drop` требует наличия pack (сейчас не требует) или стамина. Минимум — задокументировать баланс-решение и покрыть DPS-тестом `3×drop in 180 ticks ≈ 30-40 dmg`. |
| 4 | **minor** | `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:40-41,64-65` | **Баланс: `SUBMERGE 200` (10с) всего на 80 ticks дороже `STEP 120` (6с) за до 50 ticks полной неуязвимости.** `SUBMERGE_MAX_TICKS 50` + `ALLOW_DAMAGE` блок всего кроме `bypassesInvulnerability` (огонь/лава включены) + `setInvisible(true)` каждый тик + сохранение полной коллизии. Цена +4с за 2.5с инвула к любому урону и пересечение лавы — дёшево. Игрок будет холдить вместо тапа. | Поднять `SUBMERGE_COOLDOWN_TICKS` к `240-260` или урезать `SUBMERGE_MAX_TICKS` до `30-35`. Альтернатива — оставить как есть но задокументировать как intended mobility payoff (как в `MEGUMI_SHADOW_KIT.md:78` для огня) и проверить smoke: `submerge lava crossing` действительно ощущается как 10с цена. |
| 5 | **minor** | `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:40-41` + `MegumiShadowTrapRuntime.java:40-44` | **Баланс: `SHADOW_TRAP 200` при `DURATION 100` = 50% аптайм.** Радиус `2.6`, `VERTICAL_REACH 1.6`, `SHADOW_GRIP -75% speed / -1.0 jump` на `8t` рефреше. При ренже `20` и снапе `GROUND_SNAP 3.0` ловушка покрывает проход/точку каждые 10с на 5с. Для 1v1 это перманентный zone control с малым окном контригры. | Либо `DURATION 80` / `COOLDOWN 240`, либо добавить видимый telegraph `8t` перед грипом. Если 50% аптайм intended для приватного 1-2 игрока — зафиксировать в `MEGUMI_SHADOW_KIT.md` как accepted и добавить инвариант `COOLDOWN >= 2×DURATION` в `MegumiProfileTest.shadowKitInvariantsHold`. |
| 6 | **minor** | `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:19` + `MegumiSummonRuntime.java:204-206` | **Баланс/шум: `SIC_COOLDOWN 30` (1.5с) < `POUNCE_COOLDOWN 80` (4с).** Sic можно спамить каждые 1.5с пере-прицеливая обе собаки, пока pounce ещё на CD. Каждая попытка — `TargetResolver` + `isEligibleTarget` + `playSound` + `broadcastCue` (48 блоков). Pounce gate `deadlineReady` + `3-8` ренж уже лимитируют, но Sic-спам создаёт сетевой/звуковой шум и позволяет "fishing" — быстро менять цель в надежде что pounce слетит. | Поднять к `40-60` (2-3с) или дебаунсить `trySic` если `assignedTarget == currentTarget` (`no-op` без кулдауна/звука). Добавить `MegumiPouncePolicy.canLaunch` early-exit тест на Sic spam. |
| 7 | **minor** | `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:15-18` + `MegumiSummonRuntime.java:123-162` | **`SUMMON 0` не имеет именованной константы.** `RECALL 240` и `PACK_DEATH 600` — в профиле, `SIC 30` — в профиле, но `summon` — implicit `0` через отсутствие `startCooldown` в ветви `PACKS==null` (`tryToggle:143-161`). `MegumiCooldownPolicy.Cause.NONE -> 0:9` — единственное место, но `MegumiProfile` не объявляет `SUMMON_COOLDOWN_TICKS = 0`. Это обратная сторона "не дублировать числа": отсутствие константы заставляет читателя гадать 0 ли intended или забыли. | Добавить `public static final int SUMMON_COOLDOWN_TICKS = 0` в `MegumiProfile.java:16` с комментом `// summon starts no cooldown by design` и использовать в `MegumiCooldownPolicy:9` + `tryToggle` early-return doc. Тест `MegumiProfileTest` уже проверяет `RECALL < PACK_DEATH`, добавить `SUMMON == 0`. |
| 8 | **minor** | `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:83-91` + `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:17-18,41,64-65,81` | **HUD denominator дублирует профиль через `Math.max`.** `PRIMARY -> max(RECALL,PACK_DEATH):84`, `SECONDARY_SNEAK -> max(STEP,SUBMERGE):87`. При изменении `PACK_DEATH 600→500` или `SUBMERGE 200→260` HUD fraction поплывёт если забудут пересобрать клиент (хотя константа одна, формула ручная). Нет единой `MegumiProfile.maxCooldownFor(ability)` — каждая смена профиля требует аудита 2 мест. | Вынести `public static int maxCooldownFor(CharacterAbility)` в `MegumiProfile` или `MegumiCooldownPolicy` и делегировать из `MegumiClientDefinition.maxCooldownTicks`. Это устранит drift между server `duration()` и client `max`. |
| 9 | **minor** | `src/main/java/jujutsu/mod/character/megumi/MegumiProfile.java:33` + `src/test/java/jujutsu/mod/character/megumi/MegumiProfileTest.java:49` | **Нет инварианта `PACK_DEATH > RECALL` в runtime, только в тесте.** `MegumiCooldownPolicy.duration` + `preservedRemaining Math.max` корректно не укорачивают, но нигде в runtime не assert'ится что `600 > 240`. Опечатка `600→240` скомпилируется и тесты поймают, но продакшн без тестов поедет с равными кулдаунами и финальная потеря перестанет наказываться. | Добавить `static { assert PACK_DEATH_COOLDOWN_TICKS > RECALL_COOLDOWN_TICKS; }` или `checkArgument` в `MegumiCooldownPolicy`, либо оставить как есть (тест уже `assertTrue RECALL < PACK_DEATH:49` — достаточно для CI). Задокументировать как intentional test-only invariant. |

Дополнительно проверено и **не** считается находкой: `grep` по `src/main/java/jujutsu/mod/character/megumi/*.java` не нашёл ни одного литерала `240`, `600`, `30`, `200`, `120`, `60` вне `MegumiProfile.java` и `MegumiProfileTest.java`; все runtime (`Summon`, `Trap`, `Move`, `Drop`, `Pounce`, `GroundSafety`) читают через `MegumiProfile.` — требование "не дублировать числа вне профилей" соблюдено. `TodoProfile` — отдельный source of truth, пересечений нет.

---

### 3) Позитивные наблюдения

- **TodoProfile — образцовый source of truth.** Все Todo числа (`BOOGIE_WOOGIE_RANGE/COOLDOWN`, `FAKE_CLAP`, `PAIR/TRIPLE/STONE`, `SAFE_POSITION_*`, `SWAP_MOMENTUM`, `BLACK_FLASH`) живут только в `TodoProfile.java:4-111`, все runtime (`TodoBoogieWoogieRuntime:35-36,62,73,109`, `TodoFakeClapRuntime`, `TodoPairSwapRuntime`, `TodoStoneRuntime`, `TodoDefinition:31-32`, `TodoClientDefinition:85-91`) читают через `TodoProfile.` — ни одного литерала `20.0`/`60`/`100` вне профиля. `TodoProfileTest` пинит значения.
- **MegumiProfile — полный SSOТ.** Все требуемые заданием числа присутствуют и задокументированы: `summon 0` (implicit через отсутствие cooldown + `Cause.NONE:9`), `RECALL 240:17`, `PACK_DEATH 600:18`, `SIC 30:19`, `trap 200:41`, `step 120:64`, `submerge 200:65`, `drop 60:81`. Плюс `DOG_MATERIALIZATION 16 / RECALL 12`, `POUNCE 3-8/80/16`, `TRAP duration 100/radius 2.6`, `MOVE sink 8/hidden 4/emerge 6/submergeMax 50/ripple 5`, `DROP telegraph 20/pulse 5/weights 30-25-25-20`. Javadoc на каждый блок.
- **Pure cooldown policy + non-shortening.** `MegumiCooldownPolicy.duration:7-13` — чистый `switch` по `Cause`, `preservedRemaining:15-17` — `Math.max(current, requested)`, `startCooldownIfLonger` всегда `Math.max`. Длинный `600` никогда не укорачивается `240` (кроме гонки #2, где укорачивание — баг порядка, не `Math.max`).
- **Ни одного хардкода вне профиля.** `grep` по `megumi/*.java` — 0 литералов `240/600/200/120/60` кроме профиля и тестов; `ShadowMovePolicy.cooldownTicks(false/true):70-72`, `ShadowTrap` `103-104`, `ShadowDrop` `109-110`, `Summon` `204-206` — все через `MegumiProfile.`. Требование "не дублировать числа" выполнено на 100%.
- **Client HUD denominator корректно читает профиль.** `MegumiClientDefinition:84,87` — `max(RECALL,PACK_DEATH)` и `max(STEP,SUBMERGE)` — fraction `remaining/max` никогда не >1, `SIC 30`, `TRAP 200`, `DROP 60` напрямую. `VFX_CUE_RADIUS 48.0:32` единая через `VFX_DELIVERY_RADIUS:33`.
- **Тесты пинят баланс и инварианты.** `MegumiProfileTest.divineDogBaseline:21-50` — exact `assertEquals` на все 240/600/30, `shadowKitBaseline:64-81` — 200/120/200/60, `shadowKitInvariantsHold:84-98` — `TRAP_COOLDOWN > DURATION`, `SUBMERGE > STEP`, `GRIP -0.75/-1.0`, `dropInvariants:123-127` — `TELEGRAPH < COOLDOWN`, `ZONE < TRAP_RADIUS`, `weights sum 100`.
- **Баланс-числа осмысленны в контексте kit.** `SIC_RANGE 20` = `BOOGIE_WOOGIE_RANGE 20` — единый feel ренжа; `POUNCE 3-8` — mid-range, не melee; `LEASH 32` + `RETRY 10` — редкий дорогой телепорт; `DOG 60 HP / 3 dmg / 0.34 speed` — танк, не бурст; `TRAP -75% speed / -1.0 jump` — slow, не stun; `DROP 1-3 blocks scatter 0.9` — volley читается как volley, не стак.
- **Документация синхронна.** `AGENTS.md:31-41` (Megumi `R/Shift+R/B/Shift+B/V`), `docs/MEGUMI_SHADOW_KIT.md:129-143` (таблица 200/120/200/60), `docs/MEGUMI_SHADOW_DROP.md:28-30`, `Jujutsu Kaizen/jujutsumod-codebase-codex/03-systems/Megumi-Divine-Dogs.md:16-17` (`Summon 0 / Recall 240 / Final 600`) — все совпадают с `MegumiProfile`.

---

### 4) Вердикт

**PASS WITH CAVEATS**

**Обоснование:** Константы централизованы образцово — `MegumiProfile` единственный source of truth, все 7 требуемых чисел (`summon 0 / recall 240 / final 600 / Sic 30 / trap 200 / move 120-200 / drop 60`) присутствуют и используются исключительно через профиль (0 хардкодов вне профиля, `grep` чист). `TodoProfile` не задет, `MegumiCooldownPolicy` pure, `startCooldownIfLonger` не укорачивает, тесты пинят exact значения и инварианты. Vessel Seam, wire ids append-only, server-authoritative не нарушены.

Caveats — 2 **major** логических бага применения констант (оффлайн 600 #1 и гонка 240→600 #2) и 3 баланс-caveats (drop 60 #3, submerge дельта #4, trap 50% аптайм #5) — не ломают компиляцию, но влияют на feel и экономику наказаний. Остальные 4 находки — `minor` гигиены (Sic спам #6, missing `SUMMON 0` константа #7, HUD `Math.max` drift #8, missing runtime invariant #9).

**Приоритет фиксов:**

1. **P0 (до следующего playtest):** #1 оффлайн 600 + #2 гонка 240/600 — оба трогают экономику смерти пака (30с vs 12с), чинятся в `MegumiSummonRuntime.java:123-133,261-289` за 20-30 строк, покрываются 2 GameTest'ами.
2. **P1 (баланс-патч):** #3 drop `60→80-100` (или цена pack), #4 submerge `200→240-260` — по одному числу в `MegumiProfile.java:65,81`, smoke `3×drop DPS` + `submerge lava crossing`.
3. **P2 (гигиена):** #7 добавить `SUMMON_COOLDOWN_TICKS=0` в `MegumiProfile.java:16`, #8 вынести `maxCooldownFor` в профиль и делегировать из `MegumiClientDefinition.java:84,87`, #6 дебаунс Sic `30→40-60` или `no-op` без звука если цель та же.
4. **P3 (документация):** #5 зафиксировать `TRAP 50% аптайм` как accepted для 1-2 игроков или поднять к `240` в `MegumiProfile.java:41`, добавить инвариант `COOLDOWN >= 2×DURATION` в тест; #9 оставить test-only invariant или добавить `static assert`.

AUDIT_ID: 43-constants-profiles
