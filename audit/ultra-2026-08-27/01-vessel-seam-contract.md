## Vessel Seam — 01-vessel-seam-contract

### 1) Что проверено (файлы/символы)

**Инструменты:** `codegraph_explore` по `JujutsuCharacter / JujutsuCharacters / JujutsuCharacterClients / CharacterDefinition / CharacterClientDefinition`, прямые `read` каждого файла ниже, `grep -rn "JujutsuCharacter\."` и `grep -rn "NOBARA|TODO|MEGUMI"` по `src/main/java/jujutsu/mod` и `src/client/java/jujutsu/mod/client`, `grep -rn "default\s*->"` по обоим source sets, `grep -rn "switch.*ability"` для exhaustive проверки. Срез `main` на `D:/WorkFlow/jujutsu-minecraft` 2026-08-27, Fabric 1.21.8 / Java 21.

**Ядро seam:**
- `src/main/java/jujutsu/mod/character/JujutsuCharacter.java:1-43` — enum `JujutsuCharacter` (NONE/NOBARA/TODO/MEGUMI), поля `id/modelId/bodyScale`, метод `byId` — проверено `read` + `grep switch`.
- `src/main/java/jujutsu/mod/character/JujutsuCharacters.java:1-57` — серверный реестр, exhaustive `switch(character)` без `default` (`:31-36`), поля `*_DEFINITION` с суффиксом (коммент `:20-22`), `all()` через `values()` (`:49-56`), `of(player)` (`:40-42`).
- `src/client/java/jujutsu/mod/client/character/JujutsuCharacterClients.java:1-66` — клиентский реестр, exhaustive `switch` без `default` (`:26-32`), `all()` (`:39-46`), `inRosterOrder()` (`:54-58`), `registerAll()` (`:61-65`).
- `src/main/java/jujutsu/mod/character/CharacterDefinition.java:1-102` — интерфейс vessel (`id()` + `tryCast` required, `registerServerHooks/applyAttributes/removeAttributes/adjustIncomingStagger/onSelected/onDeselected/selectCurseLink` default). Проверено отсутствие `net.minecraft.client` / `jujutsu.mod.client` — `grep` пусто, тест `assertNothingClientOnlyLeakedIn` дублирует.
- `src/client/java/jujutsu/mod/client/character/CharacterClientDefinition.java:1-89` — клиентский интерфейс (`id/rosterEntry` required, `hudSlots/maxCooldownTicks/skinAnimation/playerSkin/bodyScale/rosterOrder/accent/warmth/registerClientHooks/module*` default). `maxCooldownTicks` default `0` (`:38-40`), `bodyScale()` делегирует `id().bodyScale()` (`:59-61`).

**Определения vessels:**
- `src/main/java/jujutsu/mod/character/NoneDefinition.java:1-25` и `src/client/java/jujutsu/mod/client/character/NoneClientDefinition.java:1-56` — `NONE`, `rosterOrder=Integer.MAX_VALUE` (`:33-35`), `accent 0xFF7A8796` (`:14`), пустой `hudSlots`.
- `src/main/java/jujutsu/mod/character/nobara/NobaraDefinition.java:1-61` — 8 `register()` (`:34-44`), `selectCurseLink` → `SelfResonanceRuntime.select` (`:51-53`), `onSelected` idempotent `ensureStarterTools` (`:56-60`).
- `src/client/java/jujutsu/mod/client/character/nobara/NobaraClientDefinition.java:1-129` — `ACCENT 0xFFE48A36` (`:27`), `rosterEntry` 5 abilities (`:42-59`), `hudSlots` 5 клеток (`:68-76`), `registerClientHooks` entity+nailRenderer+VFX (`:112-118`), **нет** `maxCooldownTicks` override и **нет** `rosterOrder` override.
- `src/main/java/jujutsu/mod/character/todo/TodoDefinition.java:1-66` — `apply/removeAttributes` с `TodoProfile.MELEE_DAMAGE_MULTIPLIER` (`:30-38`), `registerServerHooks` 4 runtime (`:42-49`), `adjustIncomingStaggerTicks` (`:53-58`), `onDeselected` teardown (`:61-66`).
- `src/client/java/jujutsu/mod/client/character/todo/TodoClientDefinition.java:1-147` — 6 abilities (`:42-55`), `hudSlots` 6 (`:64-74`), `maxCooldownTicks` с `default -> 0` (`:82-92`), `rosterOrder 1` (`:115-117`), `accent 0xFFA56CFF`.
- `src/main/java/jujutsu/mod/character/megumi/MegumiDefinition.java:1-51` — `FabricDefaultAttributeRegistry.register(MEGUMI_DIVINE_DOG)` внутри vessel hook (`:28-29`), 3 runtime + teardown `onDeselected` (`:44-50`).
- `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:1-144` — 6 entries (включая `DEEP_SUBMERGE S+B+` hold-маркер, `:48-53`), `hudSlots` 5 (`:64-72`, `TERTIARY_SNEAK` не имеет HUD клетки deliberately), `maxCooldownTicks` с `default -> 0` (`:82-91`), `rosterOrder 2`.

**Shared dispatch (не должен ветвиться по персонажу):**
- `src/main/java/jujutsu/mod/character/CharacterAbility.java:16-74` — 10 слотов `PRIMARY(0)…TERTIARY_SNEAK(9)`, `networkId` append-only, `byNetworkId` linear scan.
- `src/main/java/jujutsu/mod/character/CharacterAbilityExecutor.java:1-36` — gate `NONE` (`:17-23`) + `isReady` (`:27-33`) → `definition.tryCast`. Ни одного `if(TODO)` / `switch(JujutsuCharacter)` кроме `NONE`.
- `src/main/java/jujutsu/mod/character/CharacterCombatModifiers.java:1-46` — sweep `for(all) remove + selected.apply` (`:29-33`), `adjustedStaggerTicks` через `JujutsuCharacters.of` (`:40-45`).
- `src/main/java/jujutsu/mod/character/CharacterSelectionManager.java:1-79` — `select` делает `onDeselected(previous)` до смены state (`:21`), `claimStarter` для всех (`:24`), `applyForSelection` (`:26`), `broadcast` c `character.id()/modelId()` (`:74-77`).
- `src/client/java/jujutsu/mod/client/character/ClientCharacterSelectionManager.java:1-117` — `apply`/`applyLocal`/`characterOrNone` mirror, `model` mapping `slim/wide` (`:106-108`), `cancelFirstPersonOnVesselChange` (`:42-50`).
- `src/main/java/jujutsu/mod/network/JujutsuNetworking.java:31-97` — payloads регистрация (`:19-28`), `handleCharacterAbility` vessel-claim check `byId(payload.characterId()) != selected` (`:54`), `SelectCurseLink` через `JujutsuCharacters.of().selectCurseLink` (`:39-40`), `sendAbilityCooldown` резолвит vessel из `CharacterSelectionManager.selected` (`:94-95`).
- `src/main/java/jujutsu/mod/character/CharacterAbilityCooldowns.java:1-70` — `Key(UUID, JujutsuCharacter, CharacterAbility)` (`:69`), `register` только `DISCONNECT+SERVER_STOPPING` (`:26-29`), `keyFor` резолвит vessel из selection (`:65-67`).
- `src/client/java/jujutsu/mod/client/character/ClientAbilityCooldowns.java:1-57` — mirror `Key(JujutsuCharacter, CharacterAbility)` без `playerId`, `apply` через `byId` (`:17`).
- `src/client/java/jujutsu/mod/client/input/JujutsuKeybinds.java:235-268` — `slot(tap,sneak)` чистый транслятор (`:235-237`), `sendCharacterAbility` gate `NONE` + `isReady` (`:247-254`), `isTechniqueWeapon` хардкод `STRAW_DOLL_HAMMER` (`:266-268`).
- `src/main/java/jujutsu/mod/command/JujutsuCommands.java:132-141` — `castAbilitySlot` с `if(selected != NOBARA)` (`:134`).
- `src/client/java/jujutsu/mod/client/hud/AbilityHud.java:66-137` — `NONE` gate (`:72-76`), `definition.hudSlots()` + `maxCooldownTicks` denominator (`:77,103-106`).
- `src/client/java/jujutsu/mod/client/render/CharacterSkinAnimationRenderer.java:11-34`, `src/client/java/jujutsu/mod/client/mixin/CharacterSkinMixin.java:22-28`, `src/client/java/jujutsu/mod/client/render/CharacterGeoRenderers.java:23-32` (archive), `src/client/java/jujutsu/mod/client/JujutsuModClient.java:20-41` — все через `JujutsuCharacterClients.all()/definition()`, без `if vessel`.

**Роутеры (должны быть exhaustive без default):**
- `src/main/java/jujutsu/mod/character/nobara/NobaraAbilityRouter.java:29-73` — stagger gate (`:30-35`), early return 4 слота (`:41-44`), exhaustive `switch(ability)` без `default` (`:45-68`), fallback `no_target` message (`:69-71`).
- `src/main/java/jujutsu/mod/character/todo/TodoAbilityRouter.java:20-42` — exhaustive switch без `default`, `SECONDARY,SECONDARY_SNEAK` → `TodoPairSwapRuntime`, `TERTIARY,TERTIARY_SNEAK` → `TodoStoneRuntime`, остальные `UNHANDLED_FAILURE` явно.
- `src/main/java/jujutsu/mod/character/megumi/MegumiAbilityRouter.java:21-42` — `locksAbilities` gate (`:22-25`), exhaustive switch без `default`, `ATTACK_CONTEXT/USE_CONTEXT/TERTIARY_SNEAK -> UNHANDLED_FAILURE` (`:41`).
- Внутренний: `src/main/java/jujutsu/mod/character/todo/TodoStoneRuntime.java:69-75` — `switch(ability){case TERTIARY,TERTIARY_SNEAK; default->false}` — vessel-internal, но с `default`.

**Тесты / контракты:**
- `src/test/java/jujutsu/mod/character/CharacterDefinitionRegistryTest.java:26-145` — 5 ассертов: `assertEveryVesselResolves` (`:85-92`), `assertTheSweepMatchesTheSwitch` (`:94-103`), `assertTheSwitchCannotFallThrough` (`:105-117`, regex `default`), `assertNothingClientOnlyLeakedIn` (`:124-145`), `assertEveryVesselRuntimeIsRegistered` (`:41-73`, `Files.walk` + `register()` scan, `init.contains("definition.registerServerHooks()")`).
- `src/test/java/jujutsu/mod/client/character/CharacterClientRegistryTest.java:39-168` — `assertEveryVesselIsBound` (`:47-57`, regex `default`), `assertTheServerHalfStaysServerOnly` (`:63-74`, walk `src/main` на `net.minecraft.client`/`jujutsu.mod.client`), `assertNoSharedClientFileNamesAVessel` (`:77-100`, allowlist 5 файлов), `assertEveryVesselDeclaresItsOwnCard` (`:106-123`) + `slotsTheRouterAnswers` (`:132-150`, regex `case … -> false/UNHANDLED_FAILURE`).
- `src/test/java/jujutsu/mod/character/nobara/NobaraAbilitySlotsTest.java:23-227` — `default` check в реестре (`:210-214`), vessel-claim и cooldown cross-vessel checks.
- `src/test/java/jujutsu/mod/architecture/VesselBoundaryTest.java:46-344` — bytecode ArchUnit: `VESSEL_IDS` из `JujutsuCharacter.values()` (`:51-54`), `sharedServerCodeAsksTheVesselRatherThanNamingOne` (`:232-244`), `sharedClientCodeAsksTheVesselRatherThanNamingOne` (`:246-255`), `theNetworkLayerTouchesNoVesselCode` (`:318-337`, `Set.of()`), `vesselNamedClassesStayInTheirVesselPackage` (`:206-229`, pinned 6).
- `src/test/java/jujutsu/mod/architecture/SourceBoundaryTripwireTest.java:48-121` — source-text grep на `default` и `Class.forName`, tripwire для constant-folded reads.

**Сверка с документацией:**
- `AGENTS.md:219-238` (Vessel Seam), `AGENTS.md:35-36` (slots), `docs/KNOWN_ISSUES.md:E7:217-224` (9 hits классификация), `Jujutsu Kaizen/jujutsumod-codebase-codex/02-architecture/Vessel-definitions.md:54-76`, `docs/TEST_ARCHITECTURE_PLAN.md`.

---

### 2) Находки

| severity | файл:строка | описание | рекомендация |
|---|---|---|---|
| major | `src/client/java/jujutsu/mod/client/character/todo/TodoClientDefinition.java:82-92` и `src/client/java/jujutsu/mod/client/character/megumi/MegumiClientDefinition.java:82-91` | `maxCooldownTicks(CharacterAbility)` использует `switch` с `default -> 0`. Это единственное vessel-специфичное место в seam с `default`. При добавлении нового `CharacterAbility` (напр. `QUATERNARY(10)`) компиляция **не сломается** — новый слот молча получит denominator `0` и `AbilityHud:104-106` не нарисует cooldown-оверлей (`remaining>0 && total>0` false). Нарушает compile-time гарантию, которая в реестрах и роутерах специально держится exhaustive без `default`. Подтверждено `grep "default\s*->"` — других vessel-файлов с `default` нет; `CharacterClientRegistryTest:49` проверяет отсутствие `default` только в реестре, не в `maxCooldownTicks`. | Убрать `default`, перечислить все 10 `CharacterAbility` явно (неиспользуемые → `0`), как это делают роутеры. Пример: `case TERTIARY_SNEAK -> …; case USE_CONTEXT, SECONDARY_SNEAK_HOLD, SECONDARY_SNEAK_RELEASE, ATTACK_CONTEXT -> 0;` — теперь новый слот сломает компиляцию. Синхронно поправить `VesselBoundaryTest` / `SourceBoundaryTripwireTest` floor. |
| major | `src/client/java/jujutsu/mod/client/character/nobara/NobaraClientDefinition.java:61-76` (отсутствие методов) | Nobara — единственный vessel без `maxCooldownTicks` override (наследует `0` из `CharacterClientDefinition:38-40`) и без `rosterOrder` override. В результате `AbilityHud` получает `total=0` для всех слотов — cooldown-оверлей для Nobara никогда не рисуется через shared path (работает только через кастомные VFX, если есть). Аналогично `rosterOrder` остаётся `0` по умолчанию, что случайно совпадает с желаемым `0` (перед Todo=1, Megumi=2, None=MAX) и не задокументировано. Todo/Megumi оба переопределяют оба метода явно — несимметрия. | Добавить явный `@Override int maxCooldownTicks(CharacterAbility ability)` с exhaustive switch без `default` (каждый слот Nobara → профиль или `0` если слоу нет кулдауна) и `@Override int rosterOrder(){return 0;}` с комментарием. Если Nobara действительно не имеет shared cooldowns — зафиксировать это явно (`case PRIMARY, PRIMARY_SNEAK … -> 0 // no shared cooldown, trap/resonance use VFX cue`) чтобы будущий кулдаун не был забыт. |
| major | `src/main/java/jujutsu/mod/command/JujutsuCommands.java:132-141` | Единственный shared файл, который напрямую ветвит по персонажу: `if (CharacterSelectionManager.selected(player) != JujutsuCharacter.NOBARA)` (`:134`). Это заявленное в `KNOWN_ISSUES.md:E7` deliberate исключение (debug-команды `hairpin`/`projectjjk_nail`), но формально нарушает правило `AGENTS.md:231` "No per-character if or switch in a dispatch file". Комментарий (`:124-131`) честно объясняет причину — slot — это input position, поэтому `PRIMARY` у Todo это swap, и команда "Hairpin" не должна файрить чужой swap. Тем не менее seam эродировал ровно на один `if`; `VesselBoundaryTest:232-244` явно exempts `CONTENT_REGISTRIES` и `NETWORK_LAYER`, но не `JujutsuCommands`, поэтому тест не ловит этот `if` (проверяет `dependOnClassesThat resideInAnyPackage(VESSEL_PACKAGES)`, а `JujutsuCharacter` лежит в `jujutsu.mod.character`, не в vessel subpackage). | Оставить как задокументированное исключение, но вынести решение в seam: добавить `CharacterDefinition` hook напр. `default boolean supportsDebugCommand(String id){return false;}` и в `JujutsuCommands:134` заменить `!= NOBARA` на `!JujutsuCharacters.of(player).supportsDebugCommand("hairpin")`. Альтернатива — helper `JujutsuCharacters.is(player, JujutsuCharacter.NOBARA)` + запись четвёртого исключения в `Vessel-definitions.md:90-95` (сейчас там 3). Добавить ArchUnit allowlist или явный assert на этот единственный `if` чтобы второй не проскочил. |
| major | `src/main/java/jujutsu/mod/character/JujutsuCharacter.java:34-42` и `src/main/java/jujutsu/mod/network/JujutsuNetworking.java:54` и `src/client/java/jujutsu/mod/client/character/ClientCharacterSelectionManager.java:24` | `JujutsuCharacter.byId(String)` при `null`/пустой/неизвестной строке молча возвращает `NONE` без лога. Wire-claim `CharacterAbilityPayload.characterId` (`JujutsuNetworking:54` проверяет `byId(payload.characterId()) != selected`) и `SelectCharacterPayload` (`CharacterSelectionManager.select` принимает `byId` напрямую) и `CharacterSelectionSyncPayload` — все тихо становятся `NONE`. Для `CharacterAbilityPayload` это fail-open forward-compat (новый vessel id на старом сервере → `NONE` → `NONE != TODO` → отказ, но по неверной причине); для `SelectCharacterPayload` это баг — клиент может деселектнуть сосуд опечаткой без UI. Append-only гарантия предполагает silent drop, но для selection это уже наблюдаемое поведение. `CharacterDefinitionRegistryTest:88-90` ловит mis-bound, но не ловит unknown string. | Вернуть `Optional<JujutsuCharacter>` или хотя бы `LOG.warn` при unknown id в `CharacterSelectionManager.select` и `JujutsuNetworking.handleCharacterAbility`. Для wire: если `byId` неизвестен — дропать payload early с `debug` логом, не мапить в `NONE`. Добавить тест `byId("bogus")==NONE && logged`. Минимально — добавить `if(character==NONE && !"none".equals(payload.characterId())) LOGGER.debug("Unknown vessel claim {}", id)` в `handleCharacterAbility`. |
| minor | `src/main/java/jujutsu/mod/character/nobara/NobaraAbilityRouter.java:41-44` и `:62-68` | Двойной путь отказа для 4 слотов: ранний `if (ability==SECONDARY_SNEAK_HOLD \|\| SECONDARY_SNEAK_RELEASE \|\| TERTIARY \|\| TERTIARY_SNEAK) return UNHANDLED_FAILURE` (`:41-44`) + затем те же `case SECONDARY_SNEAK_HOLD -> UNHANDLED_FAILURE` и `case TERTIARY -> UNHANDLED_FAILURE` в exhaustive switch (`:62-67`). Ранний `if` делает switch-рукава мёртвыми (достижимы только для exhaustiveness), но скрывает намерение, обходит `notify` message gate (`:69-71` — ранний return до gate, switch-путь тоже silent, но future vessel может скопировать паттерн с message). Тест `CharacterClientRegistryTest:132-150 slotsTheRouterAnswers` считает refused только по regex `case … -> false/UNHANDLED_FAILURE`, ранний `if` не учитывает — для Nobara не влияет (обе ветки refused), но паттерн хрупок: если vessel добавит ранний `if` для отказа с message, тест не заметит расхождение карты/роутера. | Удалить ранний `if`, оставить только switch-рукава (switch и так exhaustive) или наоборот — оставить ранний `if` с комментарием `// silent refusal before message gate` и удалить дублирующие `case` -> но тогда switch перестанет быть exhaustive. Правильно: убрать `if`, все 10 слотов — только через switch. Если нужен silent до gate — сделать явный `case … -> { if(msg)… }` внутри switch, не до него. |
| minor | `src/client/java/jujutsu/mod/client/input/JujutsuKeybinds.java:262-268` | Shared input файл хардкодит vessel-специфичные item'ы: `stack.is(STRAW_DOLL_HAMMER) \|\| stack.is(PROJECTJJK_STRAW_DOLL_HAMMER)` (`:267`). Это последний vessel-специфичный item-check в shared коде, отмечен в `KNOWN_ISSUES.md:224` и `Vessel-definitions.md:94` как "последний долг". Нарушает идею что `CharacterClientDefinition` владеет `playerSkin/bodyScale/accent`, но не `isTechniqueWeapon`. Добавление нового vessel с technique weapon потребует правки shared файла вместо добавления метода в definition. `VesselBoundaryTest:246-255` exempts `JujutsuCharacterClients` и `MISPLACED_NAIL_RENDERER`, но не `isTechniqueWeapon` — item-check не через `JujutsuCharacter`, поэтому ArchUnit его не видит. | Добавить `CharacterClientDefinition` hook `default boolean isTechniqueWeapon(ItemStack stack){return false;}` и `JujutsuKeybinds.isTechniqueWeapon` заменить на `JujutsuCharacterClients.definition(selectedCharacter(client)).isTechniqueWeapon(stack)` с `||` по обоим рукам. `NobaraClientDefinition` override проверяет два hammer'а, остальные default false. Удалить строку из `KNOWN_ISSUES.md` debt. |
| minor | `src/main/java/jujutsu/mod/character/JujutsuCharacter.java:6-9` и `src/client/java/jujutsu/mod/client/character/CharacterClientDefinition.java:58-61` | Презентационные поля `modelId="wide/slim"` и `bodyScale=1.15f` для TODO (`:8`) живут в shared enum `src/main`, хотя это клиентская концепция (skin model, рендер-масштаб). Комментарий `Vessel-definitions.md:54-57` называет это deliberate исключением (common hook должен применять без `client` типа, sync несёт `modelId` из enum). Но дублирование неизбежно: `CharacterClientDefinition.bodyScale()` по default делегирует `id().bodyScale()` (`:60`) — две точки правды на одну цифру. Изменение масштаба требует помнить оба места; тест `assertNothingClientOnlyLeakedIn` специально не ловит эти поля, потому что они не `net.minecraft.client`. `ClientCharacterSelectionManager:106-108` дублирует `model` mapping строк → enum. | Принять как задокументированное исключение, но зафиксировать invariant тестом: `assert Todo.bodyScale()==TodoClientDefinition.bodyScale()` и `assert modelId в enum совпадает с playerSkin model` в `CharacterDefinitionRegistryTest`. Или вынести `BodyScale`/`ModelId` в отдельный shared value object, чтобы enum не смешивал gameplay id с presentation. Минимум — добавить комментарий в `JujutsuCharacter:6-9` "presentation fields, mirrored in client definition default". |
| minor | `src/main/java/jujutsu/mod/character/JujutsuCharacters.java:49-56` дублирует `src/client/java/jujutsu/mod/client/character/JujutsuCharacterClients.java:39-46` и `src/test/java/jujutsu/mod/character/CharacterDefinitionRegistryTest.java:114-116` | Идентичный шаблон `all()`: `JujutsuCharacter[] characters = JujutsuCharacter.values(); for(i…) definitions[i]=definition(characters[i])`. Копия по design (разные source sets нельзя шарить), но хрупка: тест `assertTheSweepMatchesTheSwitch:115-116` ловит наличие литерала `JujutsuCharacter[] characters = JujutsuCharacter.values()` — переименование переменной `characters` в `values` сломает тест, не меняя поведения (отмечено в `TEST_ARCHITECTURE_PLAN.md:111-121` fail-open в `isInsideVesselPackage`). Более важный риск: `READY_AT` key включает `JujutsuCharacter`, поэтому cooldown переживает vessel switch (намеренно по `E1a`), но `clear(ServerPlayer, ability)` после switch молча удаляет не тот key — поведение задокументировано, но легко скопировать неправильно. | Оставить дублирование, но ослабить source-text assert до regex `JujutsuCharacter\.values\(\)` вместо литерала с именем переменной. Добавить тест `all().length == values().length && Set(ids) == Set(values ids)` уже есть (`assertTheSweepMatchesTheSwitch:94-103`) — усилить его проверкой порядка. Для cooldown — добавить комментарий в `CharacterAbilityCooldowns:61-67` "key resolves vessel from current selection, not from caller". |
| minor | `src/test/java/jujutsu/mod/client/character/CharacterClientRegistryTest.java:76-93` (`assertNoSharedClientFileNamesAVessel`) | Allowlist проверяет лишь 5 файлов (`ClickGuiTheme`, `CharacterSkinAnimationRenderer`, `JujutsuModules`, `CharacterRosterPanel`, `JujutsuModClient`). Остальные shared client файлы (`AbilityHud`, `JujutsuKeybinds`, `VfxWorldChannel`, `CharacterSkinAnimationRenderer` уже в списке, но `AbilityHud` — нет) не проверяются; новый shared файл может тихо ввести `if(character==MEGUMI)` и не упасть. Аналогично `isTechniqueWeapon` уже проскочил, потому что проверяет `Nobara`/`Todo` строки, а файл не в allowlist'е. Тест `VesselBoundaryTest:246-255` шире (ArchUnit на все `jujutsu.mod.client..` вне vessel packages), но exempts `MISPLACED_NAIL_RENDERER` и проверяет `dependOnClassesThat resideInAnyPackage(VESSEL_PACKAGES)` — item-check через `JujutsuItems` не триггерит его. | Расширить `assertNoSharedClientFileNamesAVessel` до `Files.walk(CLIENT.resolve("jujutsu/mod/client"))` с exclude `**/character/<id>/**` и `**/render/nobara|todo|megumi/**` и `**/vfx/**`, вместо ручного списка. Или положиться на `VesselBoundaryTest.sharedClientCodeAsksTheVesselRatherThanNamingOne` и добавить второй ArchUnit rule `should().containNo vessel enum literal` через source-text. Минимум — добавить `AbilityHud` и `JujutsuKeybinds` в allowlist с явным `assert !body.contains("NOBARA")`. |
| minor | `src/main/java/jujutsu/mod/character/todo/TodoStoneRuntime.java:69-74` | Vessel-internal `switch(ability){case TERTIARY->…, case TERTIARY_SNEAK->…, default->false}` — единственный switch на `CharacterAbility` с `default` вне `maxCooldownTicks`. Не shared dispatch, а vessel runtime (внутри `todo` пакета, `VesselBoundaryTest` не применяет), но нарушает guideline `add-vessel` "Ability router exhaustive no default" — будущий слот `TERTIARY_SUPER` не заставит править этот файл, молча вернёт `false`. Роутер `TodoAbilityRouter:38-40` уже exhaustive, но inner runtime дублирует dispatch и ослабляет его. | Заменить на exhaustive без `default`: `case TERTIARY->…, case TERTIARY_SNEAK->…, case PRIMARY, PRIMARY_SNEAK, SECONDARY, SECONDARY_SNEAK, ATTACK_CONTEXT, USE_CONTEXT, SECONDARY_SNEAK_HOLD, SECONDARY_SNEAK_RELEASE -> false;`. Тогда добавление слота сломает оба уровня. Или удалить switch вовсе — `if(ability==TERTIARY)… else if(TERTIARY_SNEAK)… else return false` (меньше, но тоже не exhaustive). |
| minor | `docs/KNOWN_ISSUES.md:E7:217-224` (документация) | `E7` пересчитал оставшиеся 9 `JujutsuCharacter.NOBARA/TODO` вхождений в 3 категории (4× `id()`, 1× debug command, 4× self-check) на 2026-08-05. После Megumi slice (`MEGUMI` enum + `MegumiCooldownHud:28-30` `== MEGUMI`, `NobaraEspState` + `TodoStatusHud` уже были) счётчик устарел — `grep -rn "JujutsuCharacter\." src` теперь даёт >12 hits (добавились `MEGUMI` в `JujutsuCharacters`, `JujutsuCharacterClients`, `MegumiDefinition`, `MegumiClientDefinition`, `MegumiCooldownHud`). Doc утверждает "9 files" — уже неверно, хотя категории остались верными. `Vessel-definitions.md:90-95` тоже перечисляет 3 deliberate исключения, но `MegumiCooldownHud` self-check не упомянут. | Пересчитать `grep -rn "JujutsuCharacter\.(NOBARA\|TODO\|MEGUMI\|NONE)" src --include="*.java" | wc -l` и обновить `KNOWN_ISSUES.md:E7` с разбивкой 4× `id()` (server+client definitions) + 1× `JujutsuCommands` + 5× self-check (`TodoBlackFlashRuntime:36`, `TodoSwapMomentumRuntime:72,101`, `NobaraEspState:112`, `TodoStatusHud:94`, `MegumiCooldownHud:28`) + 2× `NONE` gate (`CharacterAbilityExecutor`, `AbilityHud`). Обновить `Vessel-definitions.md` deliberate residue с 3→4 self-check. |

---

### 3) Позитивные наблюдения

- **Compile-time гарантия держится железно.** Оба реестра — `JujutsuCharacters.definition:31-36` и `JujutsuCharacterClients.definition:26-32` — `switch(character)` без `default`, поля `*_DEFINITION` с суффиксом (коммент `:20-22` объясняет анти-транспозицию), `all()` выведен из `values()`. Проба "добавить константу → 2 ошибки компиляции" воспроизводится дословно; `CharacterDefinitionRegistryTest:105-112` и `CharacterClientRegistryTest:47-56` дополнительно ассертят отсутствие `default` regex'ом. Проверено `read` обоих файлов + `grep "default"` пусто именно в реестрах.
- **Shared код не называет vessel (кроме одного deliberate).** `CharacterAbilityExecutor:15-35` и `CharacterCombatModifiers:29-33` — образцовые seam-файлы: ни одного `if(TODO)` / `switch(JujutsuCharacter)` кроме `NONE`-gate, который по контракту `NONE` — отсутствие vessel, а не vessel. `CharacterCombatModifiers.applyForSelection` делает `for(all) remove + selected.apply` — добавление Todo-атрибутов и Megumi-dog-attributes не тронуло файл. `JujutsuMod:44-46` и `JujutsuModClient:29` ставят хуки через `for(definition : all()) registerHooks()` — ни одного `new TodoX` в init. Подтверждено `grep JujutsuCharacter` вне `*/character/<id>/*` → только `NONE` gate и `id()` декларации + 1 `JujutsuCommands` исключение + 5 self-check (см. E7).
- **Build-time тесты покрывают то, чего switch не может.** `assertEveryVesselResolves:86-91` ловит `case TODO -> NOBARA_DEFINITION` — единственное, что компилируется но неверно. `assertNothingClientOnlyLeakedIn:124-145` и `assertTheServerHalfStaysServerOnly:63-74` сканируют весь `src/main` / `src/client` на `net.minecraft.client` / `jujutsu.mod.client` — dedicated server не упадёт на load (проверено `grep` + `read` обоих тестов). `assertEveryVesselRuntimeIsRegistered:41-73` выводит ожидаемый список из файловой системы (`Files.walk` + `contains("public static void register()")`) — дропнутый `register()` не замолчит, и `JujutsuMod.java:44-46` не содержит `character.<id>` литерала. `assertEveryVesselDeclaresItsOwnCard:106-123` + `slotsTheRouterAnswers:132-150` связывают `rosterEntry` с роутером, чтобы HUD и слоты не разошлись.
- **Роутеры exhaustive и честные.** Все три `*AbilityRouter.tryCast` — `switch(ability)` без `default`, каждый неиспользуемый слот отвечает `UNHANDLED_FAILURE` явно (`Nobara:45-68`, `Todo:21-41`, `Megumi:26-42`). Новый `CharacterAbility` (напр. `QUATERNARY`) сломает компиляцию во всех трёх роутерах сразу — именно то, что требует `AGENTS.md:36`. Nobara дополнительно держит vessel-специфичный `CombatStagger` gate и `no_target` fallback внутри роутера (`:30-35, :69-71`), не в shared executor — правильно. Megumi `SECONDARY_SNEAK_HOLD/RELEASE` разнесены на 3 отдельных `case`, а не схлопнуты в `B` — hold gesture честно смоделирован.
- **Vessel изолированы по пакетам и payload'ам.** Каждый vessel живёт в `jujutsu/mod/character/<id>/` (сервер) и `jujutsu/mod/client/character/<id>/` (клиент) + `jujutsu/mod/client/render/<id>/` + `jujutsu/mod/client/vfx/<id>/`; `JujutsuNetworking:31-40` не импортирует ни одного `*.nobara.*` / `*.todo.*` / `*.megumi.*` (`VesselBoundaryTest:318-337` держит `assertEquals(Set.of(), vesselDependencies)`). Все C2S способности — один `CharacterAbilityPayload` (`ability.networkId` + `character.id` claim, `handleCharacterAbility:54` сравнивает с `CharacterSelectionManager.selected`), нет per-vessel payload'ов — wire ids append-only соблюдены (`CharacterAbility:16-55` коммент `append rather than renumber`).
- **Документация честна и двусторонняя.** `KNOWN_ISSUES.md:E7:217-224` классифицировал оставшиеся `JujutsuCharacter` вхождения (4× `id()`, 1× debug command, 4× self-check — needs recount after Megumi) + отдельно отметил `isTechniqueWeapon` как последний shared-item gate. `Vessel-definitions.md:54-57` честно называет `bodyScale/modelId` deliberate исключением, `73-76` и `AGENTS.md:223-228` описывают две гарантии (compile vs build) без приукрашивания. `TEST_ARCHITECTURE_PLAN.md:111-121` сам признаёт fail-open в `isInsideVesselPackage`.
- **Client зеркало консистентно.** `ClientCharacterSelectionManager:23-27 apply` + `characterOrNone:76-79` совпадают с серверным default `NONE`; `ClientAbilityCooldowns` ключует `(character, ability)` как и серверный `Key(playerId, character, ability)` — расхождение по vessel после switch уже исправлено (коммент `CharacterAbilityCooldowns:12-16`). `JujutsuKeybinds:235-237 slot` и `247-258 sendCharacterAbility` не знают о vessel кроме `NONE` gate и vessel-claim штампа — input layer остаётся транслятором, не диспетчером. `JujutsuModules:20-23` и `CharacterRosterPanel` строятся из `JujutsuCharacterClients.all()` — новая карта появится без правки shared.
- **VFX / render seam не протекает.** `CharacterGeoRenderers.create:23-32` (archive) и `CharacterSkinAnimationRenderer:21-23` спрашивают `definition.createRenderer` / `skinAnimation` — добавление Megumi `MegumiDivineDogRenderer` прошло только через `MegumiClientDefinition.registerClientHooks:128`, без правки shared render dispatch.

---

### 4) Вердикт

**PASS WITH CAVEATS**

Seam **не эродировал** в критическом смысле: exhaustive switches без `default` в обоих реестрах, sweep через `values()`, отсутствие vessel-ветвлений в shared dispatch (`CharacterAbilityExecutor`, `CharacterCombatModifiers`, `CharacterSelectionManager`, `JujutsuNetworking`, `JujutsuMod`, `JujutsuModClient`, `AbilityHud`, `VfxDirector`), изоляция `src/main` от client типов, per-vessel `registerHooks` и один wire payload — всё на месте и покрыто build-time тестами (`testCharacterDefinitions` / `testCharacterClients` / `VesselBoundaryTest` / `SourceBoundaryTripwireTest`). Добавление четвёртого vessel по-прежнему сломает компиляцию ровно в двух местах (`JujutsuCharacters`, `JujutsuCharacterClients`) и потребует ровно два `case` + два `Definition` файла — claim из `AGENTS.md:225` верифицирован чтением кода. `grep` по `JujutsuCharacter.` вне vessel-пакетов подтверждает: единственный shared `if` — `JujutsuCommands:134` (deliberate), остальные — `NONE` gates и `id()` декларации + 5 self-check внутри vessel-кода.

Caveats — 4 deliberate/полу-deliberate пятна + 2 места где compile-time гарантия ослаблена `default`/отсутствием override + 5 minor эрозий по краям (silent `byId`, дублирование guard в Nobara, узкий allowlist теста, дублирование `all()`, `TodoStoneRuntime` default, bodyScale дублирование). Ни одно не ломает текущие 3 vessel'а (Nobara/Todo/Megumi) — все касты, кулдауны, выбор, атрибуты, VFX и рендер работают — но каждое снижает fail-closed поведение для следующего vessel'а/слота. Без фикса следующий `CharacterAbility` слот тихо проглотит `default -> 0` в HUD, а следующий vessel тихо получит `NONE` при опечатке id.

**Приоритет фиксов:**

1. **P1 (до следующего vessel/slot — 10 строк, highest ROI):** убрать `default` из `TodoClientDefinition.maxCooldownTicks:82-92` и `MegumiClientDefinition.maxCooldownTicks:82-91` → exhaustive switch без `default`; добавить явный override в `NobaraClientDefinition` (и `rosterOrder 0`). Восстанавливает compile-time гарантию для HUD. Тесты `CharacterClientRegistryTest` не нужно трогать — они уже проверяют реестр, а router/HUD тесты сломаются правильно.
2. **P2 (до релиза, seam hygiene):** `JujutsuCommands:134` → seam hook `CharacterDefinition.supportsDebugCommand` (major #3) + `JujutsuKeybinds.isTechniqueWeapon:266-268` → `CharacterClientDefinition.isTechniqueWeapon` (minor #6). Оба — последние shared места с vessel-литералом. Заодно пересчитать и обновить `KNOWN_ISSUES.md:E7` (minor #11).
3. **P3 (hygiene, одним коммитом):** удалить дублирующий early-return в `NobaraAbilityRouter:41-44` (minor #5), заменить `default` в `TodoStoneRuntime:69-74` на exhaustive, добавить warn-логику в `JujutsuCharacter.byId` / `CharacterSelectionManager.select` (minor #4), ослабить `characters` литерал-assert до regex `JujutsuCharacter\.values\(\)` (minor #8), расширить allowlist `CharacterClientRegistryTest:76-93` до walk (minor #9). Можно вместе с P1.

AUDIT_ID: 01-vessel-seam-contract
