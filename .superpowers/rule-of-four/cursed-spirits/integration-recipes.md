# Cursed-spirit integration recipes (read-only recon, 2026-09-12, branch feat/cursed-spirits)

Source: scout `IntegrationRecipes` (agent://IntegrationRecipes). Saved verbatim by Main (the scout's session could not write files).

Shared primitives: `CharacterAbilityExecutor.tryCast(ServerPlayer, CharacterAbility, boolean)` -> `AbilityResult` (CharacterAbilityExecutor.java:15). Tri-state: SUCCESS / HANDLED_FAILURE (router silent) / UNHANDLED_FAILURE (fallback shown). Cooldown read `CharacterAbilityCooldowns.remainingTicks(player, slot)` (:43), seed `start` (:48), clear `clear` (:61). Aiming: `TodoSwapTestFixtures.aimAt(caster, absolutePoint)` writes setYRot/setXRot/setYHeadRot — NEVER forceSetRotation (packet-only on ServerPlayer) (TodoSwapTestFixtures.java:139-143). Chest aim point `target.position().add(0, bbHeight/2, 0)` (CursedSpiritGameTests.java:397). Underneath: `TargetResolver.resolve(level, owner, maxRange, eligible)` -> `Result(Mode, point, normal, entityId, maxRange)`, Mode in {ENTITY,BLOCK,MISS} (TargetResolver.java:38-41,64,71).

## 1. Nobara nail vs mob
Types: `NobaraAbilityRouter.tryCast(ServerPlayer, CharacterAbility, boolean)` -> AbilityResult (NobaraAbilityRouter.java:29). Actual throw is NOT a slot: `ProjectJjkNobaraRuntime.launchHairpin(player, hammerStack, hand, explosiveImpact)` -> boolean (:125); prep first via `prepareNails(player, usedStack, useTicks)` (:76). Production trigger: hammer use without shift (`ProjectJjkHammerItem.java:20-24`). Impact fan-out (ProjectJjkNobaraRuntime.java:192-195): `if (isSuccessfulOrdinaryHit(...)) { openNailEmbedWindow(owner, target, NAIL_DAMAGE); markTarget(level, target, owner, point); onOrdinaryNailHit(level, owner, target, point); }`. Nail entity `ProjectJjkNailEntity`: `launchAt(Vec3,int,boolean)` (:134), `isEmbedded()` (:164), `isOwnedBy(UUID)` (:168), `anchor()` (:197), `embedDepthLevel()` 1-3 (:201), `attachToEntity` (:251). Embed query to copy (NobaraHammerCombatRuntime.java:259-265): `level.getEntitiesOfClass(ProjectJjkNailEntity.class, target.getBoundingBox().inflate(NAIL_CONTEXT_SCAN_INFLATE), nail -> nail.isEmbedded() && nail.isOwnedBy(player.getUUID()) && target.getUUID().equals(nail.anchor().stableId()))`. Mark write (ProjectJjkRitualRuntime.java:86-90): `ProjectJjkNailMarks.apply(targetUUID, gameTime)` + GLOWING effect MARK_DURATION_TICKS. Closest verbatim aimed-cast shape: R14 scenario CursedSpiritGameTests.java:395-407 (line-of-sight assert + `TodoSwapTestFixtures.aimAt(caster, lesser.position().add(0, bbHeight/2, 0))` + `castPrimary(caster)` + `assertTrue(swapped...)`). Nobara caster setup has NO shared fixture — `setupNobara` is private in NobaraAbilityResultGameTests.java:227-244 (direct ServerPlayer ctor, setPosRaw to block centre, select(NOBARA), clear PRIMARY/SECONDARY/SECONDARY_SNEAK); must be duplicated, and a nail-throwing test MUST use placed `makeMockServerPlayerInLevel` instead (recording mock is never placed — safe only for refusal paths). Prerequisites: select BEFORE cooldown clears; stone under feet; open-air chest-ray corridor; hammer+nails in inventory; ensureHostileDifficulty+layStoneFloor for spirits. Oracle: embedded-owned-nail query non-empty + `ProjectJjkNailMarks.marks(targetUUID, gameTime) >= 1` (:19) + GLOWING present. UNVERIFIED: no GameTest throws a nail at any mob (gametest grep launchHairpin|prepareNails|ProjectJjkNail hits only comments). UNVERIFIED: no `routed` flag exists on the Nobara path — tryCast returns bare AbilityResult; `routed:false` prose lives only in R15 javadoc (CursedSpiritGameTests.java:469) meaning `castPrimary()==false` (deny) vs R14 allow (true). For Nobara, routed case = `tryCast(...)==SUCCESS`.

## 2. Hairpin (PRIMARY) vs marked mob; mis-aim + cooldown
Router: `case PRIMARY -> canCastMarkedHairpin(nobara) ? startDirectedHairpin(nobara) : UNHANDLED_FAILURE` (NobaraAbilityRouter.java:48-50); SECONDARY -> MegaNail `start` (:53-55). Gate `canCastMarkedHairpin` = `!isExplosiveLaunchLocked` (ProjectJjkNobaraRuntime.java:162-164). `startDirectedHairpin` (ProjectJjkRitualRuntime.java:99): chains owned nails within HAIRPIN_DIRECTED_CHAIN_RADIUS 10.0; with ZERO nails plays snap and returns SUCCESS (:105-106) — so PRIMARY can never mis-aim-fail for a fresh player. `MegaNailRuntime.start` (:53): MISS or no embedded nails on aimed target -> UNHANDLED_FAILURE -> router shows `message.jujutsumod.nobara.action.no_target` (NobaraAbilityRouter.java:69-71). Verbatim mis-aim (aim pitch -90 to sky, setup :175): NobaraAbilityResultGameTests.java:179-182 `AbilityResult result = cast(nobara, CharacterAbility.SECONDARY); assertTrue(result==UNHANDLED_FAILURE...); assertRecordedExactly(helper, fixture, "cast", nobara, List.of(NO_TARGET));`. Rationale comment :164-168 states PRIMARY deliberately unused for this gate. Cooldown ordering snippet :208-212: `CharacterAbilityCooldowns.start(nobara, PRIMARY, 100); result = cast(nobara, PRIMARY); assertTrue(UNHANDLED_FAILURE); assertRecordedExactly(... List.of(COOLDOWN));`. Window-read shape: TodoStoneGameTests.java:133-136 + CursedSpiritGameTests.java:419-422 (`remaining > 0 && remaining <= PROFILE_COOLDOWN`). Oracle: mis-aimed SECONDARY -> UNHANDLED_FAILURE + exactly [NO_TARGET]; successful PRIMARY vs marked mob -> SUCCESS, 5.0 x depthMultiplier x momentum per chained nail (ProjectJjkRitualRuntime.java:201-205; HAIRPIN_DIRECTED_DAMAGE_PER_NAIL Profile :33), marks consumed (:239). UNVERIFIED: no GameTest detonates Hairpin vs any mob. UNVERIFIED: whether startDirectedHairpin arms any cooldown — no Cooldowns.start found under character/nobara/; use remainingTicks==0 shape (assertNoPrimaryCharge, TodoSwapTestFixtures.java:247-253) if asserting absence.

## 3. Resonance (ritual) vs marked mob
Two rituals, do not conflate. (a) Self-resonance: `SelfResonanceRuntime.tryCast(ServerPlayer)` -> AbilityResult (NO notify param) (:36); slot PRIMARY_SNEAK (router :51); needs READY CurseLink involving caster (:38-51), fresh player -> HANDLED_FAILURE + self_resonance.no_link; success damages SELF_RESONANCE_LINKED_DAMAGE + HEAVY_STAGGER (:77-78). (b) Straw-doll remnant ritual: `ProjectJjkStrawDollRuntime.tryStart(caster, hammer, hand)` -> boolean (:108); NO ability slot — trigger is shift-use of hammer (ProjectJjkHammerItem.java:20-21). Requires hammer MAIN_HAND + doll offhand + bound remnant + nail, target alive + same dimension (:109-111,:150-199); impact on server tick past DOLL_STRIKE.impactTick (:124-146). Damage: RESONANCE_DAMAGE=28.0f via indirectMagic + ResonantMomentum.grant + HEAVY_STAGGER_TICKS=14 + server hit-stop ~2s half-TPS (:241-249; Profile :91,:127). Only pinned test is the refusal: NobaraAbilityResultGameTests.java:148-151 (PRIMARY_SNEAK -> HANDLED_FAILURE + [RESONANCE_NO_LINK]). Doll prerequisites: mint remnant first (needs 2 nail hits), hold kit, same dimension, wait past impact tick. Oracle: tryStart true + casting message; impact tick HP -28 banded + `CombatStagger.GLOBAL.isStaggered(targetUUID, gameTime)` true (assert shape MegumiShikigamiGameTests.java:438-441). UNVERIFIED: no GameTest exercises doll ritual or successful self-resonance.

## 4. Remnant mint
`REMNANT_HIT_THRESHOLD = 2` private (ProjectJjkStrawDollRuntime.java:36); `REMNANT_PROGRESS = new ProjectJjkRemnantProgress(THRESHOLD)` (:39). `recordHit(casterId, targetId)` true exactly at threshold (`hits.getOrDefault+1 >= hitThreshold`, Progress :15-18). Mint path `onOrdinaryNailHit(level, caster, target, wound)` (:64), called from thrown-nail impacts (ProjectJjkNobaraRuntime.java:195) and trap triggers; guards both-alive + not-self (:65-67), below-threshold return (:69-71). Output: RESONANCE_REMNANT stack + RESONANCE_TARGET binding + VISUAL + bound name, inventory else dropped (:72-90). Visual `RemnantVisualType.classify(curseTagged, animal)` -> CURSE|FLESH|TOKEN (:27-31), called with `target.getType().is(RESONANCE_REMNANT_CURSE)` + `instanceof Animal` (:101-105). Tag: `TagKey.create(Registries.ENTITY_TYPE, JujutsuMod.id("resonance_remnant_curse"))` (ProjectJjkTags.java:10-12); test spelling CursedSpiritGameTests.java:40-41. Oracle: after hit1 no remnant; after hit2 one remnant stack with targetId==mob UUID, VISUAL==CURSE for spirits. Progress cleared on target unload/death (:51-62) — keep target alive+loaded across both hits. UNVERIFIED: `classify` is package-private static (no modifier, RemnantVisualType.java:27) — NOT callable from jujutsu.mod.gametest; assert the minted item's component instead. UNVERIFIED: no GameTest mints a remnant.

## 5. Todo melee + Black Flash
Melee is NOT a slot: ATTACK_CONTEXT genuinely empty, plain vanilla + attribute modifiers (TodoAbilityRouter.java:30-31,40). Tuning: MELEE x1.50, attack speed x0.85 (TodoProfile.java:5-7); BF chance 0.10, multiplier 1.75, stagger 14 (:106-108). Bridge `TodoBlackFlashRuntime.afterDamage` (:29): skip blocked/damageTaken<=0/re-entrant bonus; require direct==entity==TODO-selected living non-spectator Todo (:30-39); `rolls(todo, 0.10)` (:40); then `BlackFlashStrike.resolve(todo, target, baseDamageTaken, 1.75, blackFlashSrc, pierce=true, 14, 2.0)` (:44-52) + VFX cue. Bonus math: `bonus = base*(mult-1)` via hurtServer with invuln-pierce under APPLYING_BONUS guard, then stagger+knockback+`BlackFlashFocus.grant` (BlackFlashStrike.java:41-77). Determinism: `ForcedBlackFlash.set(player, enabled)` (ForcedBlackFlash.java:20-23); `rolls` true when enabled (:21-23); auto-cleared on disconnect/stop (:15-18) — still clear in test cleanup. Prerequisites: setupTodoCaster, vanilla reach, TODO-selected/alive/non-spectator, set forced-BF true. Oracle (production-derived, band it): HP drop = vanilla x1.5 + bonus x0.75; isStaggered true within 14 ticks; do NOT assert isApplyingBonus post-hoc (true only mid-nested-hit). UNVERIFIED: no GameTest performs a Todo melee hit or asserts Black Flash (zero BF references in gametests; mob->victim strikes e.g. CursedSpiritGameTests.java:83-98 are the only melee oracles).

## 6. Megumi sic onto mob
Slot: `case PRIMARY_SNEAK -> MegumiShikigamiRuntime.trySic(player, notify) ? SUCCESS : UNHANDLED_FAILURE` (MegumiAbilityRouter.java:31-32); summon/swap/recall = PRIMARY -> tryPrimary (:29-30); no router fallback (:14). `MegumiShikigamiRuntime.trySic` (MegumiShikigamiRuntime.java:168): DOGS -> SummonRuntime.trySic (:170-171); aim resolve range SIC_RANGE 20.0 excluding own bodies (:192-196); `assignSicTarget` (MegumiSummonRuntime.java:202-204); PRIMARY_SNEAK 30t cooldown (:211; Profile :19). Dog summon `MegumiSummonRuntime.tryToggle(player, notify)` (:127); presence `packView(server, ownerId).isPresent()` (MegumiShikigamiGameTests.java:205). Selection `selected()` defaults DOGS (:17-19), `set(UUID,type)` (:29), `clear` (:37). Pounce rows: 3.0+2.0=5.0 dmg via playerAttack(owner), knockback 2.4, stagger 6 (MegumiSummonRuntime.java:493-500; Profile :6,:29-31). Verbatim summon+select (SUMMON_TICK=2, :58): MegumiShikigamiGameTests.java:94-98 `Selection.set(ownerId, NUE); summoned = tryPrimary(caster, false); assertTrue(summoned...)`. Dog variant :202-207 (no set needed — default DOGS). Verbatim sic :369-372: `aimAt(caster, zombie chest); sicced = trySic(caster, false); assertTrue(sicced...)`. Damage oracle :422-426 banded `dropped in [low,high]`; stagger :438-441 `isStaggered(zombieUUID, gameTime)`; first-impact poll loop :380-404 (succeed on first sighting). Shikigami-damage shape :277-280 `body.hurtServer(level, genericKill(), MAX_VALUE)` + assertTrue(damaged) (lethal variant; use non-lethal value for damage-back). Prerequisites: setupMegumiCaster; dogs tryToggle tick2, sic only after DOG_MATERIALIZATION_TICKS=16 ACTIVE phase (premise-assert body.combatEnabled() cf. :274-275; Nue tests use SIC_TICK=22); aim within 20 blocks, eligible non-own target; technique presses on different ticks (same-tick duplicates dropped). UNVERIFIED: no GameTest sics DOGS onto a mob or asserts pounce damage (sic impacts pinned only for Nue/Toad/Elephant); expected dog oracle dropped in [~4,~6] + knockback + 6t stagger. UNVERIFIED: no test asserts non-lethal damage to a shikigami body — use `dog.getHealth() < DOG_HEALTH (60.0, Profile :5)`.

## 7. Fixtures, premises, cleanup, arenas
Todo: `setupTodoCaster(helper, fixture, feet, yaw, pitch)` (TodoSwapTestFixtures.java:87-88) — placed mock, 8-arg teleportTo to block centre, select(TODO), clear PRIMARY/TERTIARY/TERTIARY_SNEAK, drop momentum, premise-asserts; setup failure -> cleanup+rethrow (:117-122). POSITION_EPSILON=1.0E-6 (:47); BodyState record+capture (:58-61); assertBodyState (:212); assertNoPrimaryCharge (:248-253); cleanupCaster (:271-279: clear cooldowns, drop momentum, TransientState.dropAll, PlayerList.remove — NEVER discard). Megumi: `setupMegumiCaster` (:54-55): same + select(MEGUMI), clear PRIMARY/PRIMARY_SNEAK, Selection.clear (->DOGS), asserts incl. selection==DOGS (:72-80); cleanup (:111-121: teardown both pack runtimes FIXTURE_RESET, clear cooldowns while still MEGUMI-selected, clear selection); runGuarded (:130-137). Nobara: private setupNobara (:227-244) + cast (:247-248 `CharacterAbilityExecutor.tryCast(nobara, slot, true)`) + cleanup (:271-275, cooldowns only — never placed). Victim: `setupVictim` (CursedSpiritTestFixtures.java:68-69) forces SURVIVAL (:75) + instabuild=false (:80), asserts alive/non-spectator/non-creative/non-invulnerable/difficulty-hostile (:83-96); cleanupVictim = PlayerList.remove (:105-110). Mob spawn: `GameTestFixtures.spawnMob` = spawnWithNoFreeWill + alive assert (GameTestFixtures.java:33-38; never pre-convert positions); spirits via `spawnSpirit` (:118-119): spawnMob + setPersistenceRequired + setNoAi(false) (NoAI bodies never move/strike) + tier assert (:122-125). Difficulty: `ensureHostileDifficulty` (:56-61, PEACEFUL->NORMAL, idempotent). Cooldown key resolves vessel from live selection — select() MUST precede clear (Todo :87-94 comment). Flat arenas: `layStoneFloor` 6x6 stone y=0 (:47-51) for ALL spirit scenarios; trap test adds 3x3 floor under aim (Nobara :104-109); Nue tests add roof block anti-sky-burn (MegumiShikigamiGameTests.java:321-323); Toad needs roof, no corridor; only Todo rollback tests use non-flat arenas. Skeleton: layStoneFloor + ensureHostileDifficulty + placed-mock vessel setup + spawnSpirit + runAtTickTime(2) { aimAt chest; act; oracle } with catch { discard spirit; cleanupCaster; throw } and succeed at end (cf. R14 :395-407 + R15 deny shape :482-505 asserting !cast + assertBodyState both bodies + assertNoPrimaryCharge). Diagnostics: `[fixture/phase @tick N caster=.. target=..] what: expected <e>, actual <a>` (Todo :76-80; Megumi :42-46; Spirit :36-46; GameTestFixtures.diagnostic). Poll loops: per-tick runAtTickTime with done-guard + terminal-tick assertFalse (CursedSpiritGameTests.java:78-107).
